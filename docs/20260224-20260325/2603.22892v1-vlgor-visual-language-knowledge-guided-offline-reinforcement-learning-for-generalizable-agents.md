---
title: "VLGOR: Visual-Language Knowledge Guided Offline Reinforcement Learning for Generalizable Agents"
title_zh: VLGOR：面向可泛化智能体的视觉-语言知识引导的离线强化学习
authors: "Pengsen Liu, Maosen Zeng, Nan Tang, Kaiyuan Li, Jing-Cheng Pang, Yunan Liu, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22892v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 将视觉和语言知识整合到代理的离线强化学习中
tldr: "本研究针对大语言模型在强化学习中缺乏物理环境感知、泛化能力受限的问题，提出了VLGOR框架。该框架通过微调视觉语言模型（VLM），结合初始视觉观察和高层指令生成具有时空一致性的虚构轨迹，并利用反事实提示增强数据多样性。实验证明，VLGOR在机器人操作任务中显著提升了智能体对未知任务的泛化性能，成功率比基准方法高出24%以上。"
selection_source: fresh_fetch
motivation: 现有的结合大语言模型的强化学习方法因缺乏对物理环境的直接感知，导致其在处理未知任务时泛化能力不足。
method: 提出VLGOR框架，通过微调视觉语言模型生成虚构的交互轨迹，并利用反事实提示增加离线训练数据的多样性。
result: "在机器人操作基准测试中，VLGOR在需要新策略的未知任务上表现优异，成功率比基准方法提升了24%以上。"
conclusion: 结合视觉与语言知识的虚构轨迹生成能有效增强离线强化学习智能体的环境感知与泛化能力。
---

## 摘要
将大语言模型（LLMs）与强化学习（RL）相结合，使智能体能够更有效地理解语言指令以执行任务。然而，LLMs 通常缺乏对物理环境的直接感知，这限制了它们对环境动力学的理解以及对未知任务的泛化能力。为了解决这一局限性，我们提出了视觉-语言知识引导的离线强化学习（VLGOR），这是一个整合视觉和语言知识以生成想象轨迹（imaginary rollouts）的框架，从而丰富了交互数据。VLGOR 的核心前提是微调一个视觉-语言模型，以初始视觉观察和高层指令为条件预测未来的状态和动作，确保生成的轨迹在时间上连贯且在空间上合理。此外，我们采用反事实提示（counterfactual prompts）为离线强化学习训练生成更多样化的轨迹，使智能体能够获取有助于遵循语言指令的知识，同时基于视觉线索在环境中实现具身落地（grounding）。在机器人操纵基准测试上的实验表明，VLGOR 显著提高了需要新型最优策略的未知任务的性能，其成功率比基准方法高出 24% 以上。

## Abstract
Combining Large Language Models (LLMs) with Reinforcement Learning (RL) enables agents to interpret language instructions more effectively for task execution. However, LLMs typically lack direct perception of the physical environment, which limits their understanding of environmental dynamics and their ability to generalize to unseen tasks. To address this limitation, we propose Visual-Language Knowledge-Guided Offline Reinforcement Learning (VLGOR), a framework that integrates visual and language knowledge to generate imaginary rollouts, thereby enriching the interaction data. The core premise of VLGOR is to fine-tune a vision-language model to predict future states and actions conditioned on an initial visual observation and high-level instructions, ensuring that the generated rollouts remain temporally coherent and spatially plausible. Furthermore, we employ counterfactual prompts to produce more diverse rollouts for offline RL training, enabling the agent to acquire knowledge that facilitates following language instructions while grounding in environments based on visual cues. Experiments on robotic manipulation benchmarks demonstrate that VLGOR significantly improves performance on unseen tasks requiring novel optimal policies, achieving a success rate over 24% higher than the baseline methods.

---

## 论文详细总结（自动生成）

### VLGOR: 视觉-语言知识引导的离线强化学习总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何提升离线强化学习（Offline RL）智能体在面对未见任务（Unseen Tasks）时的泛化能力。
*   **研究动机**：
    *   虽然大语言模型（LLM）具备丰富的语义知识，但缺乏对物理环境的直接感知，导致其生成的指令或轨迹往往违反物理约束（如物体穿透、运动不合理）。
    *   传统的离线强化学习受限于训练数据的多样性和覆盖范围，难以应对指令变化或任务变体。
    *   现有的方法（如 KALM）仅依赖文本描述状态，缺乏空间状态的显式建模，导致生成的虚构轨迹（Imaginary Rollouts）质量不高。

#### 2. 方法论
VLGOR 框架分为两个主要阶段：**轨迹生成（Rollout Generation）**和**技能获取（Skill Acquisition）**。
*   **核心思想**：利用微调后的视觉-语言模型（VLM）生成具有物理一致性的虚构轨迹，并通过反事实提示增强数据多样性，从而为离线 RL 提供高质量的增强数据集。
*   **关键技术细节**：
    *   **环境落地（Environment Grounding）**：采用 BAGEL 作为 VLM 基座，通过 LoRA 适配器进行微调。模型输入初始视觉观察、高层指令和初始动作，预测未来的视觉状态和动作。
    *   **反事实提示生成**：将指令分解为背景模板（t）、操作目标（m）和行为模式（b）。通过修改目标（Counterfactual Target）或修改行为（Counterfactual Behavior）生成原始数据中不存在的新指令。
    *   **离线强化学习训练**：将原始真实数据与 VLM 生成的虚构数据合并。VLGOR 具有通用性，可适配多种算法（如 BC, CQL, TD3, PRDC）。

#### 3. 实验设计
*   **实验场景/数据集**：
    *   **CLEVR-Robot**：智能体操纵五个球体达到目标配置。
    *   **Meta-World**：使用 Sawyer 机械臂执行多种操纵任务（如开门、按按钮、拨动开关）。
*   **任务难度分级**：
    *   **指令改写（Rephrasing）**：仅改变语言表达，任务逻辑不变。
    *   **反事实任务（Counterfactual）**：通过提示词修改目标或行为，产生新任务。
    *   **困难任务（Hard）**：目标规格与行为变化的复杂组合。
*   **对比基准（Baselines）**：
    *   基础算法：Behavior Cloning (BC), Conservative Q-Learning (CQL)。
    *   增强方法：BC/CQL + KALM（仅语言引导的轨迹生成）。

#### 4. 资源与算力
*   **硬件设备**：单台 **NVIDIA H800 80GB PCIe GPU**。
*   **训练细节**：
    *   VLM 微调：20 个 epoch，batch size 为 16。
    *   离线 RL 训练：50 个 epoch，使用 Adam 优化器。
    *   数据集规模：10,000 条轨迹用于 VLM 微调；6,400 条真实轨迹用于 RL，并辅以数千条生成的虚构轨迹。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准测试上进行了评估，涵盖了三种难度等级的未见任务。
*   **消融实验**：
    *   测试了不同数量的微调样本对 VLM 生成质量（PSNR, SSIM, MED 指标）的影响。
    *   分析了不同类型的反事实提示（仅目标、仅行为、两者结合）对成功率的贡献。
*   **客观性与公平性**：实验采用了三个随机种子以确保鲁棒性，并在保持总训练数据量一致的情况下对比了不同方法，确保了比较的公平性。

#### 6. 主要结论与发现
*   **性能提升**：VLGOR 在所有未见任务上均显著优于基准方法。在困难任务中，VLGOR 的成功率比基准方法高出 **24% 以上**。
*   **物理一致性**：相比于仅依赖语言的 KALM，VLGOR 生成的轨迹在空间布局和物体运动上更符合物理规律，减少了物体重叠等错误。
*   **泛化能力**：通过反事实提示生成的多样化数据，有效地帮助智能体学习到了能够适应新指令和新环境动态的决策策略。

#### 7. 优点（亮点）
*   **视觉-语言融合**：成功将 VLM 的感知能力引入轨迹生成，解决了纯语言模型在物理模拟中的“幻觉”问题。
*   **数据增强策略**：反事实提示提供了一种低成本、高效率的方式来扩展离线数据集的边界。
*   **算法兼容性**：VLGOR 作为一个即插即用的框架，可以轻松集成到现有的各种离线 RL 算法中。

#### 8. 不足与局限
*   **环境复杂度**：目前的实验主要集中在相对简单的机器人仿真环境（CLEVR, Meta-World），在更复杂、高维或真实世界的场景中的表现尚待验证。
*   **计算负载**：VLGOR 需要同时预测状态和动作，这增加了模型的学习负担。
*   **联合学习挑战**：模型需要同时学习环境动力学和模仿策略，未来可能需要更先进的逐步推理策略来确保状态与动作的一致性。

（完）
