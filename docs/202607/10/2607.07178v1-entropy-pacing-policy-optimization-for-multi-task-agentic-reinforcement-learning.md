---
title: Entropy Pacing Policy Optimization for Multi-Task Agentic Reinforcement Learning
title_zh: 面向多任务智能体强化学习的熵步调策略优化
authors: "Zetian Hu, Shunyu Liu, Junjie Zhang, Yongcheng Jing, Ting-En Lin, Yongbin Li, Dacheng Tao"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07178v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 多任务智能体强化学习的前沿研究
tldr: 本研究针对多任务大语言模型智能体在强化学习中的探索-利用节奏不匹配问题，提出了熵步调策略优化（EPPO）。研究发现，简单任务过早收敛和困难任务的持续探索会产生熵交叉与波动，干扰整体优化。EPPO引入了任务感知的动态剪切机制，取代了GRPO中的固定阈值，通过自适应调整更新边界来协调各任务的熵水平。实验证明，EPPO在多任务基准测试中显著优于现有方法，为构建通用智能体提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07178v1/fig-001.webp\", \"caption\": \"Table 1: Main results on five-task multi-task training. We report the average success rate over four evaluations. Our reproduced results for AgentRL differ slightly from those reported in the original paper; a detailed analysis of the discrepancy is provided in Appendix C. ALF denotes ALFWorld.\", \"page\": 7, \"index\": 1, \"width\": 827, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2607.07178v1/fig-002.webp\", \"caption\": \"Figure 7: Task-wise entropy dynamics under different ablation settings.\", \"page\": 18, \"index\": 2, \"width\": 816, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2607.07178v1/fig-003.webp\", \"caption\": \"Figure 1: Task-wise policy entropy dynamics during multi-task agentic training. (a) The AgentRL exhibits pace mismatch, characterized by asynchronous entropy collapse with frequent inter-task crossovers and late-stage entropy spikes. (b) EPPO organizes task entropies into stable, persistent bands with fewer crossovers and suppressed spikes. (c) Quantitative diagnostics of entropy instability: crossover count sums pairwise sign-flip events (lower is better); trend R2 is the average coefficient of determination from a linear fit of task entropy versus log t (higher is better); late spike amplitude is the maximum upward entropy jump in the last 30% of training (lower is better).\", \"page\": 4, \"index\": 3, \"width\": 816, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2607.07178v1/fig-004.webp\", \"caption\": \"Figure 2: EPPO overview. Per-task entropy is tracked with EMA and converted to a normalized collapse ratio; a cohort-level pacer maps relative pace to task-wise dynamic clipping ϵi(t), while a trend constraint downscales updates when entropy rises to improve stability.\", \"page\": 5, \"index\": 4, \"width\": 822, \"height\": 295}]"
motivation: 多任务强化学习中不同任务间的探索与利用节奏不一致，导致任务间熵值波动和训练不稳定。
method: 提出EPPO算法，利用任务熵感知的动态剪切机制，为不同探索阶段的任务提供自适应的更新约束。
result: 在多任务智能体基准测试中，EPPO的性能表现优于包括GRPO在内的现有主流强化学习算法。
conclusion: 通过动态协调各任务的熵步调，EPPO有效解决了多任务学习中的干扰问题，提升了通用智能体的训练效率和稳定性。
---

## 摘要
强化学习（RL）最近的突破凸显了其在复杂智能体大语言模型（LLM）任务中的潜力。然而，现有的研究主要集中在单任务设置上，而实际部署则需要能够同时解决多个任务的通用智能体。在这项工作中，我们发现了多任务智能体强化学习中一个关键但尚未得到充分探索的现象：不同任务可能表现出探索与利用步调不匹配。具体而言，较简单的任务可能会过早收敛到低熵策略，从而阻碍较难任务的学习；而较难的任务反过来又可能将较简单的任务推回高熵探索状态。这种反复的相互作用导致了任务间的熵交叉和频繁的熵峰值。受此观察启发，我们为多任务智能体大语言模型引入了熵步调策略优化（EPPO），通过协调各任务间的熵来稳定多任务优化过程。EPPO 的核心是一种任务级动态裁剪机制，它将群体相对策略优化（GRPO）中的固定裁剪阈值替换为任务熵感知的自适应边界，对过度自信的任务收紧更新，而对探索不足的任务放宽更新。在多任务智能体基准测试上的实验表明，所提出的 EPPO 取得了优于同类方法的结果。

## Abstract
Recent breakthroughs of Reinforcement Learning (RL) have highlighted its potential for complex agentic Large Language Model (LLM) tasks. However, existing efforts largely focus on single-task settings, whereas real-world deployment necessitates a generalist agent capable of solving multiple tasks simultaneously. In this work, we identify a critical yet underexplored phenomenon in multi-task agentic RL: different tasks can exhibit exploration-exploitation pace mismatch. Specifically, easier tasks may converge early to low-entropy policies that hinder learning on harder tasks, while harder tasks can, in turn, push easier tasks back toward high-entropy exploration. This back-and-forth interaction creates inter-task entropy crossovers and frequent entropy spikes. Inspired by this observation, we introduce Entropy Pacing Policy Optimization (EPPO) for multi-task agentic LLMs, which coordinates entropy across tasks to stabilize multi-task optimization. At the core of EPPO is a task-wise dynamic clipping mechanism that replaces the fixed clipping threshold in Group Relative Policy Optimization (GRPO) with a task entropy-aware adaptive bound, tightening updates for over-confident tasks while relaxing them for under-explored ones. Experiments on the multi-task agentic benchmarks demonstrate that the proposed EPPO yields results superior to its counterparts.

---

## 论文详细总结（自动生成）

这篇论文针对多任务智能体强化学习（Multi-Task Agentic RL）中的训练不稳定问题，提出了**熵步调策略优化（EPPO）**算法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
在训练能够处理多种任务（如操作系统操作、数据库查询、网页购物等）的通用大模型智能体时，研究者发现了一个关键现象：**探索-利用步调不匹配（Pace Mismatch）**。
*   **现象描述**：简单任务会迅速收敛至低熵状态（过度自信），而困难任务仍需维持高熵进行探索。
*   **负面影响**：在共享参数的联合训练中，已收敛任务的梯度会抑制困难任务的探索，而困难任务的剧烈更新又会破坏简单任务的稳定性，导致任务间出现频繁的“熵交叉”和后期“熵峰值”，严重阻碍多任务性能的提升。

### 2. 方法论：核心思想与技术细节
EPPO 的核心思想是**将 GRPO 算法中的全局固定裁剪阈值（$\epsilon$）替换为任务感知的动态裁剪边界**，通过调节更新步长来协调不同任务的进度。其流程包含三个模块：

*   **熵追踪器（Entropy Tracker）**：
    *   使用指数移动平均（EMA）实时追踪每个任务的策略熵。
    *   计算“熵坍缩率”（Entropy Collapse Ratio），即当前熵与初始熵的比值，使其在不同任务间具有可比性。
*   **进度步调器（Progress Pacer）**：
    *   计算各任务熵坍缩率的 Z-score（标准化得分），衡量该任务相对于整体进度的快慢。
    *   **动态调整**：若任务进度过快（Z-score 低），则缩小裁剪范围 $\epsilon$，限制其更新；若进度落后（Z-score 高），则放大 $\epsilon$，允许更激进的学习。
*   **稳定性趋势约束（Stability Trend Constraint）**：
    *   检测熵的变化趋势。如果某个任务的熵异常上升（出现震荡），则通过 ReLU 函数触发惩罚机制，强制收紧裁剪范围，防止策略发生剧烈偏移。

### 3. 实验设计
*   **数据集/场景**：采用 **AgentBench** 基准，涵盖 5 类交互式任务：操作系统（OS）、数据库（DB）、知识图谱（KG）、ALFWorld（文字游戏）和 WebShop（网页购物）。
*   **基准测试（Benchmark）**：
    *   **API 模型**：GPT-5、Claude-Sonnet-4、o3-mini 等。
    *   **开源模型**：Qwen2.5 系列、DeepSeek-V3/R1、AgentLM 等。
    *   **RL 算法对比**：**AgentRL**（主要基准）、DCPO（动态裁剪）、BAPO（平衡策略优化）。
*   **评估指标**：任务成功率（Success Rate）、熵动力学指标（交叉次数、趋势一致性、峰值振幅）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H20 GPU** 进行实验。
*   **模型架构**：以 **Qwen2.5-3B-Instruct** 作为基础策略模型。
*   **训练细节**：训练时长约 700 个 step，每个 batch 包含 128 个 prompt，每个 prompt 进行 8 次采样（rollouts）。
*   **对比说明**：作者指出其硬件环境（H20）与 AgentRL 原始论文（16 台 H800）存在差异，因此在复现时对 batch size 进行了适配。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **5 任务联合训练**和 **3 任务联合训练**（不同难度组合）。
    *   在不同规模的底座模型（0.5B, 3B, 8B）上验证了泛化性。
    *   进行了**通用推理能力迁移测试**（MATH500, GSM8K 等），验证智能体训练是否损害基础能力。
*   **消融实验**：分别去掉了“任务级动态裁剪”和“趋势约束”，证明了两个组件对稳定熵动力学的必要性。
*   **评价**：实验设计较为全面，不仅关注最终成功率，还通过熵曲线深入分析了训练过程的稳定性，对比了多种 SOTA 模型，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：EPPO 在 5 任务平均成功率上达到 54.3%，显著优于 AgentRL（51.6%）和其他自适应裁剪方法。
*   **训练稳定性**：EPPO 成功将各任务的熵演化组织成稳定的“带状分布”，大幅减少了任务间的熵交叉，并抑制了训练后期的不稳定性峰值。
*   **能力保持**：在提升智能体交互能力的同时，EPPO 在数学推理（GSM8K）等任务上也表现出更好的性能保持或提升。

### 7. 优点
*   **针对性强**：精准捕捉到了多任务 RL 中“步调不匹配”这一痛点，并给出了直观的物理解释。
*   **即插即用**：EPPO 仅需轻量级的统计计算，无需训练额外的 Critic 网络或复杂的奖励模型，易于集成到现有的 GRPO 框架中。
*   **理论支撑**：附录中提供了关于动态裁剪边界一致性和稳定性的理论分析。

### 8. 不足与局限
*   **任务多样性有限**：虽然涵盖了 5 类任务，但对于更大规模（如数十个任务）的异构环境，熵作为唯一步调信号的鲁棒性仍有待验证。
*   **超参数敏感性**：步调强度 $\alpha$ 等参数需要根据任务组合进行微调，虽然文中做了敏感性分析，但在自动化调优方面尚有提升空间。
*   **硬件依赖**：由于 H20 显存和带宽限制，实验未能完全对齐 H800 的超大 batch 训练效果，可能存在一定的性能天花板。

（完）
