---
title: "ASK in the Dark: Uncertainty-Gated LLM Assistance under Partial Observability"
title_zh: 黑暗中的询问：部分可观测性下基于不确定性门控的大语言模型辅助
authors: "Juarez Monteiro, Nathan Gavenski, Guilherme Lima, Francisco Galuppo, Odinaldo Rodrigues, Adriano Veloso"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02686v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 将小语言模型引导集成到强化学习智能体中
tldr: 本研究针对部分可观测环境（POMDP）下强化学习代理决策难的问题，提出ASK+框架。传统方法因缺乏上下文导致小语言模型（SLM）辅助效果不佳，ASK+通过引入轨迹感知上下文（如部分地图、动作历史）和结构化思维链，将SLM从被动检查者转变为能主动纠偏的顾问。实验证明，该方法利用预测熵实现有效的不确定性门控，显著提升了代理在复杂任务中的表现，且小规模模型即可达到优异效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02686v1/fig-001.webp\", \"caption\": \"Table 2 ASK+ prompt ablation across the three environments (we keep 𝜏 fixed accross prompt strategies for consistency).\", \"page\": 6, \"index\": 1, \"width\": 944, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2607.02686v1/fig-002.webp\", \"caption\": \"Figure 2: Threshold 𝜏 for reward and IR effects, with the selected 𝜏 for each model marked with a line.\", \"page\": 7, \"index\": 2, \"width\": 742, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2607.02686v1/fig-003.webp\", \"caption\": \"Table 1\", \"page\": 5, \"index\": 3, \"width\": 944, \"height\": 242}]"
motivation: 解决在部分可观测强化学习任务中，由于提示词缺乏上下文导致小语言模型无法有效辅助代理决策的问题。
method: 提出ASK+，通过轨迹感知上下文和结构化思维链增强SLM推理，并利用预测熵作为不确定性门控信号。
result: "在多个基准测试中显著提升了成功率，例如在FourRooms任务中从53%提升至70%，且2B模型表现优于4B模型。"
conclusion: 提示词设计和选择性门控机制比模型规模更关键，使得在部分可观测环境下利用轻量级模型进行高效辅助成为可能。
---

## 摘要
在部分可观测性下运行的强化学习智能体必须根据不完整的信息采取行动，这使得它们成为携带广泛推理先验的小语言模型（SLM）提供指导的天然候选者。然而，将 SLM 指导集成到这种设置中已被证明是困难的：在所有测试环境中，原始的基于不确定性门控的方法实现的覆盖率（overwrite rate）接近于零，这意味着 SLM 几乎从未贡献过独立的动作。我们将这一失败归因于纯粹的自我中心提示词（egocentric prompt），它为真正的推理提供了不足的上下文，并将其识别为上下文问题而非能力问题。我们提出了 ASK+，它为 SLM 提供轨迹感知上下文（部分揭示的地图、访问过的位置和动作历史）以及结构化的思维链推理，将其从被动的冗余检查转变为更具信息量的顾问，偶尔纠正策略。我们进一步确定，用于选择性查询的预测熵信号衡量的是动作不确定性而非状态不确定性，并且在部分可观测马尔可夫决策过程（POMDP）中仍然具有信息量，使得不确定性门控辅助在完全可观测设置之外也变得可行。有状态的提示词驱动了显著的提升：在 DoorKey 任务中，原始 ASK 与 PPO 持平（均为 89%），而 ASK+ 达到了 93% 的成功率；在 FourRooms 中，成功率从 53% 攀升至 70%；在 HigherLower 中，准确率达到 73.7%，达到了仅使用 SLM 的上限。在所有环境中，Qwen3.5-2B 的表现与 Qwen3.5-4B 持平或更优，证实了提示词设计和选择性门控的影响超过了模型规模，从而实现了无需大模型的指导。

## Abstract
Reinforcement learning agents operating under partial observability must act on incomplete information, making them natural candidates for guidance from small language models (SLMs) that carry broad reasoning priors. Yet integrating SLM guidance into this setting has proven difficult: across all test environments, vanilla uncertainty-gated approaches achieve an overwrite rate at or near zero, meaning the SLM almost never contributes an independent action. We trace this failure to the bare egocentric prompt, which provides insufficient context for genuine reasoning, and identify it as a context problem rather than a capacity problem. We propose ASK+, which supplies the SLM with trajectory-aware context (a partially revealed map, visited positions, and action history) and structured chain-of-thought reasoning, converting it from a passive redundancy check into a more informative consultant that occasionally corrects the policy. We further establish that the predictive entropy signal used for selective querying measures action uncertainty rather than state uncertainty and remains informative in POMDPs, making uncertainty-gated assistance viable beyond fully observable settings. The stateful prompt drives substantial gains: on DoorKey, where vanilla ASK matches PPO (both 89%), ASK+ reaches 93% success; on FourRooms, success climbs from 53% to 70%; on HigherLower, accuracy reaches 73.7%, matching the SLM-only upper bound. Across all environments, Qwen3.5-2B matches or exceeds Qwen3.5-4B, confirming that prompt design and selective gating dominate the impact of model scale, enabling guidance without large models.

---

## 论文详细总结（自动生成）

以下是对论文《ASK in the Dark: Uncertainty-Gated LLM Assistance under Partial Observability》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在部分可观测环境（POMDP）中，强化学习（RL）智能体仅能获得局部视图，容易因信息不足而陷入决策困境。虽然大语言模型（LLM）具备推理先验，但直接将其引入 RL 存在挑战。
*   **核心问题**：现有的“不确定性门控”方法（如 ASK 框架）在 POMDP 中表现不佳，其“覆盖率”（SLM 纠正 PPO 的频率）几乎为零。
*   **研究动机**：作者发现失败的原因并非 SLM 能力不足，而是**上下文瓶颈**——仅提供当前的局部观察（自我中心提示词）不足以支撑 SLM 进行有效推理。

### 2. 方法论：ASK+ 框架
*   **核心思想**：通过为小语言模型（SLM）提供**轨迹感知上下文**和**结构化思维链**，将其从被动的冗余检查者转变为能够主动纠偏的“顾问”。
*   **关键技术细节**：
    *   **不确定性估计**：使用 **MC Dropout** 对 PPO 策略进行 $N$ 次随机前向传递，计算预测熵 $H(o)$。当熵值超过阈值 $\tau$ 时，触发 SLM 查询。
    *   **有状态提示词（Stateful Prompting）**：
        1.  **丰富化（Enriched）**：增加环境特征（如前方路径深度、可见门位置、动作预览）。
        2.  **有状态（Stateful）**：注入情节记忆，包括部分揭示的地图、已访问位置计数和动作历史。
        3.  **推理（Rationale）**：激活 SLM 的“思考模式”，在输出动作前分配 10 个 token 的预算进行思维链推理。
*   **决策流程**：PPO 产生默认动作 $\rightarrow$ 计算不确定性 $\rightarrow$ 若不确定性高，则构建包含全局上下文的提示词查询 SLM $\rightarrow$ SLM 动作覆盖 PPO 动作。

### 3. 实验设计
*   **实验场景**：
    1.  **MiniGrid-FourRooms**：测试空间定位和局部视野下的导航。
    2.  **MiniGrid-DoorKey-8x8**：测试序列化子任务分解（找钥匙、开门、到达终点）。
    3.  **POPGym-HigherLower**：测试基于历史卡牌记忆的推理。
*   **对比基准（Benchmarks）**：
    *   **PPO-only**：纯强化学习基准。
    *   **SLM-only**：完全由小模型决策（2B 和 4B 版本）。
    *   **ASK**：原始的基于局部观察的门控方法。
*   **对比模型**：使用了 Qwen3.5-2B 和 Qwen3.5-4B。

### 4. 资源与算力
*   **训练细节**：PPO 策略使用 Stable-Baselines3 实现，训练了 $2 \times 10^6$ 步。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。但提到 SLM 推理是步级的（step-level），且通过使用小规模模型（2B/4B）和限制推理 token 数（10个）来确保计算效率。
*   **阈值优化**：使用 Optuna 框架进行贝叶斯优化，以自动寻找最佳的不确定性阈值 $\tau$。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个具有代表性的 POMDP 环境中进行了测试，每个环境评估了 100 个测试回合（种子点 100-199）。
*   **消融实验**：非常充分。作者分别针对**提示词组件**（基础 vs 丰富 vs 有状态 vs 推理）、**阈值 $\tau$ 的灵敏度**、以及 **PPO 策略的次优程度**（不同训练阶段的 Checkpoint）进行了详细对比。
*   **客观性**：通过验证集调整超参数，测试集保持独立，实验设计符合学术规范。

### 6. 主要结论与发现
*   **性能提升**：ASK+ 在所有环境中均优于 PPO 和原始 ASK。例如，在 FourRooms 中成功率从 53% 提升至 70%。
*   **覆盖率突破**：ASK+ 实现了非零的覆盖率（1%-6%），证明 SLM 确实在提供独立且正确的指导，而不仅仅是重复 PPO 的决定。
*   **规模非关键论**：**Qwen3.5-2B 的表现往往匹配甚至超过 4B 模型**。这表明在 POMDP 中，**提示词设计和上下文质量**比模型参数规模更重要。
*   **信号有效性**：证实了预测熵在部分可观测环境下依然是可靠的门控信号，能够识别出策略因信息缺失而产生的犹豫。

### 7. 优点
*   **诊断精准**：准确识别了 LLM-RL 结合在 POMDP 中的“上下文瓶颈”问题。
*   **高效性**：通过选择性查询（仅在不确定时询问）和小模型（2B），在提升性能的同时控制了推理成本。
*   **即插即用**：无需对预训练的 SLM 或已训练的 PPO 进行微调，具有很强的通用性。
*   **鲁棒性**：实验证明即使 PPO 策略处于训练初期的次优状态，ASK+ 也能通过频繁干预显著提升表现。

### 8. 不足与局限
*   **环境多样性有限**：虽然涵盖了空间、序列和记忆任务，但仍局限于离散动作空间的网格世界，尚未在连续控制或更复杂的 3D 环境中验证。
*   **信息对称性假设**：在某些任务（如 HigherLower）中，SLM 提示词中包含的信息实际上超过了 PPO 观察到的信息，这种“信息不对称”可能夸大了 SLM 的贡献。
*   **历史过时风险**：在动态环境中，如果 SLM 依赖的“已访问地图”信息过时，可能会导致错误的推理，论文对此讨论较少。

（完）
