---
title: "ASK in the Dark: Uncertainty-Gated LLM Assistance under Partial Observability"
authors: "Juarez Monteiro, Nathan Gavenski, Guilherme Lima, Francisco Galuppo, Odinaldo Rodrigues, Adriano Veloso"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02686v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 在部分可观测条件下将小语言模型引导集成到强化学习智能体中
tldr: 针对部分可观测环境（POMDP）下强化学习智能体决策困难的问题，本文提出了ASK+框架。该框架通过为小语言模型（SLM）提供包含轨迹感知的上下文（如部分地图、历史动作）和结构化思维链，解决了传统不确定性门控方法中SLM贡献率低的问题。实验证明，ASK+能有效利用SLM纠正策略错误，在多个任务中显著提升了成功率，且小模型表现优于大模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02686v1/fig-001.webp\", \"caption\": \"Table 2 ASK+ prompt ablation across the three environments (we keep 𝜏 fixed accross prompt strategies for consistency).\", \"page\": 6, \"index\": 1, \"width\": 944, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2607.02686v1/fig-002.webp\", \"caption\": \"Figure 2: Threshold 𝜏 for reward and IR effects, with the selected 𝜏 for each model marked with a line.\", \"page\": 7, \"index\": 2, \"width\": 742, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2607.02686v1/fig-003.webp\", \"caption\": \"Table 1\", \"page\": 5, \"index\": 3, \"width\": 944, \"height\": 242}]"
motivation: 传统的不确定性门控方法在部分可观测环境下因提示词缺乏上下文信息，导致语言模型无法提供有效的独立决策建议。
method: 提出ASK+方法，通过向SLM提供轨迹感知上下文（地图、位置、历史）和结构化思维链，并利用预测熵作为查询信号。
result: 在DoorKey、FourRooms等任务中显著提升了成功率，且2B规模的模型通过优化提示词达到了与4B模型相当甚至更好的效果。
conclusion: 良好的提示词设计和选择性门控机制比模型规模更重要，使小语言模型也能在复杂的部分可观测任务中提供有效指导。
---

## Abstract
Reinforcement learning agents operating under partial observability must act on incomplete information, making them natural candidates for guidance from small language models (SLMs) that carry broad reasoning priors. Yet integrating SLM guidance into this setting has proven difficult: across all test environments, vanilla uncertainty-gated approaches achieve an overwrite rate at or near zero, meaning the SLM almost never contributes an independent action. We trace this failure to the bare egocentric prompt, which provides insufficient context for genuine reasoning, and identify it as a context problem rather than a capacity problem. We propose ASK+, which supplies the SLM with trajectory-aware context (a partially revealed map, visited positions, and action history) and structured chain-of-thought reasoning, converting it from a passive redundancy check into a more informative consultant that occasionally corrects the policy. We further establish that the predictive entropy signal used for selective querying measures action uncertainty rather than state uncertainty and remains informative in POMDPs, making uncertainty-gated assistance viable beyond fully observable settings. The stateful prompt drives substantial gains: on DoorKey, where vanilla ASK matches PPO (both 89%), ASK+ reaches 93% success; on FourRooms, success climbs from 53% to 70%; on HigherLower, accuracy reaches 73.7%, matching the SLM-only upper bound. Across all environments, Qwen3.5-2B matches or exceeds Qwen3.5-4B, confirming that prompt design and selective gating dominate the impact of model scale, enabling guidance without large models.

---

## 论文详细总结（自动生成）

这篇论文《ASK in the Dark: Uncertainty-Gated LLM Assistance under Partial Observability》探讨了在部分可观测环境（POMDP）下，如何通过小语言模型（SLM）有效地辅助强化学习（RL）智能体。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在部分可观测环境（如视野受限的迷宫）中，RL智能体常因信息不足而陷入决策困境。虽然语言模型（LM）具备强大的推理先验，但现有的集成方法（如ASK框架）在这些环境下表现不佳。
*   **核心问题**：作者发现，传统方法在POMDP中的“覆盖率”（Overwrite Rate，即SLM纠正RL动作的频率）几乎为零。这并非因为模型能力不足，而是因为提供给SLM的提示词（Prompt）仅包含当前的局部视野，缺乏全局上下文，导致SLM无法做出比RL策略更好的判断。
*   **整体含义**：论文旨在通过增强提示词的上下文信息，将SLM从一个被动的“冗余检查员”转变为一个主动的“决策顾问”。

### 2. 方法论：ASK+ 框架
*   **核心思想**：利用不确定性门控机制，仅在RL策略不确定时请求SLM协助，并为SLM提供丰富的轨迹感知上下文。
*   **关键技术细节**：
    *   **不确定性估计**：使用 **MC Dropout** 对PPO策略进行多次随机前向传播，计算预测熵 $H(o)$。当熵值超过阈值 $\tau$ 时，触发SLM查询。
    *   **状态化提示词（Stateful Prompting）**：这是ASK+的核心改进，包含三个维度：
        1.  **丰富化（Enriched）**：加入派生的环境特征（如到墙壁的距离、动作预览）。
        2.  **状态化（Stateful）**：加入情节记忆，包括部分揭示的地图、已访问位置和动作历史。
        3.  **推理化（Rationale）**：激活SLM的“思考模式”，在输出动作前分配10个Token的预算进行思维链（CoT）推理。
*   **算法流程**：RL智能体执行 -> 计算不确定性 -> 若高不确定性，构建包含历史和地图的复杂Prompt -> SLM推理并给出建议动作 -> 覆盖或执行原动作。

### 3. 实验设计
*   **实验场景**：
    1.  **MiniGrid-FourRooms**：测试空间定位能力。
    2.  **MiniGrid-DoorKey-8x8**：测试序列化子任务分解能力（找钥匙、开门、到达目标）。
    3.  **POPGym-HigherLower**：测试时间记忆能力（扑克牌比大小，需记住已出的牌）。
*   **对比方法（Benchmarks）**：
    *   **PPO-only**：纯强化学习基准。
    *   **SLM-only**：仅使用语言模型决策。
    *   **Vanilla ASK**：原始的不确定性门控方法（仅提供局部视野）。
    *   **ASK+ (本文方法)**：使用不同规模的SLM（Qwen3.5-2B 和 4B）。

### 4. 资源与算力
*   **模型使用**：使用了 Qwen3.5-2B 和 Qwen3.5-4B 变体。
*   **训练细节**：PPO策略在每个环境中独立训练了 $2 \times 10^6$ 步。
*   **算力说明**：文中**未明确说明**具体的GPU型号、数量或总训练时长。但提到SLM无需微调（Zero-shot），且通过Optuna进行超参数（阈值 $\tau$）搜索。

### 5. 实验数量与充分性
*   **实验规模**：每个环境评估了100个测试集回合，并使用另外100个种子进行验证集调优。
*   **消融实验**：非常充分。作者分别测试了提示词组件（基础 vs 丰富 vs 状态 vs 推理）、不确定性阈值 $\tau$ 的敏感性，以及在不同训练阶段（亚优策略）下ASK+的鲁棒性。
*   **客观性**：通过对比不同规模的模型和多种基准方法，证明了“提示词设计优于模型规模”的观点，实验设计较为公正。

### 6. 主要结论与发现
*   **性能提升**：ASK+在所有任务中均优于PPO和原始ASK。在FourRooms中成功率从53%提升至70%；在DoorKey中提升至93%。
*   **上下文至上**：原始ASK失败的原因是提示词瓶颈。一旦提供了轨迹感知上下文，SLM的独立决策能力（覆盖率）显著提升。
*   **模型规模不重要**：**Qwen3.5-2B 的表现匹配甚至超过了 4B 版本**。这表明对于此类任务，合理的提示词工程和门控机制比单纯增加参数量更有效。
*   **不确定性信号有效**：证明了预测熵在部分可观测环境下依然是可靠的触发信号。

### 7. 优点
*   **即插即用**：无需对RL策略或SLM进行重新训练或微调，具有很强的通用性。
*   **效率平衡**：通过不确定性门控，仅在必要时调用SLM，降低了计算开销（干预率通常较低）。
*   **诊断深入**：准确识别了POMDP中SLM辅助失效的根本原因是“上下文缺失”而非“模型能力不足”。

### 8. 不足与局限
*   **环境复杂度**：实验仍集中在网格世界和简单的卡牌游戏，尚未在更复杂的3D环境或真实机器人任务中验证。
*   **提示词依赖**：ASK+的效果高度依赖于手动设计的环境特征（如地图聚合、动作预览），这在更复杂的任务中可能难以泛化。
*   **延迟问题**：虽然减少了调用次数，但SLM的推理延迟在实时性要求极高的RL场景中仍可能是一个挑战。

（完）
