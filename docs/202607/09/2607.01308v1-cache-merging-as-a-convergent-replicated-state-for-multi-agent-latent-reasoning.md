---
title: Cache Merging as a Convergent Replicated State for Multi-Agent Latent Reasoning
title_zh: 缓存合并作为多智能体潜空间推理的收敛复制状态
authors: "Carlos Baquero, Luís Brito"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01308v1"
tags: ["query:ag-rl"]
score: 6.5
evidence: 多智能体潜空间推理与KV缓存合并技术
tldr: 针对多智能体潜在推理中KV缓存合并顺序敏感且不可预测的问题，本文提出CanonicalMerge方法。该方法通过中间层K-范数均值对缓存进行内容寻址排序，将合并过程转化为收敛复制状态（CvRDT），实现了合并结果的交换律与幂等性。实验证明，该方法在无需预知最优顺序的情况下，性能比肩最优排序的基准方法，且显著优于输出层融合方案，为多智能体协作提供了结构性保障。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01308v1/fig-001.webp\", \"caption\": \"Figure 1: The two evaluation regimes. The KV-cache flow (solid arrows), thinkers → Merge → Judger → answer, is identical across regimes; only the routing of the global query Q (dashed arrows) differs. In (a) query-known, Q is broadcast to both thinkers and the judger, so each thinker encodes its fragment ti conditioned on the query. In (b) query-blind (federated), Q reaches only the judger, and each thinker encodes its fragment offline without seeing the query. The same merge operator and the same 100 partitionedk = 2 problems are used in both regimes; the difference shifts the K-norm content signature each thinker produces (Section 5.2).\", \"page\": 6, \"index\": 1, \"width\": 496, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-002.webp\", \"caption\": \"Table 3: Headline matrix on partitioned k = 2, 100 problems each. Best BagMerge ordering per column in bold. The best BagMerge row records which fixed π achieves it; the ∆ row records the CanonicalMerge accuracy minus the best BagMerge accuracy in the same column.\", \"page\": 10, \"index\": 2, \"width\": 978, \"height\": 606}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-003.webp\", \"caption\": \"Table 5: Routing-layer sensitivity for CanonicalMerge on partitioned k = 2 (query-known, 40 latent steps, 100 problems).\", \"page\": 13, \"index\": 3, \"width\": 640, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-004.webp\", \"caption\": \"Table 11: The k = 3 topology probe, on the two families that mark the poles of the effect (Qwen3-1.7B, greedy; across-draw mean ± sd over 3 independent problem draws, data seeds {42, 7, 100}, n = 50/family/draw; full five-family results in Section F). Both problems are near-perfectly solvable from full text (C0 ≈ 1.00), so every latent gap is composition, not difficulty. A flat 3-way merge (par3) and a pairwise merge tree (treemerge) both collapse; introducing model reprocessing at one seam (mix) or throughout (seq3) recovers it.\", \"page\": 19, \"index\": 4, \"width\": 982, \"height\": 1023}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-005.webp\", \"caption\": \"Table 4: Paired McNemar tests on the headline matrix, n = 100 per pair. Significant at α = 0.05 in bold. A multi-seed replication (3 draws, n = 300 pooled) with paired bootstrap CIs and Holm correction is in Section B.\", \"page\": 12, \"index\": 5, \"width\": 782, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-006.webp\", \"caption\": \"Table 15: Full five-family k = 3 topology probe (across-draw mean ± sd, 3 draws, greedy, n = 50). treemerge never exceeds par3 on any of the five families (colocation is not composition): it matches par3 on the chains and on recipe, and on discount falls below it (0.11 vs. 0.23), so the pairwise merge tree can even lose relative to the flat merge. The two chain families collapse under par3 and recover under reprocessing; the symmetric families span a composition-limited pole (constraint, where no topology helps), through recipe (whose 3-way merge already mostly works), to a gradient (discount). relay’s seq3 < mix is an ordering artifact: its default chain places the non-commutative pipeline’s input last.\", \"page\": 27, \"index\": 6, \"width\": 983, \"height\": 732}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-007.webp\", \"caption\": \"Table 13: Per-problem paired bootstrap (10 000 resamples, n = 300 pooled across the three draws), exact McNemar p, and Holm-corrected decision over the seven-contrast family. CanonicalMerge ties the best ordering in each regime (CM−BGSW query-known and CM−BG query-blind both straddle zero) and beats the worse ordering (+0.063, Holm-significant); BagMerge’s order-sensitivity (BG− BGSW) is significant and reverses sign between regimes.\", \"page\": 24, \"index\": 7, \"width\": 677, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-008.webp\", \"caption\": \"Table 12: Per-cell accuracy across three problem draws (Qwen3-1.7B, k = 2, greedy, n = 100 per draw; pooled 95% Wilson CI at n = 300). Per-seed values are ordered by data seed 42/7/100. CanonicalMerge and the best-of-two BagMerge ordering coincide in each regime (swap in query-known, default in query-blind), and the low across-draw s.d. shows the numbers are draw-stable except for query-blind CanonicalMerge, whose single-draw swing motivates the paired analysis below.\", \"page\": 24, \"index\": 8, \"width\": 740, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-009.webp\", \"caption\": \"Figure 2: Four ways to combine three latent traces at k = 3, a topology schematic, not an algebraic identity (accuracies in Table 11). Solid nodes (⊎) are cache-only render merges (non-idempotent; not the idempotent set-union state join ⊔ of Section 4.4, which we reserve for LatentFragmentSet). Dashed edges are model-mediated reprocessing seams, where one thinker runs with another’s KV cache as a prefix. The cache-only flat (a) and tree (b) variants both fail and collapse to the same accuracy (the treemerge ≈ par3 result), while the variants that add reprocessing ((c), (d)) recover on chain-structured tasks. The topologies differ in more than edge type, they also change which thinker reprocesses which prefix and the amount of model computation, so we read the figure as locating where reprocessing enters, not as a one-variable causal claim.\", \"page\": 20, \"index\": 9, \"width\": 637, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2607.01308v1/fig-010.webp\", \"caption\": \"Table 1: Family schemas with one example shown per family; 20 problems per family vary the surface numerical values while preserving the schema.\", \"page\": 5, \"index\": 10, \"width\": 822, \"height\": 209}]"
motivation: 现有的多智能体KV缓存合并方法受输入顺序影响大且最优顺序难以预测，缺乏一致性保障。
method: 提出CanonicalMerge，利用中间层K-范数均值对缓存片段进行确定性排序，并将其建模为满足交换律和幂等性的状态型CvRDT。
result: 在分区推理和HotpotQA任务中，该方法在无需搜索最优顺序的情况下达到了与最佳排序相当的精度，且大幅领先于输出融合基准。
conclusion: 将缓存合并视为收敛复制状态解决了顺序依赖问题，为多智能体潜在推理提供了稳健的结构化通信框架。
---

## 摘要
多智能体潜空间推理将多个智能体的 KV 缓存组合成一个上下文，供最终智能体使用。先前的工作（Agent Primitives）通过沿序列轴拼接缓存并进行 RoPE 重新编码来实现这一点，我们称之为 BagMerge。BagMerge 是非交换的，且最佳输入顺序不可预测，会随实验方案、潜空间步数预算和模型规模而变化。我们将这种交换转化为一种收敛复制状态。首先，CanonicalMerge 根据内容固定布局：通过中间层的平均 K 范数对缓存进行排序，使得合并后的缓存在任何输入排列下都保持字节一致，这已在算法上（元数 N<=5）以及真实的 Qwen3-1.7B 和 4B 状态上得到了逐位验证。其次，我们将复制状态与解码时布局分离：该状态是一组内容寻址的潜空间片段，其合并方式为集合并集，即一种基于状态的 CvRDT（具有交换性、结合性、幂等性和吸收性），而 CanonicalMerge 是其确定性渲染。由于渲染是字节等效的，所有 N=2 的准确率数值都保持不变，且重新交付的重复项会被吸收而非重新拼接。在分区推理基准测试中，CanonicalMerge 在无需预知最佳顺序的情况下，在每个“方案-预算-顺序”单元中都达到了与最佳 BagMerge 顺序相当的水平，以微小且在统计上不显著的准确率差距换取了无条件的结构性保证。这种行为可以迁移到真实的多文档问答（HotpotQA）中，而在相同预算下，最接近的免训练输出融合基准（PackLLM）落后了 45 分，这表明缓存级合并与输出级融合属于完全不同的范畴。最后，在 k>2 时，该方法传输并共置了潜空间轨迹，但其本身并不对它们进行组合，我们对此进行了表征以启发未来的工作。

## Abstract
Multi-agent latent reasoning composes agents' KV-caches into one context for a final agent. Prior work (Agent Primitives) does this by concatenating caches along the sequence axis with RoPE re-encoding, which we call BagMerge. BagMerge is non-commutative, and the best input ordering is unpredictable, shifting with the regime, the latent-step budget, and the model scale. We make this exchange a convergent replicated state. First, CanonicalMerge fixes the layout by content: ordering caches by mean K-norm at a middle layer renders the merged cache byte-identical under any input permutation, verified algorithmically (arity N<=5) and bit-for-bit on real Qwen3-1.7B and 4B state. Second, we separate the replicated state from decode-time layout: the state is a set of content-addressed latent fragments whose merge is set union, a state-based CvRDT (commutative, associative, idempotent, absorbing), and CanonicalMerge is its deterministic render. Because the render is byte-equivalent, every N=2 accuracy number carries over unchanged and re-delivered duplicates are absorbed rather than re-concatenated. On a partitioned-reasoning benchmark, CanonicalMerge matches the best BagMerge ordering in every regime-by-budget-by-ordering cell without knowing which order is best, trading a small, statistically insignificant accuracy margin for an unconditional structural guarantee. The behaviour transfers to real multi-document QA (HotpotQA), while the closest training-free output-fusion baseline (PackLLM) loses by 45 points at matched budget, placing cache-level merging in a regime distinct from output-level fusion. Finally, at k>2 the approach transports and colocates latent traces but does not by itself compose them, which we characterize to motivate future work.