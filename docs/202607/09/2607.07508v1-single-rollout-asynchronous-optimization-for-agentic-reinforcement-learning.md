---
title: Single-Rollout Asynchronous Optimization for Agentic Reinforcement Learning
title_zh: 面向智能体强化学习的单次采样异步优化
authors: "Zhenyu Hou, Yujiang Li, Jie Tang, Yuxiao Dong"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07508v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 长周期代理任务的异步强化学习优化
tldr: 本文针对大语言模型在长程智能体任务中强化学习效率低及不稳定的问题，提出了单样本异步优化（SAO）框架。SAO通过将传统的组采样替换为单样本采样，并引入双侧Token级裁剪策略和改进的价值模型训练，显著降低了异步训练中的离策效应并增强了稳定性。实验证明，SAO在编程和推理基准测试中表现优异，并已成功应用于GLM-5.2大规模模型的训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07508v1/fig-001.webp\", \"caption\": \"Figure 1: The performance of SAO on reasoning and coding benchmarks. The four reasoning benchmarks are evaluated in a reasoning-with-Python-tool setting, where the baseline is the Qwen330B-A3B SFT model; SWE-Bench Verified evaluates coding with the Qwen3-30B-A3B baseline. SAO outperforms the corresponding baseline and GRPO across all five benchmarks.\", \"page\": 1, \"index\": 1, \"width\": 964, \"height\": 732}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-002.webp\", \"caption\": \"Figure 3: Performance comparison between SAO and GRPO (w/ DIS) during training. It can be observed that SAO almost consistently outperforms the optimized GRPO during the training process on different benchmarks.\", \"page\": 6, \"index\": 2, \"width\": 818, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-003.webp\", \"caption\": \"Figure 4: Training dynamics of asynchronous single-rollout RL. (a) Explained Variance for SAO and a single-critic-update baseline. (b) Critic gradient norm during value training under full-parameter optimization and frozen-attention optimization used in SAO. (c) Token-level clip ratio during training for SAO with the proposed DIS and the VAPO baseline.\", \"page\": 7, \"index\": 3, \"width\": 805, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of SAO with single rollout design. The numbers denote the generation order of trajectories. For SAO, each trajectory becomes available for training immediately upon completion. In contrast, GRPO must wait until all trajectories in a group are generated before training can begin.\", \"page\": 3, \"index\": 4, \"width\": 571, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-005.webp\", \"caption\": \"Table 1: Experimental Results on math reasoning benchmarks(Accuracy %).\", \"page\": 5, \"index\": 5, \"width\": 796, \"height\": 367}]"
motivation: 传统的同步强化学习在处理长程智能体任务时效率低下，且现有异步框架在训练稳定性和任务有效性上存在挑战。
method: 提出SAO框架，利用单样本采样替代组采样，并结合双侧Token级裁剪策略与优化的价值模型训练来提升稳定性。
result: SAO在SWE-Bench Verified和BeyondAIME等多个基准测试中一致优于GRPO及其变体，并展现出强大的在线学习适应能力。
conclusion: 单样本异步优化能有效解决智能体强化学习中的离策与稳定性问题，是训练超大规模语言模型的高效方案。
---

## 摘要
强化学习（RL）在大语言模型（LLM）的后训练中变得越来越重要。以前的 LLM 强化学习流水线大多是同步且批次交替的，这对于长程智能体任务来说效率低下。最近，异步强化学习作为一种更高效的替代方案出现，它在采样（rollouts）到达时即更新模型。然而，现有的异步强化学习系统通常强调吞吐量，而对训练稳定性和任务有效性的探索较少。例如，一个关键挑战是广泛使用的 GRPO 框架中的分组采样（group-wise sampling）并不自然地适用于异步智能体训练。在本文中，我们提出了单次采样异步优化（SAO），以解决异步强化学习中的稳定性和离策（off-policy）挑战。为了减少离策效应并提高泛化能力，我们将分组采样替换为单次采样，即每个提示词（prompt）仅使用一次采样。我们通过实用的价值模型训练设计进一步改进了这种单次采样策略。为了提高优化稳定性，我们引入了一种严格的双侧 Token 级裁剪策略。SAO 能够稳定训练一千步，并在智能体编程和推理基准测试（如 SWE-Bench Verified、BeyondAIME 和 IMOAnswerBench）上持续优于 GRPO 及其变体。我们还证明了单次采样强化学习在模拟在线学习设置中特别有效，在这种设置下，模型必须适应不断变化的环境。为此，SAO 已成功部署在智能体强化学习流水线中，用于训练开源的 GLM-5.2 模型（750B-A40B）。

## Abstract
Reinforcement learning (RL) is becoming increasingly important for post-training large language models (LLMs). Previous RL pipelines for LLMs were mostly synchronous and batch-interleaved, which is inefficient for long-horizon agentic tasks. Recently, asynchronous RL has emerged as a more efficient alternative by updating the model as rollouts arrive. However, existing asynchronous RL systems often emphasize throughput, while leaving training stability and task effectiveness largely underexplored. For example, a key challenge is that group-wise sampling in the widely-used GRPO framework does not naturally fit asynchronous agentic training. In this paper, we present Single-rollout Asynchronous Optimization (SAO) to address the stability and off-policy challenges in asynchronous RL. To reduce off-policy effects and improve generalization, we replace group-wise sampling with single-rollout sampling, that is, using one rollout per prompt. We further improve this single-rollout strategy with practical value-model training designs. To improve optimization stability, we introduce a strict double-side token-level clipping strategy. SAO is able to train stably for one thousand steps and consistently outperform GRPO and its variants on agentic coding and reasoning benchmarks, such as SWE-Bench Verified, BeyondAIME, and IMOAnswerBench. We also demonstrate that single-rollout RL is particularly effective in a simulated online learning setting, where the model must adapt to changing evolving environments. To this end, SAO is successfully deployed in the agentic RL pipeline for training the open GLM-5.2 model (750B-A40B).

---

## 论文详细总结（自动生成）

以下是对论文《Single-Rollout Asynchronous Optimization for Agentic Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）进入后训练阶段，强化学习（RL）成为提升模型推理和智能体（Agentic）能力的关键。
*   **研究动机**：传统的同步、批次交替式 RL 流水线在处理长程智能体任务时效率极低，因为不同任务的采样长度差异巨大，导致 GPU 集群经常处于等待最慢采样的闲置状态。
*   **核心挑战**：虽然异步 RL 可以提高吞吐量，但会引入“策略滞后”（Policy Lag）问题，导致训练不稳定。此外，目前主流的 GRPO 框架依赖“组采样”（Group-wise sampling）来估计优势函数，这在异步环境下会造成严重的延迟瓶颈，且不符合环境仅提供单次反馈的真实在线学习场景。
*   **论文目标**：提出一种名为 **SAO（Single-rollout Asynchronous Optimization）** 的框架，旨在实现稳定、高效且适用于智能体任务的单样本异步强化学习。

### 2. 方法论：核心思想与关键技术
SAO 的核心思想是放弃组采样，采用单样本采样并配合增强的价值模型（Critic）和严格的裁剪策略。

*   **双侧 Token 级重要性采样裁剪（DIS）**：
    *   为了应对异步训练中的策略滞后，SAO 直接利用采样引擎生成的 log-probabilities 计算重要性采样比率。
    *   引入严格的双侧裁剪机制：将比率限制在 $[1-\epsilon_l, 1+\epsilon_h]$ 之间。与标准 PPO 不同，SAO 会将超出此范围的 Token 直接从梯度计算中屏蔽（Mask），防止极端策略偏离导致的不稳定。
*   **单样本采样与价值模型优化**：
    *   **快速价值更新**：为了降低单样本更新的高方差，SAO 使价值模型的更新频率高于策略模型（例如策略更新 1 次，价值模型更新 2 次），使其能更快适应策略变化。
    *   **冻结注意力机制训练**：在 RL 过程中冻结价值模型的 Attention 层，仅优化 MoE（混合专家）投影层，以稳定价值模型的梯度。
*   **跳过观察的 Token 级 GAE（Skip-Observation GAE）**：
    *   针对智能体轨迹（动作-观察交替），SAO 认为环境反馈（Observation）不是模型生成的，计算其价值差异会引入噪声。
    *   **技术细节**：修改贝尔曼目标，使当前动作的最后一个 Token 的价值直接指向下一个动作的第一个 Token，从而跨越环境反馈的边界，减少外部随机性干扰。

### 3. 实验设计
*   **任务场景**：数学推理（使用 Python 工具）和智能体编程（修复 GitHub Issue）。
*   **数据集/Benchmark**：
    *   **数学推理**：AIME2025、BeyondAIME、HMMT Nov 2025、IMOAnswerBench。
    *   **编程**：SWE-Bench Verified。
    *   **在线学习模拟**：设计了一个写作风格偏好动态变化的任务（Cute -> Chuunibyou -> Classical）。
*   **对比方法**：
    *   SFT 基准模型（Qwen3-30B-A3B）。
    *   标准 GRPO（带同步屏障）。
    *   改进版 GRPO（加入 DIS 策略）。
    *   VAPO（另一种高效 RL 变体）。
    *   Running-mean（使用滑动平均奖励作为基准的单样本方法）。

### 4. 资源与算力
*   **模型规模**：实验主要基于 **Qwen3-30B-A3B**（混合专家模型）。
*   **实际应用**：论文提到 SAO 已成功部署于 **GLM-5.2 (750B-A40B)** 大规模模型的训练流水线中。
*   **具体算力细节**：文中**未明确说明**具体的 GPU 数量（如 A100/H100 的具体颗数）或总训练时长，但提到了 Batch Size 为 128，最大序列长度为 128k Token。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流硬核基准上进行了测试，并提供了长达 1000 步的训练曲线。
*   **消融实验**：非常充分。作者对比了价值模型更新频率（K=1 vs K=2）、全参数更新 vs 冻结 Attention 更新、Token 级 vs Step 级 GAE 等多种配置。
*   **公平性**：通过多次运行（如 AIME 运行 16 次取平均）来降低方差，对比方法均在相同的 Batch Size 和计算资源逻辑下进行，实验设计较为客观。

### 6. 主要结论与发现
*   **性能领先**：SAO 在所有推理和编程基准上均显著优于 GRPO 及其变体。例如在 AIME2025 上，SAO 达到了 97.3% 的准确率，远高于 GRPO 的 84.2%。
*   **训练稳定性**：引入 DIS 策略后，SAO 解决了异步 RL 容易崩溃的问题，能够稳定训练超过 1000 步。
*   **在线适应性**：在模拟在线学习中，SAO 能够比 Running-mean 方法更快地捕捉到环境奖励分布的变化，证明了强价值模型在单样本场景下的必要性。
*   **粒度重要性**：实验证明 Token 级的价值训练比 Step 级（按步骤平均）更有效，能捕捉更细致的逻辑转折。

### 7. 优点（亮点）
*   **效率与效果兼得**：通过异步架构解决了长程任务的计算闲置问题，同时通过算法改进解决了异步带来的性能损失。
*   **架构简洁**：DIS 策略不需要维护复杂的旧策略模型镜像，降低了显存和计算开销。
*   **智能体适配性**：Skip-Observation GAE 的设计精准捕捉了智能体任务中“模型动作”与“环境反馈”的本质区别。

### 8. 不足与局限
*   **算力细节缺失**：未披露具体的训练资源消耗，难以评估中小团队的复现成本。
*   **依赖价值模型**：SAO 重新引入了价值模型（Critic），相比于无 Critic 的 GRPO，显存占用会增加（尽管文中通过冻结参数和 MoE 架构缓解了这一点）。
*   **应用范围限制**：实验主要集中在推理和编程等具有清晰奖励信号的任务，在通用对话或主观偏好对齐（RLHF）中的表现尚未验证。
*   **基础设施要求**：异步架构对底层分布式系统的实现要求较高，需要能够实时保存和检索 Token 级的行为概率。

（完）
