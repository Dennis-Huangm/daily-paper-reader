---
title: Automating the Design of Embodied AgentArchitectures
title_zh: 自动化具身智能体架构设计
authors: "Jian Zhou, Sihao Lin, Jin Li, Shuai Fu, Gengze Zhou, Qi Wu"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30111v1"
tags: ["query:ag-rl"]
score: 8.5
evidence: 自动化具身智能体架构设计
tldr: 本研究针对具身智能体架构依赖人工设计的问题，提出了自动化架构搜索方案。通过引入AgentCanvas图运行环境和KDLoop搜索程序，实现了对感知、记忆和规划模块的自动优化。在视觉导航、问答及操控任务上的评估显示，该方法能显著提升成功率，同时也揭示了具身任务中特有的环境噪声和局部最优等挑战，为自动化设计具身智能体提供了系统性见解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30111v1/fig-001.webp\", \"caption\": \"Table 1: Yield across the 3×4 optimizer–executor matrix. Baseline/Best are raw SR mean±sd (%) over three post-selection passes; ∆ is the mean gain in percentage points. Small variance-overlapping gains are directional, not certified. † marks a leak-bearing SmartWay run excluded from deployable claims; ‡ marks the VoxPoser logging-fault diagnosis (§4.4).\", \"page\": 6, \"index\": 1, \"width\": 740, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2606.30111v1/fig-002.webp\", \"caption\": \"Figure 1: Embodied Agent Architecture Search. (1) Four seed Executors emit per-episode traces. (2) ADAS/AFlow/KDLoop edit Executor code through a shared coding-agent harness and only the proposer differs. (3) The 3×4 ∆SR matrix shows several deployable or directional improvements plus one leak-bearing apparent gain, and surfaces three constraints of embodied AAS: evaluation noise, local edit basins, and partial credit assignment.\", \"page\": 2, \"index\": 2, \"width\": 810, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2606.30111v1/fig-003.webp\", \"caption\": \"Table 2: Per-cell attempted intervention-axis counts. Bold marks the repeated edit family analyzed in §4.3. Counts include KDLoop–VoxPoser attempts before the injected logging fault was surfaced.\", \"page\": 7, \"index\": 3, \"width\": 802, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2606.30111v1/fig-004.webp\", \"caption\": \"Figure 3: Representative search trajectories. Each panel shows single-pass SR, best-so-far SR, the shared baseline, and the rerun-confirmed best iteration. The three cells illustrate improvement, plateau, and noisy best-selection; full trajectories are in Appendix B.\", \"page\": 7, \"index\": 4, \"width\": 811, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2606.30111v1/fig-005.webp\", \"caption\": \"Figure 5: The AgentCanvas UI, for a human. The typed graph an Optimizer edits as JSON is, for a researcher, a node-and-wire canvas with an inspectable state panel (shown: the SmartWay-CE graph and its run state). The visual editor and the Optimizer’s edit/evaluate interface are two views of one artifact.\", \"page\": 13, \"index\": 5, \"width\": 826, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2606.30111v1/fig-006.webp\", \"caption\": \"Figure 4: AgentCanvas as an embodied-AAS substrate. Left: an agent factors into typed nodes, typed-port wires, and shared state in a container reached by access grants (shown: the MapGPT-MP3D loop body; node colour = source nodeset; the loop is an iter out→iter in carry, not a back-edge; §A.2–§A.3). Right: the batch-eval optimisation (§A.4). Naive synchronous lock-step (top) stalls every worker on the slowest module, while AgentCanvas (bottom) decouples workers and batches whichever FM calls arrive within a short window (default 50 ms), so throughput tracks per-worker speed.\", \"page\": 12, \"index\": 6, \"width\": 809, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2606.30111v1/fig-007.webp\", \"caption\": \"Figure 2: Top: AAS-method diagrams. Each variant differs in proposer logic and persistent memory. Bottom: the shared coding-agent harness. The outer loop invokes a proposer, implementer, and evaluator. Only the proposer is variant-specific, while implementation, evaluation, validation, and file access are shared.\", \"page\": 5, \"index\": 7, \"width\": 826, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2606.30111v1/fig-008.webp\", \"caption\": \"Figure 6: Full 3 × 4 per-cell search trajectories (rows: optimizers, columns: executors), same conventions as Fig. 3: per-iter single-pass SR (light line + markers), best-so-far envelope (heavy line), shared baseline mean ± sd (dashed line + band), and three-pass rerun mean ± sd at the selected best iter (⋆). The success-rate axis is shared across all panels. The iteration axis is 0–20 for the ADAS / AFlow rows and 0–10 for KDLoop. In-cell shading marks the substrate-level findings: KDLoop detecting the episode-wise logging bug (green), ADAS / AFlow leaving it unfound (amber), and AFlow’s SmartWay gain riding on evaluation-information leakage (red). AFlow × SmartWay’s leak iters use dashed segments and open markers (§4.4).\", \"page\": 16, \"index\": 8, \"width\": 812, \"height\": 484}]"
motivation: 具身智能体架构设计目前主要依赖研究者直觉，缺乏在复杂感知环境下自动优化模块连接与信息流的系统化方法。
method: 引入了AgentCanvas图运行框架和KDLoop编码代理搜索流程，通过闭环的实验与反思机制自动迭代智能体架构。
result: 在多种具身任务中实现了部署级的成功率提升，但也暴露出优化信号易被环境噪声掩盖以及搜索易陷入局部最优等限制。
conclusion: 自动化架构搜索对具身智能体具有显著增益，但必须克服模拟器环境下的信用分配和鲁棒性问题才能进一步发展。
---

## 摘要
具身智能体通常被构建为感知、记忆、规划和动作模块的手动设计组合。这种模块化揭示了巨大的架构设计空间，但目前的系统仍然依赖研究者的直觉来选择信息的存储位置、观测的处理方式以及模型调用的连接方式。智能体架构搜索 (AAS) 实现了文本领域智能体此类设计的自动化，但尚未通过模拟器回放对感知具身智能体进行系统评估。我们研究了这种迁移。我们引入了 AgentCanvas，这是一个类型化图运行时，它将具身执行器托管为可编辑的节点连线程序，具有模拟器感知执行和回合级日志功能；以及 KDLoop，这是一个编码智能体搜索程序，它在提议、批评、实验和蒸馏之间循环，并在停滞后触发反思。我们在涵盖视觉语言导航、具身问答和语言条件操控的四个具身执行器上评估了三种 AAS 变体。由此产生的 3x4 矩阵表明，架构级搜索可以在具身任务上产生可部署且具有方向性的成功率提升，而一个明显的高分候选方案因存在泄露而被拒绝。同时，实验揭示了在文本领域 AAS 中被淡化的约束：优化信号可能被回放噪声掩盖，搜索可能会陷入局部编辑盆地，即使在有详细日志的情况下，回合级信用分配也只是部分显现。这些结果刻画了具身智能体自动化架构搜索的前景和当前的局限性。

## Abstract
Embodied agents are typically built as hand-designed compositions of perception, memory, planning, and action modules. This modularity exposes a large architectural design space, but current systems still rely on researcher intuition to choose where information is stored, how observations are processed, and how model calls are connected. Agent Architecture Search (AAS) automates such design for text-domain agents, but has not been systematically evaluated on perceptual embodied agents through simulator rollouts. We study this transfer. We introduce AgentCanvas, a typed-graph runtime that hosts embodied executors as editable node-and-wire programs with simulator-aware execution and episode-level logs, and KDLoop, a coding-agent search procedure that cycles through proposal, critique, experiment, and distillation, with triggered reflection after stalls. We evaluate three AAS variants across four embodied executors spanning vision-language navigation, embodied question answering, and language-conditioned manipulation. The resulting 3x4 matrix shows that architecture-level search can produce deployable and directional success-rate gains on embodied tasks, while one apparent high-scoring candidate is rejected as leak-bearing. At the same time, the experiments expose constraints that are muted in text-domain AAS: optimization signals can be masked by rollout noise, search can become trapped in local edit basins, and episode-level credit assignment only partially emerges even when detailed logs are available. These results characterize both the promise and the current limits of automated architecture search for embodied agents.