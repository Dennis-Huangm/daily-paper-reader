---
title: Off-Policy Value-Based Reinforcement Learning for Large Language Models
title_zh: 面向大语言模型的离策基于价值的强化学习
authors: "Peng-Yuan Wang, Ziniu Li, Tian Xu, Bohan Yang, Tian-Shuo Liu, ChenYang Wang, Xiong-Hui Chen, Yi-Chen Li, Tianyun Yang, Congliang Chen, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23355v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 针对LLM的离策价值强化学习框架
tldr: 针对大语言模型强化学习中策略梯度方法采样效率低的问题，本文提出了一种基于价值的离线策略强化学习框架 ReVal。该方法通过贝尔曼更新结合了步骤级的一致性信号与轨迹级的验证信号，并利用经验回放机制实现数据的高效复用。实验证明，ReVal 在数学推理任务上比 GRPO 收敛更快且性能更优，在 AIME24 和 GPQA 等基准上取得了显著提升，为 LLM 训练提供了一种高效的替代方案。
selection_source: fresh_fetch
motivation: 现有的 LLM 强化学习方法多为在线策略，导致数据利用率低且在长路径任务中采样成本极高。
method: 提出 ReVal 框架，通过贝尔曼更新结合步骤级一致性与轨迹级验证信号，并支持基于经验回放的离线策略学习。
result: "ReVal 在 DeepSeek-R1-Distill-1.5B 上相比 GRPO 在 AIME24 和 GPQA 基准上分别提升了 2.7% 和 4.5%，且收敛速度更快。"
conclusion: 基于价值的强化学习是 LLM 训练中比策略梯度方法更具实践意义且高效的替代方案。
---

## 摘要
提高数据利用效率对于在生成轨迹成本高昂的长程任务中扩展强化学习（RL）至关重要。然而，目前主流的大语言模型（LLM）强化学习方法大多是同策（on-policy）的：它们对每批数据仅更新一次后便将其丢弃，随后采集新样本，导致样本效率低下。在这项工作中，我们探索了一种替代的基于价值的 LLM 强化学习框架，该框架能够自然地实现离策（off-policy）学习。我们提出了 ReVal，这是一种基于贝尔曼更新（Bellman-update）的方法，它结合了捕捉内部一致性的逐步信号和源自结果验证的轨迹级信号。ReVal 自然地支持基于经验回放池（replay-buffer）的训练，从而能够高效地复用过去的轨迹。在标准数学推理基准测试上的实验表明，ReVal 不仅收敛更快，而且在最终性能上也优于 GRPO。在 DeepSeek-R1-Distill-1.5B 上，ReVal 提高了训练效率，且相比 GRPO 在 AIME24 上提升了 2.7%，在域外基准测试 GPQA 上提升了 4.5%。这些结果表明，基于价值的强化学习是 LLM 训练中替代基于策略（policy-based）方法的一种实用方案。

## Abstract
Improving data utilization efficiency is critical for scaling reinforcement learning (RL) for long-horizon tasks where generating trajectories is expensive. However, the dominant RL methods for LLMs are largely on-policy: they update each batch of data only once, discard it, and then collect fresh samples, resulting in poor sample efficiency. In this work, we explore an alternative value-based RL framework for LLMs that naturally enables off-policy learning. We propose ReVal, a Bellman-update-based method that combines stepwise signals capturing internal consistency with trajectory-level signals derived from outcome verification. ReVal naturally supports replay-buffer-based training, allowing efficient reuse of past trajectories. Experiments on standard mathematical reasoning benchmarks show that ReVal not only converges faster but also outperforms GRPO in final performance. On DeepSeek-R1-Distill-1.5B, ReVal improves training efficiency and achieves improvement of 2.7% in AIME24 and 4.5% in out-of-domain benchmark GPQA over GRPO. These results suggest that value-based RL is a practical alternative to policy-based methods for LLM training.

---

## 论文详细总结（自动生成）

这是一份关于论文《Off-Policy Value-Based Reinforcement Learning for Large Language Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前大语言模型（LLM）的主流强化学习（RL）方法（如 PPO、GRPO）大多是**同策（On-Policy）**的。这意味着每批采集的轨迹数据在更新一次参数后就会被丢弃，导致数据利用率极低。
*   **研究背景**：随着 LLM 转向长程推理（Long-horizon reasoning）和智能体（Agentic）任务，生成长轨迹的计算成本变得异常高昂。
*   **研究动机**：探索一种**基于价值（Value-based）**的离策（Off-Policy）强化学习框架，通过经验回放机制（Replay Buffer）实现历史数据的多次复用，从而在降低训练成本的同时提升模型性能。

### 2. 方法论：ReVal 框架
*   **核心思想**：利用 LLM 的 Logits 隐式参数化 Q 函数（Logits-as-Q），将策略和价值统一在单个模型中，避免了额外价值网络带来的内存开销。
*   **关键技术细节**：
    *   **初始化校准（Calibrated Initialization）**：针对前人工作（如 TBRM）在无奖励时会产生策略漂移的问题，ReVal 通过**奖励塑造（Reward Shaping）**引入了参考策略（Reference Policy）的约束，确保在奖励为零时模型保持稳定。
    *   **贝尔曼更新（Bellman Update）**：结合了逐步（Stepwise）的一致性信号和轨迹级（Trajectory-level）的结果验证信号。
    *   **损失函数**：设计了一个基于轨迹级贝尔曼残差的平方损失函数，包含初始状态价值 $V(s_1)$、对数概率比以及规则奖励 $r_{rule}$。
    *   **经验回放机制**：引入 FIFO（先进先出）缓冲区存储历史轨迹，支持在每轮采样后进行多次离策更新（Off-policy updates）。

### 3. 实验设计
*   **模型与数据集**：
    *   **模型**：DeepSeek-R1-Distill-Qwen-1.5B 和 Qwen2.5-Math-7B。
    *   **训练数据**：DeepScaleR 数据集。
*   **Benchmark（基准测试）**：涵盖 7 个数学推理任务，包括 AIME24、AIME25、AMC、MATH、MINERVA、Olympiad Bench 和 GPQA（域外泛化测试）。
*   **对比方法**：
    *   **GRPO**：目前主流的 Actor-only 同策策略梯度方法。
    *   **TBRM**：一种基于价值但通常以同策方式运行的基准方法。

### 4. 资源与算力
*   **软件框架**：基于 Verl 大规模强化学习框架实现。
*   **训练参数**：迭代 650 次，Batch Size 为 128 个 Prompt，每个 Prompt 生成 8 个 Rollout。
*   **算力说明**：论文**未明确提及具体的 GPU 型号和数量**。但在效率分析中提到，ReVal 在 1.5B 模型上达到 SOTA 性能需 6.3 小时，而 GRPO 需 7.5 小时（减少了 18% 的总时长）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同规模和类型的模型（推理模型 vs. 普通数学模型）上进行了完整测试。
    *   针对不同任务难度（简单、中等、困难）进行了收敛速度分析。
    *   进行了**有限采样（N=1）**的极端实验，验证在数据稀缺时的鲁棒性。
*   **充分性评价**：实验设计较为全面且客观。除了最终性能对比，还包含了对 KL 正则化、参考策略重置频率、超参数 $\beta$ 以及负样本利用率的详细消融实验，提供了较强的实践指导意义。

### 6. 主要结论与发现
*   **效率提升**：ReVal 实现了平均 **4.3 倍** 的收敛加速。通过增加数据复用频率，模型能以更少的生成轮数达到目标性能。
*   **性能超越**：在 DeepSeek-R1-Distill-1.5B 上，ReVal 在 AIME24 上比 GRPO 高出 **2.7%**，在 GPQA 上高出 **4.5%**。
*   **低采样优势**：在每个 Prompt 仅生成 1 个样本（N=1）的严苛条件下，ReVal 的领先优势扩大（AIME 提升 4.8%），证明了离策复用在昂贵采样场景下的价值。
*   **泛化能力**：在域外基准（GPQA）上的显著提升表明，基于价值的学习能帮助模型学到更稳健的推理逻辑。

### 7. 优点
*   **高采样效率**：打破了同策学习“用完即弃”的限制，显著降低了 LLM 训练中最耗时的轨迹生成成本。
*   **单模型架构**：无需额外的 Critic 网络，保持了与 GRPO 相当的显存友好性。
*   **理论完备性**：解决了价值函数初始化的校准问题，通过奖励塑造保证了训练初期的稳定性。

### 8. 不足与局限
*   **回放策略简单**：目前仅使用简单的 FIFO 缓冲区，尚未探索更高级的优先经验回放（Prioritized Experience Replay）。
*   **任务局限性**：实验主要集中在具有确定性验证（Verifiable Reward）的数学推理任务上，在创意写作或开放式对话等主观任务上的表现尚待验证。
*   **超参数敏感性**：实验显示超参数 $\beta$ 对训练稳定性影响较大，且与响应长度高度耦合，可能需要针对不同模型进行精细调优。

（完）
