---
title: End-to-End Efficient RL for Linear Bellman Complete MDPs with Deterministic Transitions
title_zh: 具有确定性转移的线性 Bellman 完备 MDP 的端到端高效强化学习
authors: "Zakaria Mhammedi, Alexander Rakhlin, Nneka Okolo"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23461v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 线性 Bellman 完整 MDP 的高效强化学习算法
tldr: 本研究针对满足线性Bellman完备性的马尔可夫决策过程（MDP），提出了一种在确定性状态转移、随机初始状态和随机奖励设定下的高效强化学习算法。以往算法在处理大动作空间或计算效率方面存在局限，而本算法在有限动作空间下实现了端到端的高效性，在无限动作空间下仅需标准的argmax查询。该算法在样本和计算复杂度上均达到了关于时界、特征维度和误差倒数的多项式级别，为线性函数近似下的强化学习提供了新的理论突破。
selection_source: fresh_fetch
motivation: 现有的线性Bellman完备MDP算法在处理大动作空间时往往缺乏计算效率，或依赖于过强的特征空间预言机假设。
method: 提出了一种针对确定性转移MDP的端到端高效算法，该算法在处理大动作空间时仅需一个标准的动作最大化预言机。
result: 算法在样本复杂度和计算复杂度上均实现了关于时界、特征维度和精度要求的项式级增长。
conclusion: 该研究证明了在确定性转移条件下，线性Bellman完备MDP的强化学习可以在计算和统计上同时保持高效。
---

## 摘要
我们研究了满足“线性 Bellman 完备性”的马尔可夫决策过程（MDPs）中带有线性函数近似的强化学习（RL）。线性 Bellman 完备性是一个基础设定，即任何线性价值函数的 Bellman 备份（Bellman backup）仍然保持线性。尽管该设定在统计上是可处理的，但先前的计算高效算法要么局限于小的动作空间，要么需要对特征空间做出强算谕（oracle）假设。我们为具有确定性转移、随机初始状态和随机奖励的线性 Bellman 完备 MDP 提供了一种计算高效的算法。对于有限动作空间，我们的算法是端到端高效的；对于大动作空间或无限动作空间，我们仅需要一个标准的动作 argmax 算谕。我们的算法学习一个 ε-最优策略，其样本复杂度和计算复杂度在规划时界（horizon）、特征维度和 1/ε 上呈多项式级。

## Abstract
We study reinforcement learning (RL) with linear function approximation in Markov Decision Processes (MDPs) satisfying \emph{linear Bellman completeness} -- a fundamental setting where the Bellman backup of any linear value function remains linear. While statistically tractable, prior computationally efficient algorithms are either limited to small action spaces or require strong oracle assumptions over the feature space. We provide a computationally efficient algorithm for linear Bellman complete MDPs with \emph{deterministic transitions}, stochastic initial states, and stochastic rewards. For finite action spaces, our algorithm is end-to-end efficient; for large or infinite action spaces, we require only a standard argmax oracle over actions. Our algorithm learns an $\varepsilon$-optimal policy with sample and computational complexity polynomial in the horizon, feature dimension, and $1/\varepsilon$.

---

## 论文详细总结（自动生成）

以下是对论文《End-to-End Efficient RL for Linear Bellman Complete MDPs with Deterministic Transitions》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在线性函数近似的强化学习（RL）中，**线性 Bellman 完备性（Linear Bellman Completeness）**是一个核心假设，即任何线性价值函数的 Bellman 备份仍是线性的。尽管该设定在统计上是可处理的（即样本复杂度为多项式级），但在计算上通常被认为是困难的。
*   **研究动机**：现有的算法要么仅适用于极小的动作空间（复杂度随动作数量指数增长），要么需要极强的预言机（Oracle）假设（如对整个特征空间进行线性优化）。
*   **整体含义**：本文旨在解决在**确定性状态转移**（但初始状态和奖励是随机的）设定下，如何实现既满足统计高效（低样本量）又满足计算高效（多项式时间）的端到端强化学习算法，特别是针对大动作空间或无限动作空间的情况。

### 2. 方法论：核心思想与算法流程
论文提出了两阶段的框架：**探索（Exploration）**构建策略覆盖，以及**利用（Exploitation）**进行策略优化。

*   **核心思想**：利用确定性转移的特性，通过层级化地构建策略集来覆盖特征空间的各个维度，从而规避复杂的非凸优化问题。
*   **关键技术细节**：
    *   **有限动作空间算法**：结合了**乐观约束传播（OCP）**算法和**鲁棒重心生成器（Robust Barycentric Spanner）**。通过 OCP 解决内在奖励的优化问题，逐步构建能覆盖所有可达特征方向的策略集。
    *   **大动作空间算法**：引入了 **FQI（拟合 Q 迭代）**与一种新的 **SubspaceCover（子空间覆盖）**子程序。该程序不依赖于对特征空间的全局线性优化预言机，而是通过**拒绝采样（Rejection Sampling）**和**离群方向计算**来识别特征空间的基向量。
    *   **动作最大化预言机（Action argmax oracle）**：对于无限动作空间，仅假设存在一个能计算 $\text{argmax}_{a \in \mathcal{A}} \phi(x, a)^\top \theta$ 的标准算谕，这与线性 MDP 的标准假设一致。
*   **算法流程**：
    1.  **阶段 I（探索）**：逐层运行 `SubspaceCover`，找到每一层特征向量所处的子空间，并收集一组策略（策略覆盖），使得这些策略产生的特征向量能张成该子空间。
    2.  **阶段 II（利用）**：利用策略覆盖进行滚入（Roll-out），通过岭回归（Ridge Regression）精确估计奖励函数的参数，然后基于估计的奖励在确定性代理模型上进行规划，输出最终策略。

### 3. 实验设计
*   **研究性质**：本论文属于**纯理论证明类论文**，重点在于算法的计算复杂度和样本复杂度的数学推导。
*   **对比对象**：论文在理论层面对比了先前的研究，如：
    *   **Golowich & Moitra (2024a)**：其复杂度随动作空间大小呈指数级增长。
    *   **Wu et al. (2024)**：需要强特征空间优化预言机。
    *   **FRANCIS (Zanette et al., 2020c)**：需要极强的可达性假设。
*   **场景设定**：设定为具有确定性转移、随机初始状态和随机奖励的线性 Bellman 完备 MDP。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何具体的 GPU 型号、数量或训练时长。
*   **原因**：作为理论论文，其核心贡献是证明了算法在多项式时间内运行的可能性，而非通过实验验证性能。

### 5. 实验数量与充分性
*   **理论验证**：论文提供了长达 52 页的详细数学证明（附录部分），涵盖了从有限动作到无限动作的所有引理和定理证明。
*   **充分性评价**：从理论研究的角度看，证明过程非常详尽，涵盖了误差分解、集中不等式分析以及子空间覆盖的收敛性。但由于缺乏实证实验，算法在实际复杂环境（如高维视觉输入）中的表现尚待验证。

### 6. 主要结论与发现
*   **计算高效性**：证明了对于确定性转移的线性 Bellman 完备 MDP，存在一个算法能在 $\text{poly}(d, H, 1/\varepsilon)$ 的样本和计算时间内学习到 $\varepsilon$-最优策略。
*   **打破瓶颈**：成功消除了先前算法对动作空间大小的指数依赖，且不需要强预言机假设。
*   **参数解耦**：通过先估计奖励参数再规划的方法，避开了先前研究中必须假设 Q 函数参数有界的限制（该限制曾导致线性 MDP 无法作为特例被包含）。

### 7. 优点
*   **理论突破**：首次在不需要强预言机的情况下，实现了大动作空间下线性 Bellman 完备 MDP 的端到端高效学习。
*   **泛化性强**：该模型严格泛化了线性 MDP 和线性二次调节器（LQR）等经典模型。
*   **技术创新**：提出的 `SubspaceCover` 算法为处理确定性系统中的特征发现提供了一种高效的新工具。

### 8. 不足与局限
*   **转移确定性限制**：算法高度依赖于**确定性转移**假设。在完全随机转移的 MDP 中，如何保持计算高效性仍然是一个重大的开放性问题。
*   **应用限制**：虽然论文提到该技术可能应用于大语言模型（LLM）的微调（因为 token 生成是确定性的），但目前仅停留在理论设想阶段，缺乏实际落地案例。
*   **预言机依赖**：对于无限动作空间，虽然 argmax 预言机是标准的，但在某些复杂的连续动作空间中，实现该预言机本身可能依然具有挑战性。

（完）
