---
title: "GUIDE: Guided Updates for In-context Decision Evolution in LLM-Driven Spacecraft Operations"
title_zh: GUIDE：大语言模型驱动的航天器操作中的上下文内决策演化引导式更新
authors: "Alejandro Carrasco, Mariko Storey-Matsutani, Victor Rodriguez-Fernandez, Richard Linares"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27306v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: LLM驱动的监督代理策略改进框架
tldr: 针对航天器操作中大语言模型代理依赖静态提示词且无法跨回合改进的问题，本文提出 GUIDE 框架。该框架通过演化结构化的自然语言决策规则手册，在不更新模型权重的情况下实现跨回合策略改进。在《坎巴拉太空计划》的对抗性轨道拦截任务中，GUIDE 显著优于静态基准，证明了上下文演化在实时闭环航天交互中的有效性。
selection_source: fresh_fetch
motivation: 现有的 LLM 航天器操作代理通常使用静态提示词，缺乏在重复执行任务中学习和改进的能力。
method: 提出一种非参数化策略改进框架，通过离线反思更新结构化的自然语言决策手册，指导轻量级模型进行实时控制。
result: 在 KSP 轨道拦截任务中，GUIDE 的演化策略在性能上持续超越静态基准模型。
conclusion: LLM 代理的上下文演化本质上是在实时闭环交互中对结构化决策规则进行的策略搜索。
---

## 摘要
大语言模型（LLMs）已被提议作为航天器操作的监督智能体，但现有方法依赖于静态提示，且无法在重复执行中得到改进。我们引入了 GUIDE，这是一种非参数化策略改进框架，通过演化结构化的、基于状态条件的自然语言决策规则剧本（playbook），在不进行权重更新的情况下实现跨回合自适应。一个轻量级的执行模型负责实时控制，而离线反思则根据先前的轨迹更新剧本。在《坎巴拉太空计划》微分博弈环境下的对抗性轨道拦截任务中进行的评估表明，GUIDE 的演化性能始终优于静态基准。结果表明，LLM 智能体中的上下文演化在实时闭环航天器交互中起到了针对结构化决策规则的策略搜索作用。

## Abstract
Large language models (LLMs) have been proposed as supervisory agents for spacecraft operations, but existing approaches rely on static prompting and do not improve across repeated executions. We introduce \textsc{GUIDE}, a non-parametric policy improvement framework that enables cross-episode adaptation without weight updates by evolving a structured, state-conditioned playbook of natural-language decision rules. A lightweight acting model performs real-time control, while offline reflection updates the playbook from prior trajectories. Evaluated on an adversarial orbital interception task in the Kerbal Space Program Differential Games environment, GUIDE's evolution consistently outperforms static baselines. Results indicate that context evolution in LLM agents functions as policy search over structured decision rules in real-time closed-loop spacecraft interaction.

---

## 论文详细总结（自动生成）

以下是对论文《GUIDE: Guided Updates for In-context Decision Evolution in LLM-Driven Spacecraft Operations》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：航天器操作具有高风险、数据稀缺且难以在线更新权重的特点。虽然大语言模型（LLM）已被提议作为监督代理，但现有方法多依赖静态提示词（Static Prompting），无法在多次任务执行中积累经验或自我改进。
*   **核心问题**：如何在不更新模型参数（Weight Updates）的情况下，使 LLM 驱动的航天器代理能够通过跨回合（Cross-episode）的学习，适应复杂的动态环境和对抗性交互。
*   **整体含义**：提出了一种名为 **GUIDE** 的非参数化策略改进框架，将“学习”过程转化为对自然语言决策规则手册（Playbook）的演化搜索。

### 2. 方法论
*   **核心思想**：采用“教师-学生”分离架构。离线反思模型（Reflector）负责总结经验并更新规则手册，在线执行模型（Acting Model）则根据当前状态和手册中的规则进行实时控制。
*   **关键技术细节**：
    *   **剧本表示（Playbook Representation）**：策略被表示为一组结构化的自然语言规则（Bullets），每条规则包含触发条件（如距离阈值）和行为指导。
    *   **在线决策代理**：为了满足实时性，将执行过程分为两步：1. **受限推理**（产生意图）；2. **动作映射**（通过工具调用输出具体的推力矢量）。
    *   **离线 $\epsilon$-偏置反思采样**：在每轮任务结束后，系统以 $1-\epsilon$ 的概率反思失败轨迹，以 $\epsilon$ 的概率反思成功轨迹。
    *   **剧本演化**：利用前沿模型（如 GPT-4 级别）作为“反思者”和“策展人”，对手册执行 `ADD`（增加）、`UPDATE`（更新）、`REMOVE`（删除）操作。
    *   **版本选择**：使用 **UCB1（置信区间上界）** 算法在多个剧本版本中进行采样评估，平衡探索（尝试新规则）与利用（使用已知优秀规则）。

### 3. 实验设计
*   **实验环境**：使用《坎巴拉太空计划》微分博弈（KSPDG）环境，模拟真实的轨道力学。
*   **实验场景**：**LBG（Lady-Bandit-Guard）拦截任务**。代理控制“土匪（Bandit）”卫星，目标是接近“女士（Lady）”卫星，同时躲避“卫兵（Guard）”卫星的拦截。
*   **基准测试（Benchmark）**：
    *   **LG4/LG5**：卫兵采取激进追逐或随机策略。
    *   **LG6/LG7**：卫兵采取防御性阻截策略（难度更高）。
*   **对比方法**：
    *   静态 LLM（v0 版本，无演化）。
    *   LQR（线性二次调节器）经典控制算法。
    *   顺行对齐（Prograde-Alignment）启发式策略。

### 4. 资源与算力
*   **算力说明**：文中未明确列出具体的 GPU 型号（如 A100/H100）或具体的训练/推理总时长。
*   **模型使用**：提到使用了“前沿语言模型（Frontier LLM）”作为反思者，以及“轻量级模型”作为在线执行器，但未指明具体模型名称（推测为 GPT-4 或 Claude 系列作为教师，较小模型作为学生）。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种不同的对抗场景（LG4-LG7）进行了评估，每种策略在每个场景下运行 $N=20$ 次以获取统计显著性。
*   **充分性评价**：实验设计较为客观，引入了 p 值检验（$\alpha=0.05$）来验证改进的显著性。通过对比经典控制算法（LQR）和启发式方法，证明了 LLM 在处理复杂博弈逻辑时的优越性。演化过程展示了从 v0 到 v5 的性能提升曲线，实验逻辑闭环。

### 6 主要结论与发现
*   **性能飞跃**：GUIDE 演化出的剧本在所有场景下均显著优于静态 LLM 基准，综合评分提升了 82% 至 99%。
*   **策略搜索本质**：LLM 代理的上下文演化本质上是在结构化决策空间内进行的**非参数化策略搜索**。
*   **自动发现模式**：系统能够自动识别失败模式（如因盲目追逐目标而被卫兵拦截），并生成如“分层避障”等复杂行为规则，这些规则在执行时能显著改变航天器的轨迹。

### 7. 优点
*   **无需权重更新**：非常适合无法频繁重新训练模型的航天部署环境。
*   **可解释性强**：策略以自然语言规则形式存在，人类操作员可以轻松理解、审核和干预。
*   **实时性与推理能力的平衡**：通过离线演化和在线轻量执行的分离，解决了大模型推理延迟与实时控制之间的矛盾。

### 8. 不足与局限
*   **模型依赖性**：框架的效果高度依赖于“反思者”模型的推理能力，若基础模型存在逻辑偏差，可能生成错误的规则。
*   **仿真与现实的差距**：实验仅在 KSP 模拟器中完成，尚未在真实的航天硬件或更复杂的传感器噪声环境下验证。
*   **规模化瓶颈**：随着剧本规则数量增加，可能会面临上下文窗口限制或规则冲突的问题，文中未深入探讨规则去重和冲突检测的算法上限。

（完）
