---
title: "Heddle: A Distributed Orchestration System for Agentic RL Rollout"
title_zh: Heddle：一种用于智能体强化学习 Rollout 的分布式编排系统
authors: "Zili Zhang, Yinmin Zhong, Chengxu Yang, Chao Jin, Bingyang Wu, Xinming Wei, Yuliang Liu, Xin Jin"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28101v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 智能体强化学习Rollout的编排系统
tldr: Agentic RL 在 LLM 与工具交互的 rollout 阶段常因长尾轨迹生成面临性能瓶颈。本文提出 Heddle，一个以轨迹为中心的分布式编排系统。它通过轨迹级调度减少排队、感知轨迹的放置策略降低干扰，以及自适应资源管理优化长尾轨迹的执行速度。实验表明，Heddle 在多种工作负载下将端到端吞吐量提升了高达 2.5 倍，有效解决了长尾瓶颈问题。
selection_source: fresh_fetch
motivation: 现有的以步骤为中心的系统无法有效处理 Agentic RL 中频繁工具调用导致的长尾轨迹生成，引发了严重的排队延迟和资源干扰。
method: Heddle 引入了轨迹级调度、基于动态规划的感知放置策略以及动态调整模型并行度的自适应资源管理器。
result: 在不同 Agentic RL 任务中，Heddle 相比现有最先进基准实现了高达 2.5 倍的端到端 rollout 吞吐量提升。
conclusion: 通过从以步骤为中心转向以轨迹为中心的设计，Heddle 显著优化了分布式 Agentic RL 系统的执行效率和资源利用率。
---

## 摘要
智能体强化学习（Agentic RL）通过在数据收集的 rollout 阶段和策略训练阶段之间交替，使大语言模型（LLM）能够解决复杂任务。在 rollout 过程中，智能体生成轨迹（trajectories），即 LLM 与外部工具之间的多步交互。然而，频繁的工具调用会导致长尾轨迹生成，从而成为 rollout 的瓶颈。这源于忽略轨迹上下文的以步骤为中心（step-centric）的设计，为长尾轨迹生成引发了三个系统问题：排队延迟、干扰开销和单 token 时间膨胀。我们提出了 Heddle，这是一个以轨迹为中心（trajectory-centric）的系统，旨在优化智能体 rollout 执行的时间、位置和方式。Heddle 集成了三个核心机制：利用运行时间预测和渐进优先级进行轨迹级调度，以最小化累积排队；通过预排序动态规划和在工具调用空闲间隔期间进行机会性迁移，实现轨迹感知放置，以最小化干扰；以及轨迹自适应资源管理器，动态调整模型并行度，以加速长尾轨迹的单 token 时间，同时保持短轨迹的高吞吐量。在多种智能体强化学习工作负载上的评估表明，Heddle 有效地消除了长尾瓶颈，与最先进的基准方案相比，端到端 rollout 吞吐量提高了多达 2.5 倍。

## Abstract
Agentic Reinforcement Learning (RL) enables LLMs to solve complex tasks by alternating between a data-collection rollout phase and a policy training phase. During rollout, the agent generates trajectories, i.e., multi-step interactions between LLMs and external tools. Yet, frequent tool calls induce long-tailed trajectory generation that bottlenecks rollouts. This stems from step-centric designs that ignore trajectory context, triggering three system problems for long-tail trajectory generation: queueing delays, interference overhead, and inflated per-token time. We propose Heddle, a trajectory-centric system to optimize the when, where, and how of agentic rollout execution. Heddle integrates three core mechanisms: trajectory-level scheduling using runtime prediction and progressive priority to minimize cumulative queueing; trajectory-aware placement via presorted dynamic programming and opportunistic migration during idle tool call intervals to minimize interference; and trajectory-adaptive resource manager that dynamically tunes model parallelism to accelerate the per-token time of long-tail trajectories while maintaining high throughput for short trajectories. Evaluations across diverse agentic RL workloads demonstrate that Heddle effectively neutralizes the long-tail bottleneck, achieving up to 2.5$\times$ higher end-to-end rollout throughput compared to state-of-the-art baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Heddle: A Distributed Orchestration System for Agentic RL Rollout》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：智能体强化学习（Agentic RL）在数据收集（Rollout）阶段存在严重的**长尾瓶颈**。由于智能体需要与外部工具进行多步交互，复杂的任务会产生极长的轨迹（Trajectory），而现有的系统设计多是“以步骤为中心”（Step-centric），将每一步交互视为独立请求。
*   **研究动机**：这种设计忽略了轨迹的上下文，导致三个系统性问题：
    1.  **排队延迟**：长尾轨迹在每一步都需要重新排队。
    2.  **干扰开销**：长短轨迹混杂导致计算资源竞争，增加了干扰系数。
    3.  **单 Token 时间膨胀**：固定的资源分配无法兼顾短轨迹的高吞吐和长轨迹的低延迟需求。
*   **整体含义**：Heddle 旨在通过“以轨迹为中心”的设计，优化 Rollout 执行的“何时（调度）、何地（放置）、如何（资源分配）”。

### 2. 方法论
Heddle 提出了三个协同机制来优化 Rollout 性能：
*   **轨迹级调度（何时）**：
    *   **渐进式预测**：利用轻量级模型（如 Qwen-0.6B）在每个交互步骤后动态更新轨迹长度预测。
    *   **渐进优先级调度（PPS）**：根据预测长度动态提升长尾轨迹的优先级，并引入抢占式执行，确保长任务优先获得 GPU 资源，减少累积排队时间。
*   **轨迹感知放置（何地）**：
    *   **预排序动态规划**：基于引理（最优划分应是按长度排序后的连续子序列），利用动态规划算法将长短轨迹空间隔离，最小化长尾轨迹受到的干扰。
    *   **机会性迁移**：在工具调用的空闲间隔（GPU 不工作时），利用 GPU-Direct RDMA 异步迁移 KV 缓存，修正因预测偏差导致的负载不均。
*   **轨迹自适应资源管理（如何）**：
    *   **异构并行配置**：打破同质化限制，为长尾轨迹分配高模型并行度（MP）以降低延迟，为短轨迹分配低 MP 以保持高吞吐。
    *   **排序初始化模拟退火**：通过启发式搜索算法快速寻找近乎最优的 GPU 资源分配方案。

### 3. 实验设计
*   **数据集与场景**：
    *   **编程（Coding）**：CodeForces 数据集，使用代码执行沙箱工具。
    *   **搜索（Search）**：HotpotQA 数据集，使用 Bing 搜索工具进行多跳推理。
    *   **数学（Math）**：DAPO-Math 数据集，使用计算器和求解器工具。
*   **模型**：Qwen3 系列（8B, 14B, 32B）。
*   **基准方法（Benchmarks）**：
    *   **Slime**：基于 SGLang 的后训练框架，采用最少负载路由。
    *   **Verl**：高性能 RL 框架，采用静态缓存感知放置。
    *   **Verl***：Verl 的改进版，结合了 SGLang 路由器的混合策略。

### 4. 资源与算力
*   **硬件环境**：使用了 8 台服务器，共计 **64 块 NVIDIA Hopper GPU**（每台 8 块）。
*   **网络连接**：内部使用 900 GB/s NVLink，节点间使用 400 Gb/s InfiniBand，支持 GPUDirect RDMA。
*   **内存**：每台服务器配备 1.8 TB 主存。
*   **训练时长**：文中未明确给出总训练时长，但详细记录了算法开销（如放置算法约 0.04s，资源管理约 5s）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同类型的智能体任务、3 种模型规模，并进行了详尽的消融实验。
*   **充分性**：
    *   对比了端到端吞吐量、排队延迟、预测准确率等多个维度。
    *   消融实验分别验证了调度、放置和资源管理三个模块的独立贡献。
*   **客观性**：实验在统一的后端（SGLang）上运行基准测试，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：Heddle 相比现有最先进系统（Slime, Verl），在端到端 Rollout 吞吐量上实现了 **1.4× 至 2.5×** 的提升。
*   **规模效应**：随着模型参数量增加（从 8B 到 32B），Heddle 的优势更加明显，因为它能更有效地缓解大模型带来的计算竞争。
*   **长尾消除**：通过 PPS 调度和异构资源分配，长尾轨迹的排队延迟和执行时间显著缩短，从而降低了整个 Batch 的 Makespan。

### 7. 优点
*   **系统性创新**：从公式化定义 Rollout 瓶颈出发，针对性地解决了调度、放置和资源分配三个维度的难题。
*   **开销掩盖**：巧妙利用工具调用的“空闲期”进行预测和状态迁移，实现了几乎零开销的动态优化。
*   **灵活性**：支持异构资源配置，能够根据实时工作负载动态调整策略。

### 8. 不足与局限
*   **预测依赖性**：系统的效率高度依赖于轨迹长度预测的准确性，虽然有动态修正机制，但在极端不可预测的环境下性能可能受限。
*   **工具调用假设**：优化逻辑部分基于“工具调用期间 GPU 处于空闲”的假设，如果工具执行极快（如微秒级），迁移和预测的开销可能无法完全掩盖。
*   **应用限制**：目前主要针对同步 RL 算法（如 PPO/GRPO），对于完全异步且无状态限制的 RL 场景，其收益可能有所不同。

（完）
