---
title: "ERPO: Token-Level Entropy-Regulated Policy Optimization for Large Reasoning Models"
title_zh: ERPO：大推理模型的令牌级熵正则化策略优化
authors: "Song Yu, Li Li"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28204v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 大型推理模型的强化学习
tldr: 针对大语言模型推理中标准GRPO算法采用序列级奖励导致的信息粒度过粗、熵过早塌陷等问题，本文提出ERPO。通过识别关键决策枢纽（CDPs），引入熵感知门控、桶式隐式归一化和结果锚定优势合成，实现了从序列级到Token级的优化。实验证明ERPO在MATH和AIME等任务上显著优于GRPO，提升了推理准确性与路径简洁度。
selection_source: fresh_fetch
motivation: 标准GRPO的序列级奖励分配忽略了推理链中的Token异质性，导致模型在关键决策点缺乏有效探索并产生冗余内容。
method: 提出ERPO框架，包含熵感知门控以增强关键点探索、桶式归一化缓解难度偏差以及结果锚定的Token级优势合成。
result: 在MATH和AIME等数学基准测试中，ERPO不仅显著提升了推理准确率，还生成了更简洁、鲁棒的推导路径。
conclusion: ERPO通过精细化的Token级熵调节优化，为大型推理模型建立了一个新的效率与准确性平衡前沿。
---

## 摘要
基于可验证奖励的强化学习 (RLVR) 显著提升了大语言模型的推理能力。然而，标准的群体相对策略优化 (GRPO) 通常为所有令牌分配统一的序列级优势值，从而忽略了推理链中内在的信息异质性。我们表明，这种粗粒度的信用分配会导致过早的熵崩溃，并促使模型生成冗余且低质量的推理路径。通过系统的实证分析，我们识别出了关键决策枢纽 (CDPs)：即策略轨迹对扰动最敏感的瞬时高熵状态。这些枢纽代表了“分叉路口”，在这些位置，有效的多路径探索至关重要，但往往被统一的优势信号所抑制。基于这些见解，我们提出了熵正则化策略优化 (ERPO)，将优化重点从粗粒度序列转向细粒度令牌动态。ERPO 引入了三个协同组件：(i) 熵感知门控，自适应地放大 CDPs 处的探索，以促进多样化路径的发现；(ii) 基于桶的隐式归一化，通过对齐令牌进度窗口来减轻难度偏差；(iii) 结果锚定优势综合，通过结果驱动的锚点重新加权令牌级信号。在具有竞争力的数学基准测试（如 MATH、AIME）上的广泛实验表明，ERPO 显著优于 GRPO。值得注意的是，ERPO 不仅提高了推理准确性，还产生了显著更简洁且鲁棒的推导路径，为大推理模型建立了新的效率-准确性前沿。

## Abstract
Reinforcement learning from verifiable rewards (RLVR) has significantly advanced the reasoning capabilities of large language models. However, standard Group Relative Policy Optimization (GRPO) typically assigns a uniform, sequence-level advantage to all tokens, thereby overlooking the intrinsic information heterogeneity along reasoning chains. We show that this coarse-grained credit assignment leads to premature entropy collapse and encourages the model to generate redundant, low-quality reasoning paths. Through systematic empirical analysis, we identify Critical Decision Pivots (CDPs): transient high-entropy states where the policy's trajectory is most sensitive to perturbations. These pivots represent the "forks in the road" where effective multi-path exploration is most crucial yet often suppressed by uniform advantage signals. Building on these insights, we propose Entropy-Regulated Policy Optimization (ERPO), which transitions the optimization focus from coarse sequences to fine-grained token dynamics. ERPO introduces three synergistic components: (i) Entropy-aware Gating, which adaptively amplifies exploration at CDPs to facilitate diverse path discovery; (ii) Bucket-based Implicit Normalization, which mitigates difficulty bias by aligning token progress windows; and (iii) Result-anchored Advantage Synthesis, which re-weights token-level signals via outcome-driven anchors. Extensive experiments on competitive mathematical benchmarks (e.g., MATH, AIME) demonstrate that ERPO significantly outperforms GRPO. Notably, ERPO not only boosts reasoning accuracy but also yields significantly more concise and robust derivation paths, establishing a new efficiency-accuracy frontier for large reasoning models.

---

## 论文详细总结（自动生成）

### ERPO：大推理模型的令牌级熵正则化策略优化 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对大语言模型（LLM）在复杂推理任务中使用的**群体相对策略优化（GRPO）**算法存在的局限性进行了研究。
*   **研究动机**：标准的 GRPO 将序列级的奖励（Reward）均匀地分配给推理链中的每一个令牌（Token），这种“粗粒度”的信用分配忽略了推理过程中不同步骤的重要性差异。
*   **核心发现**：作者通过实证分析识别出**关键决策枢纽（Critical Decision Pivots, CDPs）**，即推理过程中高熵的瞬时状态。这些点是逻辑分叉路口，对扰动极度敏感。
*   **整体含义**：GRPO 的均匀分配会导致模型在关键点缺乏探索，并产生大量冗余、低质量的推理路径。ERPO 旨在通过令牌级的精细化调节，提升推理的准确性与效率。

#### 2. 方法论：核心思想与关键技术
ERPO 的核心思想是将优化重心从“序列级”转向“令牌级动态”，引入了三个协同组件：
*   **熵感知门控（Entropy-aware Gating）**：利用策略熵（Entropy）识别 CDPs。通过一个自适应门控函数 $W_{i,t}$，在模型犹豫（高熵）时放大探索信号，在确定性推导（低熵）时抑制冗余。
*   **桶式隐式归一化（Bucket-based Implicit Normalization）**：考虑到推理的不同阶段（如计划、推导、验证）令牌特征不同，ERPO 将推理过程划分为 $K$ 个时间桶（Buckets），仅在相同进度窗口内的令牌间进行归一化，消除难度偏差。
*   **结果锚定优势合成（Result-anchored Advantage Synthesis）**：将令牌级的隐式进度信号与全局序列奖励结合。使用最终结果的符号作为“锚点”，确保即使是高置信度的令牌，如果最终导致错误答案，也会受到惩罚，从而防止“奖励黑客（Reward Hacking）”行为。
*   **理论支撑**：论文证明了 ERPO 等价于一种动态熵权重的正则化，且通过零和归一化保证了训练的稳定性。

#### 3. 实验设计
*   **数据集**：训练使用 **MATH** 数据集（难度 3-5 级）。评估使用四个高难度竞赛级基准：**AMC23**、**AIME24**、**AIME25** 和 **Minerva**。
*   **模型规模**：基于 **Qwen2.5** 系列（1.5B、3B、7B）。
*   **对比方法（Benchmarks）**：
    *   基础模型（Base）与指令微调模型（Instruct）。
    *   监督微调（SFT）。
    *   **GRPO**（最核心的基线方法）。
    *   商业模型对比：如 DeepSeek-R1-671B 和 Qwen3-235B。
*   **评估指标**：准确率（Acc）、格式合格率（Fmt）、Pass@k、推理序列长度（Token Efficiency）。

#### 4. 资源与算力
*   **训练配置**：使用 LoRA（rank=32）进行高效微调，采用 TRL 框架和 DeepSpeed 优化。
*   **算力细节**：文中未明确给出具体的 GPU 型号和数量，但提到了 7B 模型的总训练时长约为 **14.46 小时**（见图 4b），且 ERPO 的计算开销与 GRPO 基本持平。
*   **超参数**：全局 Batch Size 为 16，每条提示词生成 $G=8$ 条响应。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 7B 的三个参数量级，并在四个独立的数学竞赛数据集上进行了验证。
*   **充分性**：
    *   进行了 **Pass@k**（k=2, 4, 8, 16）的多样本缩放分析。
    *   提供了详细的训练动力学曲线（奖励、熵、梯度范数、KL 散度）。
    *   包含了一项关于令牌熵与准确率因果关系的扰动实验（图 1b），证明了 CDP 的存在。
*   **客观性**：实验对比了远超自身规模的商业模型，并分析了小模型（1.5B）在训练初期的不稳定性，表现出较为客观的科研态度。

#### 6. 主要结论与发现
*   **性能超越**：ERPO 在所有规模上均显著优于 GRPO。ERPO-7B 在某些指标上甚至超越了参数量大几十倍的 DeepSeek-R1-671B。
*   **推理简洁性**：ERPO 模型生成的推理链更短、更高效，成功抑制了冗余的“废话”，实现了更好的准确性-长度平衡（Pareto Frontier）。
*   **防止熵塌陷**：相比 GRPO 在训练后期出现的熵骤降，ERPO 能维持健康的熵水平，保持了生成路径的多样性。

#### 7. 优点
*   **精细化控制**：成功解决了 RLVR 中“稀疏奖励”和“信用分配不均”的痛点。
*   **无需额外模型**：不需要训练专门的过程奖励模型（PRM），仅靠策略自身的熵和参考模型即可实现令牌级优化，成本极低。
*   **效率提升**：在提升准确率的同时缩短了推理长度，降低了推理成本。

#### 8. 不足与局限
*   **小模型不稳定性**：在 1.5B 规模上，ERPO 初期会出现探索滞后，需要更长的训练步数才能反超。
*   **任务局限性**：实验主要集中在数学推理领域，尚未在代码生成、创意写作或多模态任务中验证其普适性。
*   **超参数敏感性**：门控因子 $\gamma$ 和缩放常数 $\eta$ 等参数可能需要针对不同模型进行精细调优。

（完）
