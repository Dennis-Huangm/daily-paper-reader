---
title: "MetaClaw: Just Talk -- An Agent That Meta-Learns and Evolves in the Wild"
title_zh: "MetaClaw: Just Talk —— 一个在真实场景中元学习与进化的智能体"
authors: "Peng Xia, Jianwen Chen, Xinyu Yang, Haoqin Tu, Jiaqi Liu, Kaiwen Xiong, Siwei Han, Shi Qiu, Haonian Ji, Yuyin Zhou, Zeyu Zheng, Cihang Xie, Huaxiu Yao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17187v1"
tags: ["query:ag-rl"]
score: 8.0
evidence: 演进的大语言模型策略与行为技能
tldr: MetaClaw是一个旨在解决LLM智能体在实际应用中难以持续进化的元学习框架。它通过“技能驱动的快速适应”和“机会主义策略优化”两大机制，在不中断服务的情况下，利用用户非活跃时间进行自我进化。该框架结合了技能库自动合成与基于LoRA及RL-PRM的梯度更新，显著提升了智能体在动态任务分布下的准确性和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的LLM智能体通常是静态的，难以在不中断服务的情况下适应不断变化的用户需求和任务分布。
method: 提出MetaClaw框架，结合了基于失败轨迹分析的技能库自动合成，以及在系统空闲时段进行的LoRA微调和过程奖励模型强化学习。
result: "在MetaClaw-Bench等测试中，该框架将Kimi-K2.5的准确率从21.4%提升至40.6%，并显著增强了系统的复合鲁棒性。"
conclusion: MetaClaw证明了通过持续元学习和非侵入式优化，智能体可以在实际运行环境中实现自我进化和性能的持续增长。
---

## 摘要
大语言模型（LLM）智能体越来越多地被用于处理复杂任务，然而已部署的智能体通常保持静态，无法随着用户需求的变化而自适应。这在持续服务的需求与更新能力以匹配不断变化的任务分布的必要性之间造成了冲突。在 OpenClaw 等处理 20 多个频道多样化工作负载的平台上，现有方法要么存储原始轨迹而不提取知识，要么维护静态技能库，或者需要中断服务进行重新训练。我们提出了 MetaClaw，这是一个持续元学习框架，它共同演化基础 LLM 策略和可重用的行为技能库。MetaClaw 采用了两种互补机制。技能驱动的快速自适应通过 LLM 进化器分析失败轨迹以合成新技能，从而在零停机时间的情况下实现即时改进。机会性策略优化通过云端 LoRA 微调和带有过程奖励模型（RL-PRM）的强化学习执行基于梯度的更新。这是由机会性元学习调度器（OMLS）在用户非活跃窗口期间触发的，该调度器监控系统空闲状态和日历数据。这些机制相互增强：精炼的策略为技能合成生成更好的轨迹，而更丰富的技能为策略优化提供更高质量的数据。为了防止数据污染，版本控制机制将支持数据和查询数据分离。MetaClaw 基于代理架构构建，无需本地 GPU 即可扩展到生产级 LLM。在 MetaClaw-Bench 和 AutoResearchClaw 上的实验表明，技能驱动的自适应将准确率相对提高了高达 32%。完整流水线将 Kimi-K2.5 的准确率从 21.4% 提升至 40.6%，并将综合鲁棒性提高了 18.3%。代码可在 https://github.com/aiming-lab/MetaClaw 获取。

## Abstract
Large language model (LLM) agents are increasingly used for complex tasks, yet deployed agents often remain static, failing to adapt as user needs evolve. This creates a tension between the need for continuous service and the necessity of updating capabilities to match shifting task distributions. On platforms like OpenClaw, which handle diverse workloads across 20+ channels, existing methods either store raw trajectories without distilling knowledge, maintain static skill libraries, or require disruptive downtime for retraining. We present MetaClaw, a continual meta-learning framework that jointly evolves a base LLM policy and a library of reusable behavioral skills. MetaClaw employs two complementary mechanisms. Skill-driven fast adaptation analyzes failure trajectories via an LLM evolver to synthesize new skills, enabling immediate improvement with zero downtime. Opportunistic policy optimization performs gradient-based updates via cloud LoRA fine-tuning and Reinforcement Learning with a Process Reward Model (RL-PRM). This is triggered during user-inactive windows by the Opportunistic Meta-Learning Scheduler (OMLS), which monitors system inactivity and calendar data. These mechanisms are mutually reinforcing: a refined policy generates better trajectories for skill synthesis, while richer skills provide higher-quality data for policy optimization. To prevent data contamination, a versioning mechanism separates support and query data. Built on a proxy-based architecture, MetaClaw scales to production-size LLMs without local GPUs. Experiments on MetaClaw-Bench and AutoResearchClaw show that skill-driven adaptation improves accuracy by up to 32% relative. The full pipeline advances Kimi-K2.5 accuracy from 21.4% to 40.6% and increases composite robustness by 18.3%. Code is available at https://github.com/aiming-lab/MetaClaw.

---

## 论文详细总结（自动生成）

### MetaClaw: Just Talk —— 论文深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）智能体在实际部署中的“静态性”问题提出了挑战。尽管 LLM 智能体在多步复杂任务中表现出色，但一旦部署，其模型权重和行为逻辑通常保持不变。然而，现实世界中的用户需求和任务分布是动态变化的（Task Drift），这导致静态模型在面对新任务类型时会反复失败。

现有的改进方法存在明显局限：
*   **基于记忆的方法**：存储原始轨迹，冗余度高且难以提取可迁移的知识。
*   **基于技能的方法**：维护静态技能库，但与模型权重的优化脱节。
*   **基于强化学习（RL）的方法**：通常在离线或小规模场景运行，且在更新模型时会导致服务中断。

**MetaClaw** 的核心目标是构建一个能够“在野外”（In the Wild）持续元学习和进化的框架，在不中断服务的前提下，通过使用过程中的反馈实现自我完善。

---

#### 2. 方法论：核心思想与关键技术
MetaClaw 提出了一个双环路（Two-loop）的持续元学习架构，共同维护基础策略 $\theta$ 和演化技能库 $S$：

*   **技能驱动的快速适应（Skill-driven Fast Adaptation）**：
    *   **核心思想**：当智能体任务失败时，利用一个“LLM 进化器”（Evolver）分析失败轨迹，提取出自然语言形式的行为指令（技能）。
    *   **技术细节**：新技能被即时注入系统提示词（Prompt），无需更新参数，实现零停机时间的即时性能提升。
*   **机会主义策略优化（Opportunistic Policy Optimization）**：
    *   **核心思想**：在用户不使用系统的“空闲窗口”，通过梯度下降更新模型权重。
    *   **关键技术**：
        *   **OMLS 调度器**：监控睡眠时间、键盘/鼠标不活动状态以及 Google 日历预约情况，自动寻找训练时机。
        *   **算法**：采用基于过程奖励模型（PRM）的强化学习（如 GRPO 算法），通过云端 LoRA 进行微调。
*   **技能版本控制机制（Versioning）**：
    *   为了解决元学习中的数据有效性问题，MetaClaw 严格区分“支持数据”（用于合成技能的失败轨迹）和“查询数据”（技能生效后的新轨迹）。只有查询数据才被用于 RL 更新，防止旧技能背景下的陈旧奖励信号污染梯度。

---

#### 3. 实验设计
论文在两个主要场景下进行了评估：
*   **MetaClaw-Bench**：一个新构建的持续智能体基准测试，包含 934 个问题，模拟了 44 个工作日。任务涉及文件编辑、JSON 结构化处理、Shell 脚本编写等。
    *   **Part I**：侧重于执行可靠性，任务难度随天数单调增加。
    *   **Part II**：侧重于规则内化，测试智能体学习隐含偏好（如特定时间格式、备份协议）的速度。
*   **AutoResearchClaw**：一个包含 23 个阶段的自主科研流水线（从构思到论文撰写），用于验证框架在长程、复杂任务中的泛化能力。
*   **对比方法**：
    *   **Baseline**：原始模型（GPT-5.2, Kimi-K2.5）。
    *   **MetaClaw (Skills)**：仅开启技能进化。
    *   **MetaClaw (Full)**：开启技能进化 + 机会主义 RL 优化。

---

#### 4. 资源与算力
*   **算力架构**：MetaClaw 采用代理（Proxy-based）架构，**无需本地 GPU**。
*   **训练后端**：利用云端训练 API（如 Tinker）进行 LoRA 微调。
*   **训练时长**：文中提到针对 Kimi-K2.5 的完整流水线评估涉及了为期 **5 天** 的训练运行。
*   **具体型号**：未明确说明云端具体的 GPU 型号和数量，但强调了其对生产级 LLM 的可扩展性。

---

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了近千个测试问题和数十个模拟工作日，实验跨度较长。
*   **消融实验**：对比了“仅技能”和“全流水线”模式，清晰展示了不同机制对性能的贡献（技能提升推理质量，RL 提升执行成功率）。
*   **模型覆盖**：测试了 GPT-5.2 和 Kimi-K2.5 两个前沿模型，证明了框架对不同能力水平模型的普适性。
*   **客观性**：通过自动化检查脚本（Checker）进行评分，减少了人工评估的主观偏差。实验设计考虑了任务难度的递增和跨领域（科研流水线）的迁移，较为充分。

---

#### 6. 主要结论与发现
*   **显著提升**：技能驱动的自适应使准确率相对提升高达 32.2%。
*   **全能力补齐**：MetaClaw (Full) 将 Kimi-K2.5 的准确率从 21.4% 提升至 40.6%，在端到端任务完成率上实现了 **8.25 倍** 的增长，几乎抹平了其与 GPT-5.2 基准线之间的差距。
*   **互补性**：技能注入能快速修正逻辑错误，而权重更新（RL）对于解决复杂的执行层错误（如文件操作规范）至关重要。
*   **泛化性**：在 AutoResearchClaw 中，仅靠技能注入就将复合鲁棒性提升了 18.3%，证明了从 CLI 任务中提取的经验可以迁移到科研场景。

---

#### 7. 优点与亮点
*   **双尺度进化**：巧妙结合了“秒级”的 Prompt 适应和“小时/天级”的权重优化，兼顾了响应速度和深度学习。
*   **非侵入式设计**：OMLS 调度器利用日历和系统状态感知用户空闲，解决了 AI 进化与持续服务之间的冲突。
*   **版本控制逻辑**：通过版本戳（Version Stamp）分离数据，解决了在线强化学习中常见的奖励污染问题，具有很强的工程参考价值。
*   **低门槛部署**：代理架构使得普通用户无需昂贵的本地算力即可让自己的智能体持续进化。

---

#### 8. 不足与局限
*   **调度依赖性**：空闲窗口的检测高度依赖于用户的配置和系统权限（如访问日历），在某些受限环境下可能失效。
*   **数据偏差风险**：如果用户初期的任务分布极其单一，可能会导致模型过度拟合特定习惯，产生负迁移。
*   **模拟环境局限**：MetaClaw-Bench 虽然模拟了真实 CLI，但仍是预设的脚本环境，真实世界中不可预测的环境反馈可能比 Benchmark 更复杂。
*   **成本考量**：虽然无需本地 GPU，但频繁调用云端 LLM 进行技能合成和 LoRA 微调仍会产生持续的 API 费用。

（完）
