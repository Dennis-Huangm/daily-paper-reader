---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的分布式强化学习框架
tldr: 针对大规模视觉-语言-动作（VLA）模型在强化学习中面临的计算效率低和数据获取难的问题，本文提出了AcceRL框架。该框架采用全异步解耦设计，通过物理隔离训练、推理和采样过程消除同步瓶颈，并首次在分布式异步流水线中集成可训练的世界模型以生成虚拟经验。实验证明，AcceRL在LIBERO基准上达到了SOTA性能，显著提升了采样效率和硬件利用率。
selection_source: fresh_fetch
motivation: 解决大规模VLA模型在强化学习训练中存在的计算效率低下和数据采集困难的挑战。
method: 提出一种全异步解耦的分布式框架，并集成即插即用的世界模型来生成虚拟交互经验。
result: 在LIBERO基准测试中取得SOTA性能，并实现超线性的吞吐量扩展和极高的硬件利用率。
conclusion: AcceRL通过异步架构与世界模型的结合，为复杂控制任务提供了高效且稳定的强化学习解决方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

### AcceRL：视觉-语言-动作模型的分布式异步强化学习与世界模型框架总结

#### 1. 论文的核心问题与整体含义
论文针对大规模**视觉-语言-动作（VLA）模型**在强化学习（RL）微调过程中面临的两个核心瓶颈：
*   **计算效率低下**：传统同步 RL 框架存在“长尾效应”，即训练器必须等待所有采样节点完成数据收集，导致高性能 GPU 在等待缓慢的物理模拟器时处于闲置状态。
*   **数据获取困难**：物理模拟器运行速度慢且 VLA 模型推理延迟高，导致获取数百万次交互样本的时间成本极高。
**AcceRL** 的提出旨在通过全异步解耦架构和集成“世界模型”来打破这些限制，实现高效、高吞吐量的具身智能模型训练。

#### 2. 论文提出的方法论
AcceRL 采用了多层级的异步解耦设计和算法优化：
*   **宏观异步（训练与采样解耦）**：训练节点（Trainer）与采样节点（Rollout Worker）物理隔离。采样节点持续将轨迹存入重放池，训练节点并行采样并更新参数，通过 NCCL 广播更新推理节点的权重。
*   **微观异步（交互与推理解耦）**：采用“推理即服务（Inference-as-a-Service）”模式。采样节点仅负责环境交互，将观察结果发送至集中的推理池（Inference Pool），利用动态批处理（Dynamic Batching）最大化 GPU 利用率。
*   **世界模型（World Model）集成**：首次在异步框架中集成可训练的扩散模型（基于 DIAMOND 架构）。模型可以在“想象”中进行虚拟采样，绕过物理模拟器的速度限制，将样本效率提升了 200 倍。
*   **算法优化**：
    *   **GIPO 算法**：使用高斯重要性采样策略优化（GIPO）替代标准 PPO 的硬截断，以应对异步架构中严重的策略滞后（Policy Lag）问题。
    *   **价值重计算与全局优势归一化**：在梯度更新前重新计算状态价值，确保优势信号的准确性。
    *   **VLA 专用优化**：包括词表裁剪（Vocabulary Slimming）以减少显存占用，以及 Token 级别的策略优化以增强数值稳定性。

#### 3. 实验设计
*   **数据集/场景**：使用了 **LIBERO** 具身智能基准测试，涵盖四个子任务库：LIBERO-Spatial（空间关系）、LIBERO-Object（物体操作）、LIBERO-Goal（目标达成）和 LIBERO-Long（长程任务）。
*   **Benchmark 与对比方法**：
    *   **基准模型**：OpenVLA-OFT（基于模仿学习的基准）。
    *   **对比框架**：SimpleVLA-RL、RLinf-VLA（均为近期最先进的 VLA-RL 框架）。
    *   **评估指标**：任务成功率、系统吞吐量（SPS，每秒样本数）、硬件利用率。

#### 4. 资源与算力
*   **GPU 型号**：实验中明确提到了使用 **NVIDIA H200** GPU。
*   **规模**：
    *   **训练端**：扩展至 7 个 Trainer GPU，展示了超线性扩展能力。
    *   **采样端**：扩展至 64 个 Rollout Worker。
*   **训练时长**：文中未明确给出总训练小时数，但通过 SPS 数据（最高超过 100 SPS）强调了其极高的迭代速度。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 LIBERO 的四个维度上进行了全面测试，并进行了吞吐量扩展性实验。
*   **消融实验**：针对“价值重计算（Value Recomputation）”和“GIPO 算法 vs. PPO”进行了专门的对比实验。
*   **充分性评价**：实验设计较为充分，不仅验证了算法的最终性能（成功率），还深入分析了分布式系统的扩展效率。通过对比 SOTA 框架，证明了其在长程任务（LIBERO-Long）上的显著优势，实验结果具有较强的说服力和客观性。

#### 6. 论文的主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务中成功率达 99.1%，远超模仿学习基准（90.7%）。
*   **超线性扩展**：得益于 ZeRO-2 优化和异步架构，系统吞吐量随 GPU 数量增加呈现超线性增长，硬件利用率保持在 94% 以上。
*   **样本效率飞跃**：引入世界模型后，仅需 10,000 次真实环境交互即可实现性能突破，样本效率提升 20,000%。
*   **稳定性增强**：GIPO 算法相比 PPO 在异步环境下收敛速度快 7.5 倍，且波动更小。

#### 7. 优点
*   **架构创新**：彻底解决了 VLA 模型训练中的 GPU 空转问题，实现了真正的全异步流水线。
*   **即插即用世界模型**：将生成式 AI（扩散模型）与分布式 RL 结合，为解决具身智能数据匮乏提供了切实可行的方案。
*   **工程落地性强**：词表裁剪、Token 级优化等细节处理展示了对大模型微调痛点的深刻理解。

#### 8. 不足与局限
*   **模型规模限制**：虽然支持 7B 级别的 VLA，但尚未验证在更大规模（如 70B+）模型上的表现。
*   **任务覆盖**：实验主要集中在模拟环境（MuJoCo），缺乏在真实物理机器人上的部署验证。
*   **对齐能力**：论文提到目前尚不支持大规模语言模型的后训练（Post-training）对齐，这限制了其在更通用对话/指令遵循任务中的应用。
*   **世界模型开销**：虽然减少了环境交互，但训练和运行高质量扩散世界模型本身也需要巨大的计算资源。

（完）
