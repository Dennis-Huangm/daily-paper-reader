---
title: "Gym-V: A Unified Vision Environment System for Agentic Vision Research"
title_zh: Gym-V：一个用于智能体视觉研究的统一视觉环境系统
authors: "Fanqing Meng, Lingxiao Du, Jiawei Gu, Jiaqi Liao, Linjie Li, Zijian Wu, Xiangyan Liu, Ziqi Zhao, Mengkang Hu, Yue Zhang, Zichen Liu, Jiaheng Zhang, Michael Qizhe Shieh"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15432v2"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于智能体视觉研究的统一视觉环境系统，利用可验证奖励进行强化学习
tldr: Gym-V是一个统一的视觉环境平台，包含10个领域、179个程序化生成的视觉任务，旨在解决视觉智能体缺乏标准化基础设施的问题。研究发现，观察脚手架（如描述和规则）对训练成功的影响超过了强化学习算法的选择。该平台支持受控实验，揭示了跨领域迁移和多轮交互对模型泛化能力的影响，为视觉语言模型（VLM）智能体的训练和评估提供了重要基础。
selection_source: fresh_fetch
motivation: 视觉智能体研究缺乏标准化的基础设施，导致难以进行系统性的学习驱动因素分析和公平比较。
method: 开发了包含179个具有可控难度的程序化生成视觉环境的统一平台Gym-V，涵盖10个不同领域。
result: 实验表明观察脚手架比RL算法更关键，且多样化任务训练能显著提升泛化能力，而窄域训练可能导致负迁移。
conclusion: Gym-V为视觉智能体的训练和评估提供了便捷的基础设施，有助于加速未来视觉语言模型智能体的研究。
---

## 摘要
随着智能体系统越来越多地依赖于来自可验证奖励的强化学习，标准化的“gym”基础设施对于快速迭代、可复现性和公平比较已变得至关重要。视觉智能体缺乏此类基础设施，这限制了对驱动其学习的因素以及当前模型不足之处的系统性研究。我们推出了 Gym-V，这是一个包含 10 个领域、179 个程序化生成的视觉环境的统一平台，具有可控的难度，能够实现以前在碎片化工具包中无法进行的受控实验。通过使用该平台，我们发现观察支架（observation scaffolding）对训练成功的决定性作用超过了强化学习算法的选择，其中字幕和游戏规则决定了学习是否能够成功。跨领域迁移实验进一步表明，在多样化的任务类别上进行训练具有广泛的泛化能力，而狭隘的训练可能导致负迁移，多轮交互会放大所有这些效应。Gym-V 作为一个便捷的训练环境和评估工具包基础发布，旨在加速未来对智能体视觉语言模型（VLMs）的研究。

## Abstract
As agentic systems increasingly rely on reinforcement learning from verifiable rewards, standardized ``gym'' infrastructure has become essential for rapid iteration, reproducibility, and fair comparison. Vision agents lack such infrastructure, limiting systematic study of what drives their learning and where current models fall short. We introduce \textbf{Gym-V}, a unified platform of 179 procedurally generated visual environments across 10 domains with controllable difficulty, enabling controlled experiments that were previously infeasible across fragmented toolkits. Using it, we find that observation scaffolding is more decisive for training success than the choice of RL algorithm, with captions and game rules determining whether learning succeeds at all. Cross-domain transfer experiments further show that training on diverse task categories generalizes broadly while narrow training can cause negative transfer, with multi-turn interaction amplifying all of these effects. Gym-V is released as a convenient foundation for training environments and evaluation toolkits, aiming to accelerate future research on agentic VLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Gym-V: A Unified Vision Environment System for Agentic Vision Research》的结构化深度总结：

### 1. 研究动机与背景
*   **核心问题**：尽管文本智能体（LLM Agents）已有成熟的标准化基础设施（如 Reasoning Gym），但视觉智能体（Vision Agents）仍缺乏统一的训练与评估平台。
*   **现状挑战**：现有的视觉环境（如 VAGEN, VLM-Gym）任务种类单一、接口不统一，且视觉感知往往成为推理的瓶颈。
*   **研究意义**：Gym-V 旨在通过提供一个包含 179 个程序化生成环境的统一平台，解决视觉智能体研究中实验不可复现、难以公平比较以及训练信号难以规模化获取的问题。

### 2. 方法论与关键技术
*   **核心思想**：构建一个遵循 OpenAI Gym 标准接口的统一视觉环境系统，将交互式训练、离线监督学习和基准测试评估整合在同一套 API（`reset`/`step`）下。
*   **关键技术细节**：
    *   **程序化生成与难度控制**：所有环境均支持参数化生成，可调节网格大小、物体数量等，以提供持续的学习信号并防止模型过拟合。
    *   **可组合包装器（Wrappers）**：将观察设计（如是否提供字幕、规则描述、历史窗口长度）作为独立的实验变量，无需修改环境底层逻辑即可改变任务呈现方式。
    *   **分布式奖励服务（EaaS）**：针对生成式任务（如文生图），通过 Ray Serve 部署分布式奖励模型（如 CLIP, HPSv3），实现高效的批量评分。
    *   **多智能体支持**：原生支持单人、竞争和协作模式，接口与 Ray RLlib 兼容。

### 3. 实验设计
*   **场景与数据集**：涵盖 10 个领域（算法、ARC、认知、几何、图论、逻辑、谜题、游戏、空间导航、时序控制），共 179 个环境。
*   **对比基准（Benchmarks）**：
    *   **模型对比**：评估了包括 Gemini-3-Pro、Claude 4.5、Qwen3-VL 系列、Qwen2.5-VL 系列在内的 9 个主流视觉语言模型（VLM）。
    *   **算法对比**：在强化学习训练中对比了 GRPO、GSPO 和 SAPO 三种算法。
    *   **消融实验**：针对历史长度（Context）、游戏规则（Rules）和文本脚手架（Captions）进行了深入消融。
    *   **迁移学习**：构建了 6x6（单轮）和 4x4（多轮）的跨领域迁移矩阵。

### 4. 资源与算力
*   **模型配置**：训练实验主要使用 **Qwen2.5-VL-7B-Instruct** 作为基座模型。
*   **框架支持**：使用了 **verl** 强化学习框架，并在训练期间冻结了视觉编码器（Vision Tower）。
*   **算力说明**：论文提到所有算法在相同的计算预算和超参数下运行，单轮任务每轮迭代采样 256 个实例，多轮任务采样 256 条完整轨迹。**文中未明确给出具体的 GPU 型号、数量或总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的零样本（Zero-shot）评估、16 个环境下的 RL 算法对比、4 个游戏环境下的多维度消融实验，以及覆盖全领域的迁移学习研究。
*   **充分性评价**：实验设计非常充分且客观。通过程序化生成确保了测试集的非污染性；通过统一接口确保了算法比较的公平性；消融实验深入探讨了视觉任务中特有的“观察脚手架”问题，具有很高的科学价值。

### 6. 主要结论与发现
*   **观察脚手架至关重要**：在视觉 RL 训练中，**观察脚手架（如添加图像字幕、明确游戏规则）对成功的决定性超过了 RL 算法的选择**。
*   **算法表现具有场景相关性**：没有一种 RL 算法在所有环境下都占优，GSPO 在长程多轮任务中表现更稳健，而 SAPO 在复杂环境下可能崩溃。
*   **泛化与负迁移**：在多样化任务（如认知类）上训练能产生广泛的正向迁移，而在单一任务（如几何类）上训练可能导致负迁移。
*   **多轮交互的放大效应**：多轮交互会显著放大感知错误和策略漂移，使得脚手架设计和课程学习变得更加关键。

### 7. 优点与亮点
*   **高度统一性**：首次将视觉智能体的训练、离线学习和评估（包括判别式和生成式任务）统一在单一 Gym 接口下。
*   **实验可控性**：通过 Wrapper 设计，将“模型如何看世界”变成了可量化的实验变量。
*   **规模与多样性**：179 个环境远超同类视觉 Gym 平台，且覆盖了从静态推理到动态控制的广泛频谱。
*   **EaaS 架构**：解决了视觉奖励模型计算开销大、部署难的痛点。

### 8. 不足与局限
*   **时序任务挑战**：在 Retro Arcade（复古街机）等时序控制任务中，当前所有 VLM 表现几乎为零，显示出模型在精细时间控制上的巨大鸿沟。
*   **计算成本**：尽管有 EaaS，但大规模 VLM 的强化学习训练依然面临极高的推理成本。
*   **视觉编码器冻结**：实验中冻结了视觉编码器，可能限制了模型在极度依赖底层视觉特征任务中的学习上限。

（完）
