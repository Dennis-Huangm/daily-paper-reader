---
title: "CodeScout: An Effective Recipe for Reinforcement Learning of Code Search Agents"
title_zh: CodeScout：代码搜索智能体强化学习的有效方案
authors: "Lintang Sutawika, Aditya Bharat Soni, Bharath Sriraam R R, Apurva Gandhi, Taha Yassine, Sanidhya Vijayvargiya, Yuchen Li, Xuhui Zhou, Yilin Zhang, Leander Melroy Maben, Graham Neubig"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17829v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 代码搜索智能体的强化学习
tldr: 本研究针对大型代码库中的代码定位难题，提出了 CodeScout 框架。不同于以往依赖复杂静态分析工具的方法，CodeScout 仅利用标准 Unix 终端，通过精心设计的强化学习方案（包括环境复用、奖励设计和 RL 优化）训练智能体。实验证明，该模型在 SWE-Bench 等基准测试中表现卓越，不仅超越了规模大其数倍的开源模型，甚至能与 Claude Sonnet 等顶级闭源模型相媲美，为构建高效代码搜索智能体提供了新路径。
selection_source: fresh_fetch
motivation: 现有的代码搜索智能体通常依赖复杂的专用工具（如静态分析图），限制了其在不同环境下的通用性与部署效率。
method: 提出 CodeScout，通过强化学习训练仅配备标准 Unix 终端的智能体，并重点优化了环境适配、奖励机制和 RL 训练策略。
result: 在 SWE-Bench Verified、Pro 和 Lite 基准测试中，CodeScout 的性能优于规模大 2-18 倍的模型，且接近 Claude Sonnet 等顶级闭源模型。
conclusion: 研究证明了通过有效的强化学习配方，即使仅使用基础工具，也能训练出在复杂代码定位任务中极具竞争力的智能体。
---

## 摘要
编程智能体在大型代码库中执行任务的前提是代码定位，即识别出需要处理的相关文件、类和函数。虽然代码库级别的代码定位通常使用基于嵌入的检索方法（如向量搜索）来完成，但最近的研究重点在于开发能够定位相关代码的智能体，这种定位既可以作为独立的前置步骤，也可以与实际工作交替进行。大多数先前的智能体化代码搜索方法为智能体配备了复杂的专用工具，例如通过静态分析生成的代码库图。在本文中，我们证明了通过有效的强化学习方案，仅配备标准 Unix 终端的编程智能体也能通过训练获得出色的结果。我们在三个基准测试（SWE-Bench Verified、Pro 和 Lite）上的实验表明，我们的模型始终优于或等同于规模大 2-18 倍的基础及经过后训练的大语言模型（LLM），且在某些情况下，即使后者使用了专门的脚手架，我们的模型也能接近 Claude Sonnet 等闭源模型的性能。我们的工作特别关注于将现有的编程智能体环境重新用于代码搜索的技术、奖励设计以及强化学习优化。我们发布了由此产生的模型系列 CodeScout，以及所有代码和数据，供社区在此基础上进一步开发。

## Abstract
A prerequisite for coding agents to perform tasks on large repositories is code localization - the identification of relevant files, classes, and functions to work on. While repository-level code localization has been performed using embedding-based retrieval approaches such as vector search, recent work has focused on developing agents to localize relevant code either as a standalone precursor to or interleaved with performing actual work. Most prior methods on agentic code search equip the agent with complex, specialized tools, such as repository graphs derived from static analysis. In this paper, we demonstrate that, with an effective reinforcement learning recipe, a coding agent equipped with nothing more than a standard Unix terminal can be trained to achieve strong results. Our experiments on three benchmarks (SWE-Bench Verified, Pro, and Lite) reveal that our models consistently achieve superior or competitive performance over 2-18x larger base and post-trained LLMs and sometimes approach performance provided by closed models like Claude Sonnet, even when using specialized scaffolds. Our work particularly focuses on techniques for re-purposing existing coding agent environments for code search, reward design, and RL optimization. We release the resulting model family, CodeScout, along with all our code and data for the community to build upon.

---

## 论文详细总结（自动生成）

以下是对论文《CodeScout: An Effective Recipe for Reinforcement Learning of Code Search Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在处理大型代码库任务（如 SWE-Bench）时，智能体首先需要进行“代码定位”（识别相关文件、类和函数）。
*   **核心挑战**：现有方法通常依赖复杂的、特定编程语言的工具（如基于静态分析的代码图、符号跳转工具），这增加了系统的复杂性且难以跨语言迁移。
*   **整体含义**：论文提出了 **CodeScout**，旨在证明通过一套有效的**强化学习（RL）方案**，即使智能体仅配备最基础的 **Unix 终端工具**（如 `grep`, `ls`），也能在代码定位任务上达到甚至超越配备复杂专用工具的大型模型。

### 2. 方法论
*   **核心思想**：利用极简的工具集（Bash 终端）配合精心设计的强化学习流程，提升智能体在复杂代码库中的自主导航和搜索能力。
*   **关键技术细节**：
    *   **OpenHands-Bash 脚手架**：智能体仅使用标准 Unix 命令（`rg`, `find`, `sed` 等）和一个用于提交结果的 `LocalizationFinish` 工具。
    *   **奖励设计（Reward Design）**：奖励函数 $r$ 是文件级、模块级和函数级三个粒度的 F1 分数之和。针对 14B 模型，还引入了步数惩罚项，鼓励智能体在规定步数内完成任务。
    *   **RL 训练算法**：采用 **GSPO（Group Sequence Policy Optimization）**。该算法通过组内序列似然对比来优化策略，移除了 KL 散度正则化和优势标准化，提高了训练效率。
    *   **数据生成**：从 SWE-Smith 提取了 39K 个 Python 任务，通过解析原始补丁（Gold Patch）自动生成三层级的定位真值（Ground Truth）。

### 3. 实验设计
*   **数据集/场景**：
    *   **SWE-Bench Verified** (500 个实例)：经过人工验证的高质量 Bug 修复任务。
    *   **SWE-Bench Lite** (300 个实例)：精简版任务。
    *   **SWE-Bench Pro** (266 个 Python 实例)：更具挑战性的长程软件工程任务。
*   **对比方法**：
    *   **开源基线**：LocAgent, CoSIL, OrcaLoca, RepoSearcher, RepoNavigator, Agentless。
    *   **闭源模型**：GPT-5, Claude-3.7-Sonnet, Claude-Sonnet-4.5。
    *   **基础模型**：Qwen3 系列（1.7B, 4B, 14B, 32B）。

### 4. 资源与算力
*   **硬件支持**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   **4B 模型**：训练 200 步，使用 1.6K 实例，Batch Size 为 8，每个实例采样 8 个 Rollout。
    *   **14B 模型**：训练 300 步，使用 9.6K 实例，Batch Size 为 32，每个实例采样 4 个 Rollout。
    *   **1.7B 模型**：先通过 14B 模型的成功轨迹进行拒绝采样微调（RFT），再进行 100 步 RL 训练。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准测试上进行了全面评估，涵盖了从 1.7B 到 14B 不同规模的模型。
*   **消融实验**：论文详细对比了不同 RL 算法（GSPO, GRPO, SAPO 等）的影响，分析了工具使用行为随训练步数的演变过程，并验证了定位结果对下游 Bug 修复任务的实际增益。
*   **充分性与客观性**：实验对比了多种不同复杂度的脚手架（Scaffold），并使用了 F1 分数这一综合指标，能够客观反映模型在查准率和查全率之间的平衡，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能卓越**：CodeScout-14B 在所有基准测试中均达到了开源模型的最优水平（SoTA），在函数级定位上甚至超过了配备专用工具的 Claude 3.7。
*   **参数效率极高**：经过 RL 训练的 4B 模型性能优于规模大其 8 倍的 32B 基础模型。
*   **工具使用进化**：RL 训练使智能体学会了“精简”工具箱，从最初乱用各种命令进化到主要依赖 `ripgrep (rg)` 和 `sed` 进行高效搜索。
*   **下游增益**：精准的代码定位显著提升了 Bug 修复智能体的成功率（最高提升 3.8%），同时减少了约 17% 的 Token 消耗。

### 7. 优点
*   **极简且通用**：不依赖特定语言的静态分析工具，理论上可以轻松扩展到 Python 以外的编程语言。
*   **训练方案高效**：证明了直接在简单的终端环境上进行 RL 训练的可行性，避开了昂贵的闭源模型蒸馏。
*   **开源贡献**：完整发布了模型权重、训练代码和处理后的数据集，具有很高的社区价值。

### 8. 不足与局限
*   **语言局限性**：目前的实验和真值提取脚本主要针对 Python，尚未在多语言环境下进行大规模验证。
*   **真值定义偏差**：定位真值仅基于被修改的代码行，可能忽略了那些“为了理解上下文而必须阅读但无需修改”的文件。
*   **任务范围**：研究集中在 Bug 修复相关的定位，尚未涉及更通用的代码库问答或大规模重构场景。

（完）
