---
title: Internalizing Agency from Reflective Experience
title_zh: 从反思性经验中内化代理能力
authors: "Rui Ge, Yichao Fu, Yuyang Qian, Junda Su, Yiming Zhao, Peng Zhao, Hao Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16843v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 在LLM中学习基于反馈的代理能力的框架
tldr: 针对大语言模型智能体在长程交互中难以有效利用环境反馈的问题，本文提出LEAFE框架。该方法通过将环境反馈转化为可操作的经验，引导模型在失败点回溯并探索修正路径，最后通过监督微调将这些反思性经验内化。实验表明，LEAFE在交互式编程和智能体任务中显著提升了模型的恢复能力和解题成功率，优于传统的强化学习方法。
selection_source: fresh_fetch
motivation: 现有的结果驱动型训练方法过度关注最终成功信号，忽视了丰富的环境反馈，导致模型难以在复杂长程任务中通过自我修正来扩展解题能力。
method: 提出LEAFE框架，通过总结反馈为经验、回溯决策点并探索修正分支，最后将这些经验引导的修正行为蒸馏到模型中。
result: "在多项交互式编程和智能体任务中，LEAFE在固定交互预算下显著提升了Pass@1，且在Pass@128上比GRPO等基线高出多达14%。"
conclusion: 通过内化反思性经验，模型能够更有效地从错误中恢复，从而在长程交互任务中展现出更强的自主性和解题潜力。
---

## 摘要
大语言模型越来越多地被部署为自主代理，必须通过与提供丰富反馈的环境进行长程交互，来执行规划、行动并从错误中恢复。然而，主流的结果驱动型后训练方法（例如带有可验证奖励的强化学习）主要优化最终的成功信号，导致丰富的环境反馈未被充分利用。因此，这些方法通常会导致分布收缩：策略变得更擅长复现一小部分已经成功的行为，却未能提升在长程场景中扩展问题解决能力（如 Pass@k）所需的基于反馈的代理能力。为了解决这一问题，我们提出了 LEAFE（从反思性经验中学习基于反馈的代理能力），这是一个从反思性经验中内化恢复代理能力的框架。具体而言，在探索过程中，代理将环境反馈总结为可操作的经验，回溯到早期的决策点，并使用修正后的动作探索替代分支。随后，我们通过监督微调将这些经验引导的修正蒸馏到模型中，使策略能够在未来的交互中更有效地恢复。在固定交互预算下的一系列多样化交互式编程和代理任务中，LEAFE 在 Pass@1 上始终优于基础模型，并且比结果驱动的基准方法（如 GRPO）以及基于经验的方法（如 Early Experience）实现了更高的 Pass@k，在 Pass@128 上提升高达 14%。

## Abstract
Large language models are increasingly deployed as autonomous agents that must plan, act, and recover from mistakes through long-horizon interaction with environments that provide rich feedback. However, prevailing outcome-driven post-training methods (e.g., RL with verifiable rewards) primarily optimize final success signals, leaving rich environment feedback underutilized. Consequently, they often lead to distribution sharpening: the policy becomes better at reproducing a narrow set of already-successful behaviors, while failing to improve the feedback-grounded agency needed to expand problem-solving capacity (e.g., Pass@k) in long-horizon settings.   To address this, we propose LEAFE (Learning Feedback-Grounded Agency from Reflective Experience), a framework that internalizes recovery agency from reflective experience. Specifically, during exploration, the agent summarizes environment feedback into actionable experience, backtracks to earlier decision points, and explores alternative branches with revised actions. We then distill these experience-guided corrections into the model through supervised fine-tuning, enabling the policy to recover more effectively in future interactions. Across a diverse set of interactive coding and agentic tasks under fixed interaction budgets, LEAFE consistently improves Pass@1 over the base model and achieves higher Pass@k than outcome-driven baselines (GRPO) and experience-based methods such as Early Experience, with gains of up to 14% on Pass@128.

---

## 论文详细总结（自动生成）

以下是对论文《Internalizing Agency from Reflective Experience》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前大语言模型（LLM）智能体在长程交互任务中，主要依赖“结果驱动”的后训练方法（如基于可验证奖励的强化学习 RLVR/GRPO）。
*   **核心矛盾**：这类方法仅利用最终的成功/失败信号，忽视了环境中丰富的中间反馈（如报错信息、状态变化）。这导致了**“分布收缩”（Distribution Sharpening）**现象：模型只是更频繁地重复已有的成功行为，而没有学会如何从错误中恢复，导致其在需要多次尝试的复杂任务中（即大 $k$ 时的 Pass@k）能力提升有限。
*   **整体含义**：本文旨在通过一种名为 **LEAFE** 的框架，让模型从“反思性经验”中学习，将“识别错误并修正决策”的能力内化到模型权重中，从而提升智能体在长程任务中的自主代理能力（Agency）。

### 2. 方法论：LEAFE 框架
LEAFE 采用两阶段流程，将外部反馈转化为内在能力：
*   **阶段 1：基于树状回溯的经验生成（Tree-Based Experience Generation）**
    *   **周期性反思**：智能体在探索过程中，每隔 $K$ 步或遇到失败时，会调用反思程序，识别出导致失败的早期决策点 $\tau$ 并生成一段简洁的经验总结 $e$（诊断及修复建议）。
    *   **回溯与分支探索**：环境重置到 $\tau$ 时刻，模型在经验 $e$ 的引导下尝试新的修正动作 $a'_\tau$，并继续完成任务。这形成了一个“失败 $\rightarrow$ 反思 $\rightarrow$ 回溯 $\rightarrow$ 修正 $\rightarrow$ 成功”的树状探索路径。
*   **阶段 2：经验蒸馏（Experience Distillation）**
    *   **行为排演（Behavior Rehearsal）**：对所有成功的轨迹进行标准的监督微调（SFT），以保持基础任务能力。
    *   **经验到策略蒸馏（Experience-to-Policy Distillation）**：这是核心。将阶段 1 中由经验 $e$ 引导出的修正动作 $a'_\tau$，作为目标标签，但在训练时**不提供**经验 $e$，只提供原始上下文。
    *   **目标函数**：$L = L_{cf} + \beta L_{reh}$。通过这种方式，模型学会了在没有显式提示的情况下，自发地做出正确的纠错决策。

### 3. 实验设计
*   **数据集/场景**：
    1.  **WebShop**：模拟在线购物导航。
    2.  **ALFWorld**：基于文本的具身家庭任务。
    3.  **ScienceWorld**：多步科学实验模拟。
    4.  **Sokoban**：逻辑规划类推箱子游戏。
    5.  **CodeContests**：竞赛级编程任务（含执行反馈）。
*   **对比方法（Baselines）**：
    *   **Base**：未经任务微调的指令模型。
    *   **GRPO-RLVR**：目前主流的结果驱动强化学习方法。
    *   **EarlyExp**：通过隐式世界建模学习早期经验的方法。
    *   **ACE**：一种不更新权重、仅通过外部 Prompt 积累经验的方法。
*   **评价指标**：Pass@1（单次成功率）和 Pass@128（多次采样下的成功率上限）。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen2.5 (7B, 14B, 32B, 72B) 和 Llama-3/3.1 (8B, 70B) 系列。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到使用了 `verl` 和 `verl-agent` 分布式训练框架，SFT 阶段的学习率为 $1 \times 10^{-6}$，Batch Size 为 128。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的 Benchmark 上进行了测试，涵盖了从 7B 到 72B 的多种模型架构。
*   **充分性**：
    *   **消融实验**：验证了蒸馏损失函数中各部分的贡献。
    *   **扩展性分析**：展示了随模型参数量增加，LEAFE 带来的增益持续存在。
    *   **泛化性实验**：在 CodeContests 上训练，在 MBPP 上测试 OOD（分布外）性能，证明了模型学到的是通用代理能力而非过拟合。
*   **客观性**：对比了当前最强的 RL 算法（GRPO），并使用了 Pass@k 曲线来全面评估模型的能力边界，而非仅看 Pass@1。

### 6. 主要结论与发现
*   **突破能力上限**：LEAFE 在 Pass@128 上的表现显著优于 GRPO。在 CodeContests 上，Pass@128 提升了 14%。
*   **克服分布收缩**：GRPO 虽然能提升 Pass@1，但在大 $k$ 时往往会遇到瓶颈；而 LEAFE 通过内化纠错能力，显著扩展了模型的解题覆盖面。
*   **高效采样**：LEAFE 训练出的模型在推理时不需要额外的反思步骤或复杂的搜索算法，仅凭单次生成（或少量采样）就能达到更高的成功率。

### 7. 优点
*   **反馈利用率高**：将定性的文本反馈转化为定量的监督信号，解决了 RL 中奖励稀疏和信用分配难的问题。
*   **推理高效**：通过蒸馏将“反思”过程内化，避免了测试时高昂的计算开销（如 Tree-of-Thought 搜索）。
*   **鲁棒性强**：OOD 实验表明，这种内化的代理能力比简单的结果对齐具有更好的迁移性。

### 8. 不足与局限
*   **环境依赖**：LEAFE 依赖于环境能够提供清晰、可诊断的反馈。如果环境反馈模糊（如仅有“错误”二字），反思质量会下降。
*   **回溯假设**：该方法假设环境是可重置/可回溯的，这在某些实时或不可逆的现实场景中难以实现。
*   **训练成本**：阶段 1 的树状探索需要多次环境交互和模型调用，数据收集成本高于普通的轨迹采样。

（完）
