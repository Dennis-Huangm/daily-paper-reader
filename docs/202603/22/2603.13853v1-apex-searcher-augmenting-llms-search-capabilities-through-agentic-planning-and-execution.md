---
title: "APEX-Searcher: Augmenting LLMs' Search Capabilities through Agentic Planning and Execution"
title_zh: APEX-Searcher：通过智能体规划与执行增强大语言模型的搜索能力
authors: "Kun Chen, Qingchao Kong, Zhao Feifei, Wenji Mao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13853v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 通过智能体规划和强化学习增强LLM搜索能力
tldr: APEX-Searcher是一个旨在增强大语言模型搜索能力的智能体规划与执行框架。针对复杂多跳问题中单次检索不足及端到端训练中奖励稀疏等挑战，该框架将检索过程解耦为规划和执行两个阶段。通过强化学习优化任务分解规划，并利用监督微调提升子任务执行能力，显著提高了多跳检索增强生成（RAG）和任务规划的性能。
selection_source: fresh_fetch
motivation: 现有的多轮检索方法在处理复杂多跳问题时，面临检索路径模糊和强化学习奖励稀疏导致的性能下降问题。
method: 提出两阶段框架，先通过特定分解奖励的强化学习优化战略规划，再利用高质量轨迹的监督微调增强迭代执行能力。
result: 在多个基准测试中，该框架在多跳RAG和任务规划性能上均取得了显著提升。
conclusion: 通过解耦规划与执行并结合强化学习与微调，APEX-Searcher有效增强了LLM处理复杂搜索任务的能力。
---

## 摘要
基于大语言模型（LLMs）的检索增强生成（RAG）是在各种领域应用中检索和利用外部知识的重要方法。当面对复杂的多跳问题时，单轮检索往往不足以进行准确的推理和问题解决。为了增强复杂任务的搜索能力，现有的大多数工作通过端到端训练将多轮迭代检索与推理过程相结合。虽然这些方法显著提高了问题解决性能，但它们在任务推理和模型训练方面仍面临挑战，特别是端到端强化学习（RL）过程中模糊的检索执行路径和稀疏奖励，导致检索结果不准确和性能下降。为了解决这些问题，本文提出了 APEX-Searcher，一种新型的智能体规划与执行框架，旨在增强 LLM 的搜索能力。具体而言，我们引入了一个两阶段智能体框架，将检索过程解耦为规划和执行：首先，它采用具有分解特定奖励的强化学习来优化战略规划；在子任务分解的基础上，它随后对高质量的多跳轨迹进行监督微调，使模型具备鲁棒的迭代子任务执行能力。广泛的实验表明，我们提出的框架在多个基准测试中显著提升了多跳 RAG 和任务规划的性能。

## Abstract
Retrieval-augmented generation (RAG), based on large language models (LLMs), serves as a vital approach to retrieving and leveraging external knowledge in various domain applications. When confronted with complex multi-hop questions, single-round retrieval is often insufficient for accurate reasoning and problem solving. To enhance search capabilities for complex tasks, most existing works integrate multi-round iterative retrieval with reasoning processes via end-to-end training. While these approaches significantly improve problem-solving performance, they are still faced with challenges in task reasoning and model training, especially ambiguous retrieval execution paths and sparse rewards in end-to-end reinforcement learning (RL) process, leading to inaccurate retrieval results and performance degradation. To address these issues, in this paper, we proposes APEX-Searcher, a novel Agentic Planning and Execution framework to augment LLM search capabilities. Specifically, we introduce a two-stage agentic framework that decouples the retrieval process into planning and execution: It first employs RL with decomposition-specific rewards to optimize strategic planning; Built on the sub-task decomposition, it then applies supervised fine-tuning on high-quality multi-hop trajectories to equip the model with robust iterative sub-task execution capabilities. Extensive experiments demonstrate that our proposed framework achieves significant improvements in both multi-hop RAG and task planning performances across multiple benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《APEX-Searcher: Augmenting LLMs' Search Capabilities through Agentic Planning and Execution》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的检索增强生成（RAG）在处理**复杂多跳问题**时存在局限。传统的单次检索难以获取完整证据链，而现有的迭代式 RAG（如 IRCoT）多为“反应式”搜索（走一步看一步），缺乏全局战略规划，容易导致检索路径偏移、冗余或陷入死循环。
*   **研究背景**：端到端的强化学习（RL）训练虽然能提升推理能力，但在搜索场景下面临**奖励稀疏**和**执行路径模糊**的问题。论文旨在通过将搜索过程解耦为“高层战略规划”与“底层战术执行”，增强 LLM 的自主搜索与推理能力。

### 2. 方法论：APEX-Searcher 框架
APEX-Searcher 采用两阶段解耦架构，模拟人类“先计划、后执行”的思维模式：

*   **阶段 I：基于强化学习的智能体规划（Agentic Planning）**
    *   **核心思想**：将复杂问题分解为逻辑严密的子问题序列（如 $s_1, s_2, ..., s_n$），并支持引用机制（如 #1 代表第一个子问题的答案）。
    *   **关键技术**：使用 **GRPO（组相对策略优化）** 算法。
    *   **奖励函数设计**：定义了 $R_{plan}$。通过 Sentence-Transformer 计算生成计划与金标准计划的语义相似度，利用**匈牙利算法（Hungarian Algorithm）**进行最优二分匹配，最终以 **F1-Score** 作为奖励信号，引导模型学习高效的分解逻辑。
*   **阶段 II：基于监督微调的智能体探索（Agentic Exploration）**
    *   **核心思想**：针对分解后的子问题进行迭代执行。
    *   **执行流程**：
        1.  **知识自检**：判断现有上下文是否足以回答子问题。
        2.  **动态查询生成**：若不足，则生成针对性的搜索词。
        3.  **多轮检索与过滤**：进行多跳检索，并去重过滤无关信息。
        4.  **子答案合成**：更新知识库，为下一个子问题提供上下文。
    *   **训练细节**：利用 Self-instruct 技术构建了 1.4 万条高质量的多轮检索轨迹数据，对模型进行 SFT（监督微调）。

### 3. 实验设计
*   **数据集**：选取了四个具有挑战性的多跳 QA 基准：**HotpotQA、2WikiMultiHopQA、MuSiQue 和 Bamboogle**。
*   **评估指标**：主要使用 **EM（Exact Match，精确匹配）**。
*   **对比方法**：
    *   非检索方法：Direct Inference, CoT。
    *   标准 RAG：Standard RAG。
    *   迭代/智能体 RAG：IRCoT, Search-o1, Search-R1, ZeroSearch, StepSearch, ReasonRAG, DecEx-RAG 等。

### 4. 资源与算力
*   **基础模型**：Qwen2.5-3B-Instruct 和 Qwen2.5-7B-Instruct。
*   **软件框架**：使用 `verl` 框架实现 GRPO 强化学习，使用 `360Llamafactory` 进行全参数 SFT。
*   **训练配置**：
    *   RL 阶段：学习率 $5\times10^{-6}$，全局 Batch Size 512，使用 bfloat16 精度。
    *   SFT 阶段：学习率 $5\times10^{-6}$，使用 DeepSpeed ZeRO-3 优化。
*   **算力说明**：文中提到了微批次（micro-batch size）和序列并行（sequence parallel），但**未明确说明具体的 GPU 型号、总卡数及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流数据集上进行了全面测试，涵盖了从 3B 到 7B 不同规模的模型。
*   **消融实验**：详细对比了“仅规划”、“规划+RL”、“仅执行 SFT”以及“全量框架”的效果，验证了各模块的必要性。
*   **参数敏感性分析**：探讨了检索文档数量（Num）和推理跳数（Hop）对性能的影响。
*   **充分性评价**：实验设计较为客观、公平，通过与最新的 Search-R1 等强基线对比，证明了其优越性。

### 6. 主要结论与发现
*   **解耦的有效性**：将规划与执行分开训练，比端到端训练更能克服奖励稀疏问题。
*   **性能提升**：APEX-Searcher 在所有基准上均显著优于基线。在 3B 模型上，平均性能提升了 149%；在 7B 模型上，超越了现有的 SOTA 方法（如 DecEx-RAG）。
*   **小模型的潜力**：经过专门的规划 RL 和执行 SFT，3B 规模的小模型在多跳任务上的表现甚至能接近或超过未优化的 7B 模型。

### 7. 优点与亮点
*   **创新的奖励机制**：引入语义 F1 和匈牙利算法来评估规划质量，解决了规划任务难以量化评价的痛点。
*   **全局视野**：相比于传统的反应式迭代，APEX-Searcher 具有更强的战略前瞻性，减少了搜索过程中的冗余。
*   **鲁棒的执行逻辑**：通过 SFT 强化了模型对检索结果的判断力（是否继续检索、是否已获取足够信息）。

### 8. 不足与局限
*   **依赖金标准分解**：RL 阶段的奖励计算依赖于人工标注或强模型生成的“金标准”分解路径，这在某些极度冷门的领域可能难以获取。
*   **错误传播风险**：如果第一阶段的规划出现逻辑错误，可能会导致后续所有的检索执行全部偏离目标。
*   **实时性开销**：多阶段的规划与多轮迭代检索虽然提高了准确率，但相比单次 RAG，其推理延迟（Latency）会显著增加。

（完）
