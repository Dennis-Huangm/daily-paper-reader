---
title: "Bridging Perception and Reasoning: Token Reweighting for RLVR in Multimodal LLMs"
title_zh: 连接感知与推理：多模态大语言模型中 RLVR 的 Token 重加权
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Guoyin Wang, Jiancan Wu, Xiang Wang, Xiangnan He"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25077v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 多模态大模型的带可验证奖励强化学习
tldr: 本研究针对多模态大语言模型在可验证奖励强化学习（RLVR）中感知与推理令牌相互交织的挑战，提出了一种名为Token-Reweighting (ToR) 的即插即用策略。通过实证分析发现，仅优化感知或推理令牌的效果均不如全面优化，说明两者存在强耦合。ToR通过识别并动态重新加权这两类关键令牌，显著提升了模型在多模态推理任务中的视觉定位与逻辑推理能力，在多个基准测试中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 多模态模型在强化学习中面临感知令牌（视觉定位）与推理令牌（逻辑构建）高度耦合且难以平衡的挑战。
method: 提出Token-Reweighting (ToR) 策略，通过识别关键的感知和推理令牌并在训练中动态调整其权重来建模两者的相互依赖性。
result: 在GRPO和DAPO等方法上应用ToR后，模型在多个多模态推理基准测试中均实现了性能的持续增长并达到SOTA。
conclusion: ToR策略证明了通过显式建模感知与推理令牌的依赖关系，可以有效提升多模态大语言模型的综合推理能力。
---

## 摘要
将具有可验证奖励的强化学习（RLVR）扩展到多模态大语言模型（MLLMs）面临一个根本性挑战：它们的响应本质上交织着感知相关 token（用于定位视觉内容）和推理相关 token（用于构建推理链）。这些 token 类型实例化了不同但又相互依赖的能力——视觉定位和符号推理——这使得孤立的优化变得不足。通过 token 级别的实证分析，我们证明了仅优化感知或仅优化推理 token 的表现始终不如全面优化，这强调了它们内在的耦合性。为了解决这个问题，我们提出了一种即插即用的 Token 重加权（ToR）策略，该策略通过识别这两类关键 token 并在 RLVR 训练期间动态重新加权，显式地对这种相互依赖性进行建模。在现有方法（如 GRPO 和 DAPO）的基础上应用 ToR，在多个多模态推理基准测试中实现了持续的性能提升，在准确的视觉定位和连贯的推理方面均达到了最先进的性能。

## Abstract
Extending Reinforcement Learning with Verifiable Rewards (RLVR) to multimodal large language models (MLLMs) faces a fundamental challenge: their responses inherently interleave perception-related tokens, which ground visual content, with reasoning-related tokens, which construct reasoning chains. These token types instantiate distinct yet interdependent capacities -- visual grounding and symbolic reasoning -- making isolated optimization insufficient. Through token-level empirical analysis, we demonstrate that optimizing either perception- or reasoning-only tokens consistently underperforms full optimization, underscoring their inherent coupling. To address this, we propose a plug-and-play Token-Reweighting (ToR) strategy that explicitly models this interdependence by identifying critical tokens of both types and dynamically reweighting them during RLVR training. Applied on top of existing methods (e.g., GRPO and DAPO), ToR delivers consistent performance gains across multiple multi-modal reasoning benchmarks, achieving state-of-the-art performance with both accurate visual grounding and coherent reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Bridging Perception and Reasoning: Token Reweighting for RLVR in Multimodal LLMs》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何有效地将“带可验证奖励的强化学习”（RLVR）应用于多模态大语言模型（MLLM）。
*   **研究背景**：RLVR（如 DeepSeek-R1 所采用的技术）在提升纯文本模型的推理能力方面表现卓越。但在多模态场景下，模型的响应交织着**感知 Token**（用于定位视觉内容）和**推理 Token**（用于构建逻辑链）。
*   **动机**：现有的多模态 RL 往往将感知和推理孤立优化。作者通过实验发现，感知与推理在 Token 级别是高度耦合的，仅优化其中一类 Token 会导致性能下降（如推理模型可能产生逻辑连贯但视觉错误的回答）。因此，需要一种能同时兼顾并平衡两者的优化策略。

### 2. 方法论：Token 重加权 (ToR)
*   **核心思想**：一种即插即用的策略，通过识别响应中关键的感知和推理 Token，并在强化学习的策略梯度计算中动态调整它们的权重。
*   **关键技术细节**：
    *   **推理 Token 识别**：基于**预测熵（Entropy）**。高熵位置通常对应推理链中的关键决策点（Forking points）。
    *   **感知 Token 识别**：基于**视觉敏感度（Visual Sensitivity）**。通过计算“有图像输入”与“无图像输入（空占位符）”时 Token 对数概率的差值（Log-probability difference）来衡量该 Token 对视觉信息的依赖程度。
    *   **算法流程**：
        1.  在 Rollout 阶段生成响应。
        2.  计算每个 Token 的熵和视觉敏感度。
        3.  选取前 $\alpha_r$ 比例的高熵 Token 作为推理集合，前 $\alpha_p$ 比例的高敏感度 Token 作为感知集合。
        4.  **重加权优化**：在 GRPO 或 DAPO 的损失函数中，为这两类集合中的 Token 分配更高的权重（$\gamma_r, \gamma_p$），其余 Token 权重设为 0 或较低值。
*   **公式体现**：在策略梯度目标函数中引入掩码/权重因子，显式建模感知与推理的相互依赖。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Geometry3K（2.1K 样本，标准设置）、ViRL-39K（大规模设置）。
    *   **测试基准（Benchmarks）**：涵盖视觉推理（MathVerse, MathVision, MathVista, WeMath）和视觉感知（HalluBench）。
*   **对比方法**：
    *   **开源模型**：InternVL-2.5/3, LLaVA-OneVision, Qwen2.5-VL-7B-Instruct。
    *   **RLVR 基准**：R1-VL, Vision-R1, MM-Eureka, NoisyRollout, 以及原始的 GRPO 和 DAPO。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H800 GPU**（每张显卡 80GB 显存）。
*   **训练配置**：学习率 1e-6，全局 Batch Size 为 128，Rollout Batch Size 为 512，每个样本生成 12 个 Rollout 响应。
*   **框架**：基于 EasyR1 多模态强化学习框架实现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **消融实验**：详细测试了不同 Token 选取比例（20% 到 80%）、不同权重分配（$\gamma_p$ 从 0.1 到 1.5）的影响。
    *   **模型泛化**：在 Qwen2.5-VL 的 3B 和 7B 两个尺寸上均进行了验证。
    *   **数据缩放**：对比了 2.1K 和 39K 两种训练数据规模。
*   **充分性评价**：实验设计非常充分。作者不仅提供了最终的性能对比，还深入分析了 Token 的分布特性、感知代理指标（Prob-diff vs Logp-diff）的有效性，以及训练过程中感知与推理的“推拉”动力学关系，论证具有高度客观性。

### 6. 主要结论与发现
*   **耦合性验证**：孤立优化推理 Token 会削弱视觉定位，孤立优化感知 Token 则无法解决推理歧义。
*   **ToR 的优越性**：ToR-GRPO 和 ToR-DAPO 在所有基准测试中均优于原始算法。例如，在 Geometry3K 训练下，MathVerse 性能从 50.8 提升至 53.0。
*   **SOTA 性能**：在相同参数规模下，ToR 助力模型在多个多模态推理榜单上达到了当前最先进水平（State-of-the-art）。
*   **Token 特性**：感知依赖于少数视觉敏感 Token 的稳定性，而推理依赖于高熵 Token 的鲁棒性。

### 7. 优点
*   **即插即用**：不需要修改模型架构或引入复杂的外部奖励模型，仅通过修改损失函数的权重分配即可实现。
*   **自包含性**：关键 Token 的识别完全依赖模型自身的内在信号（熵和概率），无需外部先验知识。
*   **理论与实证结合**：通过深入的 Token 级别分析揭示了多模态 RL 的本质矛盾，并给出了简洁的解决方案。

### 8. 不足与局限
*   **感知识别精度**：目前主要依赖概率差值，未来可能需要结合更细粒度的视觉工具（如 SAM）来精确定位图像区域。
*   **上下文忽略**：Token 重加权在一定程度上将 Token 视为独立个体，可能忽略了 Token 之间复杂的语义上下文联系。
*   **任务覆盖**：实验主要集中在数学和几何推理，对于更广泛的通用多模态对话或视频理解任务的适用性尚待进一步验证。

（完）
