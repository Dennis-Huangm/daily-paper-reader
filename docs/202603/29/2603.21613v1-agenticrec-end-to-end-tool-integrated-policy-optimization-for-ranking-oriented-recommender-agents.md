---
title: "AgenticRec: End-to-End Tool-Integrated Policy Optimization for Ranking-Oriented Recommender Agents"
title_zh: AgenticRec：面向排序推荐智能体的端到端工具集成策略优化
authors: "Tianyi Li, Zixuan Wang, Guidong Lei, Xiaodong Li, Hui Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21613v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 集成工具策略优化的智能体推荐框架
tldr: AgenticRec是一个面向排序的智能体推荐框架，旨在解决现有LLM推荐系统在推理与排序反馈脱节及偏好捕捉不细致的问题。该框架通过集成推荐专用工具的ReAct循环、列表级组相对策略优化（list-wise GRPO）以及渐进式偏好细化（PPR）机制，实现了从推理、工具调用到最终排序生成的全链路端到端优化，显著提升了推荐性能。
selection_source: fresh_fetch
motivation: 现有推荐智能体在中间推理过程与最终排序反馈之间存在脱节，且难以捕捉用户的细粒度偏好。
method: 提出了集成专用工具的ReAct循环，并结合列表级GRPO算法和渐进式偏好细化（PPR）来优化决策轨迹。
result: 实验结果表明，AgenticRec在多个基准测试中显著优于现有基准模型。
conclusion: 统一推理、工具使用和排序优化对于构建高性能的推荐智能体至关重要。
---

## 摘要
基于大语言模型的推荐智能体为推荐系统提供了一种极具前景的范式。然而，现有的推荐智能体通常面临中间推理与最终排序反馈脱节的问题，且无法捕捉细粒度的偏好。为了解决这一问题，我们提出了 AgenticRec，这是一个面向排序的智能体推荐框架，能够在稀疏隐式反馈下优化整个决策轨迹（包括中间推理、工具调用和最终排序列表生成）。本研究具有三个核心贡献：首先，我们设计了一套集成在 ReAct 循环中的推荐专用工具，以支持基于证据的推理；其次，我们提出了理论上无偏的列表级组相对策略优化（list-wise GRPO），以最大化排序效用，确保复杂工具使用轨迹下的准确信用分配；第三，我们引入了渐进式偏好细化（PPR）来解决细粒度的偏好歧义。通过从排序违规中挖掘难负样本并应用双向偏好对齐，PPR 最小化了成对排序误差的凸上界。在基准数据集上的实验证实，AgenticRec 显著优于现有基准模型，验证了统一推理、工具使用和排序优化的必要性。

## Abstract
Recommender agents built on Large Language Models offer a promising paradigm for recommendation. However, existing recommender agents typically suffer from a disconnect between intermediate reasoning and final ranking feedback, and are unable to capture fine-grained preferences. To address this, we present AgenticRec, a ranking-oriented agentic recommendation framework that optimizes the entire decision-making trajectory (including intermediate reasoning, tool invocation, and final ranking list generation) under sparse implicit feedback. Our approach makes three key contributions. First, we design a suite of recommendation-specific tools integrated into a ReAct loop to support evidence-grounded reasoning. Second, we propose theoretically unbiased List-Wise Group Relative Policy Optimization (list-wise GRPO) to maximize ranking utility, ensuring accurate credit assignment for complex tool-use trajectories. Third, we introduce Progressive Preference Refinement (PPR) to resolve fine-grained preference ambiguities. By mining hard negatives from ranking violations and applying bidirectional preference alignment, PPR minimizes the convex upper bound of pairwise ranking errors. Experiments on benchmarks confirm that AgenticRec significantly outperforms baselines, validating the necessity of unifying reasoning, tool use, and ranking optimization.

---

## 论文详细总结（自动生成）

### 论文总结：AgenticRec - 面向排序推荐智能体的端到端工具集成策略优化

#### 1. 核心问题与整体含义（研究动机和背景）
传统的推荐系统（RS）正逐渐向基于大语言模型（LLM）的推荐智能体演进。然而，现有智能体存在两个核心痛点：
*   **推理与反馈脱节**：智能体的中间推理过程和工具调用通常依赖于通用的语言先验或手工设计的提示词（Prompt），而非直接由推荐任务的最终排序反馈驱动。
*   **难以捕捉细粒度偏好**：在处理高度相似的候选商品时，仅靠粗粒度的列表级信号难以区分微妙的偏好差异，导致在挑战性案例中表现脆弱。
**AgenticRec** 的提出旨在通过端到端的策略优化，将推理、工具使用和排序生成统一在一个优化框架内，利用隐式反馈指导智能体的全链路决策。

#### 2. 方法论：核心思想与关键技术
AgenticRec 采用两阶段训练范式，核心思想是“在推荐反馈中学习如何思考和使用工具”：
*   **工具集成的排序推理（TIRR）**：基于 ReAct 模式（Think-Act-Obs），为智能体配备了四类专用工具：用户画像工具、商品信息工具、行为统计工具和协同过滤工具（如 SASRec）。工具调用被视为决策轨迹的一部分，受最终奖励驱动。
*   **列表级组相对策略优化（List-Wise GRPO）**：
    *   **核心逻辑**：在第一阶段，针对每个请求采样多条轨迹，计算各轨迹生成的排序列表（NDCG@K）作为奖励。
    *   **算法细节**：通过减去组内平均奖励作为基准（Baseline）来降低方差。论文证明了该梯度估计量是**理论无偏**的，确保排序质量能准确回传至中间推理和工具调用步骤。
*   **渐进式偏好细化（PPR）**：
    *   **硬负样本挖掘**：从智能体预测错误的排序结果中自动挖掘“排序违规”样本（即排在正样本之前的负样本）。
    *   **双向偏好推理**：在第二阶段，要求智能体同时判断“哪个更可能被买”和“哪个更不可能被买”，通过双向对齐最小化成对排序误差的凸上界，强化对易混淆候选者的辨别力。

#### 3. 实验设计
*   **数据集**：使用了 Amazon 2023 蓝图中的四个子集：CDs、Instruments（乐器）、Office（办公用品）和 Games（游戏）。
*   **任务设置**：从 20 个候选商品中生成 Top-10 排序列表。
*   **对比基准（Baselines）**：
    *   **传统序列推荐**：Caser, GRU4Rec, SASRec, ReaRec。
    *   **免训练 LLM 方法**：LLMRank (GPT-4), InteRecAgent (GPT-4 + 工具)。
    *   **可训练 LLM 方法**：TALLRec, LLaRA, S-DPO, ReRe。

#### 4. 资源与算力
*   **硬件设施**：实验使用了 **4 台 NVIDIA A800 GPU (80GB 显存)**。
*   **模型骨干**：采用 **Qwen3-4B-Instruct** 作为基础模型。
*   **训练细节**：第一阶段训练 3 个 Epoch，第二阶段训练 1 个 Epoch。学习率设为 $1 \times 10^{-6}$，Batch Size 为 64，组采样大小（Group Size）为 8。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的数据集上进行了全量对比，涵盖了从传统模型到最新 LLM 强化学习方法的 10 种基准模型。
*   **消融实验**：详细对比了“纯推理”、“工具集成”以及“加入 PPR 细化”后的性能提升，验证了各模块的有效性。
*   **扩展性分析**：测试了不同参数规模（1.7B, 4B, 8B）下的表现，证明了方法的 Scaling 能力。
*   **客观性**：通过固定候选集、随机打乱顺序消除位置偏见，并提供了数学证明（无偏性与误差界限），实验设计较为严谨客观。

#### 6. 主要结论与发现
*   **端到端优化的必要性**：单纯集成工具而不进行针对性训练（免训练模式）效果有限，甚至可能引入噪声；而通过 GRPO 优化后的工具使用变得“结果导向”，显著提升了排序精度。
*   **AgenticRec 的优越性**：在所有数据集和指标（H@1/5/10, N@5/10）上均显著超过了现有最强基准（如 LLaRA 和 ReRe）。
*   **细粒度偏好的重要性**：PPR 阶段通过挖掘硬负样本，进一步提升了模型在 Top-1 和 Top-5 上的命中率。

#### 7. 优点
*   **理论支撑扎实**：不仅提出了算法，还给出了关于梯度无偏性和误差界限缩减的数学证明。
*   **工具集成深度化**：将协同过滤信号（ID 类信息）通过工具形式引入 LLM 推理链，解决了 LLM 缺乏协同感知能力的短板。
*   **自举式学习**：PPR 机制无需额外标注，利用模型自身的错误进行自我进化，具有很强的实用性。

#### 8. 不足与局限
*   **推理延迟**：由于采用了 ReAct 循环和多步推理（Think 过程），在线推理的延迟（Latency）会显著高于单次预测模型，可能限制其在超大规模实时系统中的应用。
*   **工具预算限制**：文中设定了固定的工具调用预算（10 次），对于极长序列或极复杂场景，这种硬性限制可能影响灵活性。
*   **长序列处理**：目前主要针对短中期历史（Max Length=10），对于超长历史的记忆与检索机制尚待进一步探索。

（完）
