---
title: "From AI Assistant to AI Scientist: Autonomous Discovery of LLM-RL Algorithms with LLM Agents"
title_zh: 从 AI 助手到 AI 科学家：利用 LLM 智能体自主发现 LLM-RL 算法
authors: "Sirui Xia, Yikai Zhang, Aili Chen, Siye Wu, Siyu Yuan, Yanghua Xiao"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23951v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 利用LLM智能体自动发现LLM-RL算法
tldr: 本研究针对大语言模型强化学习算法设计成本高、依赖人工的问题，提出了 POISE 自动化发现框架。该框架利用 LLM 智能体在闭环系统中进行算法提案、代码实现、标准评估及反思迭代，并维护结构化存档以复用经验。在数学推理实验中，POISE 从 GRPO 出发，自主发现了分析方差缩放和有效性掩码等改进机制，显著提升了模型性能，展示了从 AI 助手向 AI 科学家转型的潜力。
selection_source: fresh_fetch
motivation: 现有的语言模型策略优化算法发现过程高度依赖人工干预，且机制与训练动态耦合复杂，导致迭代效率低下。
method: 提出 POISE 框架，通过维护包含提案、代码、评估和反思的结构化谱系存档，实现算法的闭环自主搜索。
result: "实验表明 POISE 发现的算法变体在 AIME25 任务上的 pass@32 准确率从 26.7% 大幅提升至 43.3%。"
conclusion: 自动化算法发现不仅可行，且能产出具有可解释性设计原则的高性能强化学习机制。
---

## 摘要
为语言模型发现改进的策略优化算法目前仍是一个成本高昂的手动过程，需要反复进行机制层面的修改与验证。与简单的组合代码搜索不同，该问题要求在与训练动态紧密耦合的算法机制空间内进行搜索，并在迭代过程中复用经验证据。我们提出了 POISE，这是一个用于自动发现语言模型策略优化算法的闭环框架。POISE 维护一个结构化的、具有谱系关联的存档，将算法提议、可执行实现、标准化评估以及自然语言反思联系起来，以支持证据驱动的迭代。在以 GRPO 为起点的数学推理实验中，POISE 评估了 64 个候选算法，并发现了包括解析方差缩放（analytic-variance scaling）和有效性掩码（validity masking）在内的改进机制。其中最佳变体将加权综合得分从 47.8 提升至 52.5（+4.6），并将 AIME25 pass@32 从 26.7% 提高到 43.3%，证明了自动化策略优化发现的可行性，同时支持可解释的设计原则。

## Abstract
Discovering improved policy optimization algorithms for language models remains a costly manual process requiring repeated mechanism-level modification and validation. Unlike simple combinatorial code search, this problem requires searching over algorithmic mechanisms tightly coupled with training dynamics while reusing empirical evidence across iterations. We propose POISE, a closed-loop framework for automated discovery of policy optimization algorithms for language models. POISE maintains a structured, genealogically linked archive linking proposals, executable implementations, standardized evaluations, and natural-language reflections to support evidence-driven iteration. In mathematical reasoning experiments starting from GRPO, POISE evaluates 64 candidate algorithms and discovers improved mechanisms, including analytic-variance scaling and validity masking. The best variant improves weighted Overall from 47.8 to 52.5 (+4.6) and increases AIME25 pass@32 from 26.7% to 43.3%, demonstrating the feasibility of automated policy optimization discovery while supporting interpretable design principles.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **POISE**（Policy Optimization through Iterative Search and Evidence）的闭环框架，旨在利用大语言模型（LLM）智能体自主发现和优化用于语言模型的强化学习（RL）算法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：目前 LLM 的策略优化算法（如 PPO, GRPO）的设计高度依赖人工迭代。研究人员需要手动修改损失函数、优势估计和正则化机制，并进行耗时的实验验证。这种过程不仅成本高昂，且算法机制与训练动态（Training Dynamics）紧密耦合，简单的组合代码搜索难以奏效。
*   **研究动机**：探索 LLM 是否能从“助手”进化为“科学家”，通过自主观察实验证据、反思失败原因并提出新假设，从而发现更优的强化学习算法。

### 2. 方法论：POISE 框架
POISE 采用**认识论进化搜索（Epistemic Evolutionary Search）**，核心思想是建立一个闭环系统，将算法搜索视为证据驱动的迭代过程。
*   **结构化存档（Structured Archive）**：维护一个具有谱系关联的数据库，记录每个算法的提案、代码实现、标准化评估结果和自然语言反思。
*   **三个核心阶段**：
    1.  **阶段 I：提案生成**。利用 LLM 智能体基于存档证据和文献先验生成新算法。引入**采集函数**平衡当前性能与进化潜力（即预测哪个节点能产生更强的后代）。
    2.  **阶段 II：实现、验证与评估**。将抽象提案转化为可执行代码，通过共享接口进行标准化训练和评估，确保结果的可比性。
    3.  **阶段 III：反思分析与存档更新**。将定量信号（如学习曲线、梯度范数）转化为机制层面的诊断报告，更新谱系树。
*   **关键技术细节**：框架不仅关注最终得分，还分析训练动态（如 KL 散度峰值、熵崩塌），使后续迭代能基于“为什么失败”而不仅仅是“是否成功”来改进。

### 3. 实验设计
*   **实验场景**：专注于**数学推理任务**的策略优化。
*   **基准算法（Benchmark）**：以 **GRPO**（DeepSeek 提出的一种无需价值网络的强化学习算法）作为进化起点。
*   **数据集**：涵盖 6 个标准化推理评测集：AIME24、AIME25、AMC、MATH-500、Minerva 和 OlympiadBench。
*   **对比方法**：通过 POISE 自动生成的 63 种算法变体与原始 GRPO 进行对比。

### 4. 资源与算力
*   **硬件资源**：实验使用了 **8 张 80GB A100 GPU**。
*   **模型配置**：
    *   **智能体引擎**：使用 `Gemini-3-pro-preview` 作为框架的推理和决策核心。
    *   **策略模型**：使用 `Qwen2.5-Math-1.5B` 作为被优化的策略模型，以便快速迭代。
*   **训练细节**：每个候选算法在 MATH 数据集的 5000 个子集上训练 8 个 Epoch，全局 Batch Size 为 256。

### 5. 实验数量与充分性
*   **实验规模**：POISE 共评估了 **64 个候选算法**（1 个基准 + 63 个进化变体）。
*   **充分性评价**：
    *   **深度搜索**：进化谱系最深达到 8 代，展示了算法从简单到复杂的演进过程。
    *   **多维度验证**：除了基础性能，还专门设计了“长度压缩约束”实验，验证框架在特定指令下的可控性。
    *   **客观性**：所有算法在统一的分布式后端（VERL）和相同的超参数下运行，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能突破**：发现的最佳变体 **VM-AV-GRPO** 在 AIME25 上的 pass@32 从 26.7% 提升至 **43.3%**，综合加权得分提升了 4.6 个百分点。
*   **新机制的发现**：
    *   **解析方差缩放（Analytic-Variance Scaling）**：用理论方差代替经验方差，增强稀疏成功信号的放大效果。
    *   **有效性掩码（Validity Masking）**：排除格式错误的样本干扰，建立更清晰的梯度层级。
*   **设计原则提取**：总结出三大原则：稀疏成功需校准放大、利用负面证据剔除错误假设、通过信号解耦引入次要目标。
*   **可控性**：在引入长度约束后，框架成功发现了能在保持准确率的同时减少 29.1% 输出长度的算法（如 DACE-GRPO）。

### 7. 优点与亮点
*   **闭环证据链**：不同于随机搜索，POISE 强调“反思”和“谱系”，能够复用失败的经验，具有极强的可解释性。
*   **机制级创新**：发现的机制（如方差缩放）具有明确的数学含义，而非简单的超参数微调。
*   **自动化程度高**：实现了从想法到代码再到评估的全流程自动化，展示了 AI 智能体在科研领域的潜力。

### 8. 不足与局限
*   **计算成本**：尽管使用了小模型，但评估 64 个 RL 训练任务仍需大量算力，限制了搜索的广度。
*   **领域局限性**：实验目前集中在数学推理任务，其发现的机制是否能泛化到开放域对话、代码生成或工具调用尚待验证。
*   **因果归因风险**：虽然有反思机制，但将性能提升归因于特定代码修改仍带有假设性，缺乏严格的消融控制实验来隔离每个微小改动的影响。
*   **模型依赖**：框架的表现高度依赖于底层 LLM 智能体（如 Gemini）的推理能力。

（完）
