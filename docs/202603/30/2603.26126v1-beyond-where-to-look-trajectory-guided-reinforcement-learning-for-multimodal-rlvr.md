---
title: "Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR"
title_zh: 超越“看哪里”：面向多模态可验证奖励强化学习的轨迹引导强化学习
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Mingzhu Chen, Jiancan Wu, Kuien Liu, Xiang Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26126v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 多模态大语言模型的轨迹引导强化学习
tldr: 针对多模态大语言模型在强化学习中存在的“能看到但无法有效利用视觉证据进行推理”的问题，本文提出了轨迹引导强化学习（TGRL）。该方法利用更强模型的专家推理轨迹来引导策略模型，将视觉证据融入细粒度的推理过程中。通过引入标记级重加权和轨迹过滤技术，TGRL 在多个基准测试上显著提升了推理性能，有效弥合了视觉感知与逻辑推理之间的鸿沟。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习方法虽然提升了视觉定位能力，但模型往往无法将获取的视觉事实有效转化为逻辑推理链。
method: 提出 TGRL 框架，利用专家轨迹引导模型进行细粒度推理，并结合标记级重加权和轨迹过滤来优化策略。
result: 在多个多模态推理基准测试上的实验表明，TGRL 持续提升了模型的推理表现并增强了视觉证据的整合能力。
conclusion: TGRL 成功通过专家轨迹引导弥补了感知与推理间的断层，是提升多模态大模型逻辑推理性能的有效途径。
---

## 摘要
多模态大语言模型（MLLM）中具有可验证奖励的强化学习（RLVR）的最新进展主要集中在提高最终答案的正确性和增强视觉定位上。然而，一个关键瓶颈仍然存在：尽管模型能够关注相关的视觉区域，但它们往往无法有效地将视觉证据整合到后续推理中，导致推理链与视觉事实的关联较弱。为了解决这一问题，我们提出了轨迹引导强化学习（TGRL），该方法利用来自更强模型的专家推理轨迹，引导策略模型将视觉证据整合到细粒度的推理过程中。我们进一步引入了词元级重加权和轨迹过滤，以确保稳定且有效的策略优化。在多个多模态推理基准测试上的广泛实验表明，TGRL 持续提升了推理性能，并有效地弥合了视觉感知与逻辑推理之间的差距。

## Abstract
Recent advances in Reinforcement Learning with Verifiable Rewards (RLVR) for multimodal large language models (MLLMs) have mainly focused on improving final answer correctness and strengthening visual grounding. However, a critical bottleneck remains: although models can attend to relevant visual regions, they often fail to effectively incorporate visual evidence into subsequent reasoning, leading to reasoning chains that are weakly grounded in visual facts. To address this issue, we propose Trajectory-Guided Reinforcement Learning (TGRL), which guides the policy model to integrate visual evidence into fine-grained reasoning processes using expert reasoning trajectories from stronger models. We further introduce token-level reweighting and trajectory filtering to ensure stable and effective policy optimization. Extensive experiments on multiple multimodal reasoning benchmarks demonstrate that TGRL consistently improves reasoning performance and effectively bridges the gap between visual perception and logical reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多模态大语言模型（MLLM）的强化学习（RLVR）中，存在“感知与推理断层”的问题。即模型虽然能通过视觉定位看到相关区域，但无法有效地将视觉证据转化为逻辑推理链，导致推理过程与视觉事实脱节。
*   **研究背景**：现有的方法要么只关注结果正确性（Outcome-only），要么只加强感知阶段（Perception-level），但忽略了从“感知到推理”的转换过程。作者认为，仅仅知道“看哪里”是不够的，还需要学习“如何利用看到的信息进行推理”。

### 2. 方法论：轨迹引导强化学习（TGRL）
TGRL 的核心思想是利用更强模型的“专家推理轨迹”来显式引导策略模型的推理路径。
*   **轨迹集成（Trajectory Integration）**：在 RL 的 Rollout 阶段，将当前策略生成的在线轨迹与专家模型（如 Qwen2.5-VL-32B）生成的离线专家轨迹混合，构建联合 Rollout 组。
*   **轨迹重加权（Trajectory Reweighting）**：
    *   **重要性采样修正**：由于专家轨迹是离线的，通过计算当前策略与专家策略的概率比进行 Token 级的离线修正。
    *   **自适应 Token 权重**：引入差异度量（Discrepancy），对于那些当前策略严重低估专家概率的 Token 赋予更高权重，从而强化关键推理步骤的学习。
*   **轨迹过滤（Trajectory Filtering）**：仅保留最终答案正确的专家轨迹，剔除失败的推理路径，以减少梯度噪声。
*   **优化目标**：将上述机制集成到 GRPO 和 DAPO 等主流 RL 算法中，通过组内优势归一化，使模型在早期向专家学习，后期转向自主探索。

### 3. 实验设计
*   **训练数据集**：
    *   **Geo3K**：2.1K 几何推理样本（小规模）。
    *   **ViRL39K**：39K 多模态推理样本（大规模）。
*   **评估基准（Benchmarks）**：
    *   推理类：MathVista, WeMath, MathVision, MathVerse。
    *   感知/幻觉类：HallusionBench。
*   **对比方法**：包括基础模型 Qwen2.5-VL-7B、标准的 GRPO/DAPO、以及 SOTA 方法如 Perception-R1、ThinkLite、NoisyRollout 等。
*   **模型配置**：学生模型为 Qwen2.5-VL-3B/7B，专家模型为 Qwen2.5-VL-32B。

### 4. 资源与算力
*   **框架**：基于 EasyR1 训练框架实现。
*   **算力细节**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练时长**：提到在 Geo3K 上训练 15 个 Epoch，在 ViRL39K 上训练 2 个 Epoch。Rollout 组大小（Group Size）在不同实验中设置为 5 或 8。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小规模（2K）到中大规模（39K）的训练，并在 5 个主流 Benchmark 上进行了验证。
*   **消融实验**：针对“轨迹过滤”和“轨迹重加权”进行了详细的消融分析，验证了各组件的必要性。
*   **超参数分析**：对重加权系数 $\beta$ 进行了敏感度测试，展示了算法在不同设置下的鲁棒性。
*   **评价**：实验设计较为充分，对比了多种 SOTA 方法，且通过消融实验客观地展示了性能提升的来源。

### 6. 主要结论与发现
*   **性能提升**：TGRL 在所有基准测试中均显著优于原始的 GRPO 和 DAPO。在 39K 数据规模下，TGRL-DAPO 达到了 60.68 的平均分，刷新了 SOTA。
*   **弥合断层**：实验证明，通过专家轨迹引导，模型能更有效地将视觉感知转化为正确的推理步骤，减少了“感知正确但推理错误”的情况。
*   **专家模型门槛**：即使专家模型（32B）并非顶级闭源模型（如 Gemini 2.5 Pro），TGRL 依然能通过结构化的引导产生显著增益。

### 7. 优点与亮点
*   **理论深刻**：对多模态推理的梯度流进行了系统分析，精准定位了“感知-推理转换”这一瓶颈。
*   **即插即用**：TGRL 是一种通用的框架，可以轻松集成到现有的组优化 RL 算法（如 GRPO/DAPO）中。
*   **细粒度引导**：不同于粗糙的结果奖励，Token 级的重加权提供了更精准的信用分配（Credit Assignment）。

### 8. 不足与局限
*   **专家依赖**：该方法仍高度依赖于外部专家模型的质量。如果专家模型在某些极端样本上出错，可能会误导学生模型（尽管有过滤机制）。
*   **计算开销**：引入专家轨迹意味着在训练过程中需要额外的推理开销或预存大量轨迹数据。
*   **应用范围**：目前主要集中在数学和几何推理等具有明确可验证答案的任务，对于开放式、主观性强的多模态任务（如创意写作）适用性有待验证。

（完）
