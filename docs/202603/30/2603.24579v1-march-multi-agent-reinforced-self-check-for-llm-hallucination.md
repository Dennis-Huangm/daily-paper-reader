---
title: "MARCH: Multi-Agent Reinforced Self-Check for LLM Hallucination"
title_zh: MARCH：针对大语言模型幻觉的多智能体强化自检框架
authors: "Zhuo Li, Yupeng Zhang, Pengyu Cheng, Jiajun Song, Mengyu Zhou, Hao Li, Shujie Hu, Yu Qin, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24579v1"
tags: ["query:ag-rl"]
score: 8.0
evidence: 用于LLM事实对齐的多智能体强化框架
tldr: 针对大语言模型在RAG系统中的幻觉问题，本文提出MARCH框架。该框架通过Solver、Proposer和Checker三个智能体的协作，利用信息不对称机制打破验证过程中的确认偏差。通过多智能体强化学习（MARL）进行协同进化，MARCH显著降低了幻觉率，使8B参数模型在事实对齐方面达到了与闭源大模型相当的水平，为模型自我改进提供了新路径。
selection_source: fresh_fetch
motivation: 现有的幻觉检测方法受限于确认偏差，验证者容易受到原始生成内容的误导而重复错误。
method: 通过Solver生成响应、Proposer分解命题、Checker在信息隔离下验证，并利用多智能体强化学习实现协同进化。
result: 实验证明MARCH显著降低了幻觉率，搭载该框架的8B模型在性能上可与强大的闭源模型竞争。
conclusion: MARCH通过精心设计的信息不对称机制和强化学习，为大语言模型的事实对齐和自我优化提供了有效且可扩展的方案。
---

## 摘要
幻觉仍然是大语言模型（LLMs）的一个关键瓶颈，削弱了它们在实际应用中的可靠性，特别是在检索增强生成（RAG）系统中。虽然现有的幻觉检测方法采用“LLM作为裁判”（LLM-as-a-judge）来根据检索到的证据验证LLM输出，但它们存在固有的确认偏差，即验证者会无意中重复原始生成中的错误。为解决这一问题，我们提出了 MARCH（Multi-Agent Reinforced Self-Check for Hallucination），这是一个通过利用刻意的信息不对称来强制执行严格事实对齐的框架。MARCH 编排了一个由三个专门智能体组成的协作流水线：求解器（Solver）、提议器（Proposer）和检查器（Checker）。求解器生成初始 RAG 响应，提议器将其分解为声明级的可验证原子命题。至关重要的是，检查器在脱离求解器原始输出的隔离状态下，根据检索到的证据验证这些命题。这种精心设计的信息不对称方案打破了自我确认偏差的循环。通过使用多智能体强化学习（MARL）训练该流水线，我们使智能体能够共同进化并优化事实遵循性。在多个幻觉基准测试上的广泛实验表明，MARCH 显著降低了幻觉率。值得注意的是，配备 MARCH 的 8B 参数 LLM 达到了与强大的闭源模型相当的性能。MARCH 为 LLM 通过共同进化实现事实性自我提升开辟了一条可扩展的路径。代码位于 https://github.com/Qwen-Applications/MARCH。

## Abstract
Hallucination remains a critical bottleneck for large language models (LLMs), undermining their reliability in real-world applications, especially in Retrieval-Augmented Generation (RAG) systems. While existing hallucination detection methods employ LLM-as-a-judge to verify LLM outputs against retrieved evidence, they suffer from inherent confirmation bias, where the verifier inadvertently reproduces the errors of the original generation. To address this, we introduce Multi-Agent Reinforced Self-Check for Hallucination (MARCH), a framework that enforces rigorous factual alignment by leveraging deliberate information asymmetry. MARCH orchestrates a collaborative pipeline of three specialized agents: a Solver, a Proposer, and a Checker. The Solver generates an initial RAG response, which the Proposer decomposes into claim-level verifiable atomic propositions. Crucially, the Checker validates these propositions against retrieved evidence in isolation, deprived of the Solver's original output. This well-crafted information asymmetry scheme breaks the cycle of self-confirmation bias. By training this pipeline with multi-agent reinforcement learning (MARL), we enable the agents to co-evolve and optimize factual adherence. Extensive experiments across hallucination benchmarks demonstrate that MARCH substantially reduces hallucination rates. Notably, an 8B-parameter LLM equipped with MARCH achieves performance competitive with powerful closed-source models. MARCH paves a scalable path for factual self-improvement of LLMs through co-evolution. The code is at https://github.com/Qwen-Applications/MARCH.

---

## 论文详细总结（自动生成）

### MARCH：针对大语言模型幻觉的多智能体强化自检框架 论文总结

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）在检索增强生成（RAG）系统中的**幻觉问题**，特别是现有“LLM作为裁判”检测方法中的**确认偏差（Confirmation Bias）**。当验证模型同时看到原始查询、检索文档和生成的回答时，往往会倾向于认可错误的回答。MARCH 框架的核心含义是通过**信息不对称（Information Asymmetry）**机制，打破这种自我确认的循环，利用多智能体强化学习（MARL）实现模型事实性的自我进化。

#### 2. 方法论
MARCH 采用三个专门的智能体角色（均由同一个策略模型 $\pi_\theta$ 扮演）构建协作流水线：
*   **求解器（Solver）**：根据查询和检索文档生成初始响应。
*   **提议器（Proposer）**：作为“响应原子化器”，将响应分解为离散的、可验证的原子命题（以问答对形式呈现）。
*   **检查器（Checker）**：作为“盲审员”，**仅根据原始文档**回答提议器提出的问题，完全不接触求解器的原始输出。
*   **关键技术细节**：
    *   **零容忍奖励（Zero-Tolerance Reward, ZTR）**：采用“全或无”的二元奖励。只要有一个原子命题与检查器的独立验证不符，整个轨迹即被判定为失败（奖励为 -1，否则为 0）。
    *   **联合策略优化**：使用 PPO 算法，同时将求解器的推理路径和检查器的审计轨迹纳入优化循环，使模型同时进化出更强的生成能力和更严谨的审计能力。

#### 3. 实验设计
*   **数据集与场景**：
    *   **幻觉评估**：RAGTruth、FaithBench、ContextualJudgeBench、Facts Grounding（涵盖摘要、数据转文本、QA，涉及医疗、金融、法律等领域）。
    *   **下游推理**：HotpotQA、2WikiMultiHopQA、MuSiQue（多跳问答任务）。
*   **Benchmark 与对比方法**：
    *   **基座模型**：Llama-3.1-8B-Instruct、Qwen3-8B。
    *   **对比对象**：包括 GPT-4o、Gemini 1.5 Pro/Flash、Claude 3.5 等闭源模型，以及 RAFT、IRCoT、GenGround 等专门的 RAG 优化方法。

#### 4. 资源与算力
*   **算力说明**：论文提到使用了 **VerL** 框架进行强化学习训练，并采用 **FSDP（Fully Sharded Data Parallel）** 进行多节点、多 GPU 的高效并行训练。
*   **具体细节**：训练在 8B 参数模型上进行，设置 1 个 Epoch，全局 Batch Size 为 32，最大 Prompt 长度约 24k。
*   **缺失信息**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）以及具体的 GPU 数量和总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个幻觉基准和 3 个多跳 QA 基准上进行了测试，涵盖了 STEM 和通用领域。
*   **消融实验**：进行了多组深入的消融研究，包括：
    *   不同奖励函数（错误率奖励 vs. 零容忍奖励）的对比。
    *   不同奖励数值（0/1 vs. -1/0）的影响。
    *   联合优化（Solver + Checker）与仅优化 Solver 的对比。
    *   在不同模型家族（Llama vs. Qwen）上的泛化性验证。
*   **充分性评价**：实验设计较为全面且客观，通过多数投票（8 次采样）降低了评估的随机性，对比了当前最先进的开源和闭源模型，论证具有说服力。

#### 6. 主要结论与发现
*   **性能飞跃**：MARCH 显著提升了 8B 模型的事实对齐能力。在 Facts Grounding 榜单上，MARCH-STEM 版本的 8B 模型性能超过了 GPT-4o，接近 Gemini 1.5 Pro。
*   **协同进化**：联合训练 Solver 和 Checker 比单独训练 Solver 效果更好，证明了审计逻辑的内化有助于提升生成质量。
*   **通用性**：该方法在不同模型架构（Llama/Qwen）和不同提示策略（Few-shot/CoT）下均表现出良好的正交性和增益。

#### 7. 优点与亮点
*   **创新机制**：通过“信息不对称”巧妙解决了 LLM 自检中的确认偏差问题，这是该论文最突出的理论贡献。
*   **无需人工标注**：MARCH 是一种自我改进框架，训练过程不需要 ground-truth 答案或人工标注，具有极强的可扩展性。
*   **高效率**：仅通过 8B 规模的模型就达到了闭源巨型模型的幻觉抑制水平。

#### 8. 不足与局限
*   **奖励作弊风险（Reward Hacking）**：模型可能会通过减少生成的原子命题数量（“少说少错”）来规避惩罚。虽然论文提出了通过指令约束（要求至少生成 $k$ 个问题）来缓解，但这仍是一种启发式补救。
*   **领域局限**：目前的实现重点放在了**数值和定量验证**上，对于更复杂的定性逻辑或主观事实的验证能力尚待进一步验证。
*   **计算开销**：虽然训练是离线的，但训练过程中需要多次采样（Solver 生成、Proposer 分解、Checker 多次采样投票），相比标准 SFT，训练成本显著更高。

（完）
