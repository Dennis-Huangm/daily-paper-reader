---
title: Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning
title_zh: 基于可微世界模型的离线强化学习模型预测控制
authors: "Rohan Deb, Stephen J. Wright, Arindam Banerjee"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22430v1"
tags: ["query:ag-rl"]
score: 8.0
evidence: 离线强化学习的推理时自适应
tldr: 本研究针对离线强化学习在推理阶段缺乏灵活性的问题，提出了一种结合可微世界模型（DWM）与模型预测控制（MPC）的推理时自适应框架。该方法利用预训练策略和学习到的环境模型，通过在推理时对想象轨迹进行端到端梯度计算，实时优化策略参数。在D4RL基准测试中的实验证明，这种推理时优化机制能显著提升离线强化学习的效果，为解决离线数据分布偏移提供了新思路。
selection_source: fresh_fetch
motivation: 传统的离线强化学习方法在训练完成后直接应用策略，无法在推理阶段根据环境反馈进一步优化策略以应对分布偏移。
method: 提出一种可微世界模型（DWM）流水线，利用模型预测控制（MPC）在推理时通过想象轨迹的端到端梯度计算来动态调整策略参数。
result: 在D4RL连续控制基准测试（MuJoCo和AntMaze）中，该方法通过推理时优化在多个任务上均取得了优于强力基线模型的效果。
conclusion: 研究证明了在推理阶段利用可微世界模型实时优化策略参数是提升离线强化学习性能的一种有效且一致的方法。
---

## 摘要
离线强化学习（Offline RL）旨在从固定的离线数据集中学习最优策略，而无需与环境进行进一步交互。此类方法通常训练一个离线策略（或价值函数），并在推理阶段直接应用而不再进行改进。我们引入了一种受模型预测控制（MPC）启发的推理时自适应框架，该框架结合了预训练策略以及学习到的状态转移和奖励的世界模型。虽然现有的世界模型和扩散规划方法在训练期间利用学习到的动力学生成想象轨迹，或在推理时采样候选规划，但它们并未利用推理时信息来实时优化策略参数。相比之下，我们设计了一个可微世界模型（DWM）流水线，能够通过想象回滚（imagined rollouts）进行端到端梯度计算，从而在推理时基于 MPC 进行策略优化。我们在 D4RL 连续控制基准测试（MuJoCo 运动任务和 AntMaze）上评估了我们的算法，结果表明，利用推理时信息来优化策略参数相比强力的离线强化学习基准模型取得了持续的性能提升。

## Abstract
Offline Reinforcement Learning (RL) aims to learn optimal policies from fixed offline datasets, without further interactions with the environment. Such methods train an offline policy (or value function), and apply it at inference time without further refinement. We introduce an inference time adaptation framework inspired by model predictive control (MPC) that utilizes a pretrained policy along with a learned world model of state transitions and rewards. While existing world model and diffusion-planning methods use learned dynamics to generate imagined trajectories during training, or to sample candidate plans at inference time, they do not use inference-time information to optimize the policy parameters on the fly. In contrast, our design is a Differentiable World Model (DWM) pipeline that enables endto-end gradient computation through imagined rollouts for policy optimization at inference time based on MPC. We evaluate our algorithm on D4RL continuous-control benchmarks (MuJoCo locomotion tasks and AntMaze), and show that exploiting inference-time information to optimize the policy parameters yields consistent gains over strong offline RL baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的离线强化学习（Offline RL）方法通常遵循“先训练、后部署”的模式，在推理阶段策略参数是固定的。然而，离线学习常面临“分布偏移”问题，即推理时遇到的状态可能不在训练数据的覆盖范围内。
*   **核心问题**：能否在推理阶段利用当前环境的实时信息，通过额外的计算来进一步优化策略？
*   **整体含义**：本文提出了一种受模型预测控制（MPC）启发的推理时自适应框架。通过构建一个**可微世界模型（DWM）**，智能体可以在推理时通过“想象”未来的轨迹，并利用梯度下降实时微调策略参数，从而弥补离线预训练的不足。

### 2. 论文提出的方法论
*   **核心思想**：将推理过程视为一个短时界的优化问题。智能体利用学习到的环境动力学模型进行多步预测（Rollout），计算预测回报，并通过反向传播更新策略。
*   **关键技术细节**：
    *   **可微扩散采样器（Differentiable Diffusion Sampler）**：使用条件扩散概率模型建模状态转移 $P(s'|s, a)$。通过将扩散过程表示为确定的计算图，使得梯度可以穿透采样过程进行回传。
    *   **奖励模型与终端价值**：训练一个回归模型预测即时奖励 $r(s, a)$，并利用预训练的 Critic 函数 $Q(s, a)$ 作为有限时界回滚的终端价值估计。
    *   **MPC 优化流程**：在每个真实环境步 $t$，从当前状态 $s_t$ 出发，利用当前策略和扩散模型生成 $M$ 条长度为 $H$ 的想象轨迹。
    *   **梯度递归（Theorem 4.1）**：论文推导了通过扩散模型回滚的链式法则递归公式，将策略参数的更新与扩散模型的雅可比矩阵（Jacobians）联系起来。
*   **算法流程**：观察状态 -> 想象回滚 -> 计算代理目标函数（奖励总和+终端价值） -> 梯度上升更新策略参数 -> 执行更新后策略的第一步动作。

### 3. 实验设计
*   **数据集/场景**：使用了标准的 **D4RL** 连续控制基准测试。
    *   **Gym MuJoCo**：包含 HalfCheetah、Hopper、Walker2d 的 18 个子任务（涵盖 random, medium, expert 等不同数据质量）。
    *   **AntMaze**：包含 6 个更具挑战性的迷宫任务（umaze, medium, large），侧重于长时界规划。
*   **对比方法（Baselines）**：
    *   **无系综离线 RL**：TD3+BC, IQL, CQL, SAC-RND, ReBRAC。
    *   **生成式/模型化方法**：Decision Transformer (DT), Trajectory Transformer (TT), MOPO, MOReL, Diffuser 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   **训练细节**：提到了扩散模型和奖励模型训练了 200k 步。在推理时，每个环境步执行 $E$ 次内部梯度更新。由于扩散模型推理本身计算量大，加上梯度回传，该方法的推理延迟显著高于传统方法。

### 5. 实验数量与充分性
*   **实验规模**：在 24 个 D4RL 任务上进行了评估，每个任务使用 **10 个不同的随机种子**进行测试，提供了均值和标准差。
*   **充分性与客观性**：
    *   实验覆盖了从简单运动到复杂导航的任务，对比了多种类型的基线模型，实验设计较为全面。
    *   通过消融对比（如表 3 中 MPCwDWM 与预训练 ReBRAC 的对比），清晰地展示了“推理时优化”带来的增量收益。
    *   **公平性**：作者使用了 ReBRAC 作为预训练基础，并证明了在其基础上加入 MPC 优化能进一步提升性能，这种对比是客观的。

### 6. 论文的主要结论与发现
*   **推理时优化的有效性**：在 18 个 MuJoCo 任务中的 16 个任务上，推理时自适应都提升了性能。
*   **复杂任务表现优异**：在挑战性极大的 AntMaze Large 任务中，性能提升尤为显著（例如 Large-Diverse 从 51 分提升至 66 分）。
*   **超越生成式基线**：该方法在 MuJoCo 任务上的平均得分（94.4）显著超过了之前的扩散规划领先方法 Diffuser（77.5）。
*   **局部信息的价值**：实验证明，相比于全局固定的策略，利用当前状态进行局部策略微调能更有效地应对离线数据的局限性。

### 7. 优点（亮点）
*   **端到端可微性**：成功将复杂的扩散模型整合进可微 MPC 框架中，实现了通过生成模型进行策略优化的闭环。
*   **即插即用**：该框架可以作为现有离线 RL 算法（如 ReBRAC）的增强插件，具有较好的通用性。
*   **理论支撑**：提供了详细的梯度递归证明，为通过扩散模型进行反向传播奠定了数学基础。

### 8. 不足与局限
*   **推理开销（计算延迟）**：这是最大的局限。扩散模型的多步采样本就缓慢，再加上推理时的梯度计算和多次迭代，使得该方法难以应用于对实时性要求极高的场景（如高频机器人控制）。
*   **模型依赖性**：性能高度依赖于世界模型（扩散模型和奖励模型）的准确性。如果世界模型在某些区域存在偏差，推理时优化可能会导致策略“误入歧途”（模型剥削）。
*   **超参数敏感性**：推理时的时界 $H$、粒子数 $M$ 和更新步数 $E$ 需要仔细调优，以平衡性能和计算成本。

（完）
