---
title: Hierarchical Control Framework Integrating LLMs with RL for Decarbonized HVAC Operation
title_zh: 集成大语言模型与强化学习的脱碳暖通空调运行分层控制框架
authors: "Dianyu Zhong, Tian Xing, Kailai Sun, Xu Yang, Heye Huang, Irfan Qaisar, Tinggang Jia, Shaobo Wang, Qianchuan Zhao"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26050v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 集成LLM与强化学习的分层控制框架
tldr: 针对多区域建筑HVAC系统在脱碳运行中面临的动作空间庞大和探索效率低的问题，本文提出一种集成大语言模型（LLM）与强化学习（RL）的分层控制框架。该框架利用微调后的LLM根据历史数据生成动作掩码以缩小搜索空间，再由RL智能体进行约束优化。实验表明，该方法在提升热舒适度的同时显著降低了能耗，为复杂建筑环境的智能控制提供了新思路。
selection_source: fresh_fetch
motivation: 传统强化学习在多区域HVAC控制中面临动作空间爆炸和探索效率低的问题，而LLM虽具语义知识但缺乏闭环数值优化能力。
method: 提出一种分层框架，利用微调后的LLM生成状态相关的可行动作掩码来修剪动作空间，并结合掩码强化学习进行高效的约束优化。
result: "在7区域办公楼模拟中，该方法将平均不满意百分比（PPD）降至7.30%，且日均能耗降至140.90 kWh，均优于纯RL和纯LLM基准。"
conclusion: LLM引导的动作掩码技术能有效提升多区域HVAC控制的效率与稳定性，是实现建筑节能减排的有效途径。
---

## 摘要
供暖、通风与空调（HVAC）系统占建筑能耗的很大一部分。环境不确定性和动态的人员占用行为给脱碳 HVAC 控制带来了挑战。强化学习（RL）可以优化长周期的舒适度与能耗权衡，但在多区域建筑中面临动作空间指数级增长和探索效率低下的问题。大语言模型（LLMs）能够编码语义上下文和运行知识，但单独使用时缺乏可靠的闭环数值优化，可能导致舒适度与能耗权衡的可靠性降低。为解决这些局限性，我们提出了一种分层控制框架，其中基于历史建筑运行数据训练的微调 LLM 生成与状态相关的可行动作掩码，将组合联合动作空间剪枝为操作上合理的子集。随后，一个带掩码的基于值的 RL 智能体在缩减后的空间内执行约束优化，从而提高探索效率和训练稳定性。在使用 7 区域办公楼真实传感器和人员占用数据校准的高保真模拟器中进行评估，该方法实现了 7.30% 的平均预测不满意百分比（PPD），相比舒适度表现最好的原始 RL 基准（DQN）降低了 39.1%，相比表现最好的原始 LLM 基准降低了 53.1%，同时将每日 HVAC 能耗降至 140.90 kWh，低于所有原始 RL 基准。结果表明，LLM 引导的动作掩码是实现高效多区域 HVAC 控制的一种极具前景的途径。

## Abstract
Heating, ventilation, and air conditioning (HVAC) systems account for a substantial share of building energy consumption. Environmental uncertainty and dynamic occupancy behavior bring challenges in decarbonized HVAC control. Reinforcement learning (RL) can optimize long-horizon comfort-energy trade-offs but suffers from exponential action-space growth and inefficient exploration in multi-zone buildings. Large language models (LLMs) can encode semantic context and operational knowledge, yet when used alone they lack reliable closed-loop numerical optimization and may result in less reliable comfort-energy trade-offs. To address these limitations, we propose a hierarchical control framework in which a fine-tuned LLM, trained on historical building operation data, generates state-dependent feasible action masks that prune the combinatorial joint action space into operationally plausible subsets. A masked value-based RL agent then performs constrained optimization within this reduced space, improving exploration efficiency and training stability. Evaluated in a high-fidelity simulator calibrated with real-world sensor and occupancy data from a 7-zone office building, the proposed method achieves a mean PPD of 7.30%, corresponding to reductions of 39.1% relative to DQN, the best vanilla RL baseline in comfort, and 53.1% relative to the best vanilla LLM baseline, while reducing daily HVAC energy use to 140.90~kWh, lower than all vanilla RL baselines. The results suggest that LLM-guided action masking is a promising pathway toward efficient multi-zone HVAC control.

---

## 论文详细总结（自动生成）

### 论文总结：集成大语言模型与强化学习的脱碳暖通空调运行分层控制框架

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：暖通空调（HVAC）系统占建筑能耗的 32% 以上。在多区域建筑中，实现“以人为本”的控制（OCC）面临两大难题：
    1.  **强化学习（RL）的局限性**：随着区域增多，联合动作空间呈指数级爆炸（如本研究中 7 区域对应 16,384 种动作组合），导致探索效率极低且训练不稳定。
    2.  **大语言模型（LLM）的局限性**：虽然 LLM 具备丰富的语义上下文和操作常识，但作为独立控制器时，缺乏闭环数值优化能力，难以处理复杂的动态热力学权衡。
*   **研究目标**：提出一种分层控制框架，利用 LLM 的语义先验知识来约束 RL 的搜索空间，从而实现高效、稳定的多区域 HVAC 节能控制。

#### 2. 方法论：核心思想与技术细节
该论文提出了一种**分层 LLM-RL 控制框架**，将决策过程分为两个阶段：
*   **上层：LLM 动作掩码生成**
    *   **核心思想**：利用微调后的 LLM（基于 Meta-Llama-3-8B）作为“过滤器”。
    *   **技术细节**：通过对真实历史运行数据进行 **LoRA（低秩自适应）微调**，使 LLM 学习历史中合理的控制模式。
    *   **流程**：将当前及过去 5 分钟的室内外温度、人员数量、风机状态序列化为结构化 Prompt。LLM 输出一个 JSON 格式的建议动作集（掩码），剔除明显不合理（如空房间开启高风速）的动作。
*   **下层：带掩码的强化学习（Masked RL）**
    *   **核心思想**：在 LLM 缩减后的动作空间内进行精确的数值优化。
    *   **算法流程**：使用 **DQN（深度 Q 网络）**。在计算 Q 值和执行 $\epsilon$-greedy 探索时，通过将非法动作的 Q 值设为极小值（Masking），强制智能体仅在 LLM 建议的子集内选择动作。
*   **推理加速**：引入了 **LLM + Cache（缓存）机制**。将常见的状态-掩码对存储，避免重复调用 LLM，显著降低在线推理延迟。

#### 3. 实验设计
*   **场景与数据集**：
    *   **物理对象**：中国河北省某 7 区域办公楼。
    *   **模拟器**：基于真实传感器和人员占用数据校准的高保真 HVAC 模拟器。
    *   **数据来源**：2021 年 8 月 7 日至 22 日的真实运行数据（1 分钟采样间隔）。
*   **对比基准（Benchmarks）**：
    *   **原始 RL 方法**：A2C、PPO、DQN（全空间搜索）。
    *   **原始 LLM 方法**：Meta-Llama-3-8B、Qwen2.5-14B、Qwen2.5-72B（直接输出控制动作）。
*   **评价指标**：平均预测不满意百分比（PPD）、平均绝对预测平均投票（PMV）、每日 HVAC 总能耗（kWh）。

#### 4. 资源与算力
*   **微调参数**：使用了 LoRA 技术，FP16 精度，Batch Size 为 16（2 $\times$ 8 梯度累积），学习率 $2 \times 10^{-5}$，训练 5 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 RTX 4090）及具体的训练总时长。但从其轻量化的 SFT（监督微调）配置来看，该方法对算力的要求相对适中。

#### 5. 实验数量与充分性
*   **实验组数**：
    *   进行了 5 个随机种子的重复实验以验证 RL 的稳定性。
    *   对比了不同规模的 LLM（8B 到 72B）在直接控制下的表现。
    *   进行了**消融/对比实验**：包括冷启动分析（Masked-Random vs Full-Random）、动作空间缩减比例分析、推理效率分析。
*   **充分性评价**：实验设计较为全面，涵盖了从训练收敛性到在线推理性能的多个维度。通过真实数据校准模拟器增强了实验的客观性和说服力。

#### 6. 主要结论与发现
*   **性能卓越**：该框架实现了 **7.30% 的平均 PPD**，远低于 10% 的舒适度阈值。相比于表现最好的原始 RL（DQN），舒适度提升了 **39.1%**；相比于最强的 LLM 基准（Qwen2.5-72B），舒适度提升了 **53.1%**。
*   **节能显著**：日均能耗为 **140.90 kWh**，低于所有原始 RL 基准。
*   **空间缩减**：LLM 掩码平均剪枝了 **81.56%** 的动作空间，极大地加速了 RL 的收敛速度并提高了训练稳定性。
*   **推理优化**：Cache 机制将平均单步推理延迟从 2.27 秒降低至 **0.087 秒**（降低 96%），满足实时控制需求。

#### 7. 优点与亮点
*   **架构创新**：巧妙地结合了 LLM 的“常识/先验”和 RL 的“闭环/精确优化”，解决了 RL 在高维离散空间探索难的问题。
*   **可解释性**：通过案例研究展示了 LLM 引导下的控制逻辑（如午休时间自动调低频率），比纯黑盒 RL 更具可解释性。
*   **工程实用性**：Cache 机制和轻量化微调方案考虑了实际部署的算力限制。

#### 8. 不足与局限
*   **数据依赖**：LLM 的掩码质量高度依赖于历史运行数据的质量和多样性，若历史数据存在偏差，可能误导 RL。
*   **动作类型限制**：目前仅针对**离散动作**（风机转速 0-3 级），尚未扩展到连续变量（如冷冻水流量、设定点温度）的控制。
*   **场景单一**：实验仅在单一办公楼模拟器中完成，跨建筑、跨气候区的泛化能力有待进一步验证。
*   **冷启动风险**：虽然 LLM 辅助了冷启动，但在完全陌生的建筑环境中，LLM 的先验知识可能失效。

（完）
