---
title: "WirelessBench: A Tolerance-Aware LLM Agent Benchmark for Wireless Network Intelligence"
title_zh: WirelessBench：一种面向无线网络智能的容差感知大语言模型智能体基准测试
authors: "Jingwen Tong, Fang Liu, Linkai Xv, Shiliang Lu, Kangqi Li, Yiqian Zhang, Yijie Song, Zeyang Xue, Jun Zhang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21251v1"
tags: ["query:ag-rl"]
score: 8.0
evidence: 针对具有多步决策能力的LLM无线智能体基准测试
tldr: WirelessBench是首个针对无线网络智能LLM Agent的容错感知基准测试。它构建了涵盖领域知识推理、资源分配和多步决策的三层认知层级，并引入容错评分机制以识别单位混淆等灾难性错误。该基准集成了3GPP合规的射线追踪工具和CoT追踪轨迹，旨在为无线网络自主管理提供更符合工程实际的可靠性评估。
selection_source: fresh_fetch
motivation: 现有无线基准测试忽略了级联故障和灾难性单位错误，难以评估LLM Agent在真实无线工程环境中的可靠性。
method: 设计了包含WCHW、WCNS和WCMSA三层层级的评估框架，并结合容错评分、工具调用及CoT链条追踪进行多维度诊断。
result: "工具集成Agent的得分显著高于直接提示模型，且识别出23%的错误为传统精确匹配指标无法检测的灾难性失效。"
conclusion: WirelessBench通过细粒度的错误分类和容错评估，为开发和优化高可靠性的无线网络智能体提供了重要支撑。
---

## 摘要
大语言模型（LLM）智能体正成为实现自主无线网络管理的关键驱动力。然而，可靠地部署这些智能体需要能够反映真实工程风险的基准测试。现有的无线基准测试仅评估单一孤立的能力，并统一处理所有错误，忽略了级联链式故障和灾难性的单位混淆（例如 dB 与 dBm）。我们提出了 WirelessBench，这是首个面向基于 LLM 的无线智能体的容差感知、工具集成的基准测试。WirelessBench 按照三层认知层级进行组织：领域知识推理（WCHW，1,392 项）、意图驱动的资源分配（WCNS，1,000 项）以及移动性下的主动多步决策（WCMSA，1,000 项）。此外，WirelessBench 基于三个设计原则建立：(i) 具有灾难性错误检测功能的容差感知评分；(ii) 需要符合 3GPP 标准的射线追踪查询以获取信道质量的工具必要型任务；以及 (iii) 思维链（CoT）可追溯项，其中每个基准测试项都附带完整的 CoT 轨迹，从而能够对智能体在推理链中失败的位置进行细粒度诊断。我们的数值结果显示，直接提示模型（GPT-4o）得分为 68%，落后于工具集成智能体（84.64%）16.64 个百分点；23% 的错误是精确匹配指标无法察觉的灾难性故障。更重要的是，该层级结构将错误分解为四种可操作的诊断类别，这是扁平化评估无法揭示的。代码和数据：https://wirelessbench.github.io/。

## Abstract
LLM agents are emerging as a key enabler for autonomous wireless network management. Reliably deploying them, however, demands benchmarks that reflect real engineering risk. Existing wireless benchmarks evaluate single isolated capabilities and treat all errors uniformly, missing both cascaded-chain failures and catastrophic unit confusions (\textit{e.g.}, dB vs.\ dBm). We present \wb{}, the first tolerance-aware, tool-integrated benchmark for LLM-based wireless agents. \wb{} is organized as a three-tier cognitive hierarchy: domain knowledge reasoning (WCHW, 1{,}392 items), intent-driven resource allocation (WCNS, 1{,}000 items), and proactive multi-step decisions under mobility (WCMSA, 1{,}000 items). Moreover, \wb{} is established on three design principles: \emph{(i)}~tolerance-aware scoring with catastrophic-error detection; \emph{(ii)}~tool-necessary tasks requiring a 3GPP-compliant ray-tracing query for channel quality; and \emph{(iii)}~Chain-of-Thought (CoT)-traceable items, where every benchmark item ships with a complete CoT trajectory enabling fine-grained diagnosis of where in the reasoning chain an agent fails. Our numerical results show that the direct-prompting model (GPT-4o) scores $68\%$, trailing a tool-integrated agent ($84.64\%$) by $16.64$\,pp; $23\%$ of errors are catastrophic failures invisible to exact-match metrics. More importantly, the hierarchy decomposes errors into four actionable diagnostic categories that flat evaluation cannot reveal. Code and data: https://wirelessbench.github.io/.

---

## 论文详细总结（自动生成）

### WirelessBench：面向无线网络智能的容差感知 LLM Agent 基准测试总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着 6G 时代的到来，无线网络管理的复杂性已超出人类操作员的范畴。基于大语言模型（LLM）的智能体（Agent）因其理解意图、调用工具和多步推理的能力，被视为实现自主网络管理的希望。然而，现有的无线领域基准测试（如 TeleQnA、WirelessMathBench）存在两个致命缺陷：
*   **评估维度孤立：** 仅测试单一的知识召回或公式计算，忽略了真实部署中常见的级联链式故障。
*   **错误评价标准单一：** 采用“全对或全错”的精确匹配，无法区分“良性的数值近似”与“灾难性的工程错误”（如 dB 与 dBm 的单位混淆，这会导致 1000 倍的功率偏差）。
**WirelessBench** 的提出旨在填补这一空白，提供一个符合工程风险特征、集成工具调用且具备容差感知的可靠性评估框架。

#### 2. 论文提出的方法论
论文构建了一个 **3×3 的设计矩阵**，结合了三层认知层级和三个核心设计原则：

*   **三层认知层级：**
    1.  **WCHW（无线通信作业）：** 1,392 项，测试基础知识推理（如香农容量、误码率、衰落分析）。
    2.  **WCNS（无线网络切片）：** 1,000 项，测试意图驱动的资源分配，要求 Agent 调用射线追踪工具获取信道质量（CQI）。
    3.  **WCMSA（移动服务保障）：** 1,000 项，测试移动性下的主动决策，涉及轨迹预测、信道估计和 QoS 验证。
*   **核心技术细节：**
    *   **容差感知评分（Tolerance-Aware Scoring）：** 引入分级信用机制。对于数值误差 $\epsilon \le 1\%$ 给满分，$1\% < \epsilon \le 10\%$ 给部分分，超过 $10\%$ 或出现**单位/数量级混淆**（灾难性错误）则直接计零分。
    *   **工具必要型任务：** 集成了基于 3GPP TR 38.901 标准的射线追踪数字孪生系统。Agent 必须通过 `ray_tracing(x, y, region)` 接口获取物理层信息，无法仅靠文本生成完成任务。
    *   **CoT 轨迹追踪：** 每个题目附带完整的思维链（CoT）路径，用于诊断 Agent 是在公式选择、工具调用还是逻辑推理的哪一环出错。
*   **数据构建流水线：** 采用心理测量学启发的方法，通过多模型共识、项目反应理论（IRT）指标（如 ITC、Mokken 量表）进行数据清洗和增强，确保题目具备区分度和单维性。

#### 3. 实验设计
*   **数据集/场景：** 包含 3,392 个样本。场景基于香港科技大学（HKUST）校园的真实地理数据构建的射线追踪地图（涵盖北、中、南三个区域）。
*   **对比方法：**
    *   **直接提示（Direct Prompting）：** GPT-4o, Qwen-Turbo。
    *   **高级提示工程：** CoT-SC (k=5), MedPrompt。
    *   **自动化 Agent 框架：** ADAS, AFlow。
    *   **参考流水线：** WirelessBench-Ref（集成专用工具和 MCTS 搜索的优化 Agent）。
*   **Benchmark 构成：** 知识推理（WCHW）、意图到分配（WCNS）、主动决策链（WCMSA）。

#### 4. 资源与算力
*   **算力说明：** 论文主要评估现有的 API 模型（GPT-4o, Claude 4.5, Qwen-Turbo-Latest），未涉及大规模的本地模型训练。
*   **推理成本：** 论文详细记录了 MCTS 搜索的成本。例如，WCHW 任务的 19 轮搜索耗时 63 分钟，总成本约 4.95 美元。
*   **GPU 信息：** 文中未明确提及具体的本地 GPU 型号或数量，因为实验核心在于 Agent 框架的编排与 API 调用。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 3,392 个样本的全量测试，并针对 GPT-4o 的错误进行了细粒度的分类统计（31% 公式误用、28% 推理断裂等）。
*   **消融实验：** 在 WCHW 验证集上对参考 Agent 的组件（如领域工具、启发式评价器、经验回溯）进行了消融分析。
*   **客观性与公平性：** 引入了心理测量学过滤机制剔除低质量题目，并设置了严格的验证集/测试集划分（25%/75%），确保评估结果的客观性。

#### 6. 论文的主要结论与发现
*   **工具集成是关键：** GPT-4o 直接提示的平均得分仅 68%，而集成工具的参考 Agent 达到 84.64%，差距达 16.64 个百分点。
*   **隐藏风险暴露：** 容差感知评分揭示了 23% 的错误属于“灾难性失效”（如单位混淆），这些错误在传统的精确匹配指标下会被掩盖。
*   **级联故障诊断：** 随着任务层级提升（从 Tier 1 到 Tier 3），推理路径断裂成为主要的失败原因，证明了多步决策链评估的必要性。

#### 7. 优点：方法或实验设计上的亮点
*   **工程实用性强：** 首次将“容差”和“灾难性错误检测”引入无线 AI 评估，非常贴合电信运营商的实际风险偏好。
*   **物理接地（Physical Grounding）：** 任务不只是文本游戏，而是与 3GPP 合规的射线追踪引擎深度绑定，确保了评估的科学性。
*   **诊断深度：** 通过 CoT 轨迹和三层认知层级，能够精准定位 Agent 的能力短板（是缺知识、不会用工具，还是逻辑乱了）。

#### 8. 不足与局限
*   **环境覆盖有限：** 目前数字孪生仅覆盖单一校园环境和特定频段（3.5 GHz），泛化到其他场景（如毫米波、非地面网络）的表现尚待验证。
*   **闭源模型依赖：** 基准测试的构建和部分评估依赖于 GPT-4o 等闭源模型，可能存在潜在的偏见或数据泄露风险。
*   **协同设计偏差：** 参考 Agent（WirelessBench-Ref）与基准测试由同一团队开发，可能存在过拟合基准结构的风险（论文中已坦诚讨论此点）。
*   **静态评估：** 目前任务仍是离线的、单智能体的，尚未涵盖多智能体协作或流式实时数据的动态管理。

（完）
