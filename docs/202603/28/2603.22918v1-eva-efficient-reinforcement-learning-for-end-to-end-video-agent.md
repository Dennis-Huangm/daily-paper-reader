---
title: "EVA: Efficient Reinforcement Learning for End-to-End Video Agent"
title_zh: EVA：面向端到端视频智能体的高效强化学习
authors: "Yaolun Zhang, Ruohui Wang, Jiahao Wang, Yepeng Tang, Xuanyu Zheng, Haonan Duan, Hao Lu, Hanming Deng, Lewei Lu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22918v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 端到端视频代理的高效强化学习框架
tldr: 针对多模态大模型在长视频理解中存在的冗余和效率问题，本文提出EVA框架。EVA是一个基于强化学习的端到端视频智能体，采用“先规划后感知”的策略，通过迭代的总结-规划-行动-反思机制自主决定观察内容。通过SFT、KTO和GRPO三阶段训练，EVA在六个基准测试中表现优异，显著提升了视频理解的效率和准确性。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在处理长视频时存在计算冗余且缺乏自适应推理能力，导致理解效率低下。
method: 提出EVA框架，通过迭代的“总结-规划-行动-反思”机制实现自主视频观测，并采用SFT、KTO和GRPO三阶段强化学习流程进行训练。
result: "在六个视频理解基准测试中，EVA比通用MLLM基线提升了6-12%，比之前的自适应智能体方法提升了1-3%。"
conclusion: EVA证明了通过强化学习训练的端到端智能体能够实现更高效、更精准的查询驱动型视频理解。
---

## 摘要
由于视频包含广泛的时间依赖性和冗余帧，导致其 Token 序列过长，使用多模态大语言模型（MLLM）进行视频理解仍然具有挑战性。现有方法通常将 MLLM 视为被动识别器，在没有自适应推理的情况下处理整个视频或均匀采样的帧。最近基于智能体（agent-based）的方法引入了外部工具，但仍依赖于人工设计的工作流和感知优先策略，导致在长视频上的效率低下。我们提出了 EVA，一个面向端到端视频智能体的高效强化学习框架，它通过迭代的“总结-计划-行动-反思”推理实现了“先计划后感知”。EVA 自主决定看什么、何时看以及如何看，从而实现查询驱动且高效的视频理解。为了训练此类智能体，我们设计了一个简单而有效的三阶段学习流水线——包括监督微调（SFT）、Kahneman-Tversky 优化（KTO）和广义奖励策略优化（GRPO）——从而桥接了监督模仿学习与强化学习。我们进一步为每个阶段构建了高质量数据集，支持稳定且可复现的训练。我们在六个视频理解基准测试上评估了 EVA，展示了其全面的能力。与现有基准相比，EVA 在通用 MLLM 基准上实现了 6-12% 的显著提升，并比之前的自适应智能体方法进一步提高了 1-3%。我们的代码和模型可在 https://github.com/wangruohui/EfficientVideoAgent 获取。

## Abstract
Video understanding with multimodal large language models (MLLMs) remains challenging due to the long token sequences of videos, which contain extensive temporal dependencies and redundant frames. Existing approaches typically treat MLLMs as passive recognizers, processing entire videos or uniformly sampled frames without adaptive reasoning. Recent agent-based methods introduce external tools, yet still depend on manually designed workflows and perception-first strategies, resulting in inefficiency on long videos. We present EVA, an Efficient Reinforcement Learning framework for End-to-End Video Agent, which enables planning-before-perception through iterative summary-plan-action-reflection reasoning. EVA autonomously decides what to watch, when to watch, and how to watch, achieving query-driven and efficient video understanding. To train such agents, we design a simple yet effective three-stage learning pipeline - comprising supervised fine-tuning (SFT), Kahneman-Tversky Optimization (KTO), and Generalized Reward Policy Optimization (GRPO) - that bridges supervised imitation and reinforcement learning. We further construct high-quality datasets for each stage, supporting stable and reproducible training. We evaluate EVA on six video understanding benchmarks, demonstrating its comprehensive capabilities. Compared with existing baselines, EVA achieves a substantial improvement of 6-12% over general MLLM baselines and a further 1-3% gain over prior adaptive agent methods. Our code and model are available at https://github.com/wangruohui/EfficientVideoAgent.

---

## 论文详细总结（自动生成）

### 论文总结：EVA —— 面向端到端视频智能体的高效强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
视频理解目前面临的主要挑战在于视频序列极长，包含大量冗余信息和复杂的时间依赖。现有的多模态大语言模型（MLLM）通常扮演“被动识别器”的角色，采用全量处理或均匀采样的方式，这不仅导致计算资源浪费（Token 消耗巨大），还容易让模型被无关帧误导。
**EVA 的核心动机**是将 MLLM 从被动观察者转变为主动探索者。论文提出了“先规划后感知”（Planning-before-perception）的范式，让智能体在看到视频前先根据问题进行推理，自主决定“看哪里、看多少、怎么看”，从而实现高效且精准的视频理解。

#### 2. 方法论：核心思想与关键技术
EVA 采用了一种迭代的推理闭环，并结合了三阶段的端到端训练流水线：

*   **核心推理循环（SPAR）**：
    *   **总结（Summary）**：对已获取的视觉证据进行描述。
    *   **规划（Planning）**：根据当前信息评估成本与收益，决定下一步行动。
    *   **行动（Action）**：调用 `frame_select` 工具。该工具具有极高的自由度，可控制时间范围（Start/End）、采样频率（nframes）和空间分辨率（resize）。
    *   **反思（Reflection）**：在给出最终答案前，评估现有视觉信息是否充分，若不充分则继续迭代。
*   **三阶段训练策略**：
    1.  **SFT 冷启动**：使用教师模型（Qwen2.5-VL-72B）生成的 10k 高质量数据，训练模型掌握工具格式和基础推理模式。
    2.  **KTO（Kahneman-Tversky 优化）策略修正**：利用 11k 成功与失败的轨迹数据，通过偏好学习纠正模型常见的错误策略（如信息不足就瞎猜），为后续强化学习提供稳定的行为先验。
    3.  **数据增强型 GRPO（广义奖励策略优化）**：在线强化学习阶段。通过收集失败案例并让教师模型生成针对性的新 QA 对来动态扩充数据集。
*   **奖励函数设计**：
    *   **准确性奖励**：多选题采用 CSV（自验证）奖励，开放题采用 ROUGE 分数。
    *   **格式奖励**：给予正确调用工具但答案错误的尝试极小的补偿奖励，以防止模型跳过推理直接猜测答案（Reward Hacking）。

#### 3. 实验设计
*   **数据集与基准**：
    *   **采样困境测试**：LSDBench（评估采样效率与准确性的平衡）。
    *   **长视频理解**：LongVideoBench, MLVU, VideoMME, LVBench。
    *   **视频推理与幻觉**：Video-Holmes（零样本推理）, ELV-Halluc（语义聚合幻觉测试）。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-2.0-Flash, Gemini-1.5-Pro。
    *   **静态采样模型**：Qwen2.5-VL, InternVL2.5, Video-R1, VideoChat-R1。
    *   **自适应智能体**：FrameThinker, VideoAgent, VideoMTR。

#### 4. 资源与算力
*   **基础模型**：Qwen2.5-VL-7B-Instruct。
*   **算力配置**：
    *   **GRPO 阶段**：使用了 **32 张 NVIDIA H100 GPU**。
    *   **训练参数**：Batch size 为 64，每个样本进行 8 次 rollout，学习率为 1e-6，训练 1 个 epoch。
    *   **SFT 阶段**：Batch size 为 8，学习率 2e-6，训练 2 个 epoch。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **跨领域评测**：在 6 个主流视频理解基准上进行了测试，涵盖了从短视频到超长视频（超过 6600 秒）的场景。
*   **消融实验**：详细对比了 SFT、KTO、GRPO 三个阶段对性能、交互轮数和 Token 消耗的影响，证明了每一阶段的必要性。
*   **数据组成实验**：验证了混合开放式（OE）和多选题（MC）数据对抑制模型“刷分/作弊”行为的效果。
*   **效率分析**：通过统计不同轮次的 Token 分布，客观展示了智能体如何通过“先粗看再精看”来节省算力。

#### 6. 主要结论与发现
*   **性能卓越**：EVA 在多个基准上超越了现有的开源模型和自适应智能体，准确率提升了 1-12% 不等。
*   **极高的效率**：在 LSDBench 上，EVA 仅使用约 6.2K 视觉 Token 就达到了 51.8% 的准确率，而某些闭源模型达到类似水平需要消耗超过 700K Token。
*   **主动感知优势**：实验证明“先规划后感知”能有效避免无关帧的干扰（视觉误导），并能根据问题复杂度自主调整观察策略。

#### 7. 优点
*   **端到端自主性**：不同于以往固定工作流的智能体，EVA 拥有更大的动作空间（同时控制时空分辨率），实现了真正的自主观测。
*   **训练流程稳健**：引入 KTO 阶段作为 SFT 和 GRPO 之间的桥梁，有效解决了强化学习在复杂多轮对话任务中难以收敛的问题。
*   **可解释性强**：通过 SPAR 循环，模型的推理过程（总结、规划、反思）清晰可见，便于分析决策逻辑。

#### 8. 不足与局限
*   **工具依赖性**：目前的推理逻辑高度依赖预定义的 `frame_select` 接口，对于需要其他类型工具（如音频分析、外部搜索）的任务支持有限。
*   **分布外鲁棒性**：虽然在多个基准上表现良好，但在面对极其嘈杂或完全未见的查询分布时，智能体可能会陷入无效的循环。
*   **长程记忆缺失**：目前主要依靠上下文窗口存储历史信息，缺乏显式的跨模态长期记忆机制，处理超大规模视频库时可能受限。

（完）
