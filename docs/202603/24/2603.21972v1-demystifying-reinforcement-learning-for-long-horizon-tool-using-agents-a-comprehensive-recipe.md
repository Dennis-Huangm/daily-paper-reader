---
title: "Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe"
title_zh: 揭秘长程工具使用智能体的强化学习：一份全面的方案
authors: "Xixi Wu, Qianguo Sun, Ruiyang Zhang, Chao Song, Junlong Wu, Yiyan Qi, Hong Cheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21972v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 在大模型长程智能体中扩展强化学习的全面方案
tldr: 本研究针对大语言模型在长程规划和工具使用中的强化学习（RL）应用，通过TravelPlanner基准进行了系统性实证研究。文章从奖励设计、模型规模、数据组成、算法选择和环境稳定性五个维度拆解了智能体RL的设计空间，总结出7项关键经验，如小模型需分阶段奖励而大模型更适应简单稠密奖励。最终训练出的模型在TravelPlanner上达到了SOTA水平，显著优于现有领先LLM。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型在复杂多轮环境下进行长程规划时，缺乏实用且可扩展的强化学习训练方案的问题。
method: 通过TravelPlanner基准测试，从奖励塑造、模型缩放、数据构成、算法选择和环境稳定性五个关键维度进行受控实验。
result: 实验表明奖励和算法选择具有规模依赖性，且约1000个难度均衡的训练样本是兼顾域内与域外性能的最佳平衡点。
conclusion: 本研究提炼出的RL训练方案显著提升了智能体处理复杂约束的能力，为构建高性能长程自主智能体提供了实证指南。
---

## 摘要
强化学习 (RL) 对于将大语言模型 (LLMs) 演进为具备长程规划能力的自主智能体至关重要，但在复杂的、多轮交互的环境中扩展强化学习的实用方案仍然难以捉摸。本文利用 TravelPlanner 进行了系统的实证研究，这是一个需要协调工具以满足多方面约束的极具挑战性的测试平台。我们从五个维度分解了智能体强化学习的设计空间：奖励塑造、模型缩放、数据组成、算法选择和环境稳定性。我们的受控实验得出了 7 个关键结论，例如：(1) 奖励和算法的选择具有规模依赖性，较小的模型受益于阶段性奖励和增强的探索，而较大的模型则能通过更简单的密集奖励高效收敛；(2) 约 1000 个具有平衡难度混合的训练样本是域内和域外性能的最佳平衡点；(3) 环境稳定性对于防止策略退化至关重要。基于我们提炼的方案，我们经过强化学习训练的模型在 TravelPlanner 上实现了最先进的性能，显著优于领先的大语言模型。

## Abstract
Reinforcement Learning (RL) is essential for evolving Large Language Models (LLMs) into autonomous agents capable of long-horizon planning, yet a practical recipe for scaling RL in complex, multi-turn environments remains elusive. This paper presents a systematic empirical study using TravelPlanner, a challenging testbed requiring tool orchestration to satisfy multifaceted constraints. We decompose the agentic RL design space along 5 axes: reward shaping, model scaling, data composition, algorithm selection, and environmental stability. Our controlled experiments yield 7 key takeaways, e.g., (1) reward and algorithm choices are scale-dependent as smaller models benefit from staged rewards and enhanced exploration, whereas larger models converge efficiently with simpler dense rewards, (2) ~ 1K training samples with a balanced difficulty mixture mark a sweet spot for both in-domain and out-of-domain performance, and (3) environmental stability is critical to prevent policy degradation. Based on our distilled recipe, our RL-trained models achieve state-of-the-art performance on TravelPlanner, significantly outperforming leading LLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）正演变为能够进行推理和行动的自主智能体，但在涉及数十个步骤、复杂约束（如预算、偏好、常识）的长程（Long-Horizon）规划任务中，其表现依然不佳。
*   **核心挑战**：强化学习（RL）被认为是提升智能体能力的关键，但目前缺乏在复杂、多轮交互环境下扩展 RL 的成熟方案（Recipe）。现有的研究多集中在短程任务，且缺乏对奖励设计、数据组成、模型规模等因素的系统性分析。
*   **研究目标**：通过系统性的实证研究，拆解智能体 RL 的设计空间，为训练高性能长程工具使用智能体提供一份可落地的“配方”。

### 2. 方法论：STAR 框架
论文提出了 **STAR (Synthesis, Training, And Reinforcement)** 统一后训练流水线：
1.  **数据合成 (Synthesis)**：
    *   在本地沙盒中采样旅游元素并验证可行性。
    *   利用强模型（如 DeepSeek-V3.2-Exp）通过反向翻译生成自然语言查询。
    *   控制难度等级（简单、中等、困难），生成超过 10K 条查询。
2.  **监督微调 (SFT)**：
    *   利用强教师模型（DeepSeek-V3.2-Exp-Thinking）生成高质量轨迹。
    *   通过拒绝采样过滤出成功且格式正确的轨迹（约 1.2K 条），为 RL 提供良好的初始策略，避免冷启动问题。
3.  **强化学习 (RL)**：
    *   **算法**：采用 **GRPO**（群体相对策略优化）作为基础，并对比了 DAPO（带过滤的优化）和 ARPO（带探索的优化）。
    *   **奖励设计**：探索了从密集（SUM）、半稀疏（MACRO）到纯稀疏（SUCCESS）的奖励函数，以及分阶段的**课程奖励 (Curriculum)**。
    *   **推理范式**：采用 **ReAct** 模式，让智能体交替进行推理、行动和观察。

### 3. 实验设计
*   **主要测试平台**：**TravelPlanner**。这是一个模拟真实旅游规划的基准，包含 1,000 个测试实例，要求智能体调用 6 种工具（搜索航班、酒店、餐厅等）并满足多重硬约束。
*   **对比基准 (Benchmarks)**：
    *   **闭源模型**：GPT-4o, GPT-5, Gemini 1.5 Pro, Kimi-K2.5, Qwen 3.5 等。
    *   **开源模型**：Qwen2.5-Instruct (1.5B, 3B, 7B) 及其 SFT 版本。
*   **域外 (OOD) 评估**：在 7 个知识密集型 QA 数据集（如 Natural Questions, TriviaQA, HotpotQA）上测试，以评估模型的泛化能力和“对齐税”（Alignment Tax）。

### 4. 资源与算力
*   **硬件设备**：
    *   1.5B 和 3B 模型：使用单节点 **8 × NVIDIA A100-80G**。
    *   7B 模型：使用双节点 **16 × NVIDIA A100-80G**。
*   **训练时长**：论文提供了详细的 GPU 学时（GPU Hours）。例如，7B 模型使用 GRPO 训练约需 **368 GPU 小时**，而使用更复杂的 ARPO 算法则需 **547 GPU 小时**。

### 5. 实验数量与充分性
*   **实验规模**：论文围绕 5 个核心维度（奖励、规模、数据、算法、稳定性）开展了大规模受控实验。
*   **充分性与客观性**：
    *   每组实验均严格控制变量（每次只改变一个因素）。
    *   使用了 1,000 条测试集数据和 7 个 OOD 数据集，样本量充足。
    *   引入了环境噪声注入实验（0% 到 10% 的工具故障率），增强了结论的现实意义。
    *   实验设计非常客观，涵盖了从模型收敛曲线到最终成功率的多维度分析。

### 6. 主要结论与发现
1.  **奖励与算法的规模依赖性**：小模型（1.5B/3B）极度依赖课程奖励和重探索算法（如 ARPO）；而大模型（7B）直接使用简单密集奖励和标准 GRPO 效果更好且更高效。
2.  **数据“甜点区”**：约 **1,000 个训练样本**是性能平衡点。超过此数量会导致模型在 TravelPlanner 上过拟合，从而在 OOD 任务上表现大幅下降（泛化能力受损）。
3.  **难度混合至关重要**：纯困难数据会导致奖励过稀疏而无法学习，纯简单数据无法应对复杂约束。**4:3:3 (易:中:难)** 的比例最为理想。
4.  **环境稳定性**：智能体对轻微噪声有韧性，但当工具故障率达到 10% 时，策略性能会显著退化。
5.  **SOTA 性能**：基于该方案训练的 7B 模型在 TravelPlanner 上的成功率显著超过了 GPT-4o 和 Kimi-K2.5 等巨型商业模型。

### 7. 优点
*   **实战导向**：提供了一套清晰、可复现的训练方案，对工业界和学术界构建 Agent 具有极高的参考价值。
*   **全维度拆解**：不仅关注算法，还深入探讨了数据配比、奖励密度等往往被忽视的工程细节。
*   **高效性**：证明了通过合理的 RL 训练，较小规模的开源模型也能在特定复杂任务上超越顶级闭源模型。

### 8. 不足与局限
*   **环境局限性**：虽然 TravelPlanner 很复杂，但它仍是一个模拟沙盒，无法完全代表真实互联网环境中的不可预测性。
*   **模型规模上限**：实验最高只覆盖到 7B 模型，对于 70B 或更大规模模型的行为特征尚不明确。
*   **泛化评估范围**：OOD 评估仅限于 QA 任务，未测试跨领域的工具使用能力（如从旅游规划跨越到代码调试）。
*   **单因素分析**：主要关注单一变量的影响，未深入探讨多个因素（如数据量与算法复杂度）之间的非线性交互作用。

（完）
