---
title: Closed-Loop Verbal Reinforcement Learning for Task-Level Robotic Planning
title_zh: 针对任务级机器人规划的闭环言语强化学习
authors: "Dmitrii Plotnikov, Iaroslav Kolomiets, Dmitrii Maliukov, Dmitrij Kosenkov, Daniia Zinniatullina, Artem Trandofilov, Georgii Gazaryan, Kirill Bogatikov, Timofei Kozlov, Igor Duchinskii, Mikhail Konenkov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22169v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 使用 LLM 执行器和 VLM 评论器的语言强化学习框架用于机器人规划
tldr: 本研究针对移动机器人在不确定环境下的任务规划，提出了一种闭环言语强化学习（VRL）框架。该框架通过大语言模型（LLM）生成可执行的行为树，并利用视觉语言模型（VLM）观察执行过程并提供自然语言反馈，从而实现策略的迭代优化。与传统强化学习不同，VRL在符号规划层面进行更新，无需梯度优化，增强了系统的可解释性。实验证明，该方法能有效应对执行失败，并在真实机器人平台上实现了可靠的任务部署。
selection_source: fresh_fetch
motivation: 旨在解决移动机器人在不确定环境下执行复杂任务时，传统规划方法缺乏闭环自适应能力和决策透明度的问题。
method: 提出一种基于LLM和VLM的闭环框架，通过自然语言反馈在符号层面迭代优化行为树，而非依赖梯度下降。
result: 在真实机器人的多阶段导航与操作实验中，该框架展现了卓越的故障自适应能力和可解释的策略演进过程。
conclusion: 言语强化学习为实现透明、可靠且具备自我改进能力的机器人任务级规划提供了一种有效的非梯度优化路径。
---

## 摘要
我们提出了一种新的言语强化学习（VRL）框架，用于在存在执行不确定性的移动机器人系统中进行可解释的任务级规划。该框架采用闭环架构，通过与物理环境的交互实现策略的迭代改进。在我们的框架中，大语言模型（LLM）执行器利用视觉语言模型（VLM）评论器生成的结构化自然语言反馈，对可执行的行为树进行反复优化；该评论器负责观察物理机器人及其执行轨迹。与传统的强化学习不同，VRL 中的策略更新直接发生在符号规划层面，无需基于梯度的优化。这实现了透明的推理、显式的因果反馈以及人类可解释的策略演化。我们在真实移动机器人上验证了所提框架，该机器人需在执行不确定性下完成多阶段操作与导航任务。实验结果表明，该框架支持可解释的策略改进、对执行失败的闭环自适应，以及在物理机器人系统上的可靠部署。

## Abstract
We propose a new Verbal Reinforcement Learning (VRL) framework for interpretable task-level planning in mobile robotic systems operating under execution uncertainty. The framework follows a closed-loop architecture that enables iterative policy improvement through interaction with the physical environment. In our framework, executable Behavior Trees are repeatedly refined by a Large Language Model actor using structured natural-language feedback produced by a Vision-Language Model critic that observes the physical robot and execution traces. Unlike conventional reinforcement learning, policy updates in VRL occur directly at the symbolic planning level, without gradient-based optimization. This enables transparent reasoning, explicit causal feedback, and human-interpretable policy evolution. We validate the proposed framework on a real mobile robot performing a multi-stage manipulation and navigation task under execution uncertainty. Experimental results show that the framework supports explainable policy improvements, closed-loop adaptation to execution failures, and reliable deployment on physical robotic systems.

---

## 论文详细总结（自动生成）

以下是对论文《Closed-Loop Verbal Reinforcement Learning for Task-Level Robotic Planning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：移动机器人在现实环境中面临显著的执行不确定性（如定位误差、控制失效）。传统的任务规划方法（如行为树）虽具可解释性但需大量人工调优；而传统的强化学习（RL）虽能自主改进，但其策略通常是黑盒（亚符号级），缺乏透明度且难以调试。
*   **研究动机**：现有的基于大语言模型（LLM）的规划器多为“开环”模式，缺乏来自物理世界的接地（Grounded）反馈，难以针对执行失败进行自我修正。
*   **整体含义**：提出一种**闭环言语强化学习（VRL）**框架，通过自然语言反馈在符号层面（行为树）迭代优化机器人策略，实现可解释、无梯度的自主学习。

### 2. 方法论
*   **核心思想**：将策略表示为可执行的**行为树（Behavior Trees, BTs）**，利用 LLM 作为执行器（Actor）修改 BT，利用视觉语言模型（VLM）作为评论器（Critic）提供反馈。
*   **关键技术细节**：
    *   **策略表示**：使用 BT 保证模块化和透明度。Actor 直接修改 BT 的结构和参数。
    *   **LLM 执行器（Actor）**：输入包括任务定义、环境规范、BT 节点库和来自 Critic 的反馈。它不直接控制机器人，而是生成 BT 的代码编辑建议。
    *   **VLM 评论器（Critic）**：观察执行过程中的视觉快照和 BT 执行轨迹。输出包括：结构化自然语言反馈、警报分数 $s$（严重程度）和置信度 $c$。
    *   **闭环流程**：执行当前 BT -> VLM 观察并诊断失败原因 -> LLM 根据诊断更新 BT -> 进入下一轮迭代。
*   **算法流程**：算法 1 描述了这一过程：初始化 BT -> 捕获初始/中间/最终图像 -> Critic 生成反馈 -> 若触发警报则通知人工或记录 -> Actor 根据反馈和得分更新 BT。

### 3. 实验设计
*   **实验场景**：一个真实的移动操作机器人平台，在模拟仓库环境中执行物流任务。
*   **任务内容**：机器人需在多个装载区收集木块（需识别颜色和正反面），以 4 个为一组运送到卸货区，并移动一个货架。
*   **对比方法（Benchmark）**：
    1.  **无评论器（Baseline）**：仅依靠 BT 轨迹和最终得分进行更新。
    2.  **Qwen2.5-VL-7B**：直接作为评论器。
    3.  **Qwen2.5-VL-3B（微调版）**：在结构化执行反馈数据集上经过微调。
    4.  **Gemini-3-Pro-Preview**：作为多模态评论器。
    5.  **Gemini-3-Pro-Preview + 块信息**：消融实验，额外提供符号化的颜色/方向信息。
*   **评估指标**：任务得分（基于规则）、评论器准确率（问题检测率）、置信度、人工警报触发率。

### 4. 资源与算力
*   **算力说明**：文中提到对 **Qwen2.5-VL-3B** 进行了针对性微调，以使其生成的反馈更符合任务逻辑。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号、数量、微调时长或推理延迟等硬件细节。

### 5. 实验数量与充分性
*   **实验规模**：针对每种配置，在 **5 种不同的场地布局**下进行了实验，每种布局进行 **10 次训练迭代**（共计 50 次迭代/配置）。
*   **充分性评价**：实验设计较为充分，涵盖了从模型规模（3B vs 7B）、是否微调到额外感知信息输入（消融实验）的对比。通过真实机器人而非纯仿真进行验证，增强了结果的说服力。

### 6. 主要结论与发现
*   **领域对齐胜过模型规模**：微调后的 3B 模型比未微调的 7B 模型表现更稳定、得分更高，说明评论器与任务领域的对齐比参数量更重要。
*   **结构化反馈是关键**：没有评论器的基准线几乎无法学习，证明了数值奖励在处理复杂执行失败时信息量不足。
*   **感知是瓶颈**：引入额外的符号化感知信息（Block Info）显著提升了学习稳定性和最终得分，表明 VRL 的上限受限于 VLM 的视觉接地能力。
*   **协同进化**：Actor 会逐渐适应 Critic 的局限性（如在视觉不可靠时采取保守策略），形成一种“评论器感知”的规划模式。

### 7. 优点
*   **高可解释性**：所有的策略更新和失败诊断均以自然语言和行为树形式呈现，人类可直观理解机器人为何修改策略。
*   **无需模拟器或梯度**：直接在物理硬件上通过“言语”进行强化学习，避开了复杂的奖励函数设计和端到端训练的黑盒问题。
*   **闭环自适应**：能够有效区分“规划错误”和“执行随机误差”，并针对性地修复行为树逻辑。

### 8. 不足与局限
*   **时间窗口限制**：Critic 基于快照观察，难以处理具有长程依赖或延迟显现的故障（Latent faults）。
*   **感知依赖风险**：系统高度依赖 VLM 的视觉识别精度。若 Critic 误诊，Actor 可能会做出错误的策略调整（虽然文中提到 Actor 会参考得分来缓解此问题）。
*   **实时性挑战**：LLM 和 VLM 的推理开销可能限制其在需要极快响应的任务中的应用。

（完）
