---
title: Adaptive Robust Estimator for Multi-Agent Reinforcement Learning
title_zh: 多智能体强化学习的自适应鲁棒估计器
authors: "Zhongyi Li, Wan Tian, Jingyu Chen, Kangyao Huang, Huiming Zhang, Hui Yang, Tao Ren, Jinyang Jiang, Yijie Peng, Yikun Ban, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21574v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于大语言模型协作推理的鲁棒多智能体强化学习框架
tldr: 针对多智能体强化学习在协作推理中面临的信用分配困难及奖励噪声导致的训练不稳定问题，本文提出了一种鲁棒的多智能体强化学习框架。该框架包含DACR结构化推理流程，通过“回答-评判-重写”三阶段实现明确的贡献归因；同时引入ARE自适应鲁棒估计器，有效抵御重尾和噪声奖励对策略优化的干扰。实验证明，该方法在数学推理和具身智能任务中表现优异，显著提升了训练的稳定性和鲁棒性。
selection_source: fresh_fetch
motivation: 多智能体协作推理存在交互模糊导致的信用分配难题，且易受重尾和噪声奖励影响导致训练不稳定。
method: 提出了由DACR（双智能体回答-评判-重写）结构化流水线和ARE（自适应鲁棒估计器）组成的强化学习框架。
result: 在数学推理和具身智能基准测试中，该方法在存在奖励噪声的情况下仍能显著优于基准模型。
conclusion: 该框架通过结构化交互和鲁棒估计，有效解决了多智能体强化学习中的信用分配与噪声干扰问题，增强了训练稳定性。
---

## 摘要
多智能体协作已成为增强大语言模型推理能力的强大范式，但它面临着交互层面的模糊性，这种模糊性混淆了生成、批判和修正过程，导致智能体间的信用分配（credit assignment）变得困难。此外，在这种设定下的策略优化容易受到重尾和噪声奖励的影响，这可能会使优势估计产生偏差，并引发训练不稳定甚至发散。为了解决这两个问题，我们提出了一种用于协作推理的鲁棒多智能体强化学习框架，由两个组件组成：双智能体“回答-批判-重写”（DACR）和自适应鲁棒估计器（ARE）。DACR 将推理分解为结构化的三阶段流水线：回答、批判和重写，同时能够将每个智能体的边际贡献明确归因于其合作伙伴的性能。ARE 在多智能体策略优化过程中提供批次经验均值的鲁棒估计。在数学推理和具身智能基准测试中，即使在存在噪声奖励的情况下，我们的方法在同质和异质设定下均始终优于基准方法。这些结果表明，该方法对奖励噪声具有更强的鲁棒性，且训练动态更稳定，有效防止了由噪声奖励信号引起的优化失败。

## Abstract
Multi-agent collaboration has emerged as a powerful paradigm for enhancing the reasoning capabilities of large language models, yet it suffers from interaction-level ambiguity that blurs generation, critique, and revision, making credit assignment across agents difficult. Moreover, policy optimization in this setting is vulnerable to heavy-tailed and noisy rewards, which can bias advantage estimation and trigger unstable or even divergent training. To address both issues, we propose a robust multi-agent reinforcement learning framework for collaborative reasoning, consisting of two components: Dual-Agent Answer-Critique-Rewrite (DACR) and an Adaptive Robust Estimator (ARE). DACR decomposes reasoning into a structured three-stage pipeline: answer, critique, and rewrite, while enabling explicit attribution of each agent's marginal contribution to its partner's performance. ARE provides robust estimation of batch experience means during multi-agent policy optimization. Across mathematical reasoning and embodied intelligence benchmarks, even under noisy rewards, our method consistently outperforms the baseline in both homogeneous and heterogeneous settings. These results indicate stronger robustness to reward noise and more stable training dynamics, effectively preventing optimization failures caused by noisy reward signals.

---

## 论文详细总结（自动生成）

### 论文总结：多智能体强化学习的自适应鲁棒估计器 (ARE)

#### 1. 核心问题与研究动机
论文关注大语言模型（LLM）在多智能体强化学习（MARL）协作推理中的两大挑战：
*   **信用分配难题（Credit Assignment）：** 多智能体交互（如生成、批判、修正）过程往往边界模糊，难以量化单个智能体对最终结果的边际贡献。
*   **奖励信号的脆弱性：** 推理任务的奖励通常由验证器或奖励模型生成，具有重尾分布（Heavy-tailed）和高噪声特征。传统的策略优化方法（如 GRPO）依赖批次经验均值进行归一化，极易受异常值干扰，导致训练不稳定甚至发散。

#### 2. 方法论
论文提出了一个鲁棒的 MARL 框架，包含两个核心组件：
*   **DACR（双智能体回答-评判-重写）协议：**
    *   **流程：** 两个智能体先独立回答问题；随后交换答案并互相提供批判性建议；最后根据对方的建议重写自己的答案。
    *   **奖励设计：** 引入“跨阶段奖励”，智能体的总奖励 = 自身重写答案的得分 + 合作伙伴因其建议而产生的得分增量。这实现了明确的贡献归因。
*   **ARE（自适应鲁棒估计器）：**
    *   **核心思想：** 替代 GRPO 中脆弱的批次均值计算。它结合了“均值中位数”（Median-of-Means, MoM）思想和自适应损失函数。
    *   **技术细节：** 将样本分成多个块，在每个块内通过最小化 Barron 自适应鲁棒损失函数来估计位置参数。为了处理非凸优化，采用了**渐进非凸性（GNC）**和**迭代重权最小二乘法（IRLS）**。最后取各块估计值的中位数作为最终的鲁棒均值估计。

#### 3. 实验设计
*   **数学推理任务：**
    *   **数据集：** 在 MATH 7.5k 上训练；在 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外/泛化性测试）上评估。
    *   **对比方法：** 以标准的 GRPO 为基准（Base），对比了同质和异质智能体组合（如 Qwen2.5-1.5B/7B, Llama3.1-8B）。
*   **具身智能任务（视觉语言导航 VLN）：**
    *   **场景：** 无人机视觉导航，使用 FlightGPT 框架。
    *   **指标：** 导航误差（NE）、成功率（SR）、Oracle 成功率（OSR）、路径长度加权成功率（SPL）。
*   **鲁棒性测试：** 人为注入柯西分布（Cauchy）噪声来模拟极端奖励异常值。

#### 4. 资源与算力
*   **硬件：** 使用了单节点 **8 张 NVIDIA A800 GPU**。
*   **软件框架：** 使用 Ray 进行分布式训练编排，vLLM 作为推理引擎（4 个引擎，张量并行度为 2）。
*   **精度：** 采用 BF16 混合精度训练。
*   **时长：** 文中未明确给出具体的训练总时长，但提供了详细的超参数设置（如 Batch Size 为 32，最大 Prompt 长度 6144 等）。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 1.5B 到 8B 不同规模、不同家族的模型。
*   **消融实验：** 针对 DACR 交互协议（对比独立回答）和 ARE 估计器（对比标准均值）分别做了消融。
*   **噪声模拟：** 针对不同比例的组级污染和组内污染进行了压力测试。
*   **充分性评价：** 实验设计较为全面，不仅在纯文本推理上验证，还跨领域扩展到了具身智能任务，证明了方法的普适性。理论部分提供了详尽的收敛性和偏差界限证明，增强了实验结果的说服力。

#### 6. 主要结论与发现
*   **性能提升：** 在所有数学基准测试中，ARE 均优于 GRPO 基准，尤其在存在奖励噪声时，准确率提升显著（如 AMC23 上提升了 10%）。
*   **训练稳定性：** ARE 有效抑制了由异常奖励引起的梯度尖峰，使训练曲线更加平滑，防止了优化失败。
*   **协作价值：** DACR 的三阶段结构比简单的独立尝试更能激发模型的推理潜力，跨阶段奖励有效解决了多智能体间的信用分配。
*   **泛化能力：** 在具身智能任务中，ARE 在未见过的环境（Unseen environments）中表现出更强的定位和导航能力。

#### 7. 优点
*   **理论与实践结合：** 论文不仅提出了算法，还给出了严谨的统计学理论证明（如有限方差和重尾分布下的偏差界限）。
*   **强鲁棒性：** 专门针对 RLHF/RL 训练中常见的“奖励模型噪声”问题，具有很强的现实应用价值。
*   **结构化交互：** DACR 协议为多智能体协作提供了一个清晰、可解释的框架。

#### 8. 不足与局限
*   **计算开销：** ARE 涉及多次迭代优化（GNC/IRLS）和分块计算，相比简单的批次均值计算，会增加一定的训练计算负担。
*   **具身智能实验受限：** 在 VLN 任务中，由于算力限制，作者使用了较小的模型骨干，且未能在该任务上完全展开多智能体交互实验（仅验证了 ARE 组件）。
*   **超参数敏感性：** 尽管损失函数是自适应的，但分块数量 $k$ 和 GNC 的更新频率等参数可能仍需针对不同任务进行微调。

（完）
