---
title: Advantage-Guided Diffusion for Model-Based Reinforcement Learning
title_zh: 优势引导扩散用于基于模型的强化学习
authors: "Daniele Foffano, Arvid Eriksson, David Broman, Karl H. Johansson, Alexandre Proutiere"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09035v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 基于优势引导扩散的模型强化学习
tldr: 针对扩散世界模型在强化学习中存在的短视问题，本文提出优势引导扩散（AGD-MBRL）。该方法利用优势函数引导逆向扩散过程，使生成的轨迹向高长期回报区域集中。通过引入SAG和EAG两种引导机制，AGD在不改变训练目标的前提下，在MuJoCo任务中实现了比现有模型高出两倍的样本效率和回报，有效解决了短视决策难题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09035v1/fig-001.webp\", \"caption\": \"Fig. 2: Training curves for the Mujoco environments: HalfCheetah, Hopper, Walker and Reacher. Shaded areas indicate the standard error. The curves have been smoothed by a running average for better readability.\", \"page\": 7, \"index\": 1, \"width\": 1052, \"height\": 526}]"
motivation: 现有的扩散模型引导方式要么忽略了价值信息，要么因仅依赖短期奖励而导致在短时域内出现决策短视。
method: 提出AGD-MBRL，利用优势函数估计（SAG和EAG）引导逆向扩散过程，通过重采样机制实现策略改进。
result: 在MuJoCo基准测试中，该方法在样本效率和最终回报上显著优于PolyGRAD、奖励引导模型及PPO等无模型算法。
conclusion: 优势感知引导是克服扩散模型MBRL短视问题的有效手段，能显著提升生成轨迹的质量和智能体的性能。
---

## 摘要
采用自回归世界模型的基于模型的强化学习（MBRL）面临复合误差问题，而扩散世界模型通过联合生成轨迹段来缓解这一问题。然而，现有的扩散引导要么仅基于策略（丢弃了价值信息），要么基于奖励（在扩散时界较短时会变得短视）。我们提出了用于 MBRL 的优势引导扩散（AGD-MBRL），它利用智能体的优势估计来引导反向扩散过程，使采样集中在预期能产生超出生成窗口更高长期回报的轨迹上。我们开发了两种引导方式：(i) Sigmoid 优势引导 (SAG) 和 (ii) 指数优势引导 (EAG)。我们证明，通过 SAG 或 EAG 引导的扩散模型允许我们对轨迹进行重加权采样，其权重随状态-动作优势的增加而增加，这在标准假设下意味着策略改进。此外，我们表明，与无引导的扩散模型相比，AGD-MBRL 生成的轨迹遵循改进的策略（即具有更高的价值）。AGD 与 PolyGRAD 风格的架构无缝集成，通过引导状态分量同时保持动作生成受策略约束，且无需更改扩散训练目标。在 MuJoCo 控制任务（HalfCheetah、Hopper、Walker2D 和 Reacher）上，AGD-MBRL 在样本效率和最终回报方面优于 PolyGRAD、在线 Diffuser 风格的奖励引导以及无模型基准（PPO/TRPO），在某些情况下提升幅度达 2 倍。这些结果表明，优势感知引导是解决扩散模型 MBRL 中短时界短视问题的一种简单且有效的补救措施。

## Abstract
Model-based reinforcement learning (MBRL) with autoregressive world models suffers from compounding errors, whereas diffusion world models mitigate this by generating trajectory segments jointly. However, existing diffusion guides are either policy-only, discarding value information, or reward-based, which becomes myopic when the diffusion horizon is short. We introduce Advantage-Guided Diffusion for MBRL (AGD-MBRL), which steers the reverse diffusion process using the agent's advantage estimates so that sampling concentrates on trajectories expected to yield higher long-term return beyond the generated window. We develop two guides: (i) Sigmoid Advantage Guidance (SAG) and (ii) Exponential Advantage Guidance (EAG). We prove that a diffusion model guided through SAG or EAG allows us to perform reweighted sampling of trajectories with weights increasing in state-action advantage-implying policy improvement under standard assumptions. Additionally, we show that the trajectories generated from AGD-MBRL follow an improved policy (that is, with higher value) compared to an unguided diffusion model. AGD integrates seamlessly with PolyGRAD-style architectures by guiding the state components while leaving action generation policy-conditioned, and requires no change to the diffusion training objective. On MuJoCo control tasks (HalfCheetah, Hopper, Walker2D and Reacher), AGD-MBRL improves sample efficiency and final return over PolyGRAD, an online Diffuser-style reward guide, and model-free baselines (PPO/TRPO), in some cases by a margin of 2x. These results show that advantage-aware guidance is a simple, effective remedy for short-horizon myopia in diffusion-model MBRL.

---

## 论文详细总结（自动生成）

这篇论文提出了**优势引导扩散（Advantage-Guided Diffusion, AGD）**，旨在解决基于模型的强化学习（MBRL）中扩散世界模型的“短视”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景**：传统的自回归世界模型在生成长序列时存在**复合误差（Compounding Errors）**。扩散模型（Diffusion Models）通过联合生成整个轨迹段有效缓解了这一问题。
*   **核心问题**：现有的扩散引导机制存在缺陷：
    *   **仅基于策略的引导**（如 PolyGRAD）：忽略了价值信息，无法主动探索高回报区域。
    *   **基于奖励的引导**（如 Diffuser）：在扩散时界（Horizon）较短时会产生**短视（Myopia）**，因为它只关注生成窗口内的即时奖励，忽略了窗口之外的长期价值。
*   **研究动机**：利用强化学习中的**优势函数（Advantage Function）**来引导扩散过程，使生成的轨迹不仅符合当前策略，还能指向具有更高长期潜力的状态空间。

### 2. 方法论
AGD-MBRL 的核心思想是在扩散模型的反向去噪过程中，利用优势函数的梯度来“修正”生成的轨迹。
*   **核心技术细节**：
    *   **两种引导机制**：
        1.  **Sigmoid 优势引导 (SAG)**：将步骤优化的概率建模为优势函数的 Sigmoid 变换 $p(O_t=1|s_t, a_t) = \sigma(A(s_t, a_t))$。这是一种保守的加权方式，能抑制优势估计过高带来的风险。
        2.  **指数优势引导 (EAG)**：采用能量模型形式，权重正比于 $\exp(\sum A(s_t, a_t))$。这种方式更激进，能强烈引导采样流向高优势区域。
    *   **理论证明**：论文证明了使用 SAG 或 EAG 引导的采样等价于从一个**改进后的策略**中进行重采样。根据策略改进定理，这保证了生成数据的质量在理论上优于无引导模型。
    *   **算法流程**：在 Dyna 风格的框架下，智能体在真实环境收集数据更新扩散模型；在想象（Imagination）阶段，利用优势梯度引导生成轨迹，随后使用这些高质量轨迹通过 Actor-Critic 算法改进策略。

### 3. 实验设计
*   **实验场景**：使用了 MuJoCo 连续控制基准测试中的四个典型任务：**HalfCheetah、Hopper、Walker2D 和 Reacher**。
*   **对比基准（Benchmarks）**：
    *   **PolyGRAD**：最先进的基于策略引导的扩散 MBRL。
    *   **Online Diffuser**：作者将原始 Diffuser 适配为在线模式，使用累积奖励作为引导（代表短视的奖励引导）。
    *   **无模型基准**：PPO 和 TRPO（经典的深度强化学习算法）。
*   **实验设置**：所有方法均在 150 万（1.5M）环境交互步数下进行训练。

### 4. 资源与算力
*   **算力支持**：论文提到计算资源由瑞典国家超级计算基础设施（NAISS）提供。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或确切的训练时长。但提到扩散模型基于多层感知机（MLP），并使用 Adam 优化器，这暗示其计算开销主要集中在扩散采样的迭代过程中。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流环境上进行了对比，每个环境都展示了完整的训练曲线和最终回报均值/标准差。
*   **充分性评价**：
    *   **客观性**：对比了同类最强的扩散 MBRL（PolyGRAD）和经典的无模型算法，实验设计较为公正。
    *   **消融/变体对比**：同时测试了 SAG 和 EAG 两种变体，分析了它们在不同复杂度环境下的表现差异（如 EAG 在 HalfCheetah 表现更好，而 SAG 在 Walker2D 更稳健）。
    *   **局限性**：实验环境集中在 MuJoCo 任务，缺乏更复杂或高维（如像素级输入）的任务验证。

### 6. 主要结论与发现
*   **性能提升**：AGD-MBRL 在多数任务中显著优于 PolyGRAD 和奖励引导的 Diffuser，在 HalfCheetah 任务中性能提升甚至达到 2 倍。
*   **解决短视**：实验证明，相比于仅看短期奖励的引导，优势引导能有效利用长期价值信息，使智能体在短扩散时界下也能做出更优决策。
*   **稳定性**：优势引导（尤其是 SAG）在训练过程中表现出更好的稳定性，减少了性能回退的频率。

### 7. 优点
*   **理论完备**：提供了关于策略改进的数学证明，使引导机制不仅是启发式的，而且具有理论支撑。
*   **即插即用**：AGD 能够无缝集成到现有的扩散 MBRL 架构（如 PolyGRAD）中，只需修改几行代码来计算优势梯度，无需改变扩散模型的训练目标。
*   **效率高**：通过引导采样，显著提升了 MBRL 的样本效率。

### 8. 不足与局限
*   **计算开销**：扩散模型固有的多步迭代采样过程导致其推理速度慢，这在实时性要求高的场景中是限制因素。
*   **依赖优势估计**：引导的效果高度依赖于 RL 智能体学习到的优势函数的准确性。如果优势函数估计偏差较大，可能会引导模型生成错误的轨迹。
*   **环境覆盖面**：虽然在 MuJoCo 上表现优异，但尚未在稀疏奖励或非平稳环境中验证其鲁棒性。

（完）
