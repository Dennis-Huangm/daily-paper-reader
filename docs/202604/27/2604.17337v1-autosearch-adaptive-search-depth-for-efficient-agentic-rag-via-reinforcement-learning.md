---
title: "AutoSearch: Adaptive Search Depth for Efficient Agentic RAG via Reinforcement Learning"
title_zh: AutoSearch：通过强化学习实现高效智能体 RAG 的自适应搜索深度
authors: "Jingbo Sun, Wenyue Chong, Songjun Tu, Qichao Zhang, Yaocheng Zhang, Jiajun Chai, Xiaohan Wang, Wei Lin, Guojun Yin, Dongbin Zhao"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17337v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于高效智能体RAG系统的强化学习
tldr: 针对代理式检索增强生成（Agentic RAG）中多步搜索导致的冗余计算和延迟问题，本文提出了AutoSearch框架。该框架利用强化学习动态调整搜索深度，通过自生成中间答案机制识别最小充分搜索深度。AutoSearch在奖励达成目标的同时惩罚过度搜索，从而在保证复杂问题回答质量的前提下显著提升效率，实现了更优的准确率与效率权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17337v1/fig-001.webp\", \"caption\": \"\", \"page\": 5, \"index\": 1, \"width\": 898, \"height\": 904}]"
motivation: 现有的代理式RAG系统在多步搜索中存在冗余步骤导致的计算成本高昂，且固定搜索深度难以兼顾简单与复杂问题的需求。
method: 提出AutoSearch强化学习框架，通过自回答机制评估每步搜索的必要性，并利用奖励机制优化搜索深度与答案质量。
result: 在多个基准测试上的实验表明，AutoSearch能有效减少过度搜索，并在保持甚至提升回答准确率的同时降低延迟。
conclusion: AutoSearch通过自适应搜索深度策略，成功解决了Agentic RAG在效率与准确性之间的矛盾，为高效智能体设计提供了新思路。
---

## 摘要
智能体检索增强生成（Agentic RAG）系统使大语言模型（LLM）能够通过与外部检索工具的多步交互来解决复杂任务。然而，这种多步交互通常涉及冗余的搜索步骤，从而产生巨大的计算成本和延迟。先前的工作通过限制搜索深度（即搜索步数）来降低成本，但这往往导致对复杂问题的探索不足。为了解决这一问题，我们首先研究了搜索深度如何影响准确性，并发现了一个定义准确率与效率权衡的最小充分搜索深度，该深度由问题复杂度和智能体能力共同决定。此外，我们提出了 AutoSearch，这是一个强化学习（RL）框架，通过自生成的中间答案来评估每个搜索步骤。通过自回答机制，AutoSearch 识别出最小充分搜索深度，并通过奖励达到该深度同时惩罚过度搜索来促进高效搜索。此外，还引入了奖励机制以稳定搜索行为并提高复杂问题的回答质量。在多个基准测试上的广泛实验表明，AutoSearch 实现了卓越的准确率与效率权衡，在保持搜索质量的同时缓解了过度搜索。

## Abstract
Agentic retrieval-augmented generation (RAG) systems enable large language models (LLMs) to solve complex tasks through multi-step interaction with external retrieval tools. However, such multi-step interaction often involves redundant search steps, incurring substantial computational cost and latency. Prior work limits search depth (i.e., the number of search steps) to reduce cost, but this often leads to underexploration of complex questions. To address this, we first investigate how search depth affects accuracy and find a minimal sufficient search depth that defines an accuracy-efficiency trade-off, jointly determined by question complexity and the agent's capability. Furthermore, we propose AutoSearch, a reinforcement learning (RL) framework that evaluates each search step via self-generated intermediate answers. By a self-answering mechanism, AutoSearch identifies the minimal sufficient search depth and promotes efficient search by rewarding its attainment while penalizing over-searching. In addition, reward mechanisms are introduced to stabilize search behavior and improve answer quality on complex questions. Extensive experiments on multiple benchmarks show that AutoSearch achieves a superior accuracy-efficiency trade-off, alleviating over-searching while preserving search quality.

---

## 论文详细总结（自动生成）

这是一份关于论文《AutoSearch: Adaptive Search Depth for Efficient Agentic RAG via Reinforcement Learning》的结构化深度分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：在代理式检索增强生成（Agentic RAG）系统中，如何平衡**搜索深度（效率）**与**回答准确率**。
*   **研究动机**：
    *   **冗余开销**：现有的多步 RAG 系统往往存在“过度搜索”现象，导致不必要的计算成本和高延迟。
    *   **固定深度的局限**：简单限制搜索步数会使复杂问题探索不足，而固定多步搜索对简单问题又是浪费。
    *   **动态需求**：最优搜索深度（最小充分深度）并非固定，而是由**问题复杂度**和**智能体自身能力**共同决定的。

### 2. 方法论：AutoSearch 强化学习框架
AutoSearch 的核心思想是利用智能体在搜索过程中的“自评价”信号来动态调整搜索行为。

*   **核心机制：自回答驱动的评估**
    *   在每一个检索步骤 $t$，智能体根据当前积累的轨迹生成一个**中间答案**（Intermediate Answer）。
    *   **能力感知最优深度 ($t_c$)**：定义为智能体能够首次给出正确中间答案的最小步数。
*   **奖励函数设计 ($R_{overall}$)**：
    1.  **基础奖励 (Base Reward)**：包括格式奖励（确保输出符合规范）和结果奖励（最终答案是否正确）。
    2.  **搜索效率奖励 (Search Efficiency Reward)**：
        *   **有效搜索**：对达到 $t_c$ 之前的步骤给予正奖励。
        *   **过度搜索**：对超过 $t_c$ 之后的冗余步骤给予负惩罚。
        *   **搜索不足**：若最终未答对，给予微小正奖励鼓励继续探索。
    3.  **搜索质量奖励 (Search Quality Reward)**：计算当前步中间答案相对于前一步在 F1 分数上的**边际增益**，强化能提供有效信息的搜索行为。
*   **训练流程**：采用 PPO（近端策略优化）算法进行训练，同时验证了 GRPO（群体相对策略优化）的适用性。

### 3. 实验设计
*   **数据集**：
    *   **通用 QA（单跳）**：Natural Questions (NQ), TriviaQA, PopQA。
    *   **多跳 QA（复杂）**：HotpotQA, 2WikiMultiHopQA, Bamboogle。
*   **基准方法 (Baselines)**：
    *   **Search-R1**：交替进行推理与搜索的 RL 框架。
    *   **StepSearch**：引入信息增益步进奖励的方法。
    *   **HiPRAG**：利用外部大模型判断搜索必要性的分层奖励方法。
*   **评估指标**：准确率（EM/F1）、搜索深度（SD）、搜索效率（SE = EM/SD）、过度搜索比例（OSR）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H20 GPU**。
*   **模型规模**：基于 Qwen2.5-3B-Base 和 Qwen2.5-7B-Base 进行微调。
*   **训练细节**：总计 1005 个优化步，Actor 学习率 $1 \times 10^{-6}$，Critic 学习率 $1 \times 10^{-5}$，Batch Size 为 512。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流数据集上进行了全面测试，涵盖了从简单到极具挑战性的多跳推理场景。
*   **消融实验**：系统地移除了基础奖励、效率奖励和质量奖励，验证了各组件对平衡准确率和深度的必要性。
*   **分析维度**：除了最终性能，还深入分析了训练动态（搜索时间、Token 长度变化、动作合法率），实验设计客观且对比公平（独立复现了所有基准方法）。

### 6. 主要结论与发现
*   **自适应性**：AutoSearch 能够根据任务难度自动调整：在单跳任务上保持极浅的搜索（约 1 步），在多跳任务上自动增加深度。
*   **效率提升**：相比 Search-R1 和 StepSearch，AutoSearch 在保持同等或更高准确率的前提下，显著降低了搜索深度和过度搜索比例（OSR）。
*   **性能优越**：在 3B 和 7B 模型上均实现了最佳的“准确率-效率”权衡曲线（Pareto Front）。
*   **算法通用性**：发现该框架在 PPO 和 GRPO 下均表现良好，证明了奖励设计的鲁棒性。

### 7. 优点与亮点
*   **无需外部监督**：不同于 HiPRAG 需要昂贵的外部模型来判断搜索必要性，AutoSearch 完全依赖智能体的中间答案进行自进化，成本更低。
*   **细粒度信用分配**：通过中间答案的 F1 增益，将奖励精确分配到每一个有贡献的搜索步骤，解决了 RL 在长轨迹任务中奖励稀疏的问题。
*   **稳定性**：引入基础奖励和格式规范，解决了 Agent 在多步交互中容易出现的格式崩溃问题。

### 8. 不足与局限
*   **搜索深度上限**：实验主要集中在最大 4 步的搜索深度内，对于需要极长推理链（如 10 步以上）的超复杂任务，其自适应表现尚待验证。
*   **中间答案开销**：虽然减少了搜索步数，但在训练阶段生成中间答案会增加额外的计算量（尽管推理阶段不需要全部生成）。
*   **检索器依赖**：性能仍受限于底层检索算法（如 E5）的质量，若检索器返回的全是噪声，自评价机制可能会失效。

（完）
