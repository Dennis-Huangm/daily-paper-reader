---
title: "Combee: Scaling Prompt Learning for Self-Improving Language Model Agents"
title_zh: Combee：为自我改进的语言模型智能体扩展提示学习
authors: "Hanchen Li, Runyuan He, Qizheng Zhang, Changxiu Ji, Qiuyang Mang, Xiaokun Chen, Lakshya A Agrawal, Wei-Liang Liao, Eric Yang, Alvin Cheung, James Zou, Kunle Olukotun, Ion Stoica, Joseph E. Gonzalez"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04247v1"
tags: ["query:ag-rl"]
score: 8.0
evidence: 为自我改进智能体扩展提示学习
tldr: 针对现有提示词学习方法在处理大规模智能体轨迹时效率低且高并行下质量下降的问题，本文提出Combee框架。Combee通过并行扫描、增强洗牌机制及动态批大小控制器，实现了自我改进智能体的高效并行提示词学习。在AppWorld等多个基准测试中，Combee在保持或提升准确率的同时，实现了高达17倍的加速，显著提升了从海量轨迹中学习的效率。
selection_source: fresh_fetch
motivation: 现有的提示词学习方法主要针对单智能体或低并行场景，难以在保持质量的同时高效处理大规模并行运行产生的海量轨迹。
method: Combee框架引入了并行扫描、增强洗牌机制以及动态批大小控制器，以平衡并行学习过程中的效率、延迟与模型质量。
result: 实验表明，Combee在AppWorld等四个基准测试中实现了高达17倍的加速，且在准确率和成本上均优于或等同于现有方法。
conclusion: Combee为自我改进型智能体提供了一种高效且可扩展的并行提示词学习方案，有效解决了大规模轨迹学习中的性能瓶颈。
---

## 摘要
提示学习的最新进展使大语言模型智能体能够从推理时的上下文中获取任务相关知识，而无需更改参数。例如，现有方法（如 ACE 或 GEPA）可以基于智能体之前的运行记录来学习系统提示，从而提高准确率。然而，这些方法主要侧重于单智能体或低并行度设置，这从根本上限制了它们从大量收集的智能体轨迹（agentic traces）中高效学习的能力。并行运行提示学习以适应从大量智能体轨迹或并行智能体执行中学习的日益增长的趋势，将是高效且有益的。然而，如果没有原则性的扩展策略，现有方法在高并行度下会出现质量下降。为了同时提高提示学习的效率和质量，我们提出了 Combee，这是一个为自我改进智能体扩展并行提示学习的新型框架。Combee 加速了学习过程，并支持在并行运行多个智能体的同时从其聚合轨迹中学习，且不会造成质量下降。为实现这一目标，Combee 利用了并行扫描（parallel scans）并采用了增强的洗牌机制（augmented shuffle mechanism）；Combee 还引入了动态批大小控制器，以平衡质量和延迟。在 AppWorld、Terminal-Bench、Formula 和 FiNER 上的评估表明，Combee 在准确率相当或更高且成本相同的情况下，比之前的方法实现了高达 17 倍的加速。

## Abstract
Recent advances in prompt learning allow large language model agents to acquire task-relevant knowledge from inference-time context without parameter changes. For example, existing methods (like ACE or GEPA) can learn system prompts to improve accuracy based on previous agent runs. However, these methods primarily focus on single-agent or low-parallelism settings. This fundamentally limits their ability to efficiently learn from a large set of collected agentic traces. It would be efficient and beneficial to run prompt learning in parallel to accommodate the growing trend of learning from many agentic traces or parallel agent executions. Yet without a principled strategy for scaling, current methods suffer from quality degradation with high parallelism. To improve both the efficiency and quality of prompt learning, we propose Combee, a novel framework to scale parallel prompt learning for self-improving agents. Combee speeds up learning and enables running many agents in parallel while learning from their aggregate traces without quality degradation. To achieve this, Combee leverages parallel scans and employs an augmented shuffle mechanism; Combee also introduces a dynamic batch size controller to balance quality and delay. Evaluations on AppWorld, Terminal-Bench, Formula, and FiNER demonstrate that Combee achieves up to 17x speedup over previous methods with comparable or better accuracy and equivalent cost.

---

## 论文详细总结（自动生成）

### 论文总结：Combee —— 为自我改进的语言模型智能体扩展提示学习

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：提示学习（Prompt Learning）允许大语言模型（LLM）智能体通过“生成-反射-更新”循环，从推理时的交互轨迹中提取知识并更新系统提示（如 Playbook），而无需调整参数。
*   **核心问题**：现有的提示学习方法（如 ACE、GEPA）主要针对单智能体或低并行场景。当尝试通过增加批大小（Batch Size）进行并行扩展时，会出现**“上下文过载”（Context Overload）**现象。
*   **过载后果**：即使所有信息都在上下文窗口内，负责聚合的 LLM 也会进行有损压缩，丢弃高价值的特定细节，仅保留通用的模糊模式，导致智能体性能大幅下降（例如在 Formula 数据集上，Batch 100 的准确率比 Batch 1 下降了 14.5%）。

#### 2. 方法论：核心思想与关键技术
Combee 采用了 **Map-Shuffle-Reduce** 范式，核心组件包括：
*   **并行扫描聚合（Parallel Scan Aggregation）**：借鉴并行计算中的前缀和思想，将 $n$ 个轨迹的反射分成 $k$ 个子组（默认 $k = \lfloor\sqrt{n}\rfloor$）。先在子组内聚合，再进行层级合并，避免单次输入过多反射导致 LLM 聚合器过载。
*   **增强洗牌机制（Augmented Shuffling）**：在聚合前将每个反射复制 $p$ 次（默认 $p=2$）并随机打乱。这增加了关键洞察被聚合器采纳的机会，提高了学习的鲁棒性。
*   **动态批大小控制器（Dynamic Batch Size Controller）**：通过试运行拟合幂律延迟曲线（$T_{epoch} = A \cdot bs^{-\alpha}$），自动选择边际收益最高的批大小，在训练速度和潜在质量风险之间取得平衡。

#### 3. 实验设计
*   **数据集/场景**：
    *   **智能体基准**：AppWorld（多步 API 任务）、Terminal-Bench 2.0（命令行软件工程任务）。
    *   **领域特定任务**：FiNER（金融实体识别）、Formula（金融数值推理）。
*   **对比方法**：
    *   基础模型（Base LLM，无提示学习）。
    *   ACE 和 GEPA 的原始顺序版本（Batch 1）。
    *   朴素并行扩展（Naive Scaling，不同 Batch Size）。
    *   Top-K 检索聚合、摘要聚合（Summarization）。
*   **评估指标**：任务成功率（TGC/SGC）、准确率（Accuracy@1）、训练时间、训练成本。

#### 4. 资源与算力
*   **模型使用**：主要实验基于 **DeepSeek-V3.1**（通过 Together AI 接口），部分鲁棒性实验使用了 **GPT-OSS 120B**。
*   **算力说明**：论文未明确列出具体的 GPU 硬件型号和数量，但详细记录了**训练时长**（如 AppWorld 从 86 分钟缩短至 7 分钟）和**经济成本**（如 AppWorld 训练约 1.40 - 1.68 美元）。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同类型的基准测试上进行了广泛实验，涵盖了从 API 调用到复杂数值推理的多种场景。
*   **消融实验**：针对动态控制器、增强洗牌机制、子组大小（Subgroup Size）等关键变量进行了消融研究。
*   **公平性与客观性**：对比了多种基准方法，并在不同模型家族（DeepSeek 和 GPT-OSS）上验证了结论的普适性。实验结果通过多次运行取平均值，具有较好的客观性。

#### 6. 主要结论与发现
*   **显著加速**：Combee 在保持或提升准确率的前提下，相比顺序学习实现了 **12x 至 17x** 的训练加速。
*   **解决过载**：通过分层聚合，Combee 能够保留比朴素并行方法多得多的有效信息（例如 Playbook 长度从 526 token 恢复至 6887 token）。
*   **性能提升**：在 AppWorld 等任务中，Combee 的最终准确率甚至超过了顺序学习版本，证明了并行聚合可能带来的协同效应。
*   **成本等效**：在大幅缩短时间的同时，Combee 的 API 调用成本与原始方法基本持平。

#### 7. 优点（亮点）
*   **框架无关性**：Combee 可以无缝集成到 ACE、GEPA 等多种现有的提示学习框架中。
*   **系统化解决过载**：首次系统性地识别并解决了提示学习中的“上下文过载”问题。
*   **动态自适应**：引入动态批大小控制器，减少了人工调参的负担。

#### 8. 不足与局限
*   **同步限制**：目前的并行设计是同步的，在异构计算环境下可能会受到最慢节点的限制（长尾效应）。
*   **超参数依赖**：虽然有动态控制器，但其阈值 $\tau$ 和增强洗牌的复制次数 $p$ 可能仍需针对特定极端任务进行微调。
*   **应用范围**：主要验证了文本形式的 Playbook 和提示词更新，对于更复杂的代码库或大规模检索增强（RAG）知识库的并行更新效果尚待验证。

（完）
