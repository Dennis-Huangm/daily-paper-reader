---
title: "Agentic Open RAN: A Deterministic and Auditable Framework for Intent-Driven Radio Control"
title_zh: 代理式 Open RAN：一种用于意图驱动无线控制的确定性且可审计的框架
authors: "Hengxu Li, Dongkuan Xu, Mingzhe Chen, Yuchen Liu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13384v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 使用大模型和自主智能体的意图驱动无线电控制代理框架
tldr: 本研究提出 A1gent，一个针对 Open RAN 的确定性且可审计的代理控制框架。该框架利用大语言模型将运营商的自然语言意图转化为 A1 策略，通过解耦非实时推理与近实时执行，实现了高效的无线电控制。A1gent 包含负责策略生成的 rApp 和负责移动性、负载及能源管理的 xApp，并引入了冲突治理机制和无需训练的自适应策略微调器，确保了网络控制的可验证性和自治理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13384v1/fig-001.webp\", \"caption\": \"Fig. 4: Affected UE timeline across phases.\", \"page\": 6, \"index\": 1, \"width\": 526, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.13384v1/fig-002.webp\", \"caption\": \"Fig. 5: Capability radar summarizing A1GENT performance.\", \"page\": 6, \"index\": 2, \"width\": 454, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.13384v1/fig-003.webp\", \"caption\": \"Fig. 1: The agentic control loop consists of rApp→A1→xApps, xApps→E2, and SMO/Non-RT→O1. Telemetry (E2–KPM, O1 PM/FM) flows up to the data layer. The non-RT agentic rApp emits typed A1 policy instances to three control components: 1) QoE (E2SM–MHO), 2) Load (E2SM–RC+MHO), and 3) Energy (O1 via the SMO). The dispatcher routes operator intents, while the near-RT RIC executes E2-based control actions and the SMO’s O1 adapter carries out management actions.\", \"page\": 2, \"index\": 3, \"width\": 1065, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.13384v1/fig-004.webp\", \"caption\": \"Fig. 2: The implementation of agentic control stack with LLM and IaC. The LLM side performs intent→A1 translation, Level1 policy selection, and Level-2 objective blending. The IaC side enforces a policy catalog, guardrails, clamps, budgets, and cadence, producing typed A1 policy instances for QoE/Load/Energy xApps. A1 carries policy, E2 carries near-RT control, and O1 (via the SMO) carries slow management knobs.\", \"page\": 3, \"index\": 4, \"width\": 1064, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.13384v1/fig-005.webp\", \"caption\": \"TABLE I: Defined A1 policy fields (i.e. what travels on A1), with lineage, consumer, and actuator plane.\", \"page\": 4, \"index\": 5, \"width\": 1062, \"height\": 467}, {\"url\": \"assets/figures/arxiv/2604.13384v1/fig-006.webp\", \"caption\": \"Fig. 3: Emergency phase percentile performance.\", \"page\": 5, \"index\": 6, \"width\": 529, \"height\": 338}]"
motivation: 旨在解决 Open RAN 中复杂意图驱动控制的难题，通过大语言模型简化操作并确保控制过程的确定性与可审计性。
method: 提出了 A1gent 架构，将推理（非实时 rApp）与执行（近实时 xApp）分离，并结合护栏机制、动作合并器及基于 KPI 记忆的自适应策略微调。
result: 实现了跨 RIC 层级的可审计协调，通过确定性执行循环和冲突治理，确保了网络在不同任务场景下的稳定运行。
conclusion: A1gent 为 Open RAN 迈向可验证、自治理且可重复的代理智能提供了切实可行的技术路径。
---

## 摘要
大语言模型 (LLM) 为 Open RAN 中的代理式控制开启了新的可能性，允许运营商以自然语言表达意图，同时将底层执行委托给自主智能体。我们提出了 A1gent，这是一个将推理与实时执行解耦的代理式 RAN 控制栈。一个非实时 (non-RT) 代理式 rApp 将运营商目标编译为有类型的 A1 策略实例，三个面向任务的近实时 (near-RT) 代理式 xApp 通过具有平面范围执行能力的确定性循环来强制执行这些策略：E2 接口用于移动性和负载引导，O1 接口用于能源编排。这种代理式推理-执行分离确保了无线智能控制器 (RIC) 层级之间可审计的协调，并由编码的护栏和用于冲突治理的固定优先级动作合并器提供支持。随后，一个免训练的自适应策略微调器利用 KPI 记忆在无需重新训练的情况下优化有界参数，从而维持可预测的自适应。通过将意图驱动的规划与确定性的近实时执行相结合，A1gent 推动 Open RAN 向可验证、自治且可复现的代理式智能迈进。

## Abstract
Large language models (LLMs) open new possibilities for agentic control in Open RAN, allowing operators to express intents in natural language while delegating low-level execution to autonomous agents. We present A1gent, an agentic RAN control stack that decouples reasoning from real-time actuation. A non-RT agentic rApp compiles operator goals into typed A1 policy instances, and three task-oriented near-RT agentic xApps enforce them through a deterministic loop with plane-scoped actuation - E2 for mobility and load steering, and O1 for energy orchestration. This agentic reasoning-execution split ensures auditable coordination between RAN intelligent controller (RIC) tiers, supported by encoded guardrails and a fixed-priority action merger for conflict governance. A training-free adaptive policy tuner then refines bounded parameters using KPI memory without retraining, sustaining predictable adaptation. By integrating intent-driven planning with deterministic near-RT execution, A1gent advances Open RAN toward verifiable, self-governing, and reproducible agentic intelligence.

---

## 论文详细总结（自动生成）

以下是对论文《Agentic Open RAN: A Deterministic and Auditable Framework for Intent-Driven Radio Control》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在 Open RAN 架构中引入大语言模型（LLM）的推理能力，同时不破坏近实时（near-RT）控制循环的**确定性**、**安全性**和**可审计性**。
*   **研究动机**：
    *   运营商希望通过自然语言表达意图（Intent），而非微观管理每一个控制参数。
    *   现有的 LLM 驱动方案往往存在非确定性风险，难以满足毫秒级控制的稳定性要求。
    *   缺乏一种能够跨非实时（Non-RT）和近实时（Near-RT）层级进行协调、且具备安全护栏的统一框架。

### 2. 方法论
论文提出了 **A1GENT** 架构，其核心思想是**“推理与执行解耦”**：
*   **非实时代理式 rApp（推理层）**：
    *   作为“策略编译器”，利用 LLM 将运营商的自然语言意图转化为有类型的 A1 策略实例。
    *   **两级推理**：L1 负责选择策略类型（如 QoE 优先或节能优先）；L2 负责将意图转化为具体的数值参数（如切换阈值）。
    *   **IaC（基础设施即代码）护栏**：通过预定义的 IaC 规则强制约束 LLM 生成的参数范围，确保其在安全包络内。
*   **近实时代理式 xApp（执行层）**：
    *   包含三个任务导向的智能体：QoE 消防员（负责切换）、负载均衡器（负责资源分配）、节能器（负责基站休眠/唤醒）。
    *   **确定性执行**：xApp 不直接运行 LLM，而是根据 rApp 下发的 A1 策略执行确定性的控制算法。即使 rApp 响应延迟，xApp 仍能基于最后一次有效策略运行。
*   **关键技术细节**：
    *   **冲突治理**：采用固定优先级（节能 > QoE > 负载）的动作合并器，解决不同智能体间的指令冲突。
    *   **自适应策略微调器（APT）**：一种免训练模块，利用 KPI 记忆和“动作-结果”日志，在秒/分钟级尺度上微调 A1 策略参数，无需重新训练模型。
    *   **多接口协同**：通过 E2 接口处理移动性和负载，通过 O1 接口处理能源编排。

### 3. 实验设计
*   **实验场景**：模拟城市环境下的突发流量（Traffic Surge），分为三个阶段：常规（Normal）、紧急（Emergency）和恢复（Recovery）。
*   **仿真平台**：使用扩展的 `ns3-oran` 模拟器。
*   **网络配置**：9 个小区（3 站 3 扇区）的宏站网格，20 个具有不同移动性模型（随机方向和车载模型）的用户（UE）。
*   **Benchmark（基准方法）**：基于标准事件（A2/A4 RSRQ）的传统切换控制方案。
*   **LLM 配置**：使用 Gemini 2.5 Flash 作为推理引擎。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件算力（如 GPU 型号、数量或训练时长）。
*   **原因分析**：由于该框架的核心组件 APT 是“免训练”的，且推理部分主要依赖于 LLM API（Gemini），因此不涉及大规模的本地模型训练算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从正常运行到流量激增再到网络恢复的全生命周期测试。
*   **充分性评价**：
    *   实验设计较为**充分**，通过对下行吞吐量、SINR、负载分布、切换稳定性（驻留时间）等多个维度的量化分析，验证了框架的有效性。
    *   **客观性**：对比了传统基准方案，并展示了在紧急情况下为了公平性而进行的性能权衡（如压缩高带宽用户的吞吐量以保护边缘用户）。
    *   **局限性**：实验主要在仿真环境（ns-3）中进行，缺乏真实物理硬件（Hardware-in-the-loop）的验证。

### 6. 主要结论与发现
*   **性能提升**：在流量激增阶段，A1GENT 将下行吞吐量的底部（p10）提升了 20% 和 9%；在恢复阶段实现了 24% 的增益。
*   **负载均衡**：成功将热点区域的负载份额从 17% 优化至 44%，实现了更均匀的资源分布。
*   **稳定性**：通过 IaC 护栏和确定性循环，网络在整个过程中保持了上行稳定性，且切换抖动（Ping-pong）显著减少（紧急阶段 90% 驻留时间从 43s 提升至 53s）。
*   **意图达成**：证明了 LLM 可以有效地将抽象的运营商意图转化为可执行、可审计的底层策略。

### 7. 优点
*   **架构优雅**：通过推理与执行的解耦，既利用了 LLM 的灵活性，又保留了电信级控制所需的确定性。
*   **可审计性强**：所有 LLM 生成的策略都经过类型检查和 IaC 验证，且有完整的日志记录，方便回溯和复现。
*   **免训练自适应**：APT 模块无需昂贵的重训练过程，通过 KPI 记忆即可实现闭环优化，降低了部署门槛。
*   **冲突解决机制**：明确的优先级划分解决了多目标优化中的常见冲突问题。

### 8. 不足与局限
*   **仿真依赖**：结论完全基于 ns-3 仿真，真实网络中的多径效应、干扰复杂性和处理延迟可能对结果产生影响。
*   **LLM 延迟风险**：虽然架构上做了异步解耦，但如果 LLM 推理时间过长或 API 不可用，策略更新的及时性会受限。
*   **规模扩展性**：实验仅在 9 个小区的规模下进行，在超大规模城市网络中的计算开销和协调复杂度尚待验证。
*   **成本考量**：频繁调用商用 LLM API 可能会产生较高的运营成本。

（完）
