---
title: "SWE-Shepherd: Advancing PRMs for Reinforcing Code Agents"
title_zh: SWE-Shepherd：推进过程奖励模型以增强代码智能体
authors: "Mahir Labib Dihan, Md Ashrafur Rahman Khan"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10493v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 使用过程奖励模型（PRM）增强基于大语言模型的代码智能体
tldr: 本文提出SWE-Shepherd框架，旨在解决代码智能体在复杂软件工程任务中缺乏中间反馈的问题。通过引入过程奖励模型（PRM）为每步动作提供密集监督，并在SWE-Bench轨迹上训练轻量级模型。实验表明，该方法显著提升了智能体的交互效率和决策质量，为强化代码智能体提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10493v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the SWE-Shepherd framework. The framework consists of six stages. (1) Training tasks are collected from SWE-Bench. (2) An LLM-based agent tries to solve the tasks by interacting with the codebase and generate solution trajectories composed of intermediate reasoning steps and actions. (3) Each intermediate step is assigned a scalar reward reflecting its contribution toward solving the task. (4) A dataset is constructed where the input text (problem description, execution history, and current action) is paired with the computed reward labels. (5) A Process Reward Model (PRM) is trained by feeding text representations from the LLM into an MLP head and optimizing with mean squared error (MSE) loss to predict step-level rewards. (6) During inference, the trained PRM guides reward-aware search, prioritizing higher-quality intermediate steps to improve problem-solving performance.\", \"page\": 1, \"index\": 1, \"width\": 499, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.10493v1/fig-002.webp\", \"caption\": \"Table 1: Performance on SWE-Bench Verified using gpt-5-mini (100 tasks, max 30 steps).\", \"page\": 2, \"index\": 2, \"width\": 506, \"height\": 405}]"
motivation: 现有代码智能体缺乏对中间决策的细粒度反馈，导致在处理长程软件工程任务时效率低下且容易产生错误累积。
method: 利用SWE-Bench轨迹构建动作级奖励数据集，训练轻量级PRM在推理时评估并引导智能体选择高奖励动作。
result: 在SWE-Bench Verified测试中，该框架显著提高了智能体的交互效率和动作质量。
conclusion: 过程奖励模型能有效优化代码智能体的决策路径，但实现中间奖励与最终任务目标的完美对齐仍具挑战性。
---

## 摘要
由于需要在大型且不断演进的代码库上进行长程推理，并在相互依赖的操作之间做出一致的决策，自动化现实世界的软件工程任务对于基于大语言模型（LLM）的智能体来说仍然具有挑战性。现有方法通常依赖于静态提示策略或手工设计的启发式方法来选择代码编辑、文件导航和测试执行等操作，但它们缺乏对中间决策的细粒度反馈。这导致了低效的探索、错误传播以及脆弱的解决方案轨迹。为了解决这一局限性，我们提出了 SWE-Shepherd，这是一个引入过程奖励模型（PRMs）的框架，旨在为仓库级代码智能体提供密集的步骤级监督。利用来自 SWE-Bench 的轨迹，我们构建了一个操作级奖励数据集，并在基础 LLM 上训练了一个轻量级奖励模型，以评估中间操作的效用。在推理过程中，PRM 评估候选操作，并在无需完整强化学习的情况下引导智能体做出更高奖励的决策。在 SWE-Bench Verified 上的实验证明了交互效率和操作质量的提升，同时也揭示了将中间奖励与最终任务成功对齐所面临的挑战。

## Abstract
Automating real-world software engineering tasks remains challenging for large language model (LLM)-based agents due to the need for long-horizon reasoning over large, evolving codebases and making consistent decisions across interdependent actions. Existing approaches typically rely on static prompting strategies or handcrafted heuristics to select actions such as code editing, file navigation, and test execution, but they lack fine-grained feedback on intermediate decisions. This leads to inefficient exploration, error propagation, and brittle solution trajectories. To address this limitation, we propose SWE-Shepherd, a framework that introduces Process Reward Models (PRMs) to provide dense, step-level supervision for repository-level code agents. Using trajectories from SWE-Bench, we construct an action-level reward dataset and train a lightweight reward model on a base LLM to estimate the usefulness of intermediate actions. During inference, the PRM evaluates candidate actions and guides the agent toward higher-reward decisions without requiring full reinforcement learning. Experiments on SWE-Bench Verified demonstrate improved interaction efficiency and action quality, while also highlighting challenges in aligning intermediate rewards with final task success.

---

## 论文详细总结（自动生成）

这是一份关于论文《SWE-Shepherd: Advancing PRMs for Reinforcing Code Agents》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：自动化处理现实世界的软件工程任务（如修复 Bug、代码修改）对 LLM 智能体极具挑战，因为这需要长程推理、对大规模代码库的理解以及在相互依赖的动作序列中做出一致决策。
*   **核心问题**：现有的代码智能体主要依赖静态提示或手工设计的启发式方法，缺乏对中间决策的细粒度反馈。这导致智能体在探索过程中容易产生错误累积、效率低下且生成的解决方案轨迹非常脆弱。
*   **研究目标**：引入**过程奖励模型（Process Reward Models, PRMs）**，为仓库级代码智能体提供密集的“步骤级”监督，从而引导智能体做出更高效、更准确的决策。

### 2. 方法论
SWE-Shepherd 框架包含以下六个关键阶段：
1.  **任务收集**：从 SWE-Bench 提取任务，利用其真实的 GitHub 问题、代码库快照和测试套件。
2.  **轨迹生成**：使用基准 LLM 智能体尝试解决任务，记录包含阅读文件、编辑代码、运行测试等动作及其观察结果的完整轨迹。
3.  **奖励计算**：基于启发式规则为每个中间步骤分配标量奖励。规则包括：执行是否成功、是否访问了相关文件、是否修改了目标文件、测试结果以及是否避免了重复动作。
4.  **数据集构建**：将轨迹转化为监督学习样本，输入为“问题描述 + 交互历史 + 候选动作”，标签为归一化后的奖励值（0 到 1 之间）。
5.  **PRM 训练**：在基础 LLM（1.5B 参数）上添加一个 MLP 头部，使用 **qLoRA** 进行参数高效微调，通过均方误差（MSE）损失函数优化模型，使其能够预测步骤级奖励。
6.  **奖励引导推理**：在推理阶段，PRM 对每个步骤的多个候选动作进行评分，智能体优先选择得分最高的动作，实现无需完整强化学习的奖励引导搜索。

### 3. 实验设计
*   **数据集**：使用 **SWE-Bench Verified**（包含 500 个经过验证的任务），实验中抽取了其中的 100 个任务进行评估。
*   **对比方法（Benchmarks）**：
    *   **mini-SWE-Agent**：一个强大的顺序决策 LLM 智能体，不含显式搜索或奖励模型。
    *   **SWE-Search**：使用蒙特卡洛树搜索（MCTS）增强的搜索型框架。
    *   **SWE-Shepherd（本文方法）**：在 mini-SWE-Agent 基础上增加 PRM 引导。
*   **评估指标**：解决率（% Resolved）、平均成本（$）、平均交互步数（Avg. Steps）。

### 4. 资源与算力
*   **模型规模**：PRM 基于一个 **1.5B** 参数的基础语言模型。
*   **训练技术**：使用了 qLoRA 进行微调。
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：在 100 个 SWE-Bench Verified 任务上进行了测试，每个任务限制最大 30 个交互步骤。
*   **充分性评价**：
    *   **对比维度**：对比了纯智能体方法和重型搜索方法，维度较为全面。
    *   **局限性**：实验样本量（100个）相对于完整的 SWE-Bench 较小；缺乏针对不同奖励启发式规则的消融实验，难以判断哪些启发式信号对最终成功贡献最大。

### 6. 主要结论与发现
*   **效率提升**：SWE-Shepherd 显著减少了交互步数（从 15.2 步降至 **12.2 步**），表明 PRM 引导下的探索更加直接。
*   **性能权衡**：虽然效率提高，但解决率（51%）略低于基准 mini-SWE-Agent（57%）。这表明局部高奖励的动作并不总是能转化为全局正确的补丁。
*   **奖励相关性弱**：分析发现，已解决任务和未解决任务的平均奖励差异极小（0.4894 vs 0.4818），说明目前的启发式奖励函数与最终任务成功的相关性仍有待加强。

### 7. 优点
*   **轻量化**：相比于完整的强化学习（RL）或昂贵的 MCTS 搜索，PRM 提供了一种更易于训练和部署的中间地带方案。
*   **引导性强**：成功证明了过程监督可以改变智能体的行为模式，使其决策更加高效。
*   **开源贡献**：提供了代码库、数据集和预训练模型，便于社区复现。

### 8. 不足与局限
*   **对齐挑战**：中间行为信号（如“成功读取文件”）比仓库级正确性（“修复 Bug”）更容易建模，导致模型可能陷入局部最优，偏向于看起来“正确”但无法解决问题的动作。
*   **性能回退**：在解决率上未能超越最强基准，说明 PRM 可能会误导智能体放弃某些虽然奖励低但必要的探索路径。
*   **启发式依赖**：奖励计算高度依赖人工设计的启发式规则，可能无法覆盖复杂的软件工程逻辑。

（完）
