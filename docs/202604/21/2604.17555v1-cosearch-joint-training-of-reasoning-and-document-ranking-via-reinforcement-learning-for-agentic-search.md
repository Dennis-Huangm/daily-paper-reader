---
title: "COSEARCH: Joint Training of Reasoning and Document Ranking via Reinforcement Learning for Agentic Search"
title_zh: COSEARCH：通过强化学习联合训练推理与文档排序以实现代理式搜索
authors: "Hansi Zeng, Liam Collins, Bhuvesh Kumar, Neil Shah, Hamed Zamani"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17555v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 通过强化学习联合训练推理和排序以实现智能体搜索
tldr: 针对智能体搜索中检索系统固定导致的性能瓶颈，本文提出CoSearch框架。该框架利用GRPO算法首次实现了多步推理智能体与生成式文档排序模型的联合训练。通过语义分组策略解决排序模型训练中的输入变动问题，并结合复合奖励机制优化检索与推理过程，在多个QA基准测试中显著提升了性能，为未来搜索智能体的发展提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17555v1/fig-001.webp\", \"caption\": \"Table 6: Sub-queries after semantic grouping and filtering (Part 2 of 2, training step 100). For each initial query, sub-queries from G=8 rollouts are clustered by token-level F1 similarity (δ=0.8). Column C denotes the cluster index.\", \"page\": 21, \"index\": 1, \"width\": 826, \"height\": 1261}, {\"url\": \"assets/figures/arxiv/2604.17555v1/fig-002.webp\", \"caption\": \"Table 2: Main results (token-level F1) on seven QA benchmarks. Results are grouped by main agent backbone. The COSEARCH ranker uses Qwen2.5-7B-Instruct in both settings. Best in bold within each scale block. Oracle (gray) promotes the answer-containing documents to rank 1, serving as an upper bound on ranking.\", \"page\": 7, \"index\": 2, \"width\": 810, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2604.17555v1/fig-003.webp\", \"caption\": \"Table 7: Search turn distribution (% of questions) at validation step 100. Avg is the mean number of search turns per question.\", \"page\": 23, \"index\": 3, \"width\": 828, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2604.17555v1/fig-004.webp\", \"caption\": \"Table 3: Ablation study. The main agent uses Qwen2.5-7B-Instruct. Each row changes one component of the full model. Averages are computed over single-hop and multi-hop groups separately.\", \"page\": 8, \"index\": 4, \"width\": 826, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2604.17555v1/fig-005.webp\", \"caption\": \"Figure 1: Both panels share the same architecture: Main Agent→ Retriever→ Ranker. Top: existing approaches treat the entire retrieval system as fixed ( ) and train only the main agent. Bottom: COSEARCH decomposes the retrieval system into a fixed dense retriever and a trainable generative ranker ( ), jointly optimizing both the main agent and the ranker via GRPO.\", \"page\": 4, \"index\": 5, \"width\": 802, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.17555v1/fig-006.webp\", \"caption\": \"Figure 2: Training and evaluation dynamics across 100 steps. (a) Validation F1 (avg. over 7 datasets) every 20 steps. (b) Ranking quality (Hit@5) at each training step; y-axis broken to show Oracle = 1.0 above. (c) Average search turns per trajectory.\", \"page\": 9, \"index\": 6, \"width\": 775, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2604.17555v1/fig-007.webp\", \"caption\": \"Table 5: Sub-queries after semantic grouping and filtering (Part 1 of 2, training step 100). For each initial query, sub-queries from G=8 rollouts are clustered by token-level F1 similarity (δ=0.8). Column C denotes the cluster index. 20\", \"page\": 20, \"index\": 7, \"width\": 826, \"height\": 1442}]"
motivation: 现有智能体搜索方法将检索系统视为固定工具，限制了系统在处理复杂查询时的整体性能上限。
method: 采用GRPO算法联合训练推理智能体与生成式排序模型，并引入语义分组策略和结合排序质量与最终反馈的复合奖励。
result: 在七个单跳和多跳问答基准测试中，CoSearch均表现出优于强基线模型的性能，验证了联合训练的有效性。
conclusion: 研究证明推理智能体与检索系统的协同进化是提升智能体搜索性能的关键，为构建更强大的搜索系统指明了方向。
---

## 摘要
代理式搜索（Agentic search）——即训练能够通过迭代推理、发布查询并综合检索信息来回答复杂问题的智能体——在强化学习（RL）的推动下取得了显著进展。然而，现有方法（如 Search-R1）将检索系统视为固定工具，仅优化推理智能体，而检索组件保持不变。一项初步实验表明，在七个问答（QA）基准测试中，理想检索（oracle）与固定检索系统之间的差距最高可达 +26.8% 的相对 F1 提升，这表明检索系统是扩展代理式搜索性能的关键瓶颈。受此发现启发，我们提出了 CoSearch，这是一个通过群体相对策略优化（GRPO）联合训练多步推理智能体和生成式文档排序模型的框架。为了对排序器进行有效的 GRPO 训练（其输入随推理轨迹而变化），我们引入了一种语义分组策略，通过词元级相似度对子查询进行聚类，从而在不增加额外采样的情况下形成有效的优化组。我们进一步设计了一种复合奖励，结合了排序质量信号与轨迹级结果反馈，为排序器提供即时和长期的学习信号。在七个单跳和多跳问答基准测试上的实验表明，该方法相较于强基准模型有持续的改进，消融实验验证了每个设计选择的有效性。我们的结果表明，联合训练推理智能体和检索系统既可行又具有强大的性能，为未来的搜索智能体指明了一个关键要素。

## Abstract
Agentic search -- the task of training agents that iteratively reason, issue queries, and synthesize retrieved information to answer complex questions -- has achieved remarkable progress through reinforcement learning (RL). However, existing approaches such as Search-R1, treat the retrieval system as a fixed tool, optimizing only the reasoning agent while the retrieval component remains unchanged. A preliminary experiment reveals that the gap between an oracle and a fixed retrieval system reaches up to +26.8% relative F1 improvement across seven QA benchmarks, suggesting that the retrieval system is a key bottleneck in scaling agentic search performance. Motivated by this finding, we propose CoSearch, a framework that jointly trains a multi-step reasoning agent and a generative document ranking model via Group Relative Policy Optimization (GRPO). To enable effective GRPO training for the ranker -- whose inputs vary across reasoning trajectories -- we introduce a semantic grouping strategy that clusters sub-queries by token-level similarity, forming valid optimization groups without additional rollouts. We further design a composite reward combining ranking quality signals with trajectory-level outcome feedback, providing the ranker with both immediate and long-term learning signals. Experiments on seven single-hop and multi-hop QA benchmarks demonstrate consistent improvements over strong baselines, with ablation studies validating each design choice. Our results show that joint training of the reasoning agent and retrieval system is both feasible and strongly performant, pointing to a key ingredient for future search agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **COSEARCH** 的框架，旨在通过强化学习（RL）联合训练推理智能体与文档排序模型，以解决代理式搜索（Agentic Search）中的检索瓶颈问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的代理式搜索系统（如 Search-R1）通常将检索系统视为一个“固定工具”，仅优化推理智能体。
*   **研究动机**：作者通过“理想检索（Oracle）”实验发现，如果检索系统能完美返回包含答案的文档，智能体的 F1 分数可提升 14.7%（7B 模型）至 26.8%（3B 模型）。这表明**固定的检索系统已成为限制智能体性能进一步提升的主要瓶颈**。
*   **整体含义**：论文提出不应只让智能体学习如何使用工具，还应让工具（排序器）学习如何配合智能体，实现推理与检索的协同进化。

### 2. 方法论
COSEARCH 的核心思想是使用 **GRPO（群体相对策略优化）** 算法同时训练推理智能体（Main Agent）和生成式文档排序器（Ranker）。
*   **两阶段检索架构**：
    1.  **固定检索**：使用预训练的密集检索器（如 E5）召回前 N 个候选文档。
    2.  **可训练排序器**：一个生成式 LLM 接收子查询和候选文档，输出重排后的前 K 个文档。
*   **关键技术 1：语义分组策略（Semantic Grouping）**：
    *   GRPO 要求对同一输入的多个输出进行对比。但在多步搜索中，不同轨迹产生的子查询（即排序器的输入）各不相同。
    *   **解决方案**：通过词元级 F1 相似度对所有轨迹中的子查询进行聚类，将语义相似的查询归为一组进行优势计算，从而在不增加额外采样成本的情况下实现排序器的 GRPO 训练。
*   **关键技术 2：复合奖励设计（Composite Reward）**：
    *   结合了**即时排序信号**（Hit@k，即前 k 个文档是否包含答案）和**长期结果信号**（最终答案的 F1 分数）。
    *   这种设计既能纠正排序器的直接行为，又能确保检索到的内容对最终推理任务真正有用。

### 3. 实验设计
*   **数据集**：涵盖了 7 个 QA 基准测试，分为单跳（PopQA, NQ, TriviaQA）和多跳（HotpotQA, 2Wiki, Musique, Bamboogle）场景。
*   **对比方法**：
    *   基础模型：Direct Inference, CoT, RAG。
    *   强基线：Search-o1, Search-R1, ZeroSearch。
    *   内部变体：仅检索（无排序器）、固定排序器（不参与 RL 训练）。
*   **模型骨干**：主要使用 Qwen2.5-7B-Instruct 和 Qwen2.5-3B-Instruct。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：提到了学习率为 $1 \times 10^{-6}$，采样轨迹数 $G=8$，Rollout Batch Size 为 512，有效训练 Batch Size 为 128。
*   **训练时长**：实验分析展示了前 100 个训练步（Steps）的动态变化，但未给出总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个主流数据集上进行了全面测试，涵盖了不同难度的问答任务。
*   **消融实验**：非常充分。作者分别验证了奖励信号（去掉 F1 或 Hit@k）、语义分组策略、架构选择（共享权重 vs 独立模型）以及排序器规模的影响。
*   **客观性**：通过 Oracle 实验设定了性能上限，并对比了多种最新的 RL 搜索基线，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能提升**：COSEARCH 在 7B 规模下比 Search-R1 提升了 6.6% 的相对 F1 值，在 3B 规模下提升了 10.8%。
*   **协同效应**：联合训练显著缩小了与 Oracle 检索之间的差距（缩小了约 26%-27% 的差距）。
*   **搜索效率**：优化后的排序器能提供更高质量的文档，使得智能体平均需要的搜索轮数减少，提高了推理效率。
*   **模型规模敏感性**：较小的模型（3B）作为排序器时容易在训练中崩溃（无法生成有效的排列格式），而 7B 模型表现稳定。

### 7. 优点
*   **端到端优化**：首次实现了推理与排序在代理式搜索场景下的联合 RL 训练。
*   **高效的分组机制**：语义分组策略巧妙解决了 GRPO 在动态输入下的应用难题，且无需额外采样。
*   **轻量化奖励**：完全基于规则（F1 和 Hit@k），无需昂贵的 LLM-as-a-Judge 标注。

### 8. 不足与局限
*   **第一阶段检索固定**：目前仅训练了第二阶段的重排序器，最底层的密集检索器仍是固定的，可能限制了召回上限。
*   **小模型不稳定性**：实验显示 3B 排序器在处理 50 个候选文档的重排任务时存在格式生成不稳定的问题。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。
*   **任务局限**：主要集中在事实性问答（Factoid QA），对于更开放、长篇的研究性任务（Deep Research）尚待验证。

（完）
