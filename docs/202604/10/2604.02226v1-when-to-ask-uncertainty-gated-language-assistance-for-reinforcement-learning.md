---
title: "When to ASK: Uncertainty-Gated Language Assistance for Reinforcement Learning"
title_zh: 何时询问：强化学习中的不确定性门控语言辅助
authors: "Juarez Monteiro, Nathan Gavenski, Gianlucca Zuin, Adriano Veloso"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02226v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 将小型语言模型与训练好的强化学习策略结合，增强分布外泛化能力
tldr: 针对强化学习智能体在分布外（OOD）场景下表现不佳的问题，本文提出ASK框架。该框架通过蒙特卡洛丢弃法评估策略不确定性，仅在不确定性超过阈值时调用语言模型提供行动建议。这种选择性咨询机制结合了RL的效率与语言模型的推理能力，在不重新训练的情况下显著提升了智能体在未知环境中的泛化能力和安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02226v1/fig-001.webp\", \"caption\": \"TABLE IV: Downward generalization results. PPO trained on FrozenLake-8 and evaluated on smaller environments. Eval corresponds to validation performance, while Test reports final performance using fixed thresholds. Results are reported as mean ± standard deviation over 100 episodes.\", \"page\": 11, \"index\": 1, \"width\": 1076, \"height\": 1257}, {\"url\": \"assets/figures/arxiv/2604.02226v1/fig-002.webp\", \"caption\": \"TABLE III: Comparison between PPO baseline and ASK on FrozenLake. Eval corresponds to validation performance using the gating mechanism. The test corresponds to in-domain test performance. Results are reported as mean ± standard deviation over 100 episodes.\", \"page\": 10, \"index\": 2, \"width\": 1076, \"height\": 1082}, {\"url\": \"assets/figures/arxiv/2604.02226v1/fig-003.webp\", \"caption\": \"Fig. 2: In-domain performance of PPO, LM, and ASK on the FrozenLake environment across grid sizes for language model sizes ranging from 0.5B to 72B parameters. Values above markers denote mean reward over 100 episodes.\", \"page\": 4, \"index\": 3, \"width\": 621, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2604.02226v1/fig-004.webp\", \"caption\": \"TABLE I: Comparison of PPO baseline and ASK on FrozenLake: same-size evaluation and downward generalization.\", \"page\": 5, \"index\": 4, \"width\": 1076, \"height\": 621}]"
motivation: 强化学习智能体在面对分布外场景时常因高不确定性导致失效，而语言模型虽具备常识但计算开销大且难以直接用于实时规划。
method: 提出ASK框架，利用蒙特卡洛丢弃法量化RL策略的不确定性，并以此作为门控信号，仅在必要时请求语言模型辅助决策。
result: 在FrozenLake迁移实验中，ASK在分布外任务中取得了0.95的高奖励，展现出比纯RL策略更强的鲁棒性和泛化性能。
conclusion: 成功的神经符号集成需要精细的协调机制，通过不确定性触发的混合模式能有效平衡计算效率与复杂环境下的适应能力。
---

## 摘要
强化学习（RL）智能体通常在分布外（OOD）场景中表现不佳，导致高不确定性和随机行为。虽然语言模型（LM）包含宝贵的世界知识，但大型模型计算成本高昂，阻碍了实时应用，且在自主规划方面存在局限性。我们提出了“通过知识实现自适应安全”（Adaptive Safety through Knowledge, ASK），该方法将较小的语言模型与训练好的强化学习策略相结合，在无需重新训练的情况下增强分布外泛化能力。ASK 采用蒙特卡洛丢弃法（Monte Carlo Dropout）来评估不确定性，并仅在不确定性超过设定阈值时才向语言模型查询动作建议。这种选择性使用在保留现有策略效率的同时，利用了语言模型在不确定情况下的推理能力。在 FrozenLake 环境的实验中，ASK 在域内（in-domain）没有表现出改进，但在迁移任务中展示了稳健的导航能力，实现了 0.95 的奖励。我们的研究结果表明，有效的神经符号集成需要精心的编排而非简单的组合，强调了成功的分布外泛化需要足够的模型规模和有效的混合机制。

## Abstract
Reinforcement learning (RL) agents often struggle with out-of-distribution (OOD) scenarios, leading to high uncertainty and random behavior. While language models (LMs) contain valuable world knowledge, larger ones incur high computational costs, hindering real-time use, and exhibit limitations in autonomous planning. We introduce Adaptive Safety through Knowledge (ASK), which combines smaller LMs with trained RL policies to enhance OOD generalization without retraining. ASK employs Monte Carlo Dropout to assess uncertainty and queries the LM for action suggestions only when uncertainty exceeds a set threshold. This selective use preserves the efficiency of existing policies while leveraging the language model's reasoning in uncertain situations. In experiments on the FrozenLake environment, ASK shows no improvement in-domain, but demonstrates robust navigation in transfer tasks, achieving a reward of 0.95. Our findings indicate that effective neuro-symbolic integration requires careful orchestration rather than simple combination, highlighting the need for sufficient model scale and effective hybridization mechanisms for successful OOD generalization.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ASK (Adaptive Safety through Knowledge)** 的框架，旨在通过不确定性门控机制，将强化学习（RL）的高效执行能力与语言模型（LM）的常识推理能力相结合，以解决智能体在面对分布外（OOD）场景时的失效问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：强化学习智能体在训练分布内表现良好，但在面对未见过的状态（OOD）时，往往会表现出极高的不确定性，导致随机或危险的行为。
*   **核心矛盾**：
    *   **RL 局限性**：缺乏先验常识，泛化能力差，遇到新环境需重新训练。
    *   **LM 局限性**：大型语言模型（LLM）推理成本高，难以实时决策，且在长程自主规划上存在缺陷。
*   **核心问题**：如何以低成本、无需重训的方式，利用语言模型的知识来引导 RL 智能体安全地处理不确定状态？

### 2. 方法论
ASK 是一种**外在（Extrinsic）增强方法**，不需要修改原始 RL 策略的架构或进行重新训练。
*   **核心思想**：利用不确定性作为“触发器”。当 RL 策略对当前状态感到“困惑”时，才向语言模型寻求帮助。
*   **关键技术细节**：
    1.  **不确定性量化**：使用 **蒙特卡洛丢弃法（MC Dropout）**。在推理时对策略网络进行 $N$ 次随机丢弃的前向传播，计算：
        *   **认知不确定性（Epistemic）**：反映数据缺乏，通过互信息衡量。
        *   **偶然不确定性（Aleatoric）**：反映数据噪声，通过平均预测熵衡量。
    2.  **门控机制**：设定阈值 $\tau$。若总不确定性超过 $\tau$，则暂停 RL 动作，转而查询语言模型。
    3.  **结构化提示词（Prompting）**：将环境状态（坐标、邻居状态、前瞻信息）和 RL 的“自动驾驶建议”转化为自然语言。
    4.  **动作覆盖**：语言模型根据常识判断 RL 的建议是否安全，若不安全则提供替代动作。

### 3. 实验设计
*   **实验场景**：**FrozenLake（冰湖）** 环境，包含 4x4 到 8x8 不同尺寸的网格。
*   **任务设置**：
    *   **域内评估**：在与训练环境尺寸相同的地图上测试。
    *   **向下泛化（Downward Generalization）**：在 8x8 地图上训练 PPO 策略，然后在 4x4 到 7x7 的地图上直接测试（这是极大的分布偏移）。
*   **Benchmark 与对比方法**：
    *   **PPO Baseline**：纯强化学习算法。
    *   **LM-only**：仅使用语言模型进行导航。
    *   **ASK (不同规模)**：集成 Qwen 系列模型（0.5B 到 72B 参数）。

### 4. 资源与算力
*   **训练细节**：PPO 策略训练了 $2 \times 10^7$ 个时间步。
*   **模型来源**：使用了 HuggingFace 上的开源 Qwen2.5 权重。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练/推理耗时。但提到使用小型 LM 是为了兼顾实时性。

### 5. 实验数量与充分性
*   **实验规模**：针对每种地图尺寸创建了 300 个不同的上下文（洞穴位置不同），分为训练、验证和测试集。每组配置运行 100 个回合。
*   **充分性**：实验涵盖了从极小（0.5B）到极大（72B）的语言模型规模，并对比了纯 RL 和纯 LM 的表现。
*   **客观性**：通过引入“干预率（IR）”和“覆盖率（OR）”指标，客观地分析了 LM 介入的频率及其对决策的改变程度。

### 6. 主要结论与发现
*   **协同效应**：在 OOD 迁移任务中，PPO 和 LM 单独表现均为 0 奖励，但 **ASK 结合两者后在 4x4 任务中达到了 0.95 的奖励**。
*   **规模门槛**：存在明显的性能拐点。**32B 及以上**规模的模型在空间推理和泛化上表现出质的飞跃。
*   **U 型性能曲线**：有趣的是，极小（0.5B/1.5B）和极大（72B）模型表现较好，而中型模型（3B-14B）由于不确定性校准差，容易给出错误的干预，导致性能下降。
*   **域内无增益**：在 RL 已经熟悉的分布内任务中，引入 LM 不会提升性能，反而可能因误干预增加开销。

### 7. 优点
*   **即插即用**：作为一种外在方法，它能直接增强现有的已训练策略，无需昂贵的重训过程。
*   **效率平衡**：通过不确定性门控，避免了每一步都调用昂贵的 LLM，极大地降低了计算开销。
*   **神经符号结合**：成功地将 RL 的反应式控制与 LM 的符号化推理结合，解决了 OOD 场景下的安全导航问题。

### 8. 不足与局限
*   **环境复杂度有限**：实验仅在 FrozenLake 这种离散、小规模的网格世界中进行，尚未在连续动作空间或高维视觉输入（如 Atari 或机器人控制）中验证。
*   **阈值敏感性**：阈值 $\tau$ 需要针对不同模型和环境进行启发式搜索或优化（如文中使用的 Optuna），缺乏自适应的通用设置方法。
*   **提示词工程依赖**：性能高度依赖于如何将环境状态转化为文本，对于复杂环境，这种转化可能变得非常困难。

（完）
