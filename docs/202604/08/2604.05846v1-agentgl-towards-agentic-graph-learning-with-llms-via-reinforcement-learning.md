---
title: "AgentGL: Towards Agentic Graph Learning with LLMs via Reinforcement Learning"
title_zh: AgentGL：通过强化学习实现基于大语言模型的智能体图学习
authors: "Yuanfu Sun, Kang Li, Dongzhe Fan, Jiajin Liu, Qiaoyu Tan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05846v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 用于图学习中LLM智能体的强化学习驱动框架
tldr: 本研究针对大语言模型在处理图数据时忽视拓扑依赖的问题，提出了智能体图学习（AGL）范式及首个强化学习驱动的框架AgentGL。该框架将图学习转化为拓扑感知导航与LLM推理的交替过程，通过图原生工具、搜索约束思维及图条件课程强化学习，使智能体能自主探索复杂关系环境。实验表明，AgentGL在节点分类和链路预测任务上显著优于现有GraphLLM和GraphRAG基准，为LLM理解复杂关联数据开辟了新方向。
selection_source: fresh_fetch
motivation: 现有大模型智能体通常将外部信息视为非结构化文本，无法有效利用现实世界数据中蕴含的拓扑依赖关系。
method: 提出AgentGL框架，利用强化学习训练LLM智能体使用图原生工具进行多尺度探索，并结合搜索约束思维与课程学习策略。
result: "在文本属性图基准测试中，AgentGL在节点分类和链路预测任务上分别实现了高达17.5%和28.4%的绝对性能提升。"
conclusion: AgentGL证明了通过智能体自主导航与推理的结合，可以显著增强大语言模型在复杂关系环境下的建模与分析能力。
---

## 摘要
大语言模型 (LLMs) 越来越依赖于智能体能力——迭代检索、工具使用和决策——以克服静态参数化知识的局限性。然而，现有的智能体框架将外部信息视为非结构化文本，未能利用现实世界数据中固有的拓扑依赖关系。为了弥补这一差距，我们引入了智能体图学习 (Agentic Graph Learning, AGL)，这是一种将图学习重新定义为拓扑感知导航与基于 LLM 推理的交替过程的范式。具体而言，我们提出了 AgentGL，这是首个用于 AGL 的强化学习 (RL) 驱动框架。AgentGL 为 LLM 智能体配备了用于多尺度探索的图原生工具，通过搜索约束思维来调节工具使用以平衡准确性与效率，并采用图条件课程强化学习策略，在没有逐步监督的情况下稳定长程策略学习。在多种文本属性图 (TAG) 基准测试和多个 LLM 主干模型上，AgentGL 显著优于强大的 GraphLLMs 和 GraphRAG 基准模型，在节点分类中实现了高达 17.5% 的绝对提升，在链路预测中实现了 28.4% 的绝对提升。这些结果表明，AGL 是使 LLM 能够在复杂关系环境中自主导航和推理的一个极具前景的前沿方向。代码已公开于 https://github.com/sunyuanfu/AgentGL。

## Abstract
Large Language Models (LLMs) increasingly rely on agentic capabilities-iterative retrieval, tool use, and decision-making-to overcome the limits of static, parametric knowledge. Yet existing agentic frameworks treat external information as unstructured text and fail to leverage the topological dependencies inherent in real-world data. To bridge this gap, we introduce Agentic Graph Learning (AGL), a paradigm that reframes graph learning as an interleaved process of topology-aware navigation and LLM-based inference. Specifically, we propose AgentGL, the first reinforcement learning (RL)-driven framework for AGL. AgentGL equips an LLM agent with graph-native tools for multi-scale exploration, regulates tool usage via search-constrained thinking to balance accuracy and efficiency, and employs a graph-conditioned curriculum RL strategy to stabilize long-horizon policy learning without step-wise supervision. Across diverse Text-Attributed Graph (TAG) benchmarks and multiple LLM backbones, AgentGL substantially outperforms strong GraphLLMs and GraphRAG baselines, achieving absolute improvements of up to 17.5% in node classification and 28.4% in link prediction. These results demonstrate that AGL is a promising frontier for enabling LLMs to autonomously navigate and reason over complex relational environments. The code is publicly available at https://github.com/sunyuanfu/AgentGL.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AgentGL** 的框架，旨在通过强化学习（RL）提升大语言模型（LLM）在图结构数据上的智能体推理能力。以下是对该论文的详细总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 LLM 智能体框架（如 RAG 或搜索增强模型）主要处理非结构化文本，忽略了现实世界中广泛存在的**拓扑依赖关系**（如引用网络、社交平台）。
*   **核心挑战**：
    *   **拓扑感知导航**：图中的证据是多尺度的（局部邻域 vs. 全局模式），智能体需要在组合空间中决定搜索路径。
    *   **长程策略优化**：图推理通常需要多步探索，且缺乏现成的标注轨迹，难以平衡探索、利用与推理深度。
*   **研究意义**：提出了**智能体图学习（Agentic Graph Learning, AGL）**范式，将图学习视为拓扑感知导航与 LLM 推理的交替迭代过程。

### 2. 方法论
AgentGL 是首个由强化学习驱动的 AGL 框架，其核心组成部分包括：
*   **图原生搜索工具（GNS Tools）**：
    *   **1-hop/2-hop 邻域搜索**：获取局部拓扑证据。
    *   **结构显著性搜索（SS）**：基于 PageRank 分数识别全局关键节点。
    *   **图密集搜索（Dense）**：基于语义嵌入寻找潜在关联节点。
*   **两阶段强化学习策略**：
    1.  **策略引导阶段（Bootstrapping）**：通过奖励塑造（正确性 + 格式 + 工具覆盖率）训练模型掌握工具使用。
    2.  **搜索效率优化阶段（MSO）**：引入“搜索约束思维”，通过回顾性终止触发和认知密度正则化，惩罚冗余搜索，鼓励深度推理。
*   **图条件课程学习（GCCL）**：
    *   利用图的固有属性（如节点分类中的同质性和度数、链路预测中的语义一致性）自动评估样本难度，实现从易到难的平滑训练。
*   **优化算法**：支持 **GRPO** 和 **REINFORCE++** 两种主流的无评论员（critic-free）策略优化算法。

### 3. 实验设计
*   **数据集**：涵盖 7 个文本属性图（TAG）基准，涉及引用网络（OGB-Arxiv, PubMed, Arxiv-2023）、电商产品（OGB-Products, Amazon-Photo/Computers）和社交网络（Reddit）。
*   **任务场景**：节点分类（NC）和链路预测（LP）。
*   **对比方法（13 种基准）**：
    *   **GNNs**：GraphSAGE, GCN, RevGAT。
    *   **GraphLLMs**：LLaGA, GraphGPT, GraphICL 等。
    *   **GraphRAG**：LinearRAG, HippoRAG2, GraphCoT。
    *   **标准智能体搜索**：Search-R1, Search-O1。
    *   **基础 LLM**：Qwen2.5-3B/7B-Instruct。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100-80G-SXM5 GPU**。
*   **软件框架**：基于 OpenRLHF 进行强化学习训练。
*   **训练细节**：最大序列长度 1600，训练批次大小 128，使用 RoBERTa-Large 作为文本编码器。论文未详细说明总训练时长，但提到了课程学习有助于加速收敛。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上进行了测试，涵盖了域内（In-Domain）和零样本迁移（Zero-shot Transfer）设置。
*   **消融实验**：非常充分。分别验证了两阶段训练的必要性、奖励项（rCOV, CDR, RTT）的作用、课程学习（GCCL）的稳定性贡献，以及超参数 $\lambda_r$ 的影响。
*   **客观性**：对比了不同参数规模（3B 和 7B）的底座模型，并与最新的推理模型（如 Search-R1）进行了公平对比，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能卓越**：AgentGL 在节点分类上比基准提升高达 **17.5%**，在链路预测上提升高达 **28.4%**。
*   **适应性强**：在零样本迁移任务中表现优异，证明了智能体通过迭代搜索获取证据的能力比静态上下文注入更具鲁棒性。
*   **效率平衡**：通过第二阶段训练，模型在减少约 **17.5%** 工具调用的同时，准确率反而有所提升，实现了“思多搜少”。
*   **算法差异**：发现 GRPO 在节点分类上略强，而 REINFORCE++ 在链路预测上表现更好。

### 7. 优点
*   **范式创新**：首次将图学习定义为智能体决策过程，打破了传统 GNN 和静态 GraphLLM 的局限。
*   **工具完备**：设计的 GNS 工具兼顾了局部与全局、拓扑与语义。
*   **训练稳定**：引入 GCCL 课程学习，解决了长程图推理策略难以收敛的问题。
*   **可解释性**：通过 `<think>` 标签展示了模型如何根据搜索到的证据修正假设。

### 8. 不足与局限
*   **模态限制**：目前仅支持文本属性图，尚未扩展到包含图像或视频的多模态属性图。
*   **数据分配敏感**：两阶段训练之间的数据分配比例对最终性能有较大影响，需要精细调优。
*   **图密度挑战**：在极稠密的图结构中，搜索空间的爆炸可能仍会带来挑战。
*   **推理成本**：尽管优化了搜索次数，但多轮 LLM 推理的计算开销仍高于传统的端到端 GNN 模型。

（完）
