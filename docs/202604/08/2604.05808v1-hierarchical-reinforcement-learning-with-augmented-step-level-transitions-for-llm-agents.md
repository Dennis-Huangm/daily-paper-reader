---
title: Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents
title_zh: 面向大语言模型智能体的增强步级转移分层强化学习
authors: "Shuai Zhen, Yanhua Yu, Ruopei Guo, Nan Cheng, Yang Deng"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05808v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: LLM智能体的分层强化学习框架
tldr: 针对LLM智能体在复杂决策任务中因依赖长交互历史而导致的计算成本高和扩展性受限问题，本文提出了STEP-HRL框架。该框架采用分层强化学习，通过全局子任务进度和局部交互摘要构建增强的步级状态转移，使智能体仅需基于单步状态而非全历史进行学习。实验证明，该方法在ScienceWorld和ALFWorld基准上显著提升了性能与泛化能力，并大幅降低了Token消耗。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体在处理复杂任务时因依赖冗长交互历史而产生的计算开销大和扩展性差的问题。
method: 提出一种分层强化学习框架，通过全局子任务进度和局部交互摘要构建增强的步级转移，实现高效的单步学习。
result: 在ScienceWorld和ALFWorld基准测试中，该方法在任务成功率、泛化性能及Token节省方面均显著优于现有基准。
conclusion: STEP-HRL通过分层状态增强实现了高效的步级强化学习，为构建高性能且低成本的LLM智能体提供了有效路径。
---

## 摘要
大语言模型（LLM）智能体在复杂的交互式决策任务中展现出了强大的能力。然而，现有的 LLM 智能体通常依赖于日益增长的交互历史，导致了高昂的计算成本和有限的可扩展性。在本文中，我们提出了 STEP-HRL，这是一个分层强化学习（HRL）框架，它通过仅以单步转移而非完整交互历史为条件，实现了步级学习。STEP-HRL 对任务进行分层构建，利用已完成的子任务来表示整个任务的全局进度。通过引入局部进度模块，它还迭代且有选择性地总结每个子任务内的交互历史，从而生成局部进度的紧凑摘要。这些组件共同为高层和底层策略生成了增强的步级转移。在 ScienceWorld 和 ALFWorld 基准测试上的实验结果一致表明，STEP-HRL 在性能和泛化能力方面显著优于基准模型，同时减少了 token 的使用量。我们的代码可在 https://github.com/TonyStark042/STEP-HRL 获取。

## Abstract
Large language model (LLM) agents have demonstrated strong capabilities in complex interactive decision-making tasks. However, existing LLM agents typically rely on increasingly long interaction histories, resulting in high computational cost and limited scalability. In this paper, we propose STEP-HRL, a hierarchical reinforcement learning (HRL) framework that enables step-level learning by conditioning only on single-step transitions rather than full interaction histories. STEP-HRL structures tasks hierarchically, using completed subtasks to represent global progress of overall task. By introducing a local progress module, it also iteratively and selectively summarizes interaction history within each subtask to produce a compact summary of local progress. Together, these components yield augmented step-level transitions for both high-level and low-level policies. Experimental results on ScienceWorld and ALFWorld benchmarks consistently demonstrate that STEP-HRL substantially outperforms baselines in terms of performance and generalization while reducing token usage. Our code is available at https://github.com/TonyStark042/STEP-HRL.

---

## 论文详细总结（自动生成）

以下是对论文《Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 智能体在处理长程决策任务时，通常依赖于**全量交互历史（History-conditioned）**。
*   **研究动机**：
    *   **计算开销大**：Transformer 的注意力机制随上下文长度呈平方级增长，导致推理成本高昂。
    *   **冗余干扰**：未经过滤的历史记录包含大量无关信息，会掩盖关键决策信号，降低推理质量。
    *   **扩展性受限**：随着任务步数增加，上下文窗口限制了智能体处理极长任务的能力。
*   **整体含义**：论文旨在将 LLM 智能体从“依赖历史”转变为“依赖进度摘要”，通过分层架构实现高效的**步级（Step-level）**决策。

### 2. 方法论：STEP-HRL 框架
核心思想是将任务分解为高层子任务和底层动作，并引入一个“局部进度”模块来压缩历史。
*   **分层结构**：
    *   **高层策略 ($\pi_h$)**：根据全局指令和已完成的子任务序列，生成下一个子任务。
    *   **底层策略 ($\pi_l$)**：根据当前子任务、当前观察和局部进度摘要，生成具体的环境动作。
*   **局部进度模块 ($\pi_p$)**：这是关键创新。它是一个迭代更新的策略，负责将当前动作、观察和旧进度压缩成一个紧凑的文本摘要（Local Progress），从而替代原始的局部交互历史。
*   **增强步级转移**：通过“已完成子任务序列（全局进度）”+“局部进度摘要”，构建出固定长度的输入状态，使策略仅需基于单步转移进行学习。
*   **训练流程**：
    1.  **行为克隆 (BC)**：利用专家演示数据初始化高层、底层和进度策略。
    2.  **步级离线强化学习 (Offline RL)**：采用类似 IQL (Implicit Q-Learning) 的架构，通过值函数估计（Critic）和优势加权回归（Actor）在离线数据集上进一步优化策略。
*   **参数共享**：三个策略共享同一个 LLM 主干网络，但配备独立的 Critic 网络，以提高参数效率和知识迁移能力。

### 3. 实验设计
*   **数据集/场景**：
    *   **ScienceWorld**：复杂的科学实验环境，包含 30 个任务家族，涉及物理、化学、生物等，具有密集的奖励。
    *   **ALFWorld**：家庭作业环境，包含 6 类任务（如清洁、加热等），具有稀疏奖励。
*   **Benchmark 设置**：分为 **Seen（见过的任务）** 和 **Unseen（未见过的任务）**，重点考察泛化能力。
*   **对比方法**：
    *   提示词方法：ReAct, Reflexion。
    *   微调/强化学习方法：Swift-Sage, ETO, WKM, GLIDER（现有的分层强化学习基准）。
    *   闭源模型参考：GPT-3.5, GPT-4。
*   **主干模型**：Mistral-7B, Gemma-7B, Llama3-8B，以及小尺寸的 Llama-1B/3B。

### 4. 资源与算力
*   **硬件资源**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   使用 LoRA 进行参数高效微调。
    *   行为克隆阶段：5 个 Epoch，Batch Size 128。
    *   离线 RL 阶段：3 个 Epoch，Batch Size 256。
    *   推理限制：高层/底层输出限 32 tokens，进度摘要限 150 tokens。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个大型基准上进行了全面测试，涵盖了从 1B 到 8B 的多个模型规模。
*   **消融实验**：对比了“无局部进度”、“无分层架构”、“无强化学习（仅 BC）”三种变体，验证了各组件的必要性。
*   **效率分析**：专门对比了随步数增加的 Token 消耗曲线。
*   **敏感性分析**：对 RL 中的超参数（如温度系数 $\beta$、分位数 $\tau$）以及数据量、数据来源进行了详细讨论。
*   **评价**：实验设计非常充分且客观，通过 Seen/Unseen 的划分严格验证了泛化性，对比基准涵盖了当前最前沿的同类方法。

### 6. 主要结论与发现
*   **性能卓越**：STEP-HRL 在 ScienceWorld 和 ALFWorld 上的表现显著优于所有基准，尤其在 Unseen 任务上展现了极强的泛化能力。
*   **高效节能**：相比于传统的 RL 和 HRL，STEP-HRL 的单步 Token 消耗几乎保持恒定，大幅降低了推理延迟和计算成本。
*   **规模鲁棒性**：即使是较小的模型（如 Llama-1B），在采用该框架后也能在 ALFWorld 上达到接近 90% 的成功率。
*   **局部进度的重要性**：消融实验证明，如果没有局部进度模块对历史进行压缩，模型性能会大幅下降。

### 7. 优点
*   **架构创新**：通过“局部进度”模块巧妙解决了 LLM 智能体对长上下文依赖的顽疾。
*   **推理高效**：将输入长度限制在常数级别，解决了推理成本随任务时长爆炸的问题。
*   **训练稳健**：采用离线 RL 框架，避免了在线交互的高昂代价，同时通过参数共享提升了学习效率。

### 8. 不足与局限
*   **专家数据依赖**：该方法高度依赖高质量的专家演示数据，且需要人工或强模型（如 DeepSeek）预先生成子任务和进度标签，在缺乏专家数据的领域难以应用。
*   **子任务终止判定风险**：底层策略在生成动作的同时判定子任务是否结束，若判定失误（提前或延迟结束）会影响后续高层规划的准确性。
*   **任务边界模糊性**：对于某些边界不明显的复杂任务，如何定义“局部进度”和“子任务”仍具有挑战性。

（完）
