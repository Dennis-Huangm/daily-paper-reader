---
title: "MARL-GPT: Foundation Model for Multi-Agent Reinforcement Learning"
title_zh: MARL-GPT：多智能体强化学习的基础模型
authors: "Maria Nesterova, Mikhail Kolosov, Anton Andreychuk, Egor Cherepanov, Oleg Bulichev, Alexey Kovalev, Konstantin Yakovlev, Aleksandr Panov, Alexey Skrynnik"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05943v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 基于GPT的模型，用于跨多种任务的多智能体强化学习
tldr: 本研究针对多智能体强化学习（MARL）中模型泛化性不足的问题，提出了MARL-GPT。该方法通过离线强化学习，在包含SMACv2、Google足球和POGEMA的海量专家轨迹（共约15亿步）上训练单一的GPT架构模型。利用统一的Transformer观测编码器，MARL-GPT无需针对特定任务进行微调即可在多个异构环境中表现出色。这项工作证明了构建多智能体通用基础模型的可行性，为MARL领域的“大模型”时代开辟了道路。
selection_source: fresh_fetch
motivation: 解决多智能体强化学习通常需要为每个特定任务设计专门模型、缺乏通用性的局限性。
method: 提出MARL-GPT，利用离线强化学习在海量专家轨迹上训练一个基于GPT架构和统一Transformer观测编码器的通用模型。
result: 在SMACv2、Google足球和POGEMA等多个差异巨大的环境中，MARL-GPT均达到了与专门基准模型相当的竞争性性能。
conclusion: 证明了构建跨任务的多智能体基础模型是可行的，为实现类似自然语言处理领域的通用MARL模型奠定了基础。
---

## 摘要
多智能体强化学习（MARL）的最新进展已在众多具有挑战性的领域和环境中取得了成功，但通常需要为每个任务设计专门的模型。在这项工作中，我们提出了一种连贯的方法论，使得单个基于 GPT 的模型能够在多种不同的 MARL 环境和任务中学习并表现良好，包括星际争霸多智能体挑战赛（SMAC）、Google Research Football 和 POGEMA。我们的方法 MARL-GPT 应用离线强化学习，在专家轨迹（SMACv2 为 4 亿，GRF 为 1 亿，POGEMA 为 10 亿）上进行大规模训练，并结合了一个无需针对特定任务进行微调的单一基于 Transformer 的观测编码器。实验表明，MARL-GPT 在所有测试环境中与专门的基准模型相比，均取得了具有竞争力的性能。因此，我们的研究结果表明，为各种（显著不同的）多智能体问题构建多任务 Transformer 模型确实是可能的，这为开发基础 MARL 模型（类似于自然语言建模中的 ChatGPT、Llama、Mistral 等）铺平了道路。

## Abstract
Recent advances in multi-agent reinforcement learning (MARL) have demonstrated success in numerous challenging domains and environments, but typically require specialized models for each task. In this work, we propose a coherent methodology that makes it possible for a single GPT-based model to learn and perform well across diverse MARL environments and tasks, including StarCraft Multi-Agent Challenge, Google Research Football and POGEMA. Our method, MARL-GPT, applies offline reinforcement learning to train at scale on the expert trajectories (400M for SMACv2, 100M for GRF, and 1B for POGEMA) combined with a single transformer-based observation encoder that requires no task-specific tuning. Experiments show that MARL-GPT achieves competitive performance compared to specialized baselines in all tested environments. Thus, our findings suggest that it is, indeed, possible to build a multi-task transformer-based model for a wide variety of (significantly different) multi-agent problems paving the way to the fundamental MARL model (akin to ChatGPT, Llama, Mistral etc. in natural language modeling).

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MARL-GPT**，这是一个旨在成为多智能体强化学习（MARL）领域“基础模型”（Foundation Model）的尝试。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的 MARL 方法通常针对特定任务设计，缺乏泛化性。当智能体数量、地图布局或任务领域发生变化时，现有模型往往失效。
*   **核心目标**：构建一个通用的、基于 Transformer 的单一模型，能够同时处理多个差异巨大的 MARL 环境（如策略对战、体育竞技、路径规划），而无需针对特定任务进行架构调整或繁琐的微调。
*   **整体含义**：该研究证明了通过大规模离线数据训练，可以实现一个跨领域的通用多智能体决策模型，类似于自然语言处理领域的 GPT 模型。

### 2. 方法论
*   **核心思想**：利用大规模离线专家轨迹数据，结合统一的观测编码方案，通过行为克隆（BC）和离线强化学习（Offline RL）训练一个 GPT 架构的 Transformer 模型。
*   **关键技术细节**：
    *   **统一观测编码（Universal Observation Encoding）**：这是模型跨环境运行的关键。它将原始向量观测拆分为 Token，并为每个 Token 注入四种位置编码：
        1.  `pos_attr`：属性类型（如生命值、坐标）。
        2.  `pos_team`：团队归属（盟友、敌人）。
        3.  `pos_indx`：智能体索引。
        4.  `pos_time`：时间步（处理部分可观测性）。
    *   **离线训练目标**：
        *   **Critic（评论家）**：采用离散分箱（Discrete Bins）分类损失，结合时间差分（TD）误差和保守正则化（CR），以防止对未见动作的价值高估。
        *   **Actor（行动者）**：结合行为克隆（BC）损失和基于优势函数（Advantage）的策略梯度损失。
    *   **动作空间处理**：使用统一的输出层配合环境特定的动作掩码（Action Masking）。
    *   **在线微调**：支持使用 PPO 算法对预训练模型进行快速在线适配。

### 3. 实验设计
*   **实验场景**：
    1.  **SMACv2**：星际争霸 II 战斗，测试复杂的微操和协同。
    2.  **Google Research Football (GRF)**：足球竞技，测试随机动态环境下的策略适配。
    3.  **POGEMA**：多智能体路径规划，测试大规模智能体群体的避障与导航。
*   **数据集规模**：总计约 15 亿个样本（SMACv2 4亿、GRF 1亿、POGEMA 10亿）。
*   **对比基准（Baselines）**：
    *   **单环境基准**：Decision Transformer (DT), BC, CQL, BC-LSTM, RATE。
    *   **专家模型**：专门为每个任务训练的 IPPO 或 RHCR 模型。
*   **评估指标**：胜率（Win Rate）和吞吐量（Throughput）。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 台 NVIDIA H100 80GB GPU**。
*   **训练时长**：
    *   主实验（7M 参数模型，50万次迭代）：耗时 **161 小时**。
    *   小型消融实验（1万次迭代）：在 2 台 H100 上耗时 13 小时。
*   **模型规模**：主要测试了 7M 参数的 Transformer 模型。

### 5. 实验数量与充分性
*   **实验规模**：做了大量的跨任务实验，涵盖了不同种族（星际）、不同难度（足球）和不同地图（路径规划）。
*   **消融实验**：系统地测试了模型大小、历史窗口长度、数据集规模以及位置编码对性能的影响。
*   **泛化性测试**：包括零样本（Zero-shot）迁移到未见过的智能体数量（如 5vs5 迁移到 10vs10）和未见过的种族。
*   **充分性评价**：实验设计非常全面，不仅有仿真环境对比，还包含了**真实世界机器人（JetBot）**的验证实验，证明了模型的实用性。

### 6. 主要结论与发现
*   **单一模型胜过专门模型**：MARL-GPT 在多个任务中的表现达到或超过了专门为该任务设计的离线 RL 基准模型。
*   **位置编码至关重要**：消融实验显示，如果没有论文提出的四种位置编码，模型在处理复杂环境（如 GRF 和 POGEMA）时性能会大幅下降。
*   **微调效率高**：预训练的 MARL-GPT 在新任务上的微调速度显著快于从零开始训练的小模型。
*   **跨任务迁移可行**：模型展现出了非平凡的零样本泛化能力，尤其是在具有相似战术逻辑的任务之间。

### 7. 优点
*   **通用性强**：打破了 MARL 任务之间“一任务一模型”的壁垒。
*   **编码方案优雅**：通过结构化的位置编码解决了异构观测空间的对齐问题。
*   **开源贡献**：提供了大规模的多智能体专家数据集和预训练权重，对社区贡献巨大。
*   **实机验证**：通过物理机器人的路径规划实验增强了论文的说服力。

### 8. 不足与局限
*   **输入模态限制**：目前仅支持结构化向量输入，无法直接处理原始图像（像素）观测。
*   **手动编码依赖**：虽然编码框架通用，但针对新环境仍需手动定义属性索引（Attribute Index）。
*   **动作空间语义对齐**：不同环境的动作索引可能冲突（例如索引 1 在 A 环境是“左移”，在 B 环境是“攻击”），目前依赖掩码解决，缺乏深层的语义对齐。
*   **零样本迁移上限**：在完全陌生的领域（如从未见过的单位类型），零样本表现仍有待提高。

（完）
