---
title: "MemRerank: Preference Memory for Personalized Product Reranking"
title_zh: MemRerank：面向个性化商品重排序的偏好记忆
authors: "Zhiyuan Peng, Xuyang Wu, Huaixiao Tou, Yi Fang, Yu Gong"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29247v2"
tags: ["query:ag-rl"]
score: 9.0
evidence: 使用强化学习训练LLM智能体的记忆提取器
tldr: "针对大语言模型购物助手在处理冗长且嘈杂的用户历史记录时效率低下的问题，本文提出MemRerank框架。该框架将用户购买历史提炼为简洁、独立于查询的偏好记忆，用于个性化商品重排序。研究者构建了“五选一”基准测试，并利用强化学习以重排序性能为监督信号训练记忆提取器。实验证明，MemRerank显著优于原始历史记录等基准，准确率提升达10.61%，为电商智能体的个性化提供了有效方案。"
selection_source: fresh_fetch
motivation: 传统的将原始购买历史直接加入提示词的方法因噪声多、长度长且相关性匹配差，难以有效实现个性化。
method: 提出MemRerank框架，通过强化学习训练记忆提取器，将用户历史提炼为简洁的偏好信号以辅助重排序。
result: "在两个LLM重排序器上的实验显示，MemRerank在“五选一”任务中比基准方案最高提升了10.61%的准确率。"
conclusion: 显式的偏好记忆是提升电商智能体系统个性化能力的实用且高效的构建模块。
---

## 摘要
基于大语言模型（LLM）的购物智能体日益依赖长期的购买历史和多轮交互来实现个性化，但由于噪声、长度和相关性不匹配，直接将原始历史记录附加到提示词（prompts）中往往效果不佳。我们提出了 MemRerank，这是一个偏好记忆框架，它将用户的购买历史提炼为简洁且独立于查询的信号，用于个性化商品重排序。为了研究这一问题，我们构建了一个端到端的基准测试和评估框架，其核心是一个基于 LLM 的“五选一”（1-in-5）选择任务，该任务同时衡量记忆质量和下游重排序效用。我们进一步利用强化学习（RL）训练记忆提取器，并以下游重排序性能作为监督信号。在两个基于 LLM 的重排序器上进行的实验表明，MemRerank 始终优于无记忆、原始历史记录和现成的记忆基准，在“五选一”准确率上最高提升了 10.61 个绝对百分点。这些结果表明，显式偏好记忆是代理式电子商务系统中实现个性化的实用且有效的构建模块。

## Abstract
LLM-based shopping agents increasingly rely on long purchase histories and multi-turn interactions for personalization, yet naively appending raw history to prompts is often ineffective due to noise, length, and relevance mismatch. We propose MemRerank, a preference memory framework that distills user purchase history into concise, query-independent signals for personalized product reranking. To study this problem, we build an end-to-end benchmark and evaluation framework centered on an LLM-based \textbf{1-in-5} selection task, which measures both memory quality and downstream reranking utility. We further train the memory extractor with reinforcement learning (RL), using downstream reranking performance as supervision. Experiments with two LLM-based rerankers show that MemRerank consistently outperforms no-memory, raw-history, and off-the-shelf memory baselines, yielding up to \textbf{+10.61} absolute points in 1-in-5 accuracy. These results suggest that explicit preference memory is a practical and effective building block for personalization in agentic e-commerce systems.

---

## 论文详细总结（自动生成）

以下是对论文《MemRerank: Preference Memory for Personalized Product Reranking》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）在电商领域作为智能助手（Shopping Agents）的应用，如何利用用户长期的购买历史实现个性化推荐成为关键。
*   **核心问题**：直接将原始的购买历史（Raw History）塞进 Prompt 会引入大量噪声、导致上下文窗口过载，且历史记录与当前查询的相关性往往不匹配，导致模型性能提升有限甚至下降。
*   **论文目标**：提出一种名为 **MemRerank** 的框架，旨在将冗长、嘈杂的购买历史提炼为简洁、结构化且独立于查询的“偏好记忆”，以显著提升下游商品重排序（Reranking）的准确性。

### 2. 方法论
*   **核心思想**：采用“提取-重排”两阶段架构，并利用强化学习（RL）优化记忆提取过程，使生成的记忆直接服务于下游的排序任务。
*   **关键技术细节**：
    *   **结构化偏好记忆**：将记忆分为“类目内偏好”（Within-category）和“跨类目偏好”（Cross-category）。前者关注用户在特定类别下的细粒度需求，后者捕捉用户的通用购物风格（如预算、品牌倾向）。
    *   **记忆提取器训练（GRPO）**：使用组相对策略优化（Group Relative Policy Optimization, GRPO）算法对提取器（如 Qwen2.5-7B）进行后训练。
    *   **奖励函数设计**：
        1.  **格式奖励（Format Reward）**：确保模型输出符合预定义的 XML 标签格式。
        2.  **重排序奖励（Reranking Reward）**：将生成的记忆输入给下游重排序器，若重排序器在“五选一”任务中选中正确商品，则给予正向奖励。
    *   **提示词策略**：设计了半结构化、带证据支持（Evidence-grounded）的 Prompt，要求模型在提取偏好时引用原始历史中的片段，增强记忆的真实性。

### 3. 实验设计
*   **数据集**：基于 **Amazon-Review-2023** 和 **Amazon-C4** 构建。研究者通过 o3-mini 改写查询以模拟真实的购物意图，并利用向量嵌入技术（Qwen3-8B-Embedding）检索同类目下的硬负样本（Hard Negatives）。
*   **Benchmark**：采用 **1-in-5 选择任务**（从 1 个正样本和 4 个硬负样本中选出最相关的商品），这是集合重排序（Setwise Reranking）的基本操作。
*   **对比方法**：
    *   **No memory**：仅使用查询和候选商品。
    *   **Product-context**：直接拼接原始的类目内/跨类目商品信息。
    *   **LLM-extracted**：使用未微调的 Qwen2.5 或更强的 GPT-5.2（注：文中提及型号）直接提取记忆。
    *   **外部框架**：对比了 **Mem0** 和 **MR.Rec** 等现有的记忆管理系统。

### 4. 资源与算力
*   **模型使用**：记忆提取器基础模型为 **Qwen2.5-7B-Instruct**；下游重排序器使用了 **GPT-4.1-mini** 和 **o4-mini**。
*   **算力细节**：论文中**未明确说明**具体的 GPU 型号、数量以及训练的总时长。

### 5. 实验数量与充分性
*   **实验规模**：在“电子产品（Electronics）”核心类别上进行了深入实验（训练集 905 实例，开发/测试集各 194 实例）。
*   **充分性分析**：
    *   **消融实验**：对比了三种不同的提取 Prompt（固定维度 vs 自由发现 vs 半结构化），验证了 Prompt 设计对 RL 训练效果的影响。
    *   **长度实验**：分析了原始历史记录长度对不同重排序器性能的影响，揭示了原始信息增加带来的噪声问题。
    *   **客观性**：通过引入 Think Tag（思维链）验证了高质量记忆与模型推理能力的互补性。实验设计较为严谨，但样本量在 LLM 时代属于中等规模。

### 6. 主要结论与发现
*   **记忆优于原始数据**：显式提取的偏好记忆效果远好于直接拼接原始历史，MemRerank 在 o4-mini 上实现了 **+10.61%** 的准确率提升。
*   **RL 训练是核心**：经过 RL 训练的 7B 模型提取的记忆，其辅助效果优于未经训练的更强大模型（如 GPT-5.2）。
*   **Prompt 结构至关重要**：半结构化且要求提供证据（Evidence）的 Prompt（v3）能提供最强的归纳偏置，在 RL 训练中潜力最大。
*   **模型差异**：更强的重排序器（如 o4-mini）能更好地利用长上下文，但依然受益于压缩后的偏好记忆。

### 7. 优点
*   **闭环优化**：通过 RL 将记忆提取与下游任务的最终指标（准确率）直接挂钩，解决了“什么样的记忆才是好记忆”的问题。
*   **实用性强**：生成的记忆是独立于查询的，可以缓存并重复使用，降低了在线推理的计算开销。
*   **可解释性**：通过要求模型提供证据片段（Evidence Snippets），增强了记忆的可信度和可解释性。

### 8. 不足与局限
*   **领域覆盖有限**：实验主要集中在“电子产品”类别，不同品类（如服装、生鲜）的偏好特征差异巨大，泛化性有待进一步验证。
*   **规模限制**：目前仅验证了 1-in-5 的重排序场景，在处理更大规模候选集（如 Top-100）时的端到端表现尚未探讨。
*   **冷启动问题**：对于历史记录极少的用户，该框架的优势可能无法体现。
*   **模型版本**：文中提到的部分模型型号（如 GPT-5.2, o4-mini）可能属于特定实验环境或未来预测命名，与当前主流公开型号命名存在差异。

（完）
