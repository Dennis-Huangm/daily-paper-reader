---
title: Evolutionary Discovery of Reinforcement Learning Algorithms via Large Language Models
title_zh: 基于大语言模型的强化学习算法演化发现
authors: "Alkis Sygkounas, Amy Loutfi, Andreas Persson"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28416v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 利用大语言模型发现强化学习算法
tldr: 本研究提出了一种利用大语言模型（LLM）作为进化变异算子的框架，旨在自动发现强化学习算法。通过在可执行的更新规则空间中进行搜索，并排除传统的TD误差和Actor-Critic等预设机制，该方法成功演化出了全新的学习规则。结合LLM驱动的超参数精炼阶段，所发现的算法在多个Gymnasium基准测试中表现出与SAC、PPO等主流算法相当的竞争力，展示了自动化算法设计的巨大潜力。
selection_source: fresh_fetch
motivation: 传统的强化学习算法主要依赖人工设计且结构固定，限制了发现更高效或非标准学习机制的可能性。
method: 基于REvolve框架利用LLM生成并变异可执行的更新规则代码，并引入后期超参数精炼阶段以优化算法性能。
result: 演化出的算法在多个Gymnasium基准任务上达到了与SAC、PPO、DQN和A2C等经典算法相媲美的性能水平。
conclusion: 该研究证明了利用LLM驱动的进化搜索能够自主发现高性能且非传统的强化学习算法，为自动化算法设计开辟了新路径。
---

## 摘要
强化学习算法由其学习更新规则定义，这些规则通常是人工设计且固定的。我们提出了一种演化框架，通过直接搜索实现完整训练过程的可执行更新规则来发现强化学习算法。该方法基于 REvolve，这是一个将大语言模型作为生成式变异算子的演化系统，并将其从奖励函数发现扩展到了算法发现。为了促进非标准学习规则的出现，搜索排除了诸如演员-评论家（actor-critic）结构、时序差分（temporal-difference）损失和价值自助法（value bootstrapping）等规范机制。由于强化学习算法对内部标量参数高度敏感，我们引入了一个演化后细化阶段，由大语言模型为每个演化出的更新规则建议可行的超参数范围。通过在多个 Gymnasium 基准测试上进行完整训练运行的端到端评估，所发现的算法相对于既有的基准算法（包括 SAC、PPO、DQN 和 A2C）取得了具有竞争力的性能。

## Abstract
Reinforcement learning algorithms are defined by their learning update rules, which are typically hand-designed and fixed. We present an evolutionary framework for discovering reinforcement learning algorithms by searching directly over executable update rules that implement complete training procedures. The approach builds on REvolve, an evolutionary system that uses large language models as generative variation operators, and extends it from reward-function discovery to algorithm discovery. To promote the emergence of nonstandard learning rules, the search excludes canonical mechanisms such as actor--critic structures, temporal-difference losses, and value bootstrapping. Because reinforcement learning algorithms are highly sensitive to internal scalar parameters, we introduce a post-evolution refinement stage in which a large language model proposes feasible hyperparameter ranges for each evolved update rule. Evaluated end-to-end by full training runs on multiple Gymnasium benchmarks, the discovered algorithms achieve competitive performance relative to established baselines, including SAC, PPO, DQN, and A2C.

---

## 论文详细总结（自动生成）

以下是对论文《Evolutionary Discovery of Reinforcement Learning Algorithms via Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：强化学习（RL）算法的更新规则（如损失函数和训练逻辑）通常由人工设计且固定（如 PPO、SAC）。尽管已有研究自动化了架构设计或奖励函数，但**可执行的更新规则本身**仍是自动化设计的盲区。
*   **研究动机**：人工设计的算法可能受限于人类直觉。本文旨在探索是否可以通过演化计算结合大语言模型（LLM），在不使用标准机制（如 Actor-Critic、TD 误差、价值自助法）的前提下，自动发现全新的、具有竞争力的 RL 算法。

### 2. 方法论
*   **核心思想**：将算法发现建模为在“可执行代码”空间中的演化搜索。利用 LLM 作为变异和交叉算子，生成完整的训练逻辑代码。
*   **关键技术细节**：
    *   **REvolve 框架扩展**：继承了基于岛屿模型（Island Model）的演化结构，但将搜索目标从奖励函数转变为整个 `compute_loss` 更新规则。
    *   **变异算子**：
        *   **宏观变异（Macro-mutation）**：利用 LLM 重写算法的一个语义组件，避免微小代码修改导致的程序崩溃。
        *   **多样性感知交叉（Diversity-aware Crossover）**：结合适应度与代码间的编辑距离（Levenshtein Distance），惩罚结构过于相似的个体，防止种群多样性塌缩。
    *   **禁令约束**：在提示词（Prompt）中明确禁止使用 Actor-Critic、TD 误差等标准 RL 模块，迫使 LLM 探索非传统逻辑。
    *   **LLM-HPO（演化后超参数优化）**：演化结束后，利用 LLM 根据算法结构建议超参数范围，并在该范围内进行网格/随机搜索，以消除超参数敏感性对算法评价的影响。

### 3. 实验设计
*   **训练环境（Training Suite）**：CartPole-v1, MountainCar-v0, Acrobot-v1, LunarLander-v3, HalfCheetah-v5。涵盖了离散/连续动作空间及稀疏/稠密奖励。
*   **泛化测试环境（Unseen Envs）**：Walker2d, InvertedPendulum, Reacher, Swimmer, Pusher。
*   **对比基准（Benchmarks）**：PPO, A2C, DQN, SAC。
*   **评估指标**：归一化后的最大评估回报（Maximum Evaluation Return），取 5 个独立随机种子的平均值。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 台 NVIDIA A100 (40GB)** GPU。
*   **训练时长**：单个演化代（Generation）大约需要 **30 小时**的墙上时间（Wall-clock time）。
*   **总规模**：每个 LLM 进行 2 次独立演化运行，每运行持续 10 代，每代生成 24 个候选算法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   演化搜索：总计生成并评估了数百个候选算法。
    *   最终评估：对选出的最优算法在 10 个环境上进行了 5 种子的完整训练。
    *   消融实验：针对 Levenshtein 权重 $\alpha$ 的影响以及是否加入“终端价值自助法”进行了专门测试。
*   **充分性与公平性**：实验设计较为客观。所有算法（包括基准）使用相同的 MLP 策略架构和优化器（Adam），确保性能差异源于更新规则而非模型容量。引入泛化测试环境有效验证了算法并非对特定任务过拟合。

### 6. 主要结论与发现
*   **LLM 的有效性**：GPT-5.2 在发现高性能算法方面显著优于 Claude 4.5 Opus。
*   **发现的新算法**：
    *   **CG-FPD**：通过蒸馏来自学习到的潜动力学模型的短时域规划信号来训练策略，完全不使用价值函数。
    *   **DF-CWP-CP**：利用可微的世界模型进行短时域展开，结合置信度加权和可控性先验进行优化。
*   **性能表现**：演化出的算法在多个任务上达到了与 PPO、SAC 等成熟算法相当甚至更优的水平，且展现了良好的跨任务泛化能力。

### 7. 优点（亮点）
*   **搜索空间大胆**：直接在可执行代码层面搜索，而非简单的参数优化或公式组合。
*   **多样性维护机制**：引入代码编辑距离作为正则项，有效解决了演化算法容易陷入局部最优的问题。
*   **两阶段设计**：意识到 RL 算法对超参数的极端敏感性，通过 LLM-HPO 阶段确保了被选算法的潜力得到充分挖掘。

### 8. 不足与局限
*   **计算成本极高**：每个候选算法都需要完整的训练运行来评估适应度，限制了搜索的种群规模和代数。
*   **创新本质的质疑**：虽然禁用了标准机制，但 LLM 生成的代码往往是现有模型预测控制（MPC）或蒸馏思想的变体，而非完全“从零开始”的数学创新。
*   **LLM 偏差**：搜索结果高度依赖于 LLM 的预训练知识，可能存在“知识重组”而非“本质发现”的倾向。

（完）
