---
title: On the Reliability Limits of LLM-Based Multi-Agent Planning
title_zh: 论基于大语言模型的多智能体规划的可靠性极限
authors: "Ruicheng Ao, Siyang Gao, David Simchi-Levi"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26993v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 基于大语言模型的多智能体规划可靠性限制
tldr: 本研究探讨了基于大语言模型（LLM）的多智能体规划在可靠性方面的局限性。通过将多智能体架构建模为有限无环决策网络，研究证明了在没有外部新信号的情况下，任何委派网络在决策理论上都劣于拥有相同信息的中心化贝叶斯决策者。研究进一步量化了通信和信息压缩带来的损失，并利用后验散度表征了性能差距，为理解LLM多智能体系统的基本可靠性界限提供了理论框架。
selection_source: fresh_fetch
motivation: 旨在量化和理解基于LLM的多智能体规划系统在信息传递和协作决策过程中的可靠性极限。
method: 将多智能体架构建模为有限有向无环决策网络，并利用决策理论和信息论工具分析通信预算对决策质量的影响。
result: 证明了中心化决策优于委派式多智能体决策，并推导出通信损失与后验散度（如条件互信息）之间的数学关系。
conclusion: LLM多智能体规划的可靠性受限于通信带宽和信息压缩，其性能上限由等效的中心化贝叶斯模型决定。
---

## 摘要
本技术报告将基于大语言模型（LLM）的多智能体规划视为一种委托决策问题，并研究了其可靠性极限。我们将基于 LLM 的多智能体架构建模为一个有限无环决策网络，其中多个阶段处理共享的模型上下文信息，通过容量有限的语言接口进行通信，并可能调用人工审核。我们证明，在没有新的外生信号的情况下，任何委托网络在决策理论上都劣于拥有相同信息的中心化贝叶斯决策者。在共同证据机制下，这意味着在有限通信预算下优化多智能体有向无环图，可以重新表述为在共享信号上选择一个受预算约束的随机实验。我们还刻画了由通信和信息压缩引起的损失。在适当的评分规则下，中心化贝叶斯价值与通信后价值之间的差距可以表示为期望后验散度，这在对数损失下简化为条件互信息，在 Brier 分数下简化为期望后验平方误差。这些结果刻画了委托 LLM 规划的基本可靠性极限。在受控问题集上进行的 LLM 实验进一步验证了这些刻画。

## Abstract
This technical note studies the reliability limits of LLM-based multi-agent planning as a delegated decision problem. We model the LLM-based multi-agent architecture as a finite acyclic decision network in which multiple stages process shared model-context information, communicate through language interfaces with limited capacity, and may invoke human review. We show that, without new exogenous signals, any delegated network is decision-theoretically dominated by a centralized Bayes decision maker with access to the same information. In the common-evidence regime, this implies that optimizing over multi-agent directed acyclic graphs under a finite communication budget can be recast as choosing a budget-constrained stochastic experiment on the shared signal. We also characterize the loss induced by communication and information compression. Under proper scoring rules, the gap between the centralized Bayes value and the value after communication admits an expected posterior divergence representation, which reduces to conditional mutual information under logarithmic loss and to expected squared posterior error under the Brier score. These results characterize the fundamental reliability limits of delegated LLM planning. Experiments with LLMs on a controlled problem set further demonstrate these characterizations.

---

## 论文详细总结（自动生成）

这篇论文《On the Reliability Limits of LLM-Based Multi-Agent Planning》（论基于大语言模型的多智能体规划的可靠性极限）由麻省理工学院（MIT）和香港城市大学的研究人员撰写。该研究从决策理论的角度，深入探讨了为什么增加 LLM 智能体的数量并不总是能提高任务性能。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前 LLM 多智能体系统（如 Planner-Worker-Critic 架构）被广泛用于复杂任务，但实证研究发现其表现往往不稳定，甚至不如简单的单智能体。
*   **核心问题**：多智能体架构究竟是改变了决策所需的“信息结构”，还是仅仅在重新组织相同的信息？
*   **整体含义**：论文试图为 LLM 多智能体规划建立一个决策理论框架，确定其可靠性的基本极限，并区分“架构改进”与“信息增加”对性能的不同贡献。

### 2. 论文提出的方法论
*   **核心思想**：将多智能体系统建模为**有限无环决策网络（DAG）**。每个节点（智能体）是一个决策者，边代表具有有限容量的通信接口。
*   **关键技术细节**：
    *   **共同证据机制（Common-evidence regime）**：假设所有智能体共享相同的初始上下文（如 Prompt、检索到的文档），且没有新的外部信号进入。
    *   **决策理论界限**：证明了在没有新外生信号的情况下，任何多智能体网络在决策质量上都无法超越一个观察到所有原始信息的“中心化贝叶斯决策者”。
    *   **通信损失表征**：利用**适当评分规则（Proper Scoring Rules）**，将通信引起性能下降量化为“后验扭曲（Posterior Distortion）”。
    *   **数学公式**：在对数损失下，通信损失等于**条件互信息 $I(Y; H | M)$**；在 Brier 分数下，等于**期望后验平方误差**。
*   **人工审核**：将人工干预建模为一种基于后验风险对比的选择性操作。

### 3. 实验设计
*   **数据集/场景**：
    *   **MMLU**：从 STEM、人文、社科等领域选取 200 道四选一题目，用于测试通用推理。
    *   **合成知识库（Synthetic KB）**：包含 200 个虚构实体，模型无法通过预训练知识回答，必须通过工具调用获取信息。
*   **对比方法（9 种实验条件）**：
    *   **基准**：单智能体中心化决策（Condition A）。
    *   **委派深度**：2 阶段、3 阶段、5 阶段的串行接力（Relay），仅传递文字分析。
    *   **通信格式**：对比“自由文本接力”与“结构化后验概率向量接力”。
    *   **信号获取**：对比加入 Wikipedia、Semantic Scholar 等外部工具的效果。
*   **模型**：使用了 `gpt-4.1-mini`（支持 logprobs 提取）和 `o4-mini`（推理模型）。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 型号或训练时长，因为这是一项基于 API 的推理研究。
*   **成本**：文中明确指出 API 调用总成本约为 **100 美元**（其中原始实验约 70 美元，补充实验约 15-25 美元）。

### 5. 实验数量与充分性
*   **实验规模**：每道题目在不同条件下运行 10 到 30 次，总运行次数超过 **30,000 次**。
*   **充分性**：实验设计非常严谨，采用了“控制变量法”，每次只改变一个维度（如只增加深度或只改变接口格式）。通过计算置信区间（CI）和相关系数（r），证明了实验结果在统计上是显著且客观的。

### 6. 主要结论与发现
*   **性能衰减**：在共享信息的前提下，增加接力阶段会导致性能大幅下降。在 MMLU 上，单智能体准确率约 90%，而 5 阶段接力后降至 22.5%（低于随机猜测）。
*   **通信是瓶颈**：多智能体系统的失败往往源于通信过程中的信息压缩和扭曲，而非模型本身的能力不足。
*   **接口质量至关重要**：使用结构化的概率向量传递信息，其性能衰减速度远慢于使用自由文本。
*   **工具的价值**：只有当工具能提供“非冗余”的新信息时，多智能体架构才有意义。如果工具提供的信息模型已知，则无显著提升。

### 7. 优点
*   **理论严谨**：将模糊的“智能体协作”问题转化为清晰的“信息论与决策理论”问题。
*   **揭示本质**：明确了多智能体系统的上限是由其获取的原始信息决定的，为开发者提供了避坑指南（不要盲目增加审核/评论环节）。
*   **量化分析**：首次给出了通信损失与后验散度之间的精确数学关系。

### 8. 不足与局限
*   **任务类型单一**：实验主要集中在多选题（Multiple-choice），可能无法完全代表开放式生成或长程规划任务。
*   **无环图限制**：模型假设是无环的（DAG），未深入探讨具有复杂反馈循环（Self-correction loops）或动态终止条件的系统。
*   **信念代理**：使用模型输出的 Token 概率作为“后验信念”的代理指标，虽然在操作上可行，但可能无法完全捕捉 LLM 内部的认知状态。

（完）
