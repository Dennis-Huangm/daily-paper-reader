---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 利用强化学习优化LLM智能体记忆操作及统一框架
tldr: "MemFactory是首个针对记忆增强型智能体设计的统一推理与训练框架。针对当前记忆管理（提取、更新、检索）研究中实现碎片化的问题，该框架采用类似Lego的模块化架构，将记忆生命周期抽象为可插拔组件。它原生支持GRPO算法，通过多维环境奖励优化内存策略，并兼容Memory-R1等前沿范式。实验证明，MemFactory能显著提升智能体性能，最高增益达14.8%，为长短期记忆智能体的开发提供了标准化的基础设施。"
selection_source: fresh_fetch
motivation: 现有的记忆增强型智能体开发缺乏统一的基础设施，导致内存管理策略的训练和集成过程高度碎片化且难以复用。
method: 提出一种模块化的“乐高式”架构，将记忆生命周期抽象为原子组件，并集成GRPO算法以实现基于环境奖励的内存策略微调。
result: "在MemAgent基准测试中，MemFactory在域内和分布外数据集上均表现出色，相比基础模型实现了最高14.8%的性能提升。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，显著降低了记忆驱动型AI智能体的研究门槛。
---

## 摘要
记忆增强型大语言模型（LLM）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory：智能体记忆的统一推理与训练框架论文总结

#### 1. 论文的核心问题与整体含义
*   **研究背景**：为了让大语言模型（LLM）进化为具备长期上下文维持和个性化能力的 AI 智能体，记忆增强技术至关重要。
*   **核心问题**：尽管利用强化学习（RL）优化记忆操作（提取、更新、检索）已成为趋势，但现有的研究实现高度碎片化，缺乏统一的基础设施。开发者在复现或组合不同记忆策略（如 Memory-R1, MemAgent）时面临巨大的工程障碍。
*   **整体含义**：本文推出了 **MemFactory**，这是首个专为记忆增强型智能体设计的统一、模块化训练与推理框架。它模仿 LLaMA-Factory 的成功模式，旨在通过标准化流程降低 Memory-RL 研究的门槛。

#### 2. 论文提出的方法论
MemFactory 采用了四层解耦架构，实现了“乐高式”的灵活构建：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作，包括：
    *   **Extractor**：从原始上下文中解析事实。
    *   **Updater**：执行增删改（ADD/DEL/UPDATE/NONE）操作维护记忆库。
    *   **Retriever**：基于语义或重排序（Rerank）检索相关记忆。
    *   **Agent Module**：支持端到端的循环记忆策略。
*   **智能体层 (Agent Layer)**：作为策略执行器，负责加载预训练模型并编排各模块的交互逻辑（Rollout）。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器，提供格式奖励和“LLM-as-a-Judge”等多维反馈。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO (Group Relative Policy Optimization)** 算法。该算法通过组内奖励归一化估计优势函数，无需额外的 Critic 网络，显著降低了长上下文训练时的显存开销。

#### 3. 实验设计
*   **数据集/场景**：使用了 MemAgent 公开的训练和评估数据。
*   **Benchmark**：
    *   **域内 (In-domain)**：`eval_50` 和 `eval_100`（不同长度的文档任务）。
    *   **分布外 (OOD)**：`eval_fwe_16384`（测试模型的泛化能力）。
*   **对比方法**：以 Qwen3-1.7B 和 Qwen3-4B-Instruct 为基座模型，对比了“原始 Checkpoint”与“经过 MemFactory RL 微调后”的性能差异。

#### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步数（steps）。
*   **效率**：通过集成 FlashAttention-2 和 GRPO 算法，框架在受限的算力资源下表现出良好的可复现性。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型在三个测试集上进行了验证。
*   **客观性**：所有评分均基于 **4 次独立试验的平均值 (avg@4)**，以减少生成随机性带来的偏差。
*   **充分性**：实验涵盖了从 1.7B 到 4B 的模型，并包含了 OOD 测试，验证了框架在不同规模模型和任务迁移上的有效性。虽然实验组数不算海量，但针对核心指标的对比是客观且公平的。

#### 6. 论文的主要结论与发现
*   **性能提升**：MemFactory 显著提升了智能体的记忆管理能力。Qwen3-1.7B 在平均得分上实现了 **14.8%** 的相对增长；Qwen3-4B-Instruct 实现了 **7.3%** 的增长。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现更稳健，能够更好地将学到的记忆策略迁移到未见场景；而较小模型（1.7B）虽然在主任务提升明显，但在 OOD 上略有下降。
*   **框架有效性**：证明了通过统一框架和 GRPO 算法，即使在有限算力下也能有效优化复杂的记忆策略。

#### 7. 优点
*   **高度模块化**：即插即用的组件设计允许研究者自由组合不同的提取器、更新器和检索器。
*   **开箱即用**：内置了对 Memory-R1、RMM 和 MemAgent 等前沿算法的支持。
*   **显存友好**：通过 GRPO 算法规避了 PPO 的高显存需求，适合长上下文智能体的训练。
*   **标准化**：为碎片化的记忆研究提供了统一的接口和评估标准。

#### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了三种代表性范式，对于更复杂的分布式记忆或多智能体协同记忆的支持尚待扩展。
*   **训练效率**：虽然支持单卡运行，但在处理极长序列时的训练吞吐量仍有优化空间。
*   **小模型泛化风险**：实验显示小参数模型在经过 RL 优化后可能存在过拟合风险（OOD 性能下降）。

（完）
