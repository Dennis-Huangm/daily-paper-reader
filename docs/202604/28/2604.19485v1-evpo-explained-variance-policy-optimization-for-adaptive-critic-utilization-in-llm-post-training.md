---
title: "EVPO: Explained Variance Policy Optimization for Adaptive Critic Utilization in LLM Post-Training"
title_zh: EVPO：大语言模型后训练中用于自适应 Critic 利用的解释方差策略优化
authors: "Chengjun Pan, Shichun Liu, Jiahang Lin, Dingwei Zhu, Jiazheng Zhang, Shihan Dou, Songyang Gao, Zhenhua Han, Binghai Wang, Rui Zheng, Xuanjing Huang, Tao Gui, Yansong Feng"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19485v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 大语言模型后训练中的强化学习策略优化
tldr: 本研究针对大语言模型强化学习后训练中PPO（有评论家）与GRPO（无评论家）的选择难题，提出EVPO算法。研究发现稀疏奖励下评论家可能引入噪声增加方差，通过卡尔曼滤波理论统一了两者，并利用“解释方差（EV）”作为切换指标。EVPO在训练中动态选择最优基准，在数学推理和智能体交互等任务上均优于固定基准方法，实现了更稳健的策略优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19485v1/fig-001.webp\", \"caption\": \"Figure 4: Training and validation curves across four tasks. Top row: training success rate (smoothed). Bottom row: validation success rate (unsmoothed; stars mark each method’s peak). EVPO tracks or exceeds the best competing method throughout most of training.\", \"page\": 7, \"index\": 1, \"width\": 826, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2604.19485v1/fig-002.webp\", \"caption\": \"Table 1: Best validation success rate across four tasks. Bold indicates the best result per task. For each task, the success rate is calculated based on a total of 512 experimental results, obtained by sampling 16 times for each of the 32 queries.\", \"page\": 7, \"index\": 2, \"width\": 829, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2604.19485v1/fig-003.webp\", \"caption\": \"Figure 2: Training dynamics of PPO on Sokoban. (a) Batch-level EV over training steps. (b) Policy gradient norms. (c) Training success rate. The shaded region in (a–c) marks the early phase where EV is low. (d) Effect of injecting Gaussian noise into a converged critic on FrozenLake.\", \"page\": 3, \"index\": 3, \"width\": 826, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2604.19485v1/fig-004.webp\", \"caption\": \"Table 2: Per-task / per-method hyperparameter overrides. εlo/εhi are the PPO-style clip thresholds. Where DAPO and StarPO-S show different values from the other methods on the same task (e.g., DAPO’s clip-higher asymmetry, or the StarPO-S Sokoban configuration that preserves the original RAGEN recipe), those settings are defining features of the respective algorithms rather than tuning choices, and we therefore do not force them to match the other methods.\", \"page\": 18, \"index\": 4, \"width\": 636, \"height\": 501}, {\"url\": \"assets/figures/arxiv/2604.19485v1/fig-005.webp\", \"caption\": \"Figure 5: Gating trigger probability (êvB > 0, i.e., selection of critic over batch mean) across training.\", \"page\": 8, \"index\": 5, \"width\": 826, \"height\": 189}, {\"url\": \"assets/figures/arxiv/2604.19485v1/fig-006.webp\", \"caption\": \"Figure 6: Threshold sensitivity analysis. Top row: training metrics (smoothed); bottom row: validation metrics (unsmoothed). (a, c, e, g) Best success rate as a function of the EV switching threshold; stars mark τ=0. (b, d, f, h) Full curves under each threshold setting. Performance peaks at τ=0 on both tasks.\", \"page\": 8, \"index\": 6, \"width\": 826, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2604.19485v1/fig-007.webp\", \"caption\": \"Figure 3: Reducing critic variance helps the actor. (a) Cold-start: replacing the critic output with ḠB during early training on Sokoban (left) and FrozenLake (right). (b) Critic Warmup: pre-training the critic before actor training begins.\", \"page\": 4, \"index\": 7, \"width\": 826, \"height\": 185}]"
motivation: 旨在解决强化学习中学习到的评论家在稀疏奖励下可能因估计噪声过大而增加优势函数方差的问题。
method: 提出EVPO，利用解释方差（EV）作为指标，在每一步训练中自适应地在评论家基准和批次平均基准之间切换。
result: 实验证明EVPO在多种任务中一致优于PPO和GRPO，且能准确追踪评论家在训练过程中的成熟度。
conclusion: 解释方差是区分评论家是否有益的有效边界，EVPO通过自适应利用评论家提升了LLM后训练的效率和效果。
---

## 摘要
大语言模型（LLM）后训练中的强化学习（RL）面临一个基本的设计选择：是否使用学习到的 Critic 作为策略优化的基准。经典理论倾向于使用基于 Critic 的方法（如 PPO）来减少方差，然而像 GRPO 这样无 Critic 的替代方案因其简单性和具有竞争力的性能而得到了广泛采用。我们证明，在稀疏奖励设置下，学习到的 Critic 可能会引入超过其捕获的状态信号的估计噪声，从而增加而非减少优势函数的方差。通过将基准选择建模为卡尔曼滤波问题，我们将 PPO 和 GRPO 统一为卡尔曼增益的两个极端，并证明了可从单个训练批次计算的解释方差（EV）识别出了确切的边界：正的 EV 表示 Critic 减少了方差，而零或负的 EV 则表明它放大了方差。基于这一见解，我们提出了解释方差策略优化（EVPO），它在每个训练步骤监控批次级别的 EV，并在基于 Critic 的优势估计和批次平均优势估计之间进行自适应切换，在理论上证明了其在每一步的方差都不大于两者中较优者。在涵盖经典控制、智能体交互和数学推理的四项任务中，无论给定任务中哪种固定基准更强，EVPO 始终优于 PPO 和 GRPO。进一步的分析证实，自适应门控能够追踪训练过程中 Critic 的成熟度，且理论推导出的零阈值在经验上是最佳的。

## Abstract
Reinforcement learning (RL) for LLM post-training faces a fundamental design choice: whether to use a learned critic as a baseline for policy optimization. Classical theory favors critic-based methods such as PPO for variance reduction, yet critic-free alternatives like GRPO have gained widespread adoption due to their simplicity and competitive performance. We show that in sparse-reward settings, a learned critic can inject estimation noise that exceeds the state signal it captures, increasing rather than reducing advantage variance. By casting baseline selection as a Kalman filtering problem, we unify PPO and GRPO as two extremes of the Kalman gain and prove that explained variance (EV), computable from a single training batch, identifies the exact boundary: positive EV indicates the critic reduces variance, while zero or negative EV signals that it inflates variance. Building on this insight, we propose Explained Variance Policy Optimization (EVPO), which monitors batch-level EV at each training step and adaptively switches between critic-based and batch-mean advantage estimation, provably achieving no greater variance than the better of the two at every step. Across four tasks spanning classical control, agentic interaction, and mathematical reasoning, EVPO consistently outperforms both PPO and GRPO regardless of which fixed baseline is stronger on a given task. Further analysis confirms that the adaptive gating tracks critic maturation over training and that the theoretically derived zero threshold is empirically optimal.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《EVPO: Explained Variance Policy Optimization for Adaptive Critic Utilization in LLM Post-Training》** 的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）的强化学习后训练中，究竟应该使用**有 Critic（评论家）**的方法（如 PPO）还是**无 Critic** 的方法（如 GRPO）？
*   **研究动机**：
    *   经典理论认为 Critic 能通过状态相关的基准值减少方差，但在 LLM 的**稀疏奖励**场景下，Critic 在训练初期往往极不稳定。
    *   作者发现，当 Critic 的估计噪声超过它所能捕获的状态信号时，它反而会增加优势函数（Advantage）的方差，导致训练崩溃或效率低下。
    *   目前的研究通常在训练前就固定选择 PPO 或 GRPO，缺乏一种根据 Critic 实时质量进行自适应调整的机制。

### 2. 方法论：EVPO 算法
*   **核心思想**：将优势函数基准的选择建模为一个**卡尔曼滤波（Kalman Filtering）**问题。PPO 和 GRPO 被视为卡尔曼增益的两个极端：PPO 完全信任 Critic，而 GRPO 完全信任批次平均值（Batch-mean）。
*   **关键技术细节**：
    *   **解释方差（Explained Variance, EV）**：定义为 $1 - \frac{Var(G - \hat{V})}{Var(G)}$。它衡量了 Critic 预测值 $\hat{V}$ 解释回报 $G$ 变化的比例。
    *   **理论边界**：论文证明了 **EV = 0** 是 Critic “有益”与“有害”的确切分界点。当 EV > 0 时，Critic 的噪声小于信号，应采用 PPO 模式；当 EV ≤ 0 时，Critic 噪声过大，应退回到 GRPO 模式（使用批次平均值作为基准）。
*   **算法流程**：
    1.  在每个训练迭代中，对当前 Prompt 进行多次采样（Rollout）。
    2.  计算该批次样本的 **Batch-level EV**。
    3.  **自适应门控（Gating）**：如果 EV > 0，使用 Critic 的预测值计算优势；如果 EV ≤ 0，使用该批次的平均回报作为基准。
    4.  更新策略网络和 Critic 网络。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **Sokoban（推箱子）**：多步规划任务。
    *   **FrozenLake（冰湖）**：带有环境随机性的网格任务。
    *   **WebShop**：交互式网页购物智能体任务。
    *   **MATH**：使用 DAPO-Math-17k 数据集的复杂数学推理任务。
*   **基准模型（Base LLM）**：Qwen2.5-3B-Instruct 和 Qwen2.5-7B-Instruct。
*   **对比方法（Benchmarks）**：
    *   **有 Critic 类**：PPO、StarPO-S（针对智能体优化的 PPO 变体）。
    *   **无 Critic 类**：GRPO、DAPO（一种改进的无 Critic 算法）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号（如 A100 或 H100）、显卡数量以及总的训练时长。
*   **框架背景**：实验基于 RAGEN2 框架开发，该框架通常运行在分布式 GPU 集群上，涉及多轮迭代和大规模采样。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从简单控制到复杂推理的 4 类异构任务。
    *   每项任务均对比了 4 种主流强化学习基准方法。
    *   进行了**阈值敏感性分析**（测试 EV 切换点在 -0.2 到 0.2 之间的表现）。
    *   包含**初步干预实验**（如噪声注入、Critic 预热、冷启动切换），证明了 Critic 质量与性能的因果关系。
*   **评价**：实验设计非常充分且客观。通过跨领域的任务验证了算法的普适性，并利用消融实验验证了理论推导的“零阈值”确实是经验最优解。

### 6. 主要结论与发现
*   **EV 是可靠指标**：EV 的正负号能准确预示 Critic 是否会通过引入噪声损害策略梯度。
*   **EVPO 的优越性**：在所有测试任务中，EVPO 的最终性能均优于固定的 PPO 或 GRPO。它能自动在训练初期（Critic 较弱时）表现得像 GRPO，在训练后期（Critic 成熟后）表现得像 PPO。
*   **方差保证**：理论和实验均证明，EVPO 在每一步产生的优势估计方差都不会大于 PPO 和 GRPO 中的较优者。

### 7. 优点与亮点
*   **理论优雅**：利用卡尔曼滤波统一了 PPO 和 GRPO 两个看似对立的范式，提供了坚实的数学支撑。
*   **轻量高效**：计算 Batch-level EV 的开销极低，不需要额外的模型前向传播，易于集成到现有 RL 工作流中。
*   **自适应性强**：解决了 LLM 强化学习中“何时信任 Critic”的长期直觉性难题，使训练过程更加稳健。

### 8. 不足与局限
*   **显存开销**：虽然 EVPO 在计算上很轻量，但它仍需在内存中维护一个 Critic 网络。相比于完全无 Critic 的 GRPO，它在显存占用上没有优势（与 PPO 持平）。
*   **任务覆盖限制**：实验主要集中在**稀疏奖励**和**可验证奖励（RLVR）**场景。对于具有密集奖励（Dense Reward）或复杂奖励模型（RM）的任务，EV 的动态特性可能有所不同，尚待进一步验证。
*   **理论简化**：理论推导基于总体 EV，而实际操作中使用的是批次 EV（有限样本估计），论文未深入探讨小样本下 EV 估计偏差对切换决策的影响。

（完）
