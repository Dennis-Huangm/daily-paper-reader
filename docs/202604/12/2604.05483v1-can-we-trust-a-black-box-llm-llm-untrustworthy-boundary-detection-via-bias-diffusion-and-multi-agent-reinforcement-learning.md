---
title: Can We Trust a Black-box LLM? LLM Untrustworthy Boundary Detection via Bias-Diffusion and Multi-Agent Reinforcement Learning
title_zh: 我们能信任黑盒大语言模型吗？基于偏见扩散与多智能体强化学习的大语言模型不可信边界检测
authors: "Xiaotian Zhou, Di Tang, Xiaofeng Wang, Xiaozhong Liu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05483v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 利用多智能体强化学习探测LLM不可信边界
tldr: 针对大语言模型在特定话题下可能产生偏见或错误回答的问题，本文提出 GMRL-BD 算法，旨在黑盒环境下探测 LLM 的不可信边界。该方法结合维基百科知识图谱与多智能体强化学习，通过有限的查询高效识别模型易产生偏见的议题。研究还发布了涵盖 Llama2、Qwen2 等主流模型的偏见话题数据集，为评估 LLM 可信度提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05483v1/fig-001.webp\", \"caption\": \"Figure 3: Performance of the GMRL-BD Framework Across Six Tasks Using Llama-2, Vicuna, and Falcon Models. These graphs display the number of interactions for each LLM to detect untrustworthy boundaries across six distinct tasks. Each subplot depicts the convergence rate of the models, with lower interaction counts indicates a more efficient performance\", \"page\": 8, \"index\": 1, \"width\": 1056, \"height\": 513}, {\"url\": \"assets/figures/arxiv/2604.05483v1/fig-002.webp\", \"caption\": \"Figure 2: GMRL-BD Model Architecture. Agents interact with nodes on the KG to maximize diffusion navigation rewards and optimize graph representations through policy network. For inference stage, multiple agents explore the KG to detect biases in the black-box LLMs. Starting from different topic nodes, agents coordinate to efficiently identify untrustworthy boundaries, minimizing conflicts of action, with the AgentLinkHub facilitates communication and parameter sharing\", \"page\": 7, \"index\": 2, \"width\": 1056, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2604.05483v1/fig-003.webp\", \"caption\": \"Figure 1: GMRL-BD - Training Stage: Agent maximize rewards and enhance graph representations by interacting with nodes on theKG, target LLMoutcomes, refiningRL policies, and pinpointing biased nodes. Inference Stage:Multiple agents collaboratively navigate the KG to uncover biases in the black-box LLM. Agents coordinate to efficiently detect untrustworthy boundaries and dynamically adjust their exploration paths. For instance, nested bias topics, such as \\\"multiculturalism,\\\" can be effectively identified by leveraging agents information exchange and the hierarchical structure of categories.\", \"page\": 3, \"index\": 3, \"width\": 1002, \"height\": 513}, {\"url\": \"assets/figures/arxiv/2604.05483v1/fig-004.webp\", \"caption\": \"Figure 5: Impact of Increasing Number of Agents on Step Loss and Win Rate in Multi-Agent Collaborative Parameter Sharing\", \"page\": 9, \"index\": 4, \"width\": 432, \"height\": 336}]"
motivation: 为了解决黑盒大语言模型在不同话题下可信度不明、可能产生偏见或错误回答的安全隐患。
method: 提出一种结合维基百科知识图谱与多智能体强化学习的 GMRL-BD 算法，通过偏见扩散机制高效搜索不可信话题边界。
result: 实验表明该算法能在有限查询下精准识别不可信边界，并构建了包含 Llama2、Qwen2 等多个主流模型的偏见话题数据集。
conclusion: 该研究为探测黑盒 LLM 的知识盲区和偏见边界提供了高效工具，对提升大模型的应用安全性具有重要意义。
---

## 摘要
大语言模型（LLMs）在回答各种主题的问题方面展现出了强大的能力。然而，这些模型有时会产生带有偏见、意识形态化或错误的回答，如果在不清楚哪些主题的回答值得信赖的情况下，其应用将受到限制。在本研究中，我们提出了一种名为 GMRL-BD 的新算法，旨在仅通过黑盒访问大语言模型并在特定查询约束下，识别给定模型在主题维度的不可信边界。该算法基于从维基百科提取的通用知识图谱（KG），结合多个强化学习智能体，高效地识别出大语言模型可能产生偏见回答的主题（即知识图谱中的某些节点）。实验证明了该算法的高效性，仅需对大语言模型进行有限次数的查询即可检测到不可信边界。此外，我们还发布了一个包含 Llama2、Vicuna、Falcon、Qwen2、Gemma2 和 Yi-1.5 等主流大语言模型的新数据集，并附带了指示每个模型在哪些主题上可能存在偏见的标签。

## Abstract
Large Language Models (LLMs) have shown a high capability in answering questions on a diverse range of topics. However, these models sometimes produce biased, ideologized or incorrect responses, limiting their applications if there is no clear understanding of which topics their answers can be trusted. In this research, we introduce a novel algorithm, named as GMRL-BD, designed to identify the untrustworthy boundaries (in terms of topics) of a given LLM, with black-box access to the LLM and under specific query constraints. Based on a general Knowledge Graph (KG) derived from Wikipedia, our algorithm incorporates with multiple reinforcement learning agents to efficiently identify topics (some nodes in KG) where the LLM is likely to generate biased answers. Our experiments demonstrated the efficiency of our algorithm, which can detect the untrustworthy boundary with just limited queries to the LLM. Additionally, we have released a new dataset containing popular LLMs including Llama2, Vicuna, Falcon, Qwen2, Gemma2 and Yi-1.5, along with labels indicating the topics on which each LLM is likely to be biased.

---

## 论文详细总结（自动生成）

这是一份关于论文《Can We Trust a Black-box LLM? LLM Untrustworthy Boundary Detection via Bias-Diffusion and Multi-Agent Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）虽然能力强大，但常会产生带有偏见、意识形态或错误的回答。在黑盒环境下（无法获取模型内部参数或梯度），如何高效地识别模型在哪些话题上“不可信”是一个重大挑战。由于话题空间巨大，穷举测试成本极高。本研究的动机在于利用知识图谱（KG）的结构化信息，通过少量查询精准探测 LLM 的“不可信边界”（即模型易产生偏见的话题范围）。

### 2. 方法论：核心思想与关键技术
论文提出了 **GMRL-BD** 算法，其核心思想基于两个假设：**偏见扩散假设**（某一话题的偏见会传播至 KG 中的邻近话题）和**多智能体协作假设**（多个智能体共享信息可提升探测效率）。

*   **核心组件：**
    *   **知识图谱构建：** 基于维基百科分类树构建轻量化 KG，节点代表话题，边代表层级或拓扑关系。
    *   **节点嵌入（Node Embedding）：** 结合 LLM 生成的回答摘要作为初始特征，利用图注意力机制（GAT）和 SAGE 层聚合邻居信息，捕捉话题间的拓扑关联。
    *   **强化学习框架：** 采用 Q-Learning。智能体在 KG 上移动，目标是寻找偏见节点。
    *   **奖励函数（Reward Function）：** 设计了包含“路径奖励”（鼓励向偏见节点靠近）、“距离惩罚”和“重复探索惩罚”的综合函数。
    *   **多智能体规划（AgentLinkHub）：** 在推理阶段，多个智能体从不同起点出发，通过 AgentLinkHub 模块协调行动，避免路径重叠，实现参数共享和高效协作。

### 3. 实验设计
*   **数据集：** 构建了 **LBID（LLM Bias Identification Dataset）**，包含 22,608 个话题、113,040 个问答对。
*   **目标模型：** 涵盖 Llama2、Vicuna、Falcon、Qwen2、Gemma2 和 Yi-1.5。
*   **实验场景：** 划分为经济、教育、移民、政治、人工智能、文化 6 个任务领域。
*   **对比方法（Baselines）：** 深度优先搜索（DFS）、均匀探索策略、无探索惩罚的奖励模型、以及去除 SAGE 或注意力层的消融变体。
*   **评价指标：** 查询交互次数（效率）、偏见节点覆盖率（BNCR/召回率）。

### 4. 资源与算力
*   **硬件环境：** 使用了一台配备单张 **NVIDIA H100 GPU** 的服务器。
*   **处理器：** AMD EPYC 9124 16核处理器（2路 CPU）。
*   **内存：** 376 GiB RAM。
*   **训练细节：** 训练 600 个轮次（episodes），每轮尝试 200 个不同起点，最大步数限制为 2000 步。

### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **偏见传播验证：** 验证了偏见确实会在 KG 邻近节点间传播。
*   **多任务测试：** 在 6 个不同领域的话题图谱上进行了测试。
*   **消融实验：** 验证了 SAGE 层和注意力机制对提升成功率的必要性。
*   **智能体数量实验：** 分析了智能体数量（1-10个）对步数损失和胜率的影响。
*   **充分性评价：** 实验覆盖了主流的开源模型和多个敏感话题领域，对比了多种搜索策略，实验设计较为客观、公平，论证了多智能体协作的优越性。

### 6. 主要结论与发现
*   **高效性：** GMRL-BD 仅需 10-30 次查询即可在包含上千个节点的子图中定位不可信边界。
*   **偏见扩散：** 证实了 LLM 的偏见具有拓扑相关性，邻近话题受影响概率显著更高。
*   **协作优势：** 随着智能体数量增加，探测步数显著下降，证明了 AgentLinkHub 协调机制的有效性。
*   **模型差异：** 不同 LLM 在不同话题上的不可信边界差异巨大，Llama2 和 Falcon 在某些任务中表现出更强的边界识别稳定性。

### 7. 优点
*   **黑盒适用性：** 无需访问模型权重，适用于任何闭源或黑盒 LLM。
*   **低成本：** 极大地减少了探测偏见所需的 API 调用次数或计算资源。
*   **结构化视角：** 首次将维基百科分类体系与强化学习结合，用于系统性地扫描模型知识盲区。
*   **资源贡献：** 发布了 LBID 数据集，为后续可信 LLM 研究提供了基准。

### 8. 不足与局限
*   **话题粒度限制：** 目前主要基于预定义的话题节点，可能无法捕捉到极其细微或跨领域的复杂偏见。
*   **KG 依赖性：** 探测效果高度依赖于维基百科分类树的质量和更新速度。
*   **偏见注入偏差：** 实验中的“偏见”是通过人工注入生成的，可能与模型在自然训练中产生的原生偏见在分布上存在差异。
*   **动态性不足：** 尚未充分讨论模型在持续学习或微调后，不可信边界动态漂移的实时追踪问题。

（完）
