---
title: "ContextBudget: Budget-Aware Context Management for Long-Horizon Search Agents"
title_zh: ContextBudget：面向长程搜索智能体的预算感知上下文管理
authors: "Yong Wu, YanZhao Zheng, TianZe Xu, ZhenTao Zhang, YuanQiang Yu, JiHuai Zhu, Chao Ma, BinBin Lin, BaoHua Dong, HangCheng Zhu, RuoHui Huang, Gang Yu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01664v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: LLM智能体与强化学习在上下文管理中的集成
tldr: 针对LLM智能体在长程推理中面临的上下文窗口限制与成本压力，本文提出预算感知上下文管理（BACM）框架。该框架将上下文管理视为受限序列决策问题，通过课程强化学习（BACM-RL）训练智能体根据剩余预算动态决定压缩历史信息的时机与比例。实验表明，该方法在多目标问答和网页浏览任务中显著优于现有基准，尤其在极低预算和高复杂度场景下表现出极强的鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决长程任务中不断增长的交互历史与有限的上下文预算（内存、延迟和成本）之间的权衡难题。
method: 将上下文管理建模为受限序列决策，并采用端到端的课程强化学习训练智能体学习不同预算下的动态压缩策略。
result: 在复杂设置下比基线方法提升超过1.6倍，且在预算缩减时仍能维持高性能，而传统方法性能则显著下降。
conclusion: BACM-RL证明了动态预算感知策略在提升长程搜索智能体效率和任务成功率方面的核心价值。
---

## 摘要
基于大语言模型（LLM）的智能体在长程推理方面展现出巨大潜力，但其上下文长度受限于部署因素（如内存、延迟和成本），从而导致受限的上下文预算。随着交互历史的增长，这引发了在保留历史信息与维持在上下文限制之内之间的权衡。为了应对这一挑战，我们提出了预算感知上下文管理（BACM），它将上下文管理建模为具有上下文预算约束的序列决策问题。该方法使智能体能够在纳入新观测之前评估可用预算，并决定何时以及压缩多少交互历史。我们进一步开发了 BACM-RL，这是一种基于课程学习的端到端强化学习方法，用于学习不同上下文预算下的压缩策略。在组合式多目标问答和长程网页浏览基准测试上的实验表明，BACM-RL 在不同模型规模和任务复杂度下均一致优于现有方法。在复杂设置中，它比强基准方法实现了超过 1.6\times 的提升，并且在预算缩减时仍能保持显著优势，而大多数方法在此时表现出性能下降的趋势。

## Abstract
LLM-based agents show strong potential for long-horizon reasoning, yet their context size is limited by deployment factors (e.g., memory, latency, and cost), yielding a constrained context budget. As interaction histories grow, this induces a trade-off between retaining past information and staying within the context limit. To address this challenge, we propose Budget-Aware Context Management (BACM), which formulates context management as a sequential decision problem with a context budget constraint. It enables agents to assess the available budget before incorporating new observations and decide when and how much of the interaction history to compress. We further develop BACM-RL, an end-to-end curriculum-based reinforcement learning approach that learns compression strategies under varying context budgets. Experiments on compositional multi-objective QA and long-horizon web browsing benchmarks show that BACM-RL consistently outperforms prior methods across model scales and task complexities, achieving over $1.6\times$ gains over strong baselines in high-complexity settings, while maintaining strong advantages as budgets shrink, where most methods exhibit a downward performance trend.

---

## 论文详细总结（自动生成）

以下是对论文《ContextBudget: Budget-Aware Context Management for Long-Horizon Search Agents》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 智能体在执行长程（Long-horizon）任务时，交互历史会迅速累积，而实际部署中受限于内存、延迟和成本，上下文窗口（Context Budget）是有限的。
*   **研究动机**：现有的上下文压缩方法多为“预算无关”的静态操作（如定期压缩或满额触发），这会导致两种失败模式：
    1.  **过度压缩**：在预算充足时丢失关键证据。
    2.  **压缩不足**：在预算紧张时导致上下文溢出，引发推理崩溃。
*   **整体含义**：本文旨在将上下文管理建模为一个**受限的序列决策问题**，让智能体学会根据剩余预算动态调整压缩策略。

### 2. 方法论
核心思想是 **BACM (Budget-Aware Context Management)** 框架，通过强化学习训练智能体自主管理上下文。
*   **预算感知状态与延迟加载**：
    *   状态表示为 $b_t = (s_t, r_t, |o_t|)$，其中 $r_t$ 是剩余预算，$|o_t|$ 是待加载观测的大小（内容暂不可见）。
    *   **延迟加载机制**：智能体在看到新观测内容前，先根据其大小和剩余空间决定如何调整现有上下文，确保不会溢出。
*   **提交块聚合机制 (Commit-Block Aggregation)**：
    *   将上下文划分为语义连续的块。
    *   提供三种动作：`NULL`（不压缩）、`PARTIAL`（选择性聚合部分块）、`FULL`（全量聚合）。
*   **BACM-RL 训练算法**：
    *   **算法基础**：采用 GRPO（Group Relative Policy Optimization）进行端到端优化。
    *   **课程学习 (Curriculum Learning)**：训练过程中上下文预算从 8k 逐渐收缩至 4k，强制智能体在压力增大时进化出更高效的压缩能力。
    *   **奖励设计**：仅对任务成功且**未违反预算约束**的轨迹给予奖励。

### 3. 实验设计
*   **数据集/场景**：
    1.  **组合式多目标问答 (Multi-objective QA)**：基于 NQ、TriviaQA、HotpotQA 等构建，将多个独立问题组合成一个长程任务（包含 2 到 32 个目标）。
    2.  **BrowseComp-Plus**：一个具有验证语料库的长程网页浏览基准测试。
*   **对比方法**：
    *   **无管理方法**：ReAct, Search-R1 (纯 RL 搜索)。
    *   **反应式管理**：Summary Agent (缓冲区满时触发摘要)。
    *   **主动式管理**：MEM1 (每步迭代整合状态)。
*   **评估指标**：F1 分数总和（针对多目标 QA）、LLM-as-a-Judge 准确率（针对网页浏览）。

### 4. 资源与算力
*   **硬件设施**：使用了 16 个节点，每个节点配备 8 张 GPU（共计 **128 张 GPU**）。
*   **训练细节**：
    *   使用 `verl` 强化学习框架。
    *   训练步数为 300 步，全局 Batch Size 为 128。
    *   模型规模涵盖了 Qwen2.5-7B-Instruct 和 Qwen3-30B-A3B-Instruct。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从简单（2 目标）到极高难度（32 目标）的任务梯度。
    *   测试了从 4k 到 16k 不同的上下文预算限制。
    *   进行了详尽的消融实验，验证了预算信号、延迟加载和课程学习的必要性。
*   **充分性评价**：实验设计非常充分且客观。通过在不同规模（7B/30B）的基座模型上验证，证明了方法的普适性。对比基准涵盖了目前主流的各类上下文管理范式。

### 6. 主要结论与发现
*   **性能领先**：BACM-RL 在所有预算设置下均优于基线。在 32 目标的极端复杂场景下，性能提升高达 **1.6x 至 5x**。
*   **预算鲁棒性**：当预算从 16k 缩减到 4k 时，传统方法性能剧烈下降，而 BACM-RL 表现出极强的稳定性。
*   **效率平衡**：BACM-RL 并非通过增加压缩频率获胜，而是在简单任务中减少压缩以保持保真度，在复杂任务中精准压缩以腾出空间。

### 7. 优点
*   **动态自适应**：改变了以往“死板”的压缩规则，使智能体具备了类似人类的“资源意识”。
*   **端到端优化**：将压缩决策与下游任务成功率直接挂钩，避免了摘要过程中的信息损失。
*   **课程学习设计**：通过逐渐收紧预算的训练策略，有效解决了强化学习在受限空间下搜索难的问题。

### 8. 不足与局限
*   **算力需求高**：训练过程依赖大规模 GPU 集群（128 张卡），对于普通研究者门槛较高。
*   **压缩粒度**：目前的聚合操作是在“块”级别进行的，未来可能需要更细粒度（如 Token 或跨度级别）的显著性建模。
*   **奖励稀疏性**：依赖轨迹级别的奖励，对于极长路径的信用分配（Credit Assignment）可能存在挑战。
*   **应用范围**：主要在搜索和问答类任务中验证，在多模态或开放式工具调用场景下的表现尚待观察。

（完）
