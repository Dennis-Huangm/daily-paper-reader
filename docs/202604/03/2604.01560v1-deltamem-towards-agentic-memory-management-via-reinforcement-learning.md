---
title: "DeltaMem: Towards Agentic Memory Management via Reinforcement Learning"
title_zh: DeltaMem：通过强化学习实现智能体化记忆管理
authors: "Qi Zhang, Shen Huang, Chu Liu, Shouqing Yang, Junbo Zhao, Haobo Wang, Pengjun Xie"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01560v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 通过强化学习实现的智能体记忆管理系统
tldr: DeltaMem是一个针对个性化记忆管理的智能体系统，旨在解决多智能体框架在对话场景中信息丢失和鲁棒性差的问题。该研究将记忆管理建模为单智能体端到端任务，通过合成带有操作级标签的对话数据集，并引入基于编辑距离的奖励机制进行强化学习训练。实验证明，DeltaMem在LoCoMo等多个长期记忆基准测试中均优于现有基准。
selection_source: fresh_fetch
motivation: 现有的多智能体记忆管理框架在处理个性化记忆时存在信息丢失和跨场景脆弱性等性能瓶颈。
method: 提出DeltaMem系统，将记忆管理简化为单智能体端到端任务，并结合合成数据集与基于记忆编辑距离的强化学习框架进行优化。
result: 实验表明，DeltaMem在LoCoMo、HaluMem和PersonaMem等多个长期记忆基准测试中均显著优于现有的产品级基准。
conclusion: 通过将记忆管理转化为端到端强化学习任务，DeltaMem有效提升了智能体在处理长期个性化记忆时的准确性和稳定性。
---

## 摘要
以人格为中心的记忆（persona-centric memory）的最新进展揭示了多智能体系统在管理个人记忆方面的强大能力，特别是在对话场景中。然而，这些复杂的框架往往面临信息丢失的问题，且在不同场景下表现脆弱，导致性能欠佳。在本文中，我们提出了 DeltaMem，这是一个智能体化记忆管理系统，它将以人格为中心的记忆管理建模为单智能体设置下的端到端任务。为了进一步提高智能体化记忆管理器的性能，我们从人类记忆的演化中汲取灵感，合成了一个用户-助手对话数据集，并附带相应的操作级记忆更新标签。在此基础上，我们引入了一种新颖的基于记忆的 Levenshtein 距离来形式化记忆更新奖励，并提出了一种定制的强化学习框架，以进一步增强 DeltaMem 的管理能力。广泛的实验表明，无论是无需训练的还是经过强化学习训练的 DeltaMem，在包括 LoCoMo、HaluMem 和 PersonaMem 在内的多种长期记忆基准测试中，均优于所有产品级基准模型。

## Abstract
Recent advances in persona-centric memory have revealed the powerful capability of multi-agent systems in managing persona memory, especially in conversational scenarios. However, these complex frameworks often suffer from information loss and are fragile across varying scenarios, resulting in suboptimal performance. In this paper, we propose DeltaMem, an agentic memory management system that formulates persona-centric memory management as an end-to-end task within a single-agent setting. To further improve the performance of our agentic memory manager, we draw inspiration from the evolution of human memory and synthesize a user-assistant dialogue dataset along with corresponding operation-level memory updating labels. Building on this, we introduce a novel Memory-based Levenshtein Distance to formalize the memory updating reward, and propose a tailored reinforcement learning framework to further enhance the management capabilities of DeltaMem. Extensive experiments show that both training-free and RL-trained DeltaMem outperform all product-level baselines across diverse long-term memory benchmarks, including LoCoMo, HaluMem, and PersonaMem.

---

## 论文详细总结（自动生成）

以下是对论文《DeltaMem: Towards Agentic Memory Management via Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的个性化记忆管理系统（如 Mem0）多采用**多智能体（Multi-agent）**架构，将任务拆分为提取、检索和更新三个独立步骤。
*   **研究动机**：
    *   **信息损耗**：多智能体间的离散消息传递会导致上下文信息的“级联衰减”，尤其是在长期对话中，碎片化的会话切片加剧了这种损失。
    *   **鲁棒性差**：模块化设计在面对复杂多变的对话场景时表现脆弱。
    *   **缺乏高质量数据**：现有研究缺乏操作级别的记忆更新标注数据，导致强化学习（RL）训练时奖励信号稀疏，难以收敛。

### 2. 方法论
DeltaMem 将记忆管理重新定义为**单智能体端到端任务**，核心思想是让智能体自主决定检索什么以及如何更新记忆。

*   **DeltaMem 框架**：采用 **ReAct（Reasoning and Acting）** 范式。智能体在接收新对话时，通过推理调用搜索工具检索相关记忆，并直接输出一系列更新操作（Add, Update, None），从而维护一个动态的记忆库。
*   **记忆演化引导的数据合成**：受人类记忆演化启发，设计了四步合成流程：
    1.  **档案增强**：基于种子人格生成详细用户画像。
    2.  **事件生成**：生成 25 个跨越 1-3 年的长期时间线事件。
    3.  **记忆生成**：模拟记忆的“事件内演化”（属性变化）和“事件间演化”（回顾性认知改变），生成操作级标签。
    4.  **对话生成**：基于上述信息合成高质量的用户-助手对话。
*   **基于状态转移的强化学习（RL via State Transition）**：
    *   **核心创新：基于记忆的 Levenshtein 距离**。不再直接对比操作序列，而是对比操作后的“记忆状态”。
    *   **语义对齐**：利用最优传输（Optimal Transport）解决预测状态与目标状态间的语义等价性。
    *   **局部词汇忠实度（Local Lexical Fidelity）**：引入关键词覆盖率指标，确保事实细节的准确性。
    *   **奖励函数**：结合格式奖励、检索奖励和基于上述距离计算的软 F1 分数（状态转移奖励），使用 **GRPO** 算法进行训练。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **LoCoMo**：评估极长距离对话记忆（平均 9K token）。
    *   **HaluMem**：评估记忆系统中的幻觉行为及操作级准确性。
    *   **PersonaMem**：评估动态用户画像追踪和个性化响应能力。
*   **对比方法**：
    *   **产品级/开源基准**：Mem0, Zep, LightMem, LangMem, A-Mem, Supermemory 等。
    *   **训练模型**：Memory-R1（基于下游任务准确率作为奖励的 RL 模型）。
*   **后端模型**：使用 Qwen3-4B/8B 作为基础模型，对比 GPT-4o-mini 等。

### 4. 资源与算力
*   **数据合成**：使用 `gpt-oss-120b` 作为骨干模型，耗费约 **500 GPU 小时**。
*   **模型训练**：
    *   **硬件**：1 个节点，配备 **8 张 Nvidia A100 GPU**。
    *   **规模**：在 7,500 个合成样本上训练 1 个 epoch。
    *   **框架**：使用 VeRL 强化学习框架。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流长期记忆基准上进行了全面测试，涵盖了单跳、多跳、时间维度和开放域等多个子项。
*   **消融实验**：
    *   验证了“局部词汇忠实度”对事实提取能力的提升。
    *   分析了语义匹配阈值 $\tau$ 对性能的影响。
    *   观察了随会话数量增加（时间推移）系统性能的稳定性。
*   **充分性评价**：实验设计较为充分，不仅对比了无需训练的（Training-free）版本，还展示了 RL 训练后的增益，且对比对象涵盖了当前最先进的开源和商业记忆系统，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **单智能体优越性**：即使是不经训练的 DeltaMem，其性能也优于许多复杂的多智能体系统，证明了端到端设计的有效性。
*   **RL 显著增益**：经过 RL 训练的 DeltaMem-8B-RL 在所有基准上均达到 SOTA。例如在 LoCoMo 上，其 LLM-as-a-Judge 得分比最强基准高出约 9 分。
*   **解决检索冗余**：RL 训练后的模型能更自主地选择检索策略，显著降低了每个记忆条目的查询次数（QPM），缓解了重度检索带来的开销问题。
*   **时间稳定性**：随着会话增加，DeltaMem 的准确率下降曲线比 LightMem 等基准更平缓，表现出更强的长期稳定性。

### 7. 优点与亮点
*   **架构简化**：打破了传统的“提取-检索-更新”三段式教条，减少了信息在模块间的流失。
*   **奖励机制创新**：提出的“基于记忆的 Levenshtein 距离”解决了自然语言记忆状态难以量化评估的难题，为记忆系统的 RL 训练提供了更密集的信号。
*   **高质量合成数据**：模拟人类记忆演化逻辑生成数据，解决了操作级标注缺失的问题。

### 8. 不足与局限
*   **合成数据依赖**：模型高度依赖合成数据进行训练，虽然缓解了隐私问题，但可能存在合成数据特有的偏差（如社会刻板印象）。
*   **计算复杂度**：在计算奖励时涉及最优传输对齐和关键词匹配，在大规模在线训练时可能存在一定的计算开销。
*   **应用限制**：目前主要聚焦于“以人格为中心”的记忆，对于更通用的知识库管理或多用户协作记忆的适用性尚待验证。

（完）
