---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:ag-rl"]
score: 9.0
evidence: 应用强化学习优化智能体框架中的内存操作
tldr: MemFactory是一个专为记忆增强型智能体设计的统一推理与训练框架。针对现有记忆管理实现碎片化且缺乏标准的问题，它借鉴LLaMA-Factory的成功经验，将记忆生命周期抽象为模块化组件，支持通过“乐高式”架构构建自定义智能体。该框架集成了GRPO算法以优化记忆策略，并支持Memory-R1等前沿范式，显著提升了智能体在长期任务中的表现。
selection_source: fresh_fetch
motivation: 现有的记忆增强型智能体实现高度碎片化且缺乏统一的基础设施，阻碍了记忆管理策略的集成、训练和评估。
method: 提出模块化框架MemFactory，将记忆操作抽象为可插拔组件，并利用GRPO算法基于环境奖励对内部记忆管理策略进行微调。
result: "在MemAgent架构上的实验表明，MemFactory相比基础模型平均性能有所提升，最高相对增益达到14.8%。"
conclusion: MemFactory通过提供标准化且易用的基础设施，降低了记忆驱动型AI智能体的开发门槛，为未来相关研究奠定了基础。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备能力的长期 AI 智能体至关重要。最近，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为最近的前沿范式（包括 Memory-R1、RMM 和 MemAgent）提供开箱即用的支持。我们使用公开的训练和评估数据，在开源的 MemAgent 架构上对 MemFactory 进行了实证验证。在各个评估集中，MemFactory 的性能平均优于相应的基座模型，相对提升最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：为了让大语言模型（LLM）进化为具备长期上下文维持和个性化能力的 AI 智能体，记忆增强技术至关重要。
*   **核心问题**：虽然利用强化学习（RL）优化记忆操作（如提取、更新、检索）已成为前沿趋势（如 Memory-R1, MemAgent 等），但现有实现高度碎片化、任务特定且缺乏统一的基础设施。
*   **整体含义**：本文推出了 **MemFactory**，这是首个专为记忆增强型智能体设计的统一、模块化训练与推理框架。它旨在通过标准化的“乐高式”架构，降低记忆驱动型智能体的开发、训练和评估门槛。

### 2. 方法论
MemFactory 采用四层模块化架构：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作，包括：
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：对比新旧记忆，执行增删改（ADD/DEL/UPDATE/NONE）操作。
    *   **Retriever**：执行语义检索或利用推理模型进行重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **智能体层 (Agent Layer)**：作为策略执行器，通过组合上述模块构建自定义智能体，支持训练时的 Rollout（轨迹生成）和纯推理模式。
*   **环境层 (Environment Layer)**：负责数据加载和奖励管理。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期长文本）两种环境，并支持格式奖励和“LLM-as-a-Judge”等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO（群体相对策略优化）** 算法。该算法通过组内奖励归一化估计优势函数（Advantage），无需额外的 Critic 模型，显著降低了长文本训练时的显存开销。

### 3. 实验设计
*   **实验对象**：以 **MemAgent**（循环记忆架构）作为验证案例。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/Benchmark**：使用 MemAgent 公开的训练和评估数据。
    *   **主任务测试集**：`eval_50` 和 `eval_100`。
    *   **分布外（OOD）测试集**：`eval_fwe_16384`。
*   **对比方法**：对比了各基座模型在经过 MemFactory RL 微调前后的性能表现。

### 4. 资源与算力
*   **硬件设备**：整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步（valid steps）。为了适应长文本任务并提高效率，对训练样本进行了简化（每样本包含 50-80 个文档，约为原始长度的三分之一）。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（1.7B 和 4B）在三个不同性质的测试集上进行了实验。
*   **客观性保障**：所有评分均基于 **4 次独立试验的平均值 (avg@4)**，以减少生成随机性带来的偏差。
*   **充分性评价**：实验验证了框架在不同规模模型上的有效性，并考察了泛化能力（OOD 测试）。虽然实证部分主要集中在 MemAgent 这一种范式上，但框架本身已内置了对 Memory-R1 和 RMM 的支持，展示了较好的通用性。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 RL 微调后表现更稳健，不仅在主任务上进步明显，在 OOD 测试集中也实现了正向增长；而较小模型（1.7B）在 OOD 任务上出现了轻微性能下降。
*   **可行性验证**：证明了在有限算力（单卡）下，通过 GRPO 优化复杂的长上下文记忆策略是切实可行的。

### 7. 优点
*   **高度模块化**：通过“乐高式”设计，研究人员可以像使用 LLaMA-Factory 一样轻松更换检索器或更新策略。
*   **资源友好**：原生支持 GRPO，极大地缓解了记忆增强任务中常见的显存瓶颈。
*   **开箱即用**：预置了 Memory-R1、RMM 和 MemAgent 等前沿算法的实现，方便研究者快速复现和改进。

### 8. 不足与局限
*   **实验覆盖面**：虽然框架支持多种范式，但论文中的实证分析主要聚焦于 MemAgent，缺乏对 Memory-R1 等其他内置范式的详细对比实验。
*   **训练效率**：作者提到在训练效率方面仍有进一步优化的空间。
*   **小模型泛化风险**：实验显示小参数模型在经过特定任务的 RL 微调后，可能存在分布外泛化能力受损的风险。

（完）
