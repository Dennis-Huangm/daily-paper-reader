---
title: "APEX-EM: Non-Parametric Online Learning for Autonomous Agents via Structured Procedural-Episodic Experience Replay"
title_zh: APEX-EM：通过结构化程序-情境经验回放实现自主智能体的非参数化在线学习
authors: "Pratyay Banerjee, Masud Moshtaghi, Ankit Chadha"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29093v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 带有奖励信号的 LLM 智能体非参数在线学习
tldr: 针对LLM智能体在重复任务中缺乏持久程序化记忆的问题，本文提出APEX-EM框架。该框架通过非参数化在线学习，利用结构化经验回放（包含计划、错误分析和质量评分）实现经验的积累与重用。其核心是PRGII工作流和双重经验存储，支持跨领域结构化迁移。实验显示在代码生成和知识问答等任务上性能大幅提升，显著优于现有基准。
selection_source: fresh_fetch
motivation: 解决LLM智能体在面对结构相似任务时无法复用既有经验、需从零推导方案的问题。
method: 提出一种包含结构化经验表示、PRGII工作流及结合语义与结构匹配的混合检索机制的非参数化学习框架。
result: "在KGQAGen-10k和BigCodeBench等基准上分别实现了48.3%和29.4%的准确率提升，超越了现有SOTA方法。"
conclusion: 结构化程序化记忆能显著增强智能体的在线学习能力，且不同任务对反馈信号的依赖程度存在差异。
---

## 摘要
基于大语言模型（LLM）的自主智能体缺乏持久的程序性记忆：即使之前已经解决过结构相同的任务，它们仍会从头开始推导解决方案。我们提出了 APEX-EM，这是一种非参数化在线学习框架，能够在不修改模型权重的情况下积累、检索和重用结构化程序计划。APEX-EM 引入了：(1) 一种结构化经验表示，编码了每次执行的完整程序-情境轨迹——包括规划步骤、中间产物、带有错误分析的迭代历史以及质量评分；(2) 一种包含任务验证器（Task Verifiers）的“规划-检索-生成-迭代-摄取”（PRGII）工作流，提供多维奖励信号；(3) 一种具有双重结果的经验记忆（Experience Memory），采用结合了语义搜索、结构特征匹配和计划有向无环图（DAG）遍历的混合检索方式——实现了在词汇无重叠但操作结构相似的任务之间的跨领域迁移。成功的经验作为正向上下文示例；失败的经验则作为带有结构化错误标注的负向示例。我们使用 Claude Sonnet 4.5 和 Opus 4.5 在 BigCodeBench、KGQAGen-10k 和 Humanity's Last Exam (HLE) 上进行了评估。在 KGQAGen-10k 上，APEX-EM 达到了 89.6% 的准确率，而无记忆情况下为 41.3%（提升 48.3 个百分点），超过了真值检索（oracle-retrieval）的上限（84.9%）。在 BigCodeBench 上，其成功率（SR）从 53.9% 的基准提升至 83.3%（提升 29.4 个百分点），在可比的冻结骨干网络条件下，超过了 MemRL +11.0 个百分点的增益（注：我们在分析中对骨干网络的差异进行了控制）。在 HLE 上，实体图检索从 25.2% 提升至 48.0%（提升 22.8 个百分点）。消融实验表明，各组件的价值取决于具体任务：丰富的评判反馈对代码生成的影响微乎其微，但对结构化查询至关重要（提升 10.3 个百分点），而二值信号迭代可以部分补偿较弱的反馈。

## Abstract
LLM-based autonomous agents lack persistent procedural memory: they re-derive solutions from scratch even when structurally identical tasks have been solved before. We present \textbf{APEX-EM}, a non-parametric online learning framework that accumulates, retrieves, and reuses structured procedural plans without modifying model weights. APEX-EM introduces: (1) a \emph{structured experience representation} encoding the full procedural-episodic trace of each execution -- planning steps, artifacts, iteration history with error analysis, and quality scores; (2) a \emph{Plan-Retrieve-Generate-Iterate-Ingest} (PRGII) workflow with Task Verifiers providing multi-dimensional reward signals; and (3) a \emph{dual-outcome Experience Memory} with hybrid retrieval combining semantic search, structural signature matching, and plan DAG traversal -- enabling cross-domain transfer between tasks sharing no lexical overlap but analogous operational structure. Successful experiences serve as positive in-context examples; failures as negative examples with structured error annotations.   We evaluate on BigCodeBench~\cite{zhuo2025bigcodebench}, KGQAGen-10k~\cite{zhang2025kgqagen}, and Humanity's Last Exam~\cite{phan2025hle} using Claude Sonnet 4.5 and Opus 4.5. On KGQAGen-10k, APEX-EM achieves 89.6\% accuracy versus 41.3\% without memory (+48.3pp), surpassing the oracle-retrieval upper bound (84.9\%). On BigCodeBench, it reaches 83.3\% SR from a 53.9\% baseline (+29.4pp), exceeding MemRL's~\cite{memrl2025} +11.0pp gain under comparable frozen-backbone conditions (noting backbone differences controlled for in our analysis). On HLE, entity graph retrieval reaches 48.0\% from 25.2\% (+22.8pp). Ablations show component value is task-dependent: rich judge feedback is negligible for code generation but critical for structured queries (+10.3pp), while binary-signal iteration partially compensates for weaker feedback.

---

## 论文详细总结（自动生成）

### APEX-EM 论文结构化总结

#### 1. 核心问题与研究动机
*   **核心问题**：当前的 LLM 智能体在程序层面是“无状态”的。即使面对结构完全相同的任务，它们每次都会从零开始推导方案，无法有效积累和复用过去的执行经验。
*   **研究动机**：
    *   **现有记忆机制的局限**：如 MemRL 等现有方法通常存储扁平的文本摘要或标量 Q 值，丢失了执行过程中的程序化结构（如规划步骤、错误轨迹）。
    *   **跨领域迁移能力差**：依赖语义相似度的检索在词汇不重叠但逻辑结构相似的任务间（如体育数据查询 vs. 金融数据查询）往往失效。
    *   **缺乏负面经验利用**：大多数系统仅存储成功案例，忽略了失败案例作为“反面教材”的价值。

#### 2. 方法论：APEX-EM 框架
APEX-EM 是一种**非参数化在线学习框架**，其核心思想是通过外部存储积累结构化经验，而非修改模型权重。
*   **程序化知识图谱 (PKG)**：将经验、实体、子任务和操作（Operation）组织为有向类型图。
*   **结构化经验表示**：每个经验包含五个反射层：目标（Goal）、程序（Procedure）、证据（Evidence）、执行轨迹（Trace）、错误注册（Error Registry）和补丁（Patch）。
*   **PRGII 工作流**：
    1.  **Plan（规划）**：分解任务，提取“结构化特征”（Structural Signature，即抽象操作序列）。
    2.  **Retrieve（检索）**：混合检索机制，结合语义搜索、结构特征匹配（基于最长公共子序列 LCS）和图遍历。
    3.  **Generate（生成）**：基于检索到的正向（成功）和负向（失败）示例生成代码或查询。
    4.  **Iterate（迭代）**：通过任务验证器进行自我修正。
    5.  **Ingest（摄取）**：由 Teacher 模型评估质量，合格后存入 PKG，实现闭环学习。
*   **双重结果存储**：成功经验作为正向 In-context 示例，失败经验则带有结构化错误标注，作为避坑指南。

#### 3. 实验设计
*   **数据集/场景**：
    *   **BigCodeBench (BCB)**：复杂的代码生成，涉及多库调用。
    *   **KGQAGen-10k**：基于 Wikidata 的多步结构化查询生成（SPARQL）。
    *   **Humanity’s Last Exam (HLE)**：跨学科的专家级知识推理。
*   **基准与对比方法**：
    *   **对比方法**：No Memory (Baseline), RAG, Self-RAG, Mem0, MemP, Reflexion, 以及当前的 SOTA 方法 **MemRL**。
    *   **骨干网络**：Claude Sonnet 4.5, Claude Opus 4.5, GPT-4o (用于对比)。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号或总算力消耗（如 GPU-hours）。
*   **实现细节**：提到了实验运行了 10 个 Epoch，使用了并行处理（Parallelism p=3 或 5），并使用 Neptune 进行实验监控。模型调用主要通过 API（Claude 系列和 Amazon Titan Embeddings）完成。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个迥异的领域（代码、知识图谱、通用推理）进行了大规模测试。
    *   **消融实验**：设计了 A0-A5 等多组消融，分别验证了记忆、评判反馈（Judge）、迭代循环、检索策略（语义 vs. 实体图）的贡献。
    *   **跨任务迁移测试**：在 342 个未见过的 BCB 任务和 1079 个 KGQA 问题上验证了泛化能力。
*   **充分性评价**：实验设计较为全面，通过控制变量法（相同骨干网络、相同数据集切分）与 MemRL 等基准进行了公平对比，客观展示了结构化特征在低语义相关场景下的优势。

#### 6. 主要结论与发现
*   **性能飞跃**：在 KGQA 任务上，准确率从 41.3% 提升至 89.6%（+48.3pp），超过了真值检索上限。在 BCB 上提升了 29.4pp。
*   **任务依赖性**：丰富的评判反馈对代码生成增益较小（二值信号已足够），但对结构化查询至关重要（+10.3pp）。
*   **效率提升**：随着记忆积累，智能体解决任务所需的迭代次数显著减少（KGQA 任务减少了 37-43%），实现了从“自我修正”到“经验引导”的转变。
*   **结构化迁移**：结构特征匹配允许智能体在零词汇重叠的情况下复用解题逻辑。

#### 7. 优点
*   **创新的结构化表示**：通过 PKG 将程序逻辑与实体知识解耦，支持知识的自动更新和传播。
*   **结构特征匹配**：解决了传统 RAG 依赖表面语义相似性的痛点，增强了跨领域迁移能力。
*   **负面经验的深度利用**：通过 Error Registry 和 Patch 层，将失败转化为具体的编程/逻辑约束。
*   **非参数化在线学习**：无需微调即可让智能体在部署过程中不断进化。

#### 8. 不足与局限
*   **模型依赖**：高性能表现高度依赖于强大的骨干网络（如 Claude 4.5）和高质量的 Teacher 模型。
*   **分类体系局限**：目前的结构化特征分类（Taxonomy）仍包含部分人工设计成分，自动化发现机制尚不完善。
*   **推理开销**：虽然减少了迭代次数，但复杂的混合检索和多层反射增加了单次请求的上下文长度和处理复杂度。
*   **时间偏差风险**：论文标注日期为 2026 年（可能为预印本或特定设定），其实验结果在当前主流模型环境下的可复现性需进一步验证。

（完）
