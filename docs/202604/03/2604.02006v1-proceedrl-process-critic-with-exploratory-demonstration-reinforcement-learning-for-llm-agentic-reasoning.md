---
title: "ProCeedRL: Process Critic with Exploratory Demonstration Reinforcement Learning for LLM Agentic Reasoning"
title_zh: ProCeedRL：结合探索性演示与过程评论家的强化学习，用于大语言模型智能体推理
authors: "Jingyue Gao, Yanjiang Guo, Xiaoshuai Chen, Jianyu Chen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02006v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 用于多轮任务中大语言模型智能体推理的强化学习
tldr: 针对大语言模型在多轮智能体任务中因错误累积导致探索失效的问题，本文提出ProCeedRL框架。该方法通过引入过程级评论家实时监控交互，并结合基于反思的探索性演示进行主动干预，打破了传统被动探索的局限。实验证明，ProCeedRL显著提升了探索效率，在复杂深度搜索和具身任务中表现优异，有效解决了长程交互中的推理难题。
selection_source: fresh_fetch
motivation: 旨在解决多轮智能体任务中，次优动作引发的错误累积反馈循环导致标准探索策略失效的问题。
method: 提出一种结合过程级评论家实时监控与反思性演示引导的主动干预式强化学习框架ProCeedRL。
result: 该方法显著超越了模型的探索性能瓶颈，在复杂深度搜索和具身任务中实现了更优的性能和更高的探索效率。
conclusion: 通过将探索从被动选择转为主动干预，ProCeedRL为提升LLM智能体在长程、随机环境下的推理能力提供了有效方案。
---

## 摘要
强化学习 (RL) 显著增强了大语言模型 (LLM) 的推理能力，但由于交互的长程特性和环境反馈的随机性，将其应用于多轮智能体任务仍然具有挑战性。我们识别出智能体探索中的一种结构性失效模式：次优动作会引发带有噪声的观测，进而形成误导性的上下文，这进一步削弱了后续的决策能力，使恢复变得愈发困难。这种错误的累积反馈循环使得标准的探索策略失效，且容易受到模型推理和环境随机性的影响。为了缓解这一问题，我们提出了 ProCeedRL：结合探索性演示的过程评论家强化学习，将探索从被动选择转变为主动干预。ProCeedRL 采用过程级评论家来实时监控交互，并结合基于反思的演示来引导智能体停止错误的累积。我们发现，该方法显著超过了模型的饱和探索性能，展示了实质性的探索收益。通过从探索性演示和同策略样本中学习，ProCeedRL 显著提高了探索效率，并在复杂的深度搜索和具身任务中取得了优异的性能。

## Abstract
Reinforcement Learning (RL) significantly enhances the reasoning abilities of large language models (LLMs), yet applying it to multi-turn agentic tasks remains challenging due to the long-horizon nature of interactions and the stochasticity of environmental feedback. We identify a structural failure mode in agentic exploration: suboptimal actions elicit noisy observations into misleading contexts, which further weaken subsequent decision-making, making recovery increasingly difficult. This cumulative feedback loop of errors renders standard exploration strategies ineffective and susceptible to the model's reasoning and the environment's randomness. To mitigate this issue, we propose ProCeedRL: Process Critic with Explorative Demonstration RL, shifting exploration from passive selection to active intervention. ProCeedRL employs a process-level critic to monitor interactions in real time, incorporating reflection-based demonstrations to guide agents in stopping the accumulation of errors. We find that this approach significantly exceeds the model's saturated exploration performance, demonstrating substantial exploratory benefits. By learning from exploratory demonstrations and on-policy samples, ProCeedRL significantly improves exploration efficiency and achieves superior performance on complex deep search and embodied tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《ProCeedRL: Process Critic with Exploratory Demonstration Reinforcement Learning for LLM Agentic Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多轮智能体（Agentic）任务中，大语言模型（LLM）面临“**错误累积的恶性循环**”。即：一个次优动作会导致环境返回带有噪声或误导性的观测，这些观测进入上下文后会进一步削弱模型的推理能力，导致后续决策更加困难，最终使标准强化学习（RL）的探索策略失效。
*   **研究背景**：虽然基于结果验证的强化学习（RLVR）在数学等单轮任务中表现优异，但在长程、随机的环境交互中，简单的重复采样（Vanilla Exploration）很难突破模型固有的推理上限。

### 2. 方法论：ProCeedRL 框架
*   **核心思想**：将探索从“被动选择”转变为“**主动干预**”。通过引入过程级评论家实时监控交互，一旦发现错误立即“回溯”并提供“精炼演示”，从而打破错误累积的循环。
*   **关键技术细节**：
    *   **过程评论家 (Process Critic)**：在每个步骤 $t$，评论家 $\phi$ 根据历史轨迹和当前动作/观测给出评分 $l_t$ 和文本分析 $c_t$。
    *   **回溯与精炼 (Rewind & Refine)**：若评分低于预设阈值 $l_{th}$，系统会撤销该动作，利用精炼策略 $\mu$（可以是模型自身或更强的模型）生成一个修正后的动作 $a'_t$ 替代原动作，引导智能体重回正确路径。
    *   **策略优化**：将收集到的“精炼演示”与“同策略样本”混合。为了解决分布偏移问题，采用了 **DAPO** 算法，并引入动态权重系数 $\sigma$ 来稳定训练，对偏离较大的演示进行降权。
    *   **掩码机制**：在优化过程中，对失败轨迹中的演示步骤进行掩码处理，避免模型学习到导致失败的非策略行为。

### 3. 实验设计
*   **实验场景**：
    1.  **深度搜索 QA (Deep Search QA)**：要求智能体通过多轮搜索工具调用回答复杂问题。
    2.  **具身任务 (Embodied Task)**：在虚拟家庭环境（ALFWorld）中进行长程规划。
*   **数据集/Benchmark**：
    *   搜索类：MuSiQue, WebWalkerQA, GAIA, Frames, Bamboogle。
    *   具身类：ALFWorld（包含分布内和分布外测试集）。
*   **对比方法**：
    *   基准模型：Qwen3-1.7B/8B。
    *   对比算法：SFT-only, RFT（拒绝采样微调）, 标准 RL 算法（GRPO, DAPO/Search-R1），以及相关工作如 GiGPO 和 DeepResearcher。

### 4. 资源与算力
*   **硬件型号**：使用 Nvidia **H100** 和 **A100-80G** GPU。
*   **算力消耗**：总计算预算约为 **10 个 GPU 月 (GPU-months)**。
*   **训练配置**：1.7B 模型通常使用 2 张 GPU，8B 模型使用 8 张 GPU。
*   **开销分析**：由于引入了评论家和精炼步骤，ProCeedRL 的采样成本约为标准采样的 **1.8 到 2.5 倍**。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流 Benchmark 上进行了测试，涵盖了搜索和具身两大领域。
*   **消融实验**：
    *   **探索效率分析**：通过 Pass@k 指标对比了 ProCeed 采样与标准采样的效率。
    *   **评论家类型**：对比了自评论（Self-critic）、同族评论家和外部强模型评论家的效果。
    *   **回溯阈值**：研究了不同评分阈值对最终性能的影响。
*   **充分性评价**：实验设计较为全面，不仅关注最终准确率，还深入分析了探索效率和计算成本的权衡，验证了方法在不同规模模型上的泛化性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **突破上限**：ProCeedRL 能够显著超越模型在标准 RL 探索下的性能饱和点（Ceiling），证明了主动干预对提升推理上限的有效性。
*   **效率提升**：在达到相同准确率的情况下，ProCeedRL 所需的生成样本量远少于标准重复采样。
*   **自评论有效性**：即使使用模型自身作为评论家（Self-critic），也能产生显著的性能增益，这表明该框架不完全依赖外部知识，而是通过优化推理路径获益。
*   **性能表现**：在 MuSiQue 等高难度任务上，ProCeedRL 相比强基线（如 Search-R1）有显著提升（约 3-6% 的绝对值）。

### 7. 优点
*   **主动纠错机制**：有效解决了长程交互中“一步错步步错”的痛点。
*   **训练与推理分离**：虽然训练时需要评论家，但通过强化学习将这种能力内化到了模型中，**测试时无需额外的评论家或复杂的 Pipeline**。
*   **模型无关性**：框架具有通用性，可适配不同的基座模型和评论家模型。

### 8. 不足与局限
*   **计算开销**：训练阶段的采样成本较高（约 2 倍于常规 RL），对于算力受限的环境不够友好。
*   **缺乏理论保证**：该方法依赖于 LLM 内部知识来识别和修正错误，如果模型对某领域完全无知，评论家可能会给出错误引导。
*   **阈值敏感性**：回溯阈值的设定需要针对不同任务进行校准，过度回溯可能会引入不必要的噪声或干扰模型的正常推理。

（完）
