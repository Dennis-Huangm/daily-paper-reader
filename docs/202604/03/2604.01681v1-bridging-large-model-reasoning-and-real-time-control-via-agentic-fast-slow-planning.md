---
title: Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning
title_zh: 通过智能体快慢规划连接大模型推理与实时控制
authors: "Jiayi Chen, Shuai Wang, Guangxu Zhu, Chengzhong Xu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01681v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于实时控制和推理的智能体快慢规划
tldr: "针对大模型推理与实时控制结合中存在的脆弱性、难以验证及高延迟问题，本文提出Agentic Fast-Slow Planning框架。该框架通过解耦感知、推理、规划与控制，利用VLM和LLM生成语义指令，并结合语义引导的A*算法与智能体优化模块，将指令转化为可执行轨迹，最后由MPC实时跟踪。实验证明该方法在CARLA环境下显著提升了系统的鲁棒性和效率。"
selection_source: fresh_fetch
motivation: 现有方法在将大模型语义意图映射到实时控制时，存在轨迹生成脆弱、难以验证或慢决策与快控制边界模糊的问题。
method: "提出一种分层框架，包含Perception2Decision（云端LLM决策）和Decision2Trajectory（语义引导A*与参数自适应）两个桥梁，并由MPC执行实时跟踪。"
result: "在CARLA仿真中，该方法相比纯MPC和基准算法，侧向偏差降低了45%，完成时间缩短了12%以上。"
conclusion: 该框架成功实现了大模型的高级推理能力与底层实时控制的解耦与融合，提升了自动驾驶系统在复杂场景下的鲁棒性。
---

## 摘要
大型基础模型为自主系统提供了强大的推理能力，但将语义意图映射为可靠的实时控制仍具挑战。现有方法要么 (i) 让大语言模型 (LLM) 直接生成轨迹——这种方式脆弱、难以验证且易产生延迟；要么 (ii) 在线调整模型预测控制 (MPC) 的目标——这混合了慢速深思熟虑与快速控制，模糊了接口界限。我们提出了智能体快慢规划（Agentic Fast-Slow Planning），这是一个在自然时间尺度上解耦感知、推理、规划和控制的分层框架。该框架包含两个桥梁：“感知到决策”（Perception2Decision）利用车载视觉语言模型 (VLM) 检测器将场景压缩为以自我为中心的拓扑结构，随后通过云端 LLM 决策器将其映射为符号化驾驶指令，在保持可解释性的同时减少了带宽和延迟；“决策到轨迹”（Decision2Trajectory）将指令转换为可执行路径，其中“语义引导 A*”（Semantic-Guided A*）将源自语言的软成本嵌入经典搜索中，使解偏向可行轨迹，而“智能体精炼模块”（Agentic Refinement Module）则利用反馈和记忆调整规划器的超参数。最后，MPC 实时跟踪轨迹，并针对困难情况提供可选的云端引导参考。在 CARLA 中的实验表明，与纯 MPC 和 A* 引导的 MPC 基准相比，智能体快慢规划提高了扰动下的鲁棒性，横向偏差减少了高达 45%，完成时间缩短了 12% 以上。代码见 https://github.com/cjychenjiayi/icra2026_AFSP。

## Abstract
Large foundation models enable powerful reasoning for autonomous systems, but mapping semantic intent to reliable real-time control remains challenging. Existing approaches either (i) let Large Language Models (LLMs) generate trajectories directly - brittle, hard to verify, and latency-prone - or (ii) adjust Model Predictive Control (MPC) objectives online - mixing slow deliberation with fast control and blurring interfaces. We propose Agentic Fast-Slow Planning, a hierarchical framework that decouples perception, reasoning, planning, and control across natural timescales. The framework contains two bridges. Perception2Decision compresses scenes into ego-centric topologies using an on-vehicle Vision-Language Model (VLM) detector, then maps them to symbolic driving directives in the cloud with an LLM decision maker - reducing bandwidth and delay while preserving interpretability. Decision2Trajectory converts directives into executable paths: Semantic-Guided A* embeds language-derived soft costs into classical search to bias solutions toward feasible trajectories, while an Agentic Refinement Module adapts planner hyperparameters using feedback and memory. Finally, MPC tracks the trajectories in real time, with optional cloud-guided references for difficult cases. Experiments in CARLA show that Agentic Fast-Slow Planning improves robustness under perturbations, reducing lateral deviation by up to 45% and completion time by over 12% compared to pure MPC and an A*-guided MPC baseline. Code is available at https://github.com/cjychenjiayi/icra2026_AFSP.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了如何将**大语言模型（LLM）的高级推理能力**与**自动驾驶的实时控制**有效结合。
*   **研究动机**：现有方法存在两极化问题：一是让 LLM 直接生成轨迹，这会导致高延迟、输出脆弱且难以验证；二是利用语言调整模型预测控制（MPC）的目标，这模糊了“慢速决策”与“快速控制”的界限。
*   **核心思想**：受人类认知的“双系统理论”启发，提出 **Agentic Fast-Slow Planning (AFSP)** 框架。通过在不同时间尺度上解耦感知、推理、规划和控制，实现语义意图到物理执行的可靠映射。

### 2. 方法论：核心思想与关键技术
该框架通过两个核心“桥梁”连接云端推理与车载控制：
*   **Perception2Decision（感知到决策）**：
    *   **车载端**：使用轻量化视觉语言模型（VLM）作为拓扑检测器，将原始图像压缩为以自我为中心的拓扑图（包含物体类别、距离、方位等），极大地降低了上传云端的带宽需求。
    *   **云端**：LLM 决策器接收拓扑图，通过思维链（CoT）输出符号化驾驶指令（如 LEFT, RIGHT, KEEP）和驾驶风格。
*   **Decision2Trajectory（决策到轨迹）**：
    *   **语义引导 A* 算法**：在经典 A* 搜索中引入“语义软成本”。将 LLM 的指令转化为成本函数的一部分，引导搜索算法生成符合语义意图的路径，同时保留几何可行性。
    *   **智能体精炼模块（Agentic Refinement Module）**：利用 LLM 作为一个闭环优化器，根据规划反馈和历史记忆自动调整 A* 算法的超参数（如各项成本权重），替代人工调参。
*   **实时控制**：采用云端引导的 MPC 实时跟踪生成的参考轨迹，确保动力学可行性和安全性。

### 3. 实验设计
*   **实验环境**：使用 CARLA 自动驾驶仿真器。
*   **数据集**：在 Town2-7 和 Town10HD 中采集了约 20 万帧图像用于 VLM 训练，最终筛选出 5 万帧高质量数据。
*   **Benchmark（基准方法）**：
    1.  **纯 MPC**：仅依赖局部避障。
    2.  **A*-MPC**：结合经典 A* 规划与 MPC 控制。
*   **测试场景**：包括标准地图场景、带扰动的偏移地图（模拟感知误差）以及包含多种静态/动态障碍物的复杂场景。

### 4. 资源与算力
*   **训练算力**：使用了 **4 台 NVIDIA A100 GPU** 进行 VLM 的微调。
*   **训练时长**：微调过程持续了 12 个 Epoch，学习率为 $8 \times 10^{-5}$。
*   **推理延迟**：实验对比了云端 LLM（DeepSeek-V3）与车载 VLM 的推理速度，LLM 平均延迟约为 4.13 秒，而 VLM 约为 10.24 秒（体现了云端推理的效率优势）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 VLM 微调进行了 3 种策略的对比实验。
    *   针对 LLM 决策质量进行了 200 帧数据的匹配度测试。
    *   在 CARLA 中设置了 3 种不同难度的场景，每个场景重复运行 10 次以减少随机性影响。
*   **充分性评价**：实验设计较为全面，涵盖了从感知精度、决策一致性到闭环控制性能的各个环节。通过消融实验和扰动测试（地图偏移），客观地验证了系统在面对不确定性时的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：AFSP 框架在 CARLA 实验中显著优于基准方法。相比纯 MPC，其**最大侧向偏差降低了约 45%**，**任务完成时间缩短了 12% 以上**。
*   **鲁棒性**：语义引导的 A* 算法在地图发生几何偏移时，仍能保持语义一致性（如坚持完成左转指令），而传统 A* 则容易失效。
*   **效率**：通过拓扑图压缩，实现了低带宽下的云端协作，证明了“边缘感知+云端推理”模式的可行性。

### 7. 优点
*   **架构解耦**：成功分离了高频控制（10Hz+）与低频语义推理，解决了 LLM 响应慢的问题。
*   **可解释性**：中间层输出（拓扑图、符号指令、语义成本）均可被人理解，增强了系统的透明度。
*   **自动调参**：引入智能体精炼模块，利用 LLM 的先验知识自动化处理复杂的规划器调参工作。

### 8. 不足与局限
*   **云端依赖**：系统高度依赖云端连接，若发生网络中断或极端延迟，高级推理功能将受限（虽然有本地 MPC 保底，但性能会下降）。
*   **仿真局限**：实验主要在 CARLA 仿真环境中完成，尚未在真实道路或复杂天气条件下进行验证，存在“从仿真到现实”的迁移风险。
*   **计算开销**：虽然解耦了频率，但云端运行大型模型（如 DeepSeek-V3）的推理成本在商业化大规模部署时仍是一个挑战。

（完）
