---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 具有可学习自适应策略的语言智能体，用于测试时学习
tldr: 本研究针对语言智能体在推理阶段的测试时学习（TTL）展开，指出传统方法依赖人工设计的固定适配策略。为此，作者提出了 Meta-TTL 框架，将适配策略的发现建模为双层优化问题：内层执行 TTL 过程，外层通过进化算法在多种任务上搜索最优策略。实验证明，Meta-TTL 在 Jericho 和 WebArena-Lite 任务中表现优异，不仅超越了手工基准，还展现出强大的跨任务泛化能力。
selection_source: fresh_fetch
motivation: 现有的测试时学习方法主要依赖人工直觉设计的固定适配策略，限制了智能体在复杂环境中的优化潜力。
method: 引入 Meta-TTL 框架，利用双层优化和进化搜索，从任务环境中自动学习并迭代优化适配策略。
result: 在多个基准测试和不同模型骨干上，Meta-TTL 在分布内及分布外设置下均显著优于传统的手工策略。
conclusion: 实验结果表明，通过学习获得的适配策略具有良好的可迁移性，能有效提升语言智能体在推理时的自我演进能力。
---

## 摘要
测试时学习 (TTL) 使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略 (actor policy)，从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游改进对其进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为此，我们引入了 Meta-TTL，这是一个将有效自适应策略的发现建模为双层优化问题的框架。在该框架内，内层循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在智能体性能的指导下，外层循环在多样化的训练任务分布上采用进化搜索，以迭代优化自适应策略。我们在 Jericho 和 WebArena-Lite 上评估了 Meta-TTL，涵盖了分布内 (ID) 和分布外 (OOD) 设置，并使用了多种元智能体骨干网络。两个基准测试的结果表明，Meta-TTL 一致优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

### 论文总结：学习在测试时学习（Meta-TTL）

#### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）智能体具备强大的零样本能力，但在面对陌生环境时，往往无法像人类一样通过“失败-诊断-调整-重试”的循环进行自我进化。
*   **核心问题**：现有的“测试时学习”（Test-Time Learning, TTL）方法（如 Reflexion）大多依赖于人工设计的、固定的适配规则（即如何根据过去经验更新策略）。这些手工规则往往缺乏针对性，限制了智能体在复杂任务中的改进潜力。
*   **整体含义**：本文提出将“适配策略”本身视为一个可学习的组件。通过在多样化的任务分布上进行元训练（Meta-Training），智能体可以自动发现比人类设计更有效的自我改进算法。

#### 2. 方法论
*   **核心思想**：将 TTL 形式化为一个**双层优化（Bi-level Optimization）问题**。
    *   **内层循环（Inner Loop）**：在特定任务上执行 $K$ 个回合。元智能体（Meta-Agent）观察执行者（Actor）的失败轨迹，并根据“元提示词”（Meta-Prompt）重写执行者的系统提示词，以优化下一回合的表现。
    *   **外层循环（Outer Loop）**：在训练任务集上利用**进化搜索（Evolutionary Search）**优化元提示词。
*   **关键技术细节**：
    *   **目标函数**：使用加权学习曲线下面积（W-AUC）作为评分标准，赋予后期回合更高的权重，以奖励持续的性能提升。
    *   **进化流程**：包含“提议（Proposal）”、“本地验证（Local Validation）”和“全局验证（Global Validation）”。只有在当前任务和整个验证集上均表现优异的候选策略才会被存入“专家池”。
    *   **专家选择**：通过对不同任务的 W-AUC 进行 z-score 归一化处理，消除任务难度差异带来的偏差，从而选出泛化性最强的适配策略。

#### 3. 实验设计
*   **数据集/场景**：
    *   **Jericho**：交互式小说文本游戏（如 Zork 1, Detective），具有密集的动作奖励。
    *   **WebArena-Lite**：网页导航任务（如 GitLab, 购物网站），仅有二元（成功/失败）奖励。
*   **Benchmark 设置**：分为分布内（ID）训练与评估，以及完全未见的分布外（OOD）任务测试。
*   **对比方法**：
    *   **Static**：无适配的基准。
    *   **Reflexion**：经典的手工设计反馈方法。
    *   **Memory Agent**：基于记忆积累的适配方法。
    *   **Naive Meta-Agent**：使用未经过进化优化的初始元提示词。
*   **模型骨干**：执行者统一使用 Gemini 1.5 Flash（文中标记为 Gemini 3 Flash），元智能体分别测试了 Gemini、GLM 和 GPT 系列的高级版本。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或数量。由于该方法基于 Prompt 优化而非参数微调，其算力消耗主要体现为对 LLM API 的调用。
*   **训练时长**：文中提到一个典型的优化轨迹（以 GPT-5 为元智能体在 Jericho 任务上为例）包含 26 次迭代，总耗时约 **27 小时**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个截然不同的领域（文本决策与网页操作），并在每个领域都设置了 ID 和 OOD 场景。
*   **充分性**：实验对比了多种主流的 TTL 基准，并跨越了三个不同的模型家族作为元智能体骨干，证明了该框架的普适性。
*   **客观性**：通过引入 z-score 归一化来选择专家，避免了模型偏向简单任务的风险，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **性能飞跃**：Meta-TTL 在 Jericho ID 任务上的平均得分从 50.4 提升至 110.8（约 120% 的提升），在 WebArena 上也有显著增长。
*   **强泛化性**：优化后的适配策略在 OOD 任务上依然有效，证明其学习到的是通用的“学习方法”而非简单的任务答案。
*   **策略涌现**：进化过程自动发现了诸如“信用分配（哪些动作得分了）”、“受控探索（每次只尝试一个新变量）”和“条件化知识库”等高级适配策略。

#### 7. 优点
*   **范式创新**：将适配过程从“人工设计”转向“自动发现”，为智能体的自我演进提供了新思路。
*   **无需微调**：完全在提示词空间（Prompt Space）操作，无需更新模型权重，部署轻量且具有良好的解释性。
*   **结构化反馈**：通过进化产生的元提示词强制要求元智能体输出结构化的诊断和行动脚本，显著降低了幻觉风险。

#### 8. 不足与局限
*   **反馈信号依赖**：在 WebArena 等仅有二元奖励的任务中，由于搜索空间稀疏，优化难度较大，提升幅度不如密集奖励任务明显。
*   **搜索成本**：进化搜索需要大量的内层循环评估，对于 API 调用成本较高的模型，训练开销较大。
*   **模型依赖**：适配策略的效果高度依赖于元智能体本身的推理能力，较弱的骨干模型可能无法有效执行复杂的适配指令。

（完）
