---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v2"
tags: ["query:ag-rl"]
score: 9.0
evidence: 通过优化获得可学习自适应策略的语言智能体
tldr: 针对现有测试时学习（TTL）方法依赖人工设计固定策略的局限，本文提出Meta-TTL框架，通过双层优化自动学习适应策略。该方法在内循环执行TTL过程，外循环利用进化搜索在多样化任务上优化策略。实验表明，Meta-TTL在Jericho和WebArena-Lite基准测试中显著优于手工基准，展现出强大的泛化能力和跨任务的可迁移性。
selection_source: fresh_fetch
motivation: 现有的测试时学习方法主要依赖人工设计的固定适应策略，而非针对下游任务性能进行优化。
method: 提出Meta-TTL框架，将适应策略的发现建模为双层优化问题，并利用进化搜索在多样化任务上迭代优化策略。
result: 在Jericho和WebArena-Lite基准测试中，Meta-TTL在分布内和分布外设置下均一致优于手工设计的基准方法。
conclusion: 通过学习获得的适应策略能够编码可迁移的策略，显著提升语言智能体在推理阶段的自我修正与泛化表现。
---

## 摘要
测试时学习（TTL）使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略（actor policy），从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游改进对其进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为此，我们引入了 Meta-TTL，这是一个将有效自适应策略的发现表述为双层优化问题的框架。在该框架内，内循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在智能体性能的指导下，外循环在多样化的训练任务分布上采用进化搜索，以迭代地优化自适应策略。我们在 Jericho 和 WebArena-Lite 上评估了 Meta-TTL，涵盖了分布内（ID）和分布外（OOD）设置，并使用了多种元智能体骨干网络。两个基准测试的结果表明，Meta-TTL 一致优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

### 论文总结：学习在测试时学习（Meta-TTL）

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）智能体在部署到新环境时难以即时自适应的问题，探讨了**测试时学习（Test-Time Learning, TTL）**。
*   **研究动机**：现有的 TTL 方法（如 Reflexion）通常依赖于人工设计的、固定的自适应规则（即如何根据失败经验更新策略）。作者认为，这种“学习如何学习”的策略不应由人类直觉驱动，而应通过任务环境自动优化。
*   **核心目标**：将自适应策略（Adaptation Policy）本身作为优化对象，使智能体能够通过跨任务的元训练，掌握一套可迁移的、在测试阶段无需权重更新即可实现自我改进的能力。

#### 2. 方法论
论文提出了 **Meta-TTL** 框架，将其建模为一个**双层优化（Bi-level Optimization）**问题：
*   **内循环（Inner Loop）**：执行标准的 TTL 过程。智能体在特定任务中进行多个回合（Episodes）的尝试。在每个回合结束后，元智能体（Meta-Agent）根据历史轨迹重写执行者（Actor）的系统提示词（System Prompt），目标是最大化**加权学习曲线下面积（W-AUC）**。
*   **外循环（Outer Loop）**：利用**进化搜索（Evolutionary Search）**优化元提示词（Meta-Prompt）。
    *   **提议与验证**：提议模型（Proposer）观察内循环的失败或成功，提出新的元提示词候选。
    *   **专家池（Expert Pool）**：维护一个针对不同验证任务的最优策略池。
    *   **专家选择**：通过归一化的 Z-score 分数从池中选出泛化性能最好的单一元提示词作为最终的自适应策略。
*   **关键公式**：使用 $W-AUC$ 作为指标，对后期回合赋予更高权重，以奖励持续的改进表现。

#### 3. 实验设计
*   **数据集/场景**：
    1.  **Jericho**：交互式小说游戏（文本冒险），具有密集的得分奖励。
    2.  **WebArena-Lite**：真实网页导航任务，具有稀疏的二元奖励（成功/失败）。
*   **基准测试（Benchmark）**：分为分布内（ID）训练/验证和分布外（OOD）测试，验证策略的迁移能力。
*   **对比方法**：
    *   **Static**：无自适应的零样本表现。
    *   **Reflexion**：经典的手工设计反馈机制。
    *   **Memory Agent**：带有进化内存机制的智能体。
    *   **Naive Meta-Agent**：使用相同架构但未经元训练优化的基准。
*   **模型骨干**：采用了 Gemini 3 Flash、GLM-5 和 GPT-5（注：文中提及的模型版本反映了论文发表时的前瞻性设定）。

#### 4. 资源与算力
*   **训练时长**：论文提到 GPT-5 元智能体在 Jericho ID 任务上的优化轨迹经历了 26 次迭代，耗时约 **27 小时**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号或数量。由于该方法基于提示词优化（Prompt-based），主要的算力消耗在于调用 LLM API 进行大量的内循环模拟和外循环评估。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个截然不同的领域（游戏与网页），使用了三种不同的顶级模型骨干，并进行了 ID 和 OOD 的全面对比。
*   **充分性**：实验设计较为充分。除了最终得分，还分析了每回合的得分轨迹（Learning Curves）、元训练的优化轨迹，并对进化出的元提示词进行了定性分析。
*   **客观性**：通过引入 Z-score 归一化来平衡不同难度任务的权重，避免了单一简单任务主导优化方向，体现了实验的公平性。

#### 6 主要结论与发现
*   **性能提升**：Meta-TTL 在所有基准上均显著优于手工设计的基准。在 Jericho ID 上平均分提升了约 120%，在 WebArena-Lite 上也有稳定增长。
*   **强泛化性**：学习到的自适应策略在未见过的 OOD 任务上依然有效，证明其捕获的是通用的“学习策略”而非简单的任务知识。
*   **策略涌现**：进化过程自动发现了诸如**信用分配（Credit Assignment）**、**受控探索（Disciplined Exploration）**和**条件化事实库（Conditional Fact Banks）**等高级策略。

#### 7. 优点
*   **原则性框架**：将 TTL 形式化为元学习问题，为智能体自我改进提供了理论支撑。
*   **可解释性**：优化的结果是自然语言形式的元提示词，人类可阅读、可理解其演化出的逻辑。
*   **轻量化部署**：测试时无需梯度下降或权重微调，仅通过提示词工程即可实现跨模型的策略迁移。

#### 8. 不足与局限
*   **反馈敏感性**：在 WebArena 等二元奖励任务中，由于信号稀疏，优化难度显著高于具有连续得分的任务。
*   **元训练成本**：虽然测试时很轻量，但离线的进化搜索需要进行大量的环境交互和 API 调用，成本较高。
*   **任务覆盖**：虽然涵盖了两个领域，但对于更复杂的长程推理或多模态任务的适用性仍有待进一步验证。

（完）
