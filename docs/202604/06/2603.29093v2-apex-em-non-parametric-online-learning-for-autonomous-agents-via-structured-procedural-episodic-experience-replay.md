---
title: "APEX-EM: Non-Parametric Online Learning for Autonomous Agents via Structured Procedural-Episodic Experience Replay"
title_zh: APEX-EM：通过结构化程序-情境经验回放实现的自主智能体非参数化在线学习
authors: "Pratyay Banerjee, Masud Moshtaghi, Ankit Chadha"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29093v2"
tags: ["query:ag-rl"]
score: 8.0
evidence: 具有奖励信号和经验回放的自主智能体
tldr: APEX-EM是一个针对LLM自主智能体的非参数化在线学习框架，旨在解决智能体无法持久化存储和复用程序化经验的问题。它通过构建包含执行轨迹、错误分析和质量评分的结构化经验库，并结合PRGII工作流与混合检索机制（语义、结构及DAG遍历），实现了跨领域经验迁移。实验表明，该方法在代码生成和复杂查询等任务上显著提升了性能，超越了现有基准。
selection_source: fresh_fetch
motivation: 现有的LLM智能体缺乏持久的程序化记忆，导致在处理结构相似的任务时仍需从零开始推导方案。
method: 提出APEX-EM框架，通过结构化经验表示、PRGII工作流以及结合语义与结构特征的混合检索机制，实现经验的积累与复用。
result: "在KGQAGen-10k和BigCodeBench等基准测试中，APEX-EM分别实现了48.3%和29.4%的成功率提升，显著优于无记忆基准和现有方法。"
conclusion: 结构化的程序化经验回放能有效提升智能体的在线学习能力，且不同任务对反馈信号和检索维度的依赖程度存在差异。
---

## 摘要
基于大语言模型（LLM）的自主智能体缺乏持久的程序性记忆：即使之前已经解决过结构相同的任务，它们仍会从头开始推导解决方案。我们提出了 APEX-EM，这是一种非参数化在线学习框架，它在不修改模型权重的情况下积累、检索并重用结构化程序计划。APEX-EM 引入了：(1) 一种结构化经验表示，编码了每次执行的完整程序-情境追踪——包括规划步骤、中间产物、带有错误分析的迭代历史以及质量评分；(2) 一种“规划-检索-生成-迭代-摄取”（PRGII）工作流，其中任务验证器提供多维奖励信号；(3) 一种具有双重结果的经验记忆，采用结合了语义搜索、结构特征匹配和计划有向无环图（DAG）遍历的混合检索方式——从而实现在词汇无重叠但操作结构相似的任务之间的跨领域迁移。成功的经验作为正向上下文示例；失败的经验则作为带有结构化错误标注的负面示例。我们使用 Claude Sonnet 4.5 和 Opus 4.5 在 BigCodeBench、KGQAGen-10k 和 Humanity's Last Exam 上进行了评估。在 KGQAGen-10k 上，APEX-EM 达到了 89.6% 的准确率，而无记忆情况下的准确率为 41.3%（提升了 48.3 个百分点），超过了真值检索（oracle-retrieval）的上限（84.9%）。在 BigCodeBench 上，其成功率从 53.9% 的基准提升至 83.3%（提升了 29.4 个百分点），在可比的冻结骨干网络条件下，超过了 MemRL 11.0 个百分点的增益。在 HLE 上，实体图检索从 25.2% 提升至 48.0%（提升了 22.8 个百分点）。消融实验表明，各组件的价值取决于具体任务：丰富的评判反馈对代码生成的影响微乎其微，但对结构化查询至关重要（+10.3pp），而基于二元信号的迭代可以部分补偿较弱的反馈。

## Abstract
LLM-based autonomous agents lack persistent procedural memory: they re-derive solutions from scratch even when structurally identical tasks have been solved before. We present APEX-EM, a non-parametric online learning framework that accumulates, retrieves, and reuses structured procedural plans without modifying model weights. APEX-EM introduces: (1) a structured experience representation encoding the full procedural-episodic trace of each execution -- planning steps, artifacts, iteration history with error analysis, and quality scores; (2) a Plan-Retrieve-Generate-Iterate-Ingest (PRGII) workflow with Task Verifiers providing multi-dimensional reward signals; and (3) a dual-outcome Experience Memory with hybrid retrieval combining semantic search, structural signature matching, and plan DAG traversal -- enabling cross-domain transfer between tasks sharing no lexical overlap but analogous operational structure. Successful experiences serve as positive in-context examples; failures as negative examples with structured error annotations.   We evaluate on BigCodeBench, KGQAGen-10k, and Humanity's Last Exam using Claude Sonnet 4.5 and Opus 4.5. On KGQAGen-10k, APEX-EM achieves 89.6% accuracy versus 41.3% without memory (+48.3pp), surpassing the oracle-retrieval upper bound (84.9%). On BigCodeBench, it reaches 83.3% SR from a 53.9% baseline (+29.4pp), exceeding MemRL's +11.0pp gain under comparable frozen-backbone conditions (noting backbone differences controlled for in our analysis). On HLE, entity graph retrieval reaches 48.0% from 25.2% (+22.8pp). Ablations show component value is task-dependent: rich judge feedback is negligible for code generation but critical for structured queries (+10.3pp), while binary-signal iteration partially compensates for weaker feedback.

---

## 论文详细总结（自动生成）

### APEX-EM：通过结构化程序-情境经验回放实现的自主智能体非参数化在线学习

#### 1. 核心问题与研究动机
*   **核心问题**：当前的 LLM 智能体在程序层面是“无状态”的。即使面对结构完全相同的任务，它们也会从头开始推导方案，无法有效积累和复用过去的执行经验。
*   **研究动机**：
    *   现有的记忆机制（如 RAG 或 MemRL）多存储非结构化的文本摘要或简单的标量效用值，丢失了执行过程中的因果链条（如规划步骤、错误轨迹）。
    *   当任务之间缺乏词汇重叠（Lexical Overlap）但具有相似的操作结构时，现有的语义检索方法往往失效，导致跨领域知识迁移困难。
    *   缺乏对失败经验的系统性利用（负面示例）。

#### 2. 方法论：核心思想与技术细节
APEX-EM 提出了一种**非参数化在线学习**框架，无需修改模型权重，通过外部存储实现智能体能力的持续进化。
*   **核心思想**：将每次任务执行转化为一个结构化的“程序-情境”追踪，并利用图结构和结构特征匹配进行精准检索。
*   **关键技术细节**：
    1.  **程序化知识图谱 (PKG)**：将经验存储为有向类型图。节点包括实体（Entity）、经验（Experience）、子任务、操作（Operation）等。这种结构允许世界知识（实体变更）与程序逻辑分离。
    2.  **结构化经验表示**：每个经验包含五个反射层：目标反射（RG）、程序反射（RP）、证据反射（RE）、错误注册表（Rerr）和补丁反射（Rpatch）。
    3.  **PRGII 工作流**：
        *   **Plan（规划）**：分解任务并提取“结构特征”（Structural Signature，如：实体解析 $\rightarrow$ 时间过滤 $\rightarrow$ 聚合）。
        *   **Retrieve（检索）**：混合检索机制，结合语义搜索、结构特征匹配（基于最长公共子序列 LCS）和图遍历。
        *   **Generate（生成）**：基于检索到的正向（成功）和负向（失败）示例生成产物。
        *   **Iterate（迭代）**：利用验证器进行自我修正。
        *   **Ingest（摄取）**：通过质量门控（Quality Gate）将新经验存入记忆库。
    4.  **双重结果记忆**：成功经验作为正向 In-context 示例，失败经验则带有结构化错误标注，作为避坑指南。

#### 3. 实验设计
*   **数据集/场景**：
    *   **BigCodeBench (BCB)**：复杂的 Python 代码生成，涉及多库调用。
    *   **KGQAGen-10k**：基于 Wikidata 的多跳结构化查询（SPARQL）生成。
    *   **Humanity’s Last Exam (HLE)**：跨领域专家级知识推理。
    *   **其他**：Lifelong Agent Bench (OS/DB 交互) 和 ALFWorld (具身导航) 的基准对比。
*   **对比方法**：No Memory（基准）、RAG、Self-RAG、Reflexion、Mem0、MemP 以及当前最强的冻结骨干网络基准 **MemRL**。
*   **骨干网络**：主要使用 Claude Sonnet 4.5 和 Claude Opus 4.5。

#### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 训练时长或显卡数量。
*   **模型资源**：实验依赖于闭源模型的 API（Claude 系列），嵌入模型使用了 Amazon Titan Embed v2。
*   **运行配置**：实验通常运行 10 个 Epoch 以观察在线学习的收敛情况，设置了并行度 $p=3$ 或 $p=5$。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主要基准上进行了完整测试，每个任务运行 10 个 Epoch。
*   **消融实验**：非常充分。分别测试了“仅语义检索”、“仅实体图检索”、“有无评判反馈”、“有无迭代循环”等多种组合，揭示了不同组件在不同任务中的贡献。
*   **客观性**：通过在相同数据集分割（如 BCB 的 7:3 训练/迁移分割）下对比 MemRL 的公开数据，并进行了跨任务迁移（Cross-Task Transfer）测试，验证了记忆的泛化能力而非简单的过拟合。

#### 6. 主要结论与发现
*   **性能飞跃**：在 KGQA 任务上，准确率从 41.3% 提升至 89.6%（+48.3pp），超过了真值检索上限。在 BCB 上提升了 29.4pp。
*   **任务依赖性**：高质量的评判反馈（Judge Feedback）对结构化查询至关重要，但对代码生成（已有二元测试信号）增益较小。
*   **效率提升**：随着记忆积累，智能体解决任务所需的迭代次数显著减少（KGQA 任务减少了 43% 的推理调用）。
*   **结构迁移**：结构特征匹配允许智能体在词汇完全不同的领域（如体育数据 vs 金融数据）之间迁移操作逻辑。

#### 7. 优点
*   **结构化程度高**：相比于 MemRL 的扁平化摘要，APEX-EM 的五层反射模型和 PKG 图结构提供了更深层的因果理解。
*   **跨领域能力**：引入“结构特征”解决了语义检索在低词汇重叠场景下的失效问题。
*   **负面经验利用**：通过结构化错误注册表，实现了类似强化学习中“后视经验回放”的效果。

#### 8. 不足与局限
*   **骨干网络差异**：在与 MemRL 对比时，使用了不同的 Claude 模型作为骨干，虽然控制了基准线，但直接的数值对比可能受模型固有能力影响。
*   **分类体系依赖**：结构特征的分类法（Taxonomy）目前是人工设计的，自动化发现操作模式的能力尚待加强。
*   **成本与延迟**：复杂的 PRGII 工作流涉及多次 LLM 调用和图检索，在实时性要求极高的场景下可能存在延迟挑战。
*   **收敛保证**：作为非参数化方法，虽然实验表现良好，但缺乏像强化学习那样的理论收敛证明。

（完）
