---
title: "UniDoc-RL: Coarse-to-Fine Visual RAG with Hierarchical Actions and Dense Rewards"
title_zh: UniDoc-RL：具有层级化动作与稠密奖励的由粗及精视觉检索增强生成
authors: "Jun Wang, Shuo Tan, Zelong Sun, Tiancheng Gu, Yongle Zhao, Ziyong Feng, Kaicheng Yang, Zhiwu Lu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14967v2"
tags: ["query:ag-rl"]
score: 9.0
evidence: LVLM智能体执行具有分层动作的序列决策
tldr: UniDoc-RL是一个统一的强化学习框架，旨在解决视觉检索增强生成（RAG）中细粒度语义缺失的问题。该框架将视觉信息获取建模为序列决策过程，通过分层动作空间实现从粗粒度文档检索到细粒度区域裁剪的逐级精炼。结合密集多奖励机制和GRPO算法，UniDoc-RL显著提升了大型视觉语言模型在复杂推理任务中的表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14967v2/fig-001.webp\", \"caption\": \"Figure 1: Three critical factors for Visual RAG. UniDoc-RL address these challenges through the (a) Precise Selection action to bridge the semantic gap between coarse retrieval and reasoning, and an (b) Active Visual Perception action to focus on information-dense regions, both optimized via a (c) Dense multi-reward mechanism.\", \"page\": 2, \"index\": 1, \"width\": 858, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2604.14967v2/fig-002.webp\", \"caption\": \"Table 1: Main Results. The best performance are marked in bold. SlideVQA and ViDoSeek mainly focus on reasoning type, while MMLongBench focuses on the visual type of reference content. OCR-based ( ) RAG and purely visual ( ) RAG are evaluated with the same prompt and setting.\", \"page\": 7, \"index\": 2, \"width\": 885, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2604.14967v2/fig-003.webp\", \"caption\": \"Figure 6: Case Study for UniDoc-RL.\", \"page\": 14, \"index\": 3, \"width\": 898, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2604.14967v2/fig-004.webp\", \"caption\": \"Figure 2: Overview of UniDoc-RL. (a)(b)(c) demonstrates the “Search-Select-Perceive” coarse-to-fine action space. (d) is the specially designed reward for UniDoc-RL. (e) shows the interaction process between model and external environment, as well as the implementation of the GRPO algorithm.\", \"page\": 4, \"index\": 4, \"width\": 879, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2604.14967v2/fig-005.webp\", \"caption\": \"Figure 8: Prompt for Training and Testing.\", \"page\": 16, \"index\": 5, \"width\": 855, \"height\": 829}, {\"url\": \"assets/figures/arxiv/2604.14967v2/fig-006.webp\", \"caption\": \"Figure 7: Prompt for Reward Model.\", \"page\": 16, \"index\": 6, \"width\": 872, \"height\": 398}]"
motivation: 现有的视觉RAG系统依赖通用检索信号，忽略了复杂推理所需的细粒度视觉语义。
method: 提出一种基于分层动作空间和密集多奖励机制的强化学习框架，使模型能自主进行文档检索、图像筛选及区域裁剪。
result: "在三个基准测试中均超越了现有最先进模型，相比之前的强化学习方法提升了高达17.7%。"
conclusion: UniDoc-RL证明了通过分层决策和任务感知监督，可以有效对齐视觉RAG系统与复杂推理目标。
---

## 摘要
检索增强生成（RAG）通过外部视觉知识扩展了大语言视觉模型（LVLMs）。然而，现有的视觉 RAG 系统通常依赖于通用的检索信号，忽略了复杂推理所需的细粒度视觉语义。为了解决这一局限性，我们提出了 UniDoc-RL，这是一个统一的强化学习框架，其中 LVLM 智能体共同执行检索、重排序、主动视觉感知和推理。UniDoc-RL 将视觉信息获取建模为一个具有层级化动作空间的序列决策问题。具体而言，它将视觉证据从粗粒度的文档检索逐步细化为细粒度的图像选择和主动区域裁剪，使模型能够抑制无关内容并关注信息密集区域。为了实现有效的端到端训练，我们引入了一种稠密多奖励方案，为每个动作提供任务感知的监督。基于组相对策略优化（GRPO），UniDoc-RL 在不依赖独立价值网络的情况下，使智能体行为与多个目标保持一致。为了支持这种训练范式，我们构建了一个包含高质量推理轨迹和细粒度动作标注的综合数据集。在三个基准测试上的实验表明，UniDoc-RL 持续超越了最先进的基准模型，相比之前的基于强化学习的方法提升高达 17.7%。

## Abstract
Retrieval-Augmented Generation (RAG) extends Large Vision-Language Models (LVLMs) with external visual knowledge. However, existing visual RAG systems typically rely on generic retrieval signals that overlook the fine-grained visual semantics essential for complex reasoning. To address this limitation, we propose UniDoc-RL, a unified reinforcement learning framework in which an LVLM agent jointly performs retrieval, reranking, active visual perception, and reasoning. UniDoc-RL formulates visual information acquisition as a sequential decision-making problem with a hierarchical action space. Specifically, it progressively refines visual evidence from coarse-grained document retrieval to fine-grained image selection and active region cropping, allowing the model to suppress irrelevant content and attend to information-dense regions. For effective end-to-end training, we introduce a dense multi-reward scheme that provides task-aware supervision for each action. Based on Group Relative Policy Optimization (GRPO), UniDoc-RL aligns agent behavior with multiple objectives without relying on a separate value network. To support this training paradigm, we curate a comprehensive dataset of high-quality reasoning trajectories with fine-grained action annotations. Experiments on three benchmarks demonstrate that UniDoc-RL consistently surpasses state-of-the-art baselines, yielding up to 17.7% gains over prior RL-based methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《UniDoc-RL: Coarse-to-Fine Visual RAG with Hierarchical Actions and Dense Rewards》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：现有的视觉检索增强生成（Visual RAG）系统在处理包含图表、扫描文档等复杂视觉信息时，面临三大挑战：
    1.  **检索不精准**：通用检索器难以捕捉复杂推理所需的细粒度语义。
    2.  **视觉利用率低**：模型通常被动接收全图，忽略了高分辨率细节，且受背景噪声干扰。
    3.  **优化困难**：现有的强化学习（RL）方法多采用稀疏的最终结果奖励，导致中间步骤（如检索、裁剪）难以获得有效反馈。
*   **核心动机**：通过构建一个统一的强化学习框架，使模型具备“由粗及精”的主动信息获取能力，将视觉 RAG 转化为一个包含层级化动作和密集奖励的序列决策问题。

### 2. 方法论
*   **核心思想**：将 LVLM 建模为一个智能体，通过“思考-行动-观察”循环，依次执行**搜索（Search）**、**筛选（Select）**和**感知（Perceive）**动作。
*   **关键技术细节**：
    *   **层级化动作空间**：
        1.  **Image Search**：调用外部工具进行初步的候选文档检索。
        2.  **Precise Selection**：由模型对候选图进行语义重排序，过滤无关噪声。
        3.  **Visual Perception**：模型主动生成边界框（Bounding Box），对关键区域进行裁剪和缩放，获取高分辨率细节。
    *   **密集多奖励机制（Dense Multi-Rewards）**：
        *   **检索奖励 ($r_{ir}$)**：基于 NDCG 评估搜索结果的质量。
        *   **选择奖励 ($r_{sel}$)**：评估模型是否从候选集中选出了正确的金标准图片。
        *   **感知奖励 ($r_{crop}$)**：计算预测裁剪框与真实区域的 IoU。
        *   **模式奖励 ($r_{pat}$)**：确保模型遵循预定义的 XML 标签格式。
        *   **结果奖励 ($r_{ans}$)**：由大模型（如 Qwen2.5-72B）评估最终答案的正确性。
    *   **算法流程**：采用 **GRPO（组相对策略优化）**。该算法通过组内样本的相对得分来估计优势函数，无需独立的价值网络（Value Network），显著降低了显存开销。

### 3. 实验设计
*   **数据集/场景**：
    *   **SlideVQA**：多图幻灯片问答，侧重多跳推理。
    *   **ViDoSeek**：视觉文档搜索与推理。
    *   **MMLongBench**：长文档理解，侧重细粒度视觉细节。
*   **对比基准（Baselines）**：
    *   **OCR-based RAG**：基于文本提取的传统方法。
    *   **Vanilla RAG / ReAct**：标准的检索与思考-行动框架。
    *   **Search-R1 / Search-R1-VL**：近期基于 RL 的搜索增强模型。
    *   **VRAG-RL**：之前最先进的视觉感知 RL 框架。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 (80G) GPU**。
*   **训练细节**：
    *   使用 `llama-factory` 进行 SFT（冷启动）。
    *   使用 `verl` 框架进行 GRPO 训练。
    *   GRPO 组大小（Group Size）设置为 5。
    *   训练时长未明确说明，但提到 SFT 阶段为 3 个 Epoch，RL 阶段为 1 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3B 和 7B 两种参数规模的模型上进行了主实验。
    *   进行了**动作空间消融**（验证 Select 和 VP 动作的必要性）。
    *   进行了**奖励函数消融**（验证各项密集奖励对最终性能的贡献）。
    *   提供了**定性分析**（如裁剪频率变化、案例可视化）。
*   **充分性评价**：实验设计较为全面，涵盖了从模型规模、任务类型到组件贡献的多个维度。通过对比 OCR 和纯视觉路径，客观地展示了视觉 RAG 的优势。

### 6. 主要结论与发现
*   **性能领先**：UniDoc-RL 在所有基准测试中均达到 SOTA，相比之前的 RL 方法（VRAG-RL），在 3B 和 7B 模型上分别实现了 **17.5% 和 17.7%** 的大幅提升。
*   **主动性增强**：RL 训练显著改变了模型的行为模式。相比 SFT 模型，RL 后的模型更倾向于执行“主动裁剪”动作（裁剪频率从 2.4% 提升至 13.3%），且裁剪精度更高。
*   **纠错能力**：Precise Selection 动作有效弥补了外部检索器的不足，显著提升了金标准图片的召回率。

### 7. 优点
*   **层级化决策**：模拟人类“先扫视、再定睛”的视觉注意力机制，逻辑清晰且高效。
*   **密集监督**：通过分解奖励函数，解决了长链条推理中的信用分配问题，使中间步骤的优化更加透明。
*   **数据贡献**：构建并开源了包含 1.2 万条 SFT 和 5.5 千条 RL 轨迹的高质量数据集，对社区具有参考价值。

### 8. 不足与局限
*   **数据合成依赖**：训练轨迹高度依赖于超大规模教师模型（Qwen3-VL-235B）的合成，这可能引入教师模型的偏见，且合成成本较高。
*   **动作空间固定**：目前的“搜索-选择-感知”流程是相对固定的，对于不需要检索或极其简单的视觉任务，可能会增加不必要的推理开销。
*   **实时性挑战**：多轮“思考-行动”循环虽然提升了准确率，但也增加了推理延迟，论文未详细讨论推理速度的权衡。

（完）
