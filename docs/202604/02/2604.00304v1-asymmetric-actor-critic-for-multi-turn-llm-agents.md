---
title: Asymmetric Actor-Critic for Multi-turn LLM Agents
title_zh: 面向多轮 LLM 智能体的不对称 Actor-Critic 框架
authors: "Shuli Jiang, Zhaoyang Zhang, Yi Zhang, Shuo Yang, Wei Xia, Stefano Soatto"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00304v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于多轮LLM智能体的不对称Actor-Critic框架
tldr: 针对多轮对话智能体在单次尝试中可靠性不足的问题，本文提出一种非对称Actor-Critic框架。该框架利用强大的闭源模型作为Actor，配合轻量级开源模型作为Critic进行实时监督与干预。通过利用生成与验证之间的不对称性，该方法在不修改Actor的前提下，通过微调小型Critic显著提升了任务成功率，为增强闭源模型智能体的可靠性提供了新方案。
selection_source: fresh_fetch
motivation: 现有的智能体优化方法通常依赖多次尝试或需要模型完全可训练，难以在不可重试的场景中有效利用强大的闭源大模型。
method: 提出一种非对称架构，由闭源大模型负责执行，而经过专门微调的小型开源模型作为Critic在同一对话轨迹中进行实时监控和干预。
result: 在τ-bench和UserBench上的实验证明，该方法显著提升了任务成功率，且微调后的轻量级Critic在监督效果上甚至超越了大型闭源模型。
conclusion: 非对称Actor-Critic框架证明了通过轻量级模型对强大闭源模型进行实时监督是提升智能体可靠性的一种高效且可行的方法。
---

## 摘要
大语言模型（LLMs）展现出强大的推理和对话能力，但在多轮交互中确保其行为的可靠性仍然具有挑战性。在许多实际应用中，智能体必须在无法重试的单次尝试（one-shot）场景下取得成功。现有方法要么依赖于反思或事后评估（这需要额外的尝试），要么假设模型是完全可训练的，从而无法利用闭源 LLMs。我们提出了一种用于构建可靠对话智能体的不对称 Actor-Critic 框架。一个强大的闭源 LLM 充当 Actor，而一个较小的开源 Critic 提供运行时监督，监控 Actor 的动作并在同一交互轨迹内进行干预。与基于训练的 Actor-Critic 方法不同，我们的框架对在开放式对话环境中运行的固定 Actor 进行监督。该设计利用了生成与验证之间的不对称性：虽然高质量的生成需要大模型，但有效的监督通常可以通过较小的模型来实现。我们还引入了一个数据生成流水线，在不修改 Actor 的情况下为 Critic 微调生成监督信号。在 $τ$-bench 和 UserBench 上的实验表明，与强大的单智能体基准相比，我们的方法显著提高了可靠性和任务成功率。此外，轻量级开源 Critic 在担任 Critic 角色时可以媲美甚至超越更大的闭源模型，且 Critic 微调在多种最先进方法的基础上带来了额外的提升。

## Abstract
Large language models (LLMs) exhibit strong reasoning and conversational abilities, but ensuring reliable behavior in multi-turn interactions remains challenging. In many real-world applications, agents must succeed in one-shot settings where retries are impossible. Existing approaches either rely on reflection or post-hoc evaluation, which require additional attempts, or assume fully trainable models that cannot leverage proprietary LLMs. We propose an asymmetric actor-critic framework for reliable conversational agents. A powerful proprietary LLM acts as the actor, while a smaller open-source critic provides runtime supervision, monitoring the actor's actions and intervening within the same interaction trajectory. Unlike training-based actor-critic methods, our framework supervises a fixed actor operating in open-ended conversational environments. The design leverages a generation-verification asymmetry: while high-quality generation requires large models, effective oversight can often be achieved by smaller ones. We further introduce a data generation pipeline that produces supervision signals for critic fine-tuning without modifying the actor. Experiments on $τ$-bench and UserBench show that our approach significantly improves reliability and task success over strong single-agent baselines. Moreover, lightweight open-source critics rival or surpass larger proprietary models in the critic role, and critic fine-tuning yields additional gains over several state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 论文总结：面向多轮 LLM 智能体的不对称 Actor-Critic 框架

#### 1. 核心问题与研究动机
*   **核心问题**：如何在**不可重试**（One-shot）且**Actor模型不可训练**（如闭源 API）的实际应用场景中，提升多轮对话智能体（LLM Agents）的可靠性。
*   **研究动机**：
    *   **现有方法局限性**：反思（Reflection）机制通常需要多次尝试（Retries），不适用于即时交互；传统的 Actor-Critic 框架假设 Actor 和 Critic 均可训练且动作空间受限，难以应用于开放域对话。
    *   **生成与验证的不对称性**：高质量的内容生成需要庞大的模型，但识别错误（验证）往往可以通过较小的模型完成。
    *   **闭源模型限制**：最强大的模型（如 Claude、GPT 系列）通常无法微调，限制了通过强化学习直接优化策略的可能性。

#### 2. 方法论
*   **核心思想**：提出**不对称 Actor-Critic (Asym-AC)** 框架。使用强大的固定闭源模型作为 **Actor** 负责生成，使用轻量级可微调的开源模型作为 **Critic** 负责运行时监督。
*   **关键技术细节**：
    *   **运行时干预机制**：在每一轮对话中，Actor 先提出一个候选动作（如工具调用或回复），Critic 实时评估该动作。若发现违规（如违反政策、幻觉、偏好不符），Critic 提供反馈，Actor 在同一轮次内修正动作后再执行。
    *   **不对称设计**：Actor 固定不变，Critic 可根据特定领域任务进行微调。
*   **数据生成流水线（Critic-Oriented Pipeline）**：
    *   **任务过滤**：识别单 Actor 容易失败的“硬任务”。
    *   **自我博弈重放**：在硬任务上运行 Actor-Critic 系统，保留那些通过 Critic 干预后转败为胜的成功轨迹。
    *   **样本提取**：将这些轨迹中的（对话历史、Actor 提议、Critic 反馈）提取为监督微调（SFT）数据。

#### 3. 实验设计
*   **数据集/场景**：
    *   **$\tau$-bench**：模拟零售和航空领域的客户服务，侧重于政策合规性和工具调用。
    *   **UserBench**：模拟旅游规划，侧重于主动询问和满足用户隐式/显式偏好。
*   **对比方法**：
    *   **单 Actor 基准**：仅使用 Claude-4 Sonnet。
    *   **CRITIC**：基于提示词的自我修正方法。
    *   **ReAct**：经典的推理-动作交替框架。
    *   **APIGen-MT & IRMA**：针对特定 Benchmark 的最先进（SOTA）基线。
*   **模型配置**：Actor 统一使用 Claude-4 Sonnet；Critic 测试了 Claude-4 以及 Qwen3 系列（4B, 8B, 32B）。

#### 4. 资源与算力
*   **训练细节**：使用了 TRL 框架进行微调，采用 LoRA 技术（Rank=16, Alpha=32）。
*   **超参数**：学习率在 $10^{-3}$ 到 $10^{-6}$ 之间搜索，训练 5-15 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但考虑到 Critic 模型仅为 8B 规模且使用 LoRA，其算力需求相对较低。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型 Benchmark 的多个子任务（零售、航空、T-22/33/44）上进行了测试，每个任务运行 5 次独立实验取平均值。
*   **充分性**：
    *   涵盖了从 4B 到 32B 不同规模的 Critic 对比。
    *   进行了消融实验，对比了“无微调 Critic”与“SFT 微调后 Critic”的效果。
    *   提供了定性案例分析，展示了 Critic 如何在不同领域纠正 Actor。
*   **客观性**：对比了多种 SFT 和 Prompt-based 基线，实验设计较为公平。

#### 6. 主要结论与发现
*   **不对称架构有效性**：Asym-AC 显著优于单 Actor 模式，在 $\tau$-bench 和 UserBench 上均取得了更高的任务成功率。
*   **验证优势**：轻量级开源模型（如 Qwen3-8B/32B）作为 Critic 的表现**媲美甚至超过**了规模大得多的闭源模型（Claude-4），验证了“小模型也能管好大模型”的假设。
*   **微调增益**：通过论文提出的数据流水线微调后的 Critic（Asym-AC-SFT），性能进一步提升，达到了 SOTA 水平。

#### 7. 优点
*   **实用性强**：无需访问闭源模型的权重即可实现行为控制，非常适合企业级 API 调用场景。
*   **低延迟修正**：在同一交互轨迹内完成干预，避免了多轮重试带来的用户体验下降。
*   **数据效率**：通过重放硬任务生成高质量监督信号，解决了 Critic 缺乏训练数据的问题。

#### 8. 不足与局限
*   **推理开销**：引入 Critic 意味着每一轮对话至少增加一次额外的模型推理，会增加系统延迟和 API 成本。
*   **Actor 依赖性**：如果 Actor 本身的指令遵循能力极差，即使 Critic 提供了正确反馈，Actor 可能也无法正确修正。
*   **干预时机**：目前的干预逻辑（$\delta_t$）在某些任务中依赖预设触发点（如工具调用时），对于更隐蔽的逻辑错误可能覆盖不足。
*   **算力细节缺失**：未披露具体的训练资源消耗，不便于复现者评估成本。

（完）
