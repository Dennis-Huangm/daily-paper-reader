---
title: "RefineRL: Advancing Competitive Programming with Self-Refinement Reinforcement Learning"
title_zh: RefineRL：通过自我修正强化学习提升竞赛编程能力
authors: "Shaopeng Fu, Xingxing Zhang, Li Dong, Di Wang, Furu Wei"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00790v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 针对大语言模型智能体的自我修正强化学习
tldr: 针对大语言模型在竞赛编程中多侧重单次尝试而忽视迭代优化的问题，本文提出RefineRL框架。该框架包含具备怀疑态度的Skeptical-Agent，利用本地执行工具进行自我验证与强制重构，并结合强化学习算法在标准数据上训练模型的自我修复能力。实验显示，经过训练的4B小模型在性能上超越了32B模型，并接近235B模型的单次尝试水平，显著提升了推理任务的扩展潜力。
selection_source: fresh_fetch
motivation: 现有大模型在解决竞赛编程问题时大多仅进行单次尝试，缺乏有效的迭代自我修复和优化机制。
method: 提出Skeptical-Agent代理机制进行工具辅助的强制性自我重构，并利用强化学习在可验证数据上激励模型的自我修复行为。
result: 经过RefineRL训练的4B参数模型在竞赛编程任务中超越了32B模型，并达到了接近235B模型的性能水平。
conclusion: 自我修复机制是提升大语言模型推理能力的有效途径，为小参数模型实现高性能推理提供了新方向。
---

## 摘要
尽管大语言模型（LLMs）在竞赛编程（CP）等复杂推理任务中表现出强大的性能，但现有方法主要集中在单次尝试设置上，忽略了它们进行迭代修正的能力。在本文中，我们提出了 RefineRL，这是一种旨在释放 LLMs 在解决 CP 问题时的自我修正能力的新方法。RefineRL 引入了两个关键创新点：(1) Skeptical-Agent，一种配备本地执行工具的迭代自我修正智能体，用于根据 CP 问题的公开测试用例验证生成的解决方案。该智能体始终对其自身输出保持怀疑态度，从而即使在验证结果显示正确时，也会强制执行严格的自我修正。(2) 一种强化学习（RL）方案，仅使用标准的 RLVR 数据（即配有可验证答案的问题）来激励 LLMs 进行自我修正。在 Qwen3-4B 和 Qwen3-4B-2507 上的广泛实验表明，我们的方法带来了显著提升：经过我们的 RL 训练后，这些集成了 Skeptical-Agent 的紧凑型 4B 模型不仅优于大得多的 32B 模型，而且接近了 235B 模型的单次尝试性能。这些发现表明，自我修正对于扩展 LLM 推理能力具有巨大的前景，并具有进一步提升的显著潜力。

## Abstract
While large language models (LLMs) have demonstrated strong performance on complex reasoning tasks such as competitive programming (CP), existing methods predominantly focus on single-attempt settings, overlooking their capacity for iterative refinement. In this paper, we present RefineRL, a novel approach designed to unleash the self-refinement capabilities of LLMs for CP problem solving. RefineRL introduces two key innovations: (1) Skeptical-Agent, an iterative self-refinement agent equipped with local execution tools to validate generated solutions against public test cases of CP problems. This agent always maintains a skeptical attitude towards its own outputs and thereby enforces rigorous self-refinement even when validation suggests correctness. (2) A reinforcement learning (RL) solution to incentivize LLMs to self-refine with only standard RLVR data (i.e., problems paired with their verifiable answers). Extensive experiments on Qwen3-4B and Qwen3-4B-2507 demonstrate that our method yields substantial gains: after our RL training, these compact 4B models integrated with the Skeptical-Agent not only outperform much larger 32B models but also approach the single-attempt performance of 235B models. These findings suggest that self-refinement holds considerable promise for scaling LLM reasoning, with significant potential for further advancement.

---

## 论文详细总结（自动生成）

这是一份关于论文《RefineRL: Advancing Competitive Programming with Self-Refinement Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有大语言模型（LLM）在竞赛编程（CP）中多采用“单次尝试（Single-attempt）”模式，且现有的迭代修正方法（如 Reflexion）过度依赖稀疏的公开测试用例（Public Test Cases）。
*   **研究动机**：
    *   **假阳性风险**：代码可能通过了公开测试，但在未见的私有测试（Private Test Cases）中失败。
    *   **缺乏显式训练**：现有的自我修正多是推理时的 Prompt 技巧，模型本身缺乏优化修正过程的内在能力。
    *   **扩展潜力**：探索除了增加参数量之外，如何通过迭代推理（Test-time Scaling）提升小模型的逻辑性能。

### 2. 方法论：RefineRL 框架
RefineRL 由两个核心组件组成：**Skeptical-Agent** 和 **自我修正强化学习（Self-Refinement RL）**。

*   **Skeptical-Agent（怀疑论智能体）**：
    *   **核心思想**：对自身输出保持持续怀疑。即使代码通过了所有公开测试，也会触发“怀疑驱动反馈（Skepticism-driven feedback）”，强制模型检查是否存在硬编码作弊、边界情况遗漏或复杂度不达标。
    *   **反馈机制**：
        *   **错误驱动反馈**：若本地执行失败，提供具体的错误类型（WA, TLE, RE 等）和失败样例。
        *   **怀疑驱动反馈**：若通过测试，引导模型根据 Check List（正确性、复杂度、是否硬编码）进行自我审计。
    *   **控制策略**：引入“重启规则（防止陷入局部最优）”和“终止规则（连续多次通过后停止）”。

*   **自我修正强化学习（Self-Refinement RL）**：
    *   **数据合成**：利用模型自身的修正轨迹构建训练集，包含“错误修正”和“怀疑引导下的优化”两类样本。
    *   **平方激励奖励函数（Squared-incentive Reward）**：
        $$R(r_{code}, T) = (k^* / |T|)^2$$
        其中 $k^*$ 是通过的测试用例数量。相比传统的 0/1 奖励，该函数能提供更密集的信号，鼓励模型解决更多测试用例，并利用平方项放大完全解决问题的奖励。
    *   **算法**：采用 GRPO 算法（去除了 KL 散度项）进行大规模强化学习训练。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 HardTest 数据集中筛选出 4.5K 个适合自我修正的问题（输入输出长度适中），合成约 8.7K 条训练轨迹。
    *   **评估集**：LiveCodeBench v5（167 题）和 v6（175 题），涵盖 2024 年 10 月至 2025 年 3 月的最新题目。
*   **对比基准（Baselines）**：
    *   **模型对比**：Qwen3-4B/8B/32B/235B 系列，以及 DeepSeek-R1-Distill-Qwen-8B 等。
    *   **策略对比**：Pass@1、Random@16（并行采样）、LongCoT@16（选最长思维链）、RejSamp@16（拒绝采样）、Reflexion@16（标准自我修正）。

### 4. 资源与算力
*   **软件框架**：使用 VeRL 框架进行强化学习，Ray 进行分布式计算，SGLang 进行高性能推理。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了训练时的超参数：Batch Size 为 32，Rollout 数量为 8，最大生成长度在 32K 到 64K 之间。

### 5. 实验数量与充分性
*   **实验规模**：针对两个基座模型（Qwen3-4B 和 Qwen3-4B-2507）进行了完整的 RL 训练和多轮推理测试。
*   **消融实验**：
    *   验证了“自我修正轨迹数据”的必要性（对比标准 RL）。
    *   验证了“平方激励奖励函数”的有效性（对比二元 0/1 奖励）。
*   **充分性评价**：实验设计较为严谨，通过与不同参数量（从 4B 到 235B）的模型对比，证明了该方法在提升“计算效率/性能比”上的卓越表现。

### 6. 主要结论与发现
*   **小模型逆袭**：经过 RefineRL 训练的 **4B 模型**，在配备 Skeptical-Agent 后，性能超越了 **32B 模型** 的单次尝试表现，并逼近 **235B 模型** 的水平。
*   **思维深度增加**：RL 训练并没有让模型简单地“多试几次”，而是显著增加了单次尝试中的思维链（CoT）长度，表明模型学会了更深层的推理和自我调试。
*   **数据效率高**：仅使用约 4.5K 个原始问题合成的数据，即可实现显著的性能跨越。

### 7. 优点与亮点
*   **怀疑机制**：打破了“通过测试即正确”的盲目信任，有效解决了 CP 任务中的假阳性问题。
*   **密集奖励设计**：平方奖励函数解决了代码 RL 中奖励过于稀疏、模型难以收敛的痛点。
*   **推理时扩展**：展示了如何通过强化学习将推理时的计算量（Test-time Compute）转化为实际的逻辑能力提升。

### 8. 不足与局限
*   **模型覆盖面**：实验主要集中在 4B 参数规模的模型上，尚未验证该方法在更大规模模型（如 70B+）上的边际收益。
*   **任务局限性**：该方法高度依赖于“可验证的反馈”（测试用例），对于缺乏自动评测工具的通用推理任务（如创意写作、开放式问答）可能难以直接迁移。
*   **算力透明度**：缺乏具体的算力消耗数据，难以评估中小开发者复现该方案的成本。

（完）
