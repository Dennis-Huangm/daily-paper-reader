---
title: "Agent Q-Mix: Selecting the Right Action for LLM Multi-Agent Systems through Reinforcement Learning"
title_zh: Agent Q-Mix：通过强化学习为大语言模型多智能体系统选择正确动作
authors: "Eric Hanchen Jiang, Levina Li, Rui Sun, Xiao Liang, Yubei Li, Yuchen Wu, Haozheng Luo, Hengli Li, Zhi Zhang, Zhaolu Kang, Kai-Wei Chang, Ying Nian Wu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00344v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于大模型多智能体拓扑选择的多智能体强化学习框架
tldr: 针对大语言模型多智能体系统中的拓扑选择与协作难题，本文提出 Agent Q-Mix 框架。该方法将智能体间的通信决策建模为协作式多智能体强化学习问题，利用 QMIX 价值分解实现去中心化的通信决策。通过结合 GNN 编码器与 CTDE 范式，在平衡任务准确率与 Token 成本的同时，显著提升了多智能体系统的推理性能、效率及鲁棒性。
selection_source: fresh_fetch
motivation: 解决复杂任务需要多个大模型智能体协作，但如何有效选择和连接这些智能体以实现最优拓扑结构仍是一个待解决的挑战。
method: 将拓扑选择重构为多智能体强化学习问题，采用 QMIX 价值分解、GNN 编码器和 CTDE 范式来学习去中心化的通信决策。
result: 在编程、推理和数学等 7 个基准测试中取得最高平均准确率，并在 Token 效率和抗故障鲁棒性方面优于现有主流框架。
conclusion: 通过强化学习实现的去中心化拓扑优化能有效突破多智能体推理系统的性能边界，为构建高效协作的智能体系统提供了新路径。
---

## 摘要
大语言模型（LLMs）在完成各种任务方面表现出了卓越的性能。然而，解决复杂问题通常需要多个智能体的协作，这提出了一个根本性问题：如何有效地选择这些智能体并将其互连。在本文中，我们提出了 Agent Q-Mix，这是一个强化学习框架，它将拓扑选择重新表述为一个协作式多智能体强化学习（MARL）问题。我们的方法利用 QMIX 价值分解来学习去中心化的通信决策，其中每个智能体从一组通信动作中进行选择，这些动作共同诱导产生逐轮的通信图。Agent Q-Mix 的核心是在集中式训练、去中心化执行（CTDE）范式下，结合了拓扑感知的图神经网络（GNN）编码器、门控循环单元（GRU）记忆和每个智能体的 Q 头。该框架优化了一个平衡任务准确率与 Token 成本的奖励函数。在涵盖编程、推理和数学的七个核心基准测试中，与现有方法相比，Agent Q-Mix 实现了最高的平均准确率，同时展示了卓越的 Token 效率和针对智能体故障的鲁棒性。值得注意的是，在以 Gemini-3.1-Flash-Lite 为骨干网络的极具挑战性的 Humanity's Last Exam (HLE) 测试中，Agent Q-Mix 达到了 20.8% 的准确率，优于 Microsoft Agent Framework (19.2%) 和 LangGraph (19.2%)，随后是 AutoGen 和 OpenClaw 的 Lobster。这些结果强调了学习型去中心化拓扑优化在拓展多智能体推理边界方面的有效性。

## Abstract
Large Language Models (LLMs) have shown remarkable performance in completing various tasks. However, solving complex problems often requires the coordination of multiple agents, raising a fundamental question: how to effectively select and interconnect these agents. In this paper, we propose \textbf{Agent Q-Mix}, a reinforcement learning framework that reformulates topology selection as a cooperative Multi-Agent Reinforcement Learning (MARL) problem. Our method learns decentralized communication decisions using QMIX value factorization, where each agent selects from a set of communication actions that jointly induce a round-wise communication graph. At its core, Agent Q-Mix combines a topology-aware GNN encoder, GRU memory, and per-agent Q-heads under a Centralized Training with Decentralized Execution (CTDE) paradigm. The framework optimizes a reward function that balances task accuracy with token cost. Across seven core benchmarks in coding, reasoning, and mathematics, Agent Q-Mix achieves the highest average accuracy compared to existing methods while demonstrating superior token efficiency and robustness against agent failure. Notably, on the challenging Humanity's Last Exam (HLE) using Gemini-3.1-Flash-Lite as a backbone, Agent Q-Mix achieves 20.8\% accuracy, outperforming Microsoft Agent Framework (19.2\%) and LangGraph (19.2\%), followed by AutoGen and Lobster by OpenClaw. These results underscore the effectiveness of learned, decentralized topology optimization in pushing the boundaries of multi-agent reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：Agent Q-Mix —— 通过强化学习优化大模型多智能体协作拓扑

#### 1. 核心问题与整体含义（研究动机和背景）
在解决复杂任务时，多智能体系统（MAS）通常优于单个大语言模型（LLM）。然而，**通信拓扑（即智能体之间如何交换信息）**是决定系统性能、Token 效率和鲁棒性的关键。
*   **现有问题：** 静态拓扑（如链式、全连接）缺乏灵活性；现有的自适应方法多采用中心化生成器，导致智能体在执行时缺乏独立决策能力，且难以平衡性能与成本。
*   **研究动机：** 探索如何让每个智能体根据当前任务状态，自主决定通信行为，从而在保证准确率的同时降低 Token 消耗。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Agent Q-Mix** 框架，将拓扑选择建模为“网络化多智能体马尔可夫决策过程”（Networked MMDP）。
*   **核心思想：** 采用**集中式训练、去中心化执行（CTDE）**范式。在训练时利用全局信息优化，在执行时每个智能体仅根据自身观察独立选择通信动作。
*   **关键技术细节：**
    *   **离散动作空间：** 定义了 6 种具有图论意义的动作：`solo process`（独立工作）、`broadcast all`（广播）、`selective query`（定向查询）、`aggregate refine`（聚合）、`execute verify`（转发验证）、`debate check`（双向辩论）。
    *   **架构设计：** 采用 **GNN-GRU-MLP** 结构。GNN 负责提取当前拓扑特征，GRU 维护多轮通信的记忆，MLP 输出每个动作的 Q 值。
    *   **QMIX 价值分解：** 使用单调混合网络将各智能体的局部 Q 值整合为全局 Q 值，确保去中心化决策与全局目标的一致性（满足 IGM 属性）。
    *   **奖励函数：** $R = w_{acc} \cdot \text{accuracy} - w_{tok} \cdot \text{token\_cost}$，通过权重平衡准确率与成本。

#### 3. 实验设计
*   **数据集/场景：** 涵盖三大领域共 7 个核心基准测试：
    *   **编程：** LiveCodeBench (v6), HumanEval。
    *   **推理：** MMLU-Pro, Humanity’s Last Exam (HLE)。
    *   **数学：** AIME 2025/2026, Beyond-AIME, HMMT 2025。
*   **骨干模型：** GPT-OSS:120B 和 Gemini-3.1-Flash-Lite。
*   **对比方法：**
    *   **基础线：** 单智能体直接提示。
    *   **静态/自适应方法：** LLM-Debate, GPTSwarm, G-Designer, GTD, TopoDIM 等。
    *   **商业/工业框架：** AutoGen, LangGraph, Microsoft Agent Framework, Lobster。

#### 4. 资源与算力
*   **训练时长：** 论文提到在每个领域（如数学）的训练大约需要 **30-60 分钟**。
*   **硬件设备：** 明确指出训练是在 **单台 CPU** 上完成的。
*   **说明：** 由于主要的计算开销在于调用 LLM API（获取推理结果），而非强化学习网络（GNN/MLP）的参数更新，因此对本地 GPU 算力要求极低。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 7 个基准测试进行了完整评估，并额外在极难的 HLE 数据集上验证。
*   **消融实验：** 进行了四组深入的消融研究，包括智能体数量（1-10个）、训练样本量（5-100个）、奖励权重分配、以及通信轮数（1-4轮）的影响。
*   **鲁棒性测试：** 专门设计了“对抗智能体”实验，验证系统在某个智能体故意报错时的容错能力。
*   **评价：** 实验设计非常充分且具有说服力。通过跨模型（GPT/Gemini）和跨领域（代码/数学/推理）的测试，证明了方法的通用性和客观性。

#### 6. 主要结论与发现
*   **性能领先：** Agent Q-Mix 在所有基准测试中均取得了最高平均准确率。在 HLE 测试中以 20.8% 的准确率超过了微软和 LangChain 的官方框架。
*   **Token 高效：** 相比 AutoGen 等框架，Token 消耗降低了 4 到 24 倍，实现了“简单题目少说话，难题多协作”的自适应行为。
*   **强鲁棒性：** 在引入对抗智能体时，准确率仅下降 2.86%，远低于其他方法的 8%-10%，证明系统学会了通过拓扑调整来“隔离”不可靠的节点。
*   **泛化能力：** 仅需 15 个训练样本即可学习到有效的通信策略，并能泛化到未见过的测试集。

#### 7. 优点（亮点）
*   **去中心化决策：** 解决了以往方法中中心化控制器的瓶颈问题，使智能体具备真正的“自主通信”能力。
*   **可解释性：** 预定义的 6 种通信动作直观且对应经典的协作模式（如辩论、聚合）。
*   **成本感知：** 显式地将 Token 成本纳入强化学习目标，具有极高的工业应用价值。
*   **架构融合：** 巧妙地将经典 MARL 算法（QMIX）与现代 LLM 智能体系统结合。

#### 8. 不足与局限
*   **动作空间固定：** 虽然 6 种动作涵盖了主流模式，但可能无法覆盖所有极端的协作场景。
*   **训练依赖 API：** 尽管 RL 训练快，但获取训练标签（LLM 推理结果）仍依赖 API 调用，存在一定的成本和网络延迟依赖。
*   **智能体规模：** 实验主要集中在 3-10 个智能体的小型团队，在超大规模（如成百上千个智能体）系统中的表现尚未验证。
*   **提示词敏感性：** 系统的最终表现仍受限于底层智能体的系统提示词（System Prompt）质量。

（完）
