---
title: Multi-Agent LLM Governance for Safe Two-Timescale Reinforcement Learning in SDN-IoT Defense
title_zh: 面向 SDN-IoT 防御中安全双时间尺度强化学习的多智能体 LLM 治理
authors: "Saeid Jamshidi, Negar Shahabi, Foutse Khomh, Carol Fung, Mohammad Hamdaqa"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01127v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于强化学习的多智能体LLM治理
tldr: 本研究针对SDN-IoT防御中缓解措施可能导致控制平面不稳定的问题，提出双时间尺度防御方案。该方案结合快速尺度的PPO强化学习代理进行实时缓解，以及慢速尺度的多智能体LLM治理引擎，通过更新全局策略章程实现可审计的策略演进，在提升检测性能的同时显著降低了系统过载风险。
selection_source: fresh_fetch
motivation: 现有的SDN-IoT防御方法往往忽略了缓解动作对控制器负载和系统稳定性的负面影响，且缺乏结构化、可审计的策略演进机制。
method: 提出一种双时间尺度架构，利用PPO代理进行受限的快速缓解，并由多智能体LLM治理引擎生成并验证全局策略更新。
result: "实验显示该方法在Macro-F1上优于基准模型，同时将控制器积压峰值降低了42.7%，并将灾难性过载率从11.6%降至2.3%。"
conclusion: 该研究证明了结合LLM治理与强化学习的双时间尺度架构能有效平衡SDN-IoT防御的安全性与系统稳定性。
---

## 摘要
软件定义网络（SDN）因其集中控制和可编程转发特性，正越来越多地被用于保障物联网（IoT）网络的安全。然而，SDN-IoT 防御本质上是一个闭环控制问题，其中缓解动作会影响控制器负载、队列动态、规则下发延迟以及未来的流量观测。激进的缓解措施可能会导致控制平面不稳定、降低服务质量（QoS）并放大系统性风险。现有的基于学习的方法优先考虑检测准确性，却忽略了控制器耦合以及缺乏结构化、可审计策略演进的短视界强化学习（RL）优化。本文提出了一种自我反思的双时间尺度 SDN-IoT 防御方案，将快速缓解与慢速策略治理相分离。在快速时间尺度上，每个交换机的近端策略优化（PPO）智能体在安全约束和动作掩码下执行感知控制器的缓解。在慢速时间尺度上，多智能体大语言模型（LLM）治理引擎生成全局策略章程 Pi 的机器可解析更新，该章程编码了允许的动作、安全阈值和奖励优先级。更新（Delta Pi）通过压力测试进行验证，且仅在具备回归测试和安全保证的情况下部署，从而在无需重新训练 RL 智能体的情况下确保了可审计的演进。在异构 IoT 流量和对抗压力下的评估显示，相比 PPO 提升了 9.1% 的 Macro-F1，相比静态基准提升了 15.4%。在高强度攻击下，最坏情况下的性能下降减少了 36.8%，控制器积压峰值降低了 42.7%，且 RTT p95 增幅保持在 5.8% 以下。策略演进在五个周期内收敛，将灾难性过载从 11.6% 降低到 2.3%。

## Abstract
Software-Defined Networking (SDN) is increasingly adopted to secure Internet-of-Things (IoT) networks due to its centralized control and programmable forwarding. However, SDN-IoT defense is inherently a closed-loop control problem in which mitigation actions impact controller workload, queue dynamics, rule-installation delay, and future traffic observations. Aggressive mitigation may destabilize the control plane, degrade Quality of Service (QoS), and amplify systemic risk. Existing learning-based approaches prioritize detection accuracy while neglecting controller coupling and short-horizon Reinforcement Learning (RL) optimization without structured, auditable policy evolution. This paper introduces a self-reflective two-timescale SDN-IoT defense solution separating fast mitigation from slow policy governance. At the fast timescale, per-switch Proximal Policy Optimization (PPO) agents perform controller-aware mitigation under safety constraints and action masking. At the slow timescale, a multi-agent Large Language Model (LLM) governance engine generates machine-parsable updates to the global policy constitution Pi, which encodes admissible actions, safety thresholds, and reward priorities. Updates (Delta Pi) are validated through stress testing and deployed only with non-regression and safety guarantees, ensuring an auditable evolution without retraining RL agents. Evaluation under heterogeneous IoT traffic and adversarial stress shows improvements of 9.1% Macro-F1 over PPO and 15.4% over static baselines. Worst-case degradation drops by 36.8%, controller backlog peaks by 42.7%, and RTT p95 inflation remains below 5.8% under high-intensity attacks. Policy evolution converges within five cycles, reducing catastrophic overload from 11.6% to 2.3%.

---

## 论文详细总结（自动生成）

### 论文结构化总结：面向 SDN-IoT 防御中安全双时间尺度强化学习的多智能体 LLM 治理

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是 **SDN-IoT 环境下的安全防御自动化**。核心挑战在于：SDN-IoT 防御本质上是一个**闭环控制问题**，而非简单的流量分类。
*   **耦合风险**：防御动作（如丢包、隔离、限速）本身会消耗 SDN 控制器的计算资源，增加规则下发延迟，甚至导致控制平面崩溃或 QoS 严重下降。
*   **现有技术局限**：传统的强化学习（RL）防御往往只优化短期奖励，缺乏对控制器负载的感知，且其策略演进过程（权重更新）是“黑盒”状态，不可审计且难以在生产环境中安全部署。
*   **研究目标**：在保证防御效能的同时，确保控制平面的稳定性和策略演进的可解释性。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**自我反思的双时间尺度（Two-Timescale）控制框架**：

*   **快速时间尺度（数据平面/实时响应）**：
    *   **去中心化 PPO 智能体**：每个 SDN 交换机部署独立的 PPO 代理，根据本地遥测信号（流量速率、队列长度、流表压力等）执行实时缓解动作。
    *   **安全过滤器（Safety Filter）**：所有动作在执行前必须通过全局策略章程 $\Pi$ 的过滤。如果动作违反了当前的控制器负载约束，则强制执行“回退动作”（如限速而非完全丢包）。
*   **慢速时间尺度（控制平面/策略治理）**：
    *   **多智能体 LLM 治理引擎**：由四个角色的 LLM（Critic, Compiler, Red-Team, Judge）组成。
    *   **策略章程 $\Pi$**：这是一个显式的、机器可读的结构化对象，定义了动作掩码、安全阈值、奖励权重和执行上限。
    *   **演进流程**：LLM 引擎分析过去一段时间的系统轨迹（Trace），生成策略增量 $\Delta\Pi$。该更新需经过 Red-Team 的压力测试和 Judge 的非回归验证（Non-regression testing），确保新策略不会导致系统性能下降或安全违规。
*   **核心公式**：通过建模控制器积压动态 $d_{t+1} = \max(0, d_t + \sum \rho_i - \mu)$，将防御动作与控制器服务率 $\mu$ 显式耦合，并利用风险敏感的 CVaR 目标函数来优化最坏情况下的系统表现。

#### 3. 实验设计
*   **实验场景**：构建了一个闭环 SDN-IoT 仿真环境，模拟异构 IoT 流量（如智能家居、工业传感器）和多种对抗压力（高强度突发攻击、慢速扫描、同步伪装攻击）。
*   **Benchmark（基准）**：
    1.  **静态阈值防御**：基于固定规则的传统方法。
    2.  **无约束 PPO**：标准的强化学习防御，不考虑控制器负载。
    3.  **受限 PPO（无治理）**：带有固定安全约束但策略不随环境演进的 RL。
*   **评估指标**：Macro-F1（检测效能）、RTT p95（延迟稳定性）、控制器积压峰值、灾难性过载事件率。

#### 4. 资源与算力
*   **LLM 后端**：实验使用了 `gpt-4o-mini` 作为治理引擎的骨干模型，通过固定的系统提示词（System Prompts）和 JSON Schema 约束输出。
*   **算力细节**：论文**未明确说明**训练 PPO 代理所使用的具体 GPU 型号、数量或训练总时长。但提到 LLM 治理运行在慢速尺度（每 $K$ 步触发一次），因此不占用实时数据平面的计算资源。

#### 5. 实验数量与充分性
*   **实验规模**：进行了多组独立随机种子的运行，评估了不同 PPO 裁剪参数（$\epsilon = 0.1, 0.2, 0.3$）下的稳定性。
*   **治理周期**：展示了 5 个反射周期内的策略演进过程，记录了 25 个候选策略更新的接受/拒绝情况。
*   **充分性评价**：实验设计较为**充分且客观**。它不仅关注平均性能，还专门分析了“最坏情况”下的表现（Tail-risk），并利用 Welch's t-test 等统计检验验证了结果的显著性。通过对比“接受”与“拒绝”的策略增量，证明了非回归闸门（Gating）的有效性。

#### 6. 主要结论与发现
*   **防御效能提升**：该方案比无约束 PPO 提升了 **9.1%** 的 Macro-F1，比静态阈值提升了 **15.4%**。
*   **系统稳定性增强**：控制器积压峰值降低了 **42.7%**，高强度攻击下的 RTT p95 增幅控制在 **5.8%** 以内。
*   **风险控制**：将灾难性过载事件从 11.6% 显著降低至 **2.3%**，且无需重新训练 RL 神经网络参数。
*   **收敛性**：策略章程 $\Pi$ 在 5 个周期内趋于稳定，拒绝率随时间下降，证明了治理逻辑的收敛性。

#### 7. 优点（亮点）
*   **解耦设计**：将“快速执行”与“慢速治理”分离，既保留了 RL 的灵活性，又通过 LLM 引入了人类可理解的治理逻辑。
*   **可审计性**：策略更新以结构化 Delta（$\Delta\Pi$）形式存在，所有变更均可追溯、可解释，符合工业界对安全设备“变更管理”的要求。
*   **闭环建模**：不同于大多数只看准确率的论文，本文深入探讨了防御动作对 SDN 控制平面的物理反馈影响。

#### 8. 不足与局限
*   **理论保证缺失**：虽然实验证明了稳定性，但缺乏严格的 Lyapunov 稳定性证明或形式化验证。
*   **遥测依赖**：治理效果高度依赖于遥测数据的质量和摘要（Summarization）的准确性，若攻击者能操纵遥测计数器，可能诱导 LLM 做出错误决策。
*   **LLM 成本与延迟**：虽然在慢速尺度运行，但在超大规模网络中，频繁调用 LLM API 可能带来额外的运营成本。
*   **基础设施假设**：假设 SDN 控制器本身是受信任的，未考虑控制器被直接攻破的情况。

（完）
