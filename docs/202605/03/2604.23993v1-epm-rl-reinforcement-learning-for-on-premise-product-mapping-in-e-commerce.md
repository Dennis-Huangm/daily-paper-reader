---
title: "EPM-RL: Reinforcement Learning for On-Premise Product Mapping in E-Commerce"
title_zh: EPM-RL：电子商务中本地化产品映射的强化学习框架
authors: "Minhyeong Yu, Wonduk Seo"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23993v1"
tags: ["query:ag-rl"]
score: 9.5
evidence: 电子商务中基于大模型智能体的强化学习框架
tldr: 针对电商产品映射中标题冗余及外部API成本高、隐私风险大的问题，本文提出EPM-RL框架。该框架通过强化学习将高成本的智能体推理能力蒸馏至本地小模型。首先利用LLM生成的推理过程进行参数高效微调，再结合格式、准确性和推理偏好的奖励机制进行RL优化。实验证明，该方法在实现私有化部署的同时，提供了优于商业API的性价比。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23993v1/fig-001.webp\", \"caption\": \"Table 1: Illustrative examples of product pairs in e-commerce product mapping.\", \"page\": 2, \"index\": 1, \"width\": 506, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.23993v1/fig-002.webp\", \"caption\": \"Table 2: Prompt templates used in our experiments. We use a shared Baseline prompt for single-inference baselines and a structured PEFT prompt for supervised fine-tuning.\", \"page\": 5, \"index\": 2, \"width\": 506, \"height\": 673}, {\"url\": \"assets/figures/arxiv/2604.23993v1/fig-003.webp\", \"caption\": \"Table 3: Main experiment results on the internal product-mapping test set. We report Accuracy (Acc), Precision (Prec), Recall (Rec), and F1. Single inference uses one-pass prompting without external tools; RAG augments the prompt with BM25-retrieved evidence; Multi-Agent RAG coordinates multiple retrieval-and-reasoning agents over the same BM25 evidence pool. Ours summarizes our parameter-efficient variants (LoRA + Reasoning; LoRA + GRPO Reasoning). For each metric, bold indicates the best score and underline indicates the second best score.\", \"page\": 6, \"index\": 3, \"width\": 910, \"height\": 594}]"
motivation: 现有的基于LLM的电商产品映射方法存在成本高、延迟大且难以在隐私敏感的本地环境中大规模部署的问题。
method: 采用强化学习框架，先通过PEFT学习LLM生成的推理逻辑，再利用包含格式、标签准确性和推理偏好的综合奖励函数进行模型优化。
result: EPM-RL在性能上优于仅使用PEFT的模型，且在质量与成本的权衡上超过了商业API基准。
conclusion: 强化学习能有效将复杂的智能体推理流程转化为可扩展、可检查且低成本的本地化生产系统。
---

## 摘要
产品映射（Product mapping）是判断两个电子商务列表是否指向同一产品的任务，是价格监控和渠道可见性的核心问题。然而，在实际市场中，卖家经常在标题中加入促销关键词、平台特定标签和捆绑销售描述，导致同一产品以许多不同的名称出现。最近基于大语言模型（LLM）和多智能体框架的方法提高了在这些困难案例上的鲁棒性和可解释性，但它们通常依赖昂贵的外部 API、重复检索和复杂的推理时编排，使得在隐私敏感的企业环境中进行大规模部署成本高昂且困难。为了解决这些问题，我们提出了 EPM-RL，这是一个基于强化学习的框架，用于构建准确且高效的本地化电子商务产品映射模型。我们的核心思想是将高成本的智能体推理蒸馏到可训练的内部模型中。从一组经过人工验证且包含 LLM 生成理由的精选产品对开始，我们首先使用结构化推理输出对小型学生模型进行参数高效微调（PEFT）。然后，我们利用强化学习（RL）进一步优化模型，使用基于智能体的奖励机制，该机制共同评估输出格式合规性、标签正确性以及来自专门设计的评判模型的推理偏好得分。初步结果表明，EPM-RL 相比仅使用 PEFT 的训练有持续改进，并且比基于商业 API 的基准线提供了更强的质量-成本权衡，同时实现了私有化部署和更低的运营成本。这些发现表明，强化学习可以将产品映射从高延迟的智能体流水线转变为可扩展、可检查且生产就绪的内部系统。

## Abstract
Product mapping, the task of deciding whether two e-commerce listings refer to the same product, is a core problem for price monitoring and channel visibility. In real marketplaces, however, sellers frequently inject promotional keywords, platform-specific tags, and bundle descriptions into titles, causing the same product to appear under many different names. Recent LLM-based and multi-agent frameworks improve robustness and interpretability on such hard cases, but they often rely on expensive external APIs, repeated retrieval, and complex inference-time orchestration, making large-scale deployment costly and difficult in privacy-sensitive enterprise settings. To address these issues, we present EPM-RL, a reinforcement-learning-based framework for building an accurate and efficient on-premise e-commerce product mapping model. Our central idea is to distill high-cost agentic reasoning into a trainable in-house model. Starting from a curated set of product pairs with LLM-generated rationales and human verification, we first perform parameter-efficient fine-tuning (PEFT) on a small student model using structured reasoning outputs. We then further optimize the model with Reinforcement Learning (RL) using an agent-based reward that jointly evaluates output-format compliance, label correctness, reasoning--preference scores from specially designed judge models. Preliminary results show that EPM-RL consistently improves over PEFT-only training and offers a stronger quality--cost trade-off than commercial API-based baselines, while enabling private deployment and lower operational cost. These findings suggest that reinforcement learning can turn product mapping from a high-latency agentic pipeline into a scalable, inspectable, and production-ready in-house system.

---

## 论文详细总结（自动生成）

这是一份关于论文《EPM-RL: Reinforcement Learning for On-Premise Product Mapping in E-Commerce》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于电子商务中的**产品映射（Product Mapping）**任务，即判断两个不同的商品标题是否指向同一个实体产品。
*   **研究动机**：在实际电商场景中，卖家常在标题中加入促销词、平台标签或捆绑信息，导致“同物不同名”或“名似实异”（如型号微差、数量不同）。
*   **现有痛点**：目前的先进方案多依赖大语言模型（LLM）或多智能体框架，虽然效果好，但存在**API成本高昂、推理延迟大、隐私数据泄露风险**以及**难以在本地环境大规模部署**等问题。
*   **核心目标**：开发一种能够将高成本的智能体推理能力“蒸馏”到本地小型模型中的强化学习框架，实现高精度、低成本且私有化的部署。

### 2. 方法论：核心思想与技术细节
EPM-RL 框架通过三个阶段将复杂的推理逻辑内化到本地模型中：
*   **数据增强与预处理**：利用强大的教师模型（如 Nemotron-3-120B）为人工标注的商品对生成“反思性推理轨迹”（Reflective Reasoning Traces），构建包含“输入-推理-标签”的结构化数据集。
*   **参数高效微调（PEFT）**：使用 **LoRA** 技术对 30B 规模的混合专家模型（MoE）进行监督微调。模型被训练为按照特定格式输出：`<reason>推理过程</reason><label>0/1</label>`。
*   **基于 GRPO 的强化学习优化**：采用组相对策略优化（Group Relative Policy Optimization, GRPO）进一步精炼模型。其核心在于**多维度奖励函数**：
    1.  **格式奖励**：检查 XML 标签是否合规。
    2.  **准确性奖励**：判断最终分类标签是否与真值一致。
    3.  **智能体评判奖励（LLM-as-a-Judge）**：引入三个专门的评判智能体（核心身份、型号标识符、变体冲突），对推理过程的逻辑严密性进行打分。

### 3. 实验设计
*   **数据集**：使用内部收集的 **12,000 对**标注数据，涵盖 500 多个品牌，反映了真实市场中复杂的命名规范和捆绑描述。数据按 6K (PEFT)、4K (RL)、1K (验证)、1K (测试) 划分。
*   **Benchmark 与对比方法**：
    *   **编码器基准**：BERT-base/large, RoBERTa-base/large。
    *   **提示词基准**：Zero-shot, Chain-of-Thought (CoT), Entity-Attribute Reasoning。
    *   **检索与智能体基准**：标准 RAG (BM25), Multi-Agent RAG。
    *   **闭源强模型**：GPT-5.4 reasoning（作为性能上限参考）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 张 NVIDIA H100 GPU** 的节点上完成。
*   **软件框架**：使用 Megatron-Bridge 进行监督微调，使用 Hugging Face 的 TRL 库进行强化学习训练。
*   **训练细节**：PEFT 阶段训练 5 个 epoch，RL 阶段训练 1 个 epoch。文中未明确说明具体的总训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了 4 种编码器模型、3 种 Prompt 策略、2 种检索增强方案以及 1 种顶级商业模型，实验覆盖面较广。
*   **客观性与公平性**：所有基线模型使用相同的指令模板；数据集采用了分层抽样以保证品牌多样性和标签平衡（正样本约 70.6%）。
*   **充分性评价**：实验对比了不同技术路径（判别式 vs 生成式 vs 检索式），能够客观反映 EPM-RL 在综合性能上的优势。但由于主要在内部数据集上测试，缺乏在公开数据集（如 WDC）上的验证。

### 6. 主要结论与发现
*   **性能领先**：EPM-RL 在 F1 分数上达到了 **0.8120**，显著优于所有编码器基线和纯 Prompt 方案。
*   **平衡性优越**：相比于 GPT-5.4 或 Multi-Agent RAG 表现出的“极高精确率、极低召回率”（过于保守），EPM-RL 能够更好地找回匹配项，实现了精确率与召回率的平衡。
*   **成本效益**：通过将智能体能力蒸馏至本地 30B 模型，避免了昂贵的外部 API 调用和复杂的推理时编排，适合工业级大规模生产。

### 7. 优点（亮点）
*   **推理内化**：成功将原本需要多步检索和多智能体协作的复杂流程简化为单次模型推理。
*   **细粒度奖励设计**：通过三个专门的评判智能体（核心身份、型号、变体）来约束推理质量，解决了 LLM 容易产生幻觉或推理逻辑断层的问题。
*   **本地化友好**：采用 PEFT 和 GRPO 使得中等规模模型也能具备极强的领域推理能力，满足企业隐私和成本控制需求。

### 8. 不足与局限
*   **泛化性验证不足**：实验仅限于内部数据集，未在公开的跨域产品匹配基准上进行测试。
*   **消融实验缺失**：文中未详细展示各奖励组件（如三个评判智能体各自的贡献度）的消融分析。
*   **定性分析较少**：缺乏对模型生成的推理轨迹的具体案例分析，难以直观判断推理过程的“忠实度”。
*   **延迟与成本量化**：虽然声称更高效，但未给出具体的推理延迟（Latency）和实际节省成本的量化对比数据。

（完）
