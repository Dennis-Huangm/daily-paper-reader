---
title: "Selective Rollout: Mid-Trajectory Termination for Multi-Sample Agent RL"
authors: "Zhiyuan Zhai, Xin Wang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05802v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 通过选择性回放优化LLM智能体的强化学习训练
tldr: "本研究针对多样本强化学习（如GRPO）中长轨迹采样成本高昂的问题，提出了“选择性回滚”（Selective Rollout）策略。该方法通过监控并行轨迹间的相似度，在预测到该组样本将产生零奖励方差（即无梯度贡献）时提前终止采样。实验表明，该方法在ALFWorld任务中不仅将训练速度提升了10.7%，还通过减少梯度稀释提升了模型的泛化性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05802v1/fig-001.webp\", \"caption\": \"Table 2: Experimental setup across the three integration tiers. In Tier 1 the same 100 tasks are run twice (baseline and gated) with matched random seeds. In Tier 3 the trained policy weights are loaded back into the inference engine after every iteration.\", \"page\": 6, \"index\": 1, \"width\": 772, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-002.webp\", \"caption\": \"Table 12: Strong-baseline arm comparison on the same N=100 buffer; cut count, TP/FP composition, precision, rollout step-tokens saved, and GRPO advantage L2-norm preserved (ratio to no-gate). Random-cut at the same ∼23-cut budget destroys ∼12 pp of L2 because its FPs land on non-zerovariance groups whose advantages are real. Oracle (cut iff truly zero-variance) sets the upper bound at 23.1% saving; our single-axis gate dK < 0.12 recovers 51% of that at 96.7% L2-preservation. DAPO-oracle and OURS+DAPO save the same rollout step-tokens as their pre-DAPO counterparts because DAPO is a post-rollout filter that drops groups from the gradient batch only; it changes the training-phase cost (App. E.7), not the rollout-phase cost. The single-axis τK ≥ 0.90 clause on its own saves zero rollout compute because the groups it fires on have already terminated by step K.\", \"page\": 23, \"index\": 2, \"width\": 836, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-003.webp\", \"caption\": \"Table 5: Tier 3. On-policy GRPO training A/B with the gate (K = 10, dL = 0.12) on Qwen2.57B + LoRA, 60 iterations, 10 prompts/iter, G = 8, averaged over n = 4 seeds ({7, 13, 23, 42}). Held-out eval is greedy on 50 valid_seen ALFWorld tasks, never seen during training. Per-seed pre-training eval values are 38, 44, 38, 38% (mean 39.5±3.0%); per-seed held-out delta at iter 60 is +4,+6,−2,+2 pp. Standard deviations across the 4 seeds: total wall-clock ±187 / ±534 s; gradient L2 ±0.001 / ±0.008; held-out eval ±4.4 / ±7.1 pp.\", \"page\": 8, \"index\": 3, \"width\": 765, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-004.webp\", \"caption\": \"Figure 4: On-policy GRPO A/B on Qwen2.5-7B + LoRA, gate CUTK=10,dL=0.12, averaged over n = 4 seeds {7, 13, 23, 42}; bands are ±1 std across seeds. Top-left: held-out success on 50 unseen tasks (every 10 iter). Top-right: cumulative wall-clock; gated runs 10.7% shorter at iter 60. Bottom-left: per-iter mean training reward (5-iter rolling), gated averaged over uncut groups only. Bottom-right: per-iter gate cut count (orange) vs. baseline zero-variance count (blue); the gate fires predominantly on the same groups the baseline arm later finds to be zero-variance.\", \"page\": 8, \"index\": 4, \"width\": 767, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-005.webp\", \"caption\": \"Figure 2: Spearman ρ (left) and AUROC for non-zero-variance classification (right) for each (metric, K) cell, N=100, G=8. The single-axis signal peaks at K∈{10, 15} and degrades at the extremes.\", \"page\": 4, \"index\": 5, \"width\": 749, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-006.webp\", \"caption\": \"Figure 5: Spearman ρ between mid-rollout divergence dK and final reward variance σ2 r , plotted as a function of the gate evaluation step K, for two group sizes (G=8 and G=16) on the same 100 ALFWorld tasks. The shape of the curve is the same in both group sizes; the peak value is slightly lower at G=16 (0.37 vs 0.42), which we explain in the text.\", \"page\": 19, \"index\": 6, \"width\": 551, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-007.webp\", \"caption\": \"Table 8: Per-task-type AUROC for the (binary) zero-variance vs non-zero-variance classification at the published gate’s evaluation step K=10. Global d10 AUROC is the metric the gate actually uses; Best AUROC is the maximum over (metric,K) tuned per task type, with the winning pair in parentheses. n = groups in that type; nzv = zero-variance groups (the positive class).\", \"page\": 19, \"index\": 7, \"width\": 862, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-008.webp\", \"caption\": \"Figure 7: Mechanistic evolution across the 60-iteration on-policy GRPO training, averaged over n=4 seeds {7, 13, 23, 42}; shaded bands are ±1 std across seeds. Top-left: per-iter zv rate (5-iter rolling) drifts downward in the baseline as the policy learns; gated tracks closely on the uncut groups. Top-right: gate cut rate (orange) vs ground-truth zv rate (blue) per iter; per-iter recall averages 37.2%. Bottom-left: gradient L2 norm — gated runs 1.16× baseline on average (1.16± 0.07 across seeds), matching the dilution prediction (§4.4, Table 5). Bottom-right: zero-advantage-item rate in the train batch: baseline 40.6%, gated 25.1%, a −15.5 pp reduction which is the proximate mechanism for the held-out gain.\", \"page\": 24, \"index\": 8, \"width\": 767, \"height\": 502}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-009.webp\", \"caption\": \"Figure 6: 20-step off-policy GRPO training A/B on Qwen2.5-7B + LoRA. Top left: cumulative wall-clock; the gated arm finishes 32.2% faster end-to-end. Top right: per-step gradient L2-norm; the gated arm runs at about 114% of baseline. Bottom left: number of training items per step (one item = one trajectory × one action position; max 128 as explained above). The baseline arm always uses the maximum 128 items because no group is cut. The gated arm drops to between 64 and 128 items per step depending on how many groups the gate cut at that step. Bottom right: per-step training loss. Both arms sit in the same noisy band around zero. This is expected: by definition the GRPO advantages within a group sum to zero, so the population loss for the population gradient estimator is zero up to noise. A loss near zero in this plot does not mean the policy stopped learning; held-out evaluation in Tier 3 is the metric that captures policy quality.\", \"page\": 20, \"index\": 9, \"width\": 764, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-010.webp\", \"caption\": \"Figure 3: Distribution of dK=10 on the N = 100 buffer, coloured by the group’s final outcome (determined only after the full rollout completes, by inspecting the multiset of G final rewards). At dL=0.12 (red dashed line) the gate catches 7/14 all-succeed and 10/25 all-fail groups (the 17 true positives) plus 4/61 mixed groups (false positives). The high-dK tail of the all-fail cluster overlaps with the mixed cluster, so raising dL would break the precision floor (§5).\", \"page\": 5, \"index\": 10, \"width\": 618, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2605.05802v1/fig-011.webp\", \"caption\": \"Table 6: Hyperparameters across the three integration tiers.\", \"page\": 16, \"index\": 11, \"width\": 826, \"height\": 340}]"
motivation: "针对GRPO训练中约40%的样本组因奖励一致而产生零梯度、导致大量计算资源被浪费的问题。"
method: 引入一种单参数门控机制，当并行轨迹的部分动作序列平均成对编辑距离低于阈值时，判定其将收敛于相同结果并提前停止。
result: "在Qwen2.5-7B模型上的实验显示，该方法使训练耗时缩短10.7%，并在测试集上取得了2.5%的成功率提升。"
conclusion: 证明了在轨迹中途进行干预能有效优化RL训练效率，并能通过改善梯度质量来增强智能体的学习效果。
---

## Abstract
Group-relative RL training (GRPO) samples a small group of parallel rollouts for every training prompt and uses their within-group reward spread to compute per-trajectory advantages. In agentic environments each rollout is a long multi-turn dialogue with one LLM call per step, so this multi-sample multiplier dominates the total training cost. When every rollout of a prompt ends with the same reward, the group has zero reward variance and contributes no gradient, so the extra rollouts add no information; such groups are common in practice (typically around 40% of all groups), so the wasted-compute fraction is substantial rather than marginal. Existing methods filter such groups at the prompt level, either after their rollouts are paid for or before any rollout begins, but both decide without using information that becomes available during the rollout itself. We instead ask whether the in-group divergence between the partial trajectories at an intermediate step can already predict that the group will be zero-variance: when the parallel rollouts have already converged on the same action prefix, the group is on track to produce a single reward, and we can stop early. We propose a one-parameter gate that stops a group when the mean pairwise prefix edit distance between its partial action sequences falls below a threshold. On a 60-iteration on-policy GRPO run on ALFWorld with Qwen2.5-7B, averaged over four random seeds, the gated arm finishes 10.7% faster in wall-clock (bootstrap 95% CI excludes 0) and shifts held-out success rate on 50 unseen tasks by +2.5 pp, with the held-out gain tracing to a measurable reduction in zero-advantage gradient-batch dilution. Code is available at https://github.com/zhiyuanZhai20/selective-rollout.

---

## 论文详细总结（自动生成）

这篇论文由复旦大学的研究团队提出，旨在解决大语言模型（LLM）在智能体（Agent）场景下进行强化学习（RL）训练时计算资源浪费的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多样本强化学习（如 GRPO）中，每个提示词（Prompt）需要生成多个并行轨迹（Rollouts）来计算相对优势。在智能体任务中，这些轨迹通常很长（20-30步），导致采样阶段占据了约 95% 的训练耗时。
*   **研究动机**：作者观察到，实践中约 40% 的样本组具有“零奖励方差”（即组内所有轨迹的最终奖励相同）。这些样本组产生的梯度为零，对模型更新毫无贡献，导致了巨大的计算浪费。现有的过滤方法要么在采样前（仅凭 Prompt），要么在采样后（浪费了采样计算），缺乏在采样过程中根据中间信息进行干预的手段。

### 2. 方法论：选择性回滚（Selective Rollout）
*   **核心思想**：在轨迹生成的中间步骤 $K$，通过评估组内并行轨迹的**分歧度（Divergence）**来预测该组是否会走向零方差结果。如果轨迹已经高度趋同，则提前终止采样。
*   **关键技术细节**：
    *   **门控机制**：引入一个单参数门控。在第 $K$ 步时，计算组内 $G$ 个部分轨迹的**平均成对前缀编辑距离（Mean Pairwise Prefix Edit Distance, $d_K$）**。
    *   **判定逻辑**：如果 $d_K$ 低于阈值 $d_L$，则判定该组已收敛（无论是共同成功还是共同陷入死循环），立即停止该组剩余步骤的生成。
    *   **训练集成**：被门控拦截的样本组将直接从梯度更新批次中剔除，既节省了后半段的采样成本，也节省了反向传播的计算成本。
*   **公式逻辑**：$Gate( \tau_{1:G} ) = [d_K < d_L]$。当门控触发时，该组对策略梯度损失函数 $L(\theta)$ 的贡献被设为零。

### 3. 实验设计
*   **场景与数据集**：主要在 **ALFWorld**（具身智能体交互环境）上进行验证，涵盖 6 种任务类型。
*   **基座模型**：Qwen2.5-7B-Instruct。
*   **Benchmark**：标准的 GRPO 算法（无门控）。
*   **对比与验证维度**：
    *   **Tier 1 (采样层)**：纯采样 A/B 测试，衡量墙钟时间（Wall-clock）节省。
    *   **Tier 2 (离线层)**：在固定缓存上训练，衡量训练阶段的加速。
    *   **Tier 3 (在线层)**：全流程在线 GRPO 训练，衡量最终模型性能和泛化能力。
    *   **对比方法**：包括随机裁剪（Random-cut）、理想预知门控（Oracle）、以及后置过滤方法（DAPO）。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA RTX 6000 Ada (48GB) GPU。
*   **算力消耗**：
    *   Tier 1 使用 4 张 GPU 并行。
    *   Tier 2/3 使用 2 张 GPU（一张采样，一张更新）。
    *   单次在线训练任务（60 轮迭代）耗时约 4.5 - 4.8 小时。
    *   论文总计实验消耗约 30 GPU-hours。

### 5. 实验数量与充分性
*   **实验规模**：
    *   离线分析使用了 100 个样本组。
    *   在线训练进行了 60 轮迭代，每轮 10 个 Prompt，组大小 $G=8$。
    *   **重复实验**：在线训练使用了 4 个随机种子进行重复，并提供了 95% 的置信区间（Bootstrap CI）。
    *   **消融实验**：对比了 7 种不同的分歧度指标（如动作熵、唯一前缀比例等），验证了不同 $K$ 值和组大小 $G$（8 vs 16）的影响。
*   **充分性评价**：实验设计较为严谨，通过三层验证（Tier 1-3）清晰地剥离了采样节省和训练优化的贡献，统计学分析（如置信区间排除 0）增强了结论的可信度。

### 6. 主要结论与发现
*   **效率提升**：在在线训练中，该方法实现了 **10.7% 的端到端墙钟时间节省**。
*   **性能增强**：在 50 个未见任务的测试集上，成功率提升了 **2.5 个百分点**。
*   **机制解释（梯度稀释理论）**：论文发现，剔除零方差样本组实际上减少了梯度批次中的“稀释”效应。因为零优势样本虽然不贡献梯度，但会占用分母，导致有效梯度变小。剔除它们后，有效梯度模长提升了约 1.16 倍，从而加快了学习。

### 7. 优点与亮点
*   **简单高效**：仅需一个超参数（阈值 $d_L$），计算开销极低（微秒级）。
*   **即插即用**：作为一种采样策略，它可以与现有的 Prompt 过滤方法或后置梯度过滤方法（如 DAPO）完美兼容。
*   **理论闭环**：不仅展示了速度提升，还从“梯度稀释”的角度合理解释了为什么减少计算量反而能提升模型性能。

### 8. 不足与局限
*   **任务覆盖面**：实验主要集中在 ALFWorld，尚未在 WebShop 或数学推理等其他长轨迹任务上广泛验证。
*   **信号局限性**：基于动作前缀的分歧度无法识别“虽然动作不同但最终奖励都为零”的情况（即高分歧度的失败组），这部分浪费仍未被完全回收。
*   **静态阈值**：中间步骤 $K$ 是预设的固定值，而非根据任务难度动态调整。
*   **统计显著性**：虽然性能有提升趋势，但在 4 个种子的样本量下，成功率的提升在统计学上尚未达到极显著水平（存在一定的波动）。

（完）
