---
title: Recursive Agent Optimization
title_zh: 递归智能体优化
authors: "Apurva Gandhi, Satyaki Chakraborty, Xiangjun Wang, Aviral Kumar, Graham Neubig"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06639v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 训练递归智能体的强化学习方法
tldr: 本研究提出递归智能体优化（RAO），这是一种通过强化学习训练递归智能体的新方法。该智能体能够递归地创建并委派子任务给自身的实例，实现推理时扩展。RAO 解决了智能体在何时及如何进行任务委派和通信的训练难题，使其能够通过分治策略处理超出上下文窗口的长文本任务，并显著提升了在复杂问题上的泛化能力、训练效率及运行速度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06639v1/fig-001.webp\", \"caption\": \"Figure 9: ART-E (Email Search) training curves for Qwen-3-14B (moving average; window size 10).\", \"page\": 15, \"index\": 1, \"width\": 650, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.06639v1/fig-002.webp\", \"caption\": \"Figure 1: Example of recursive agent inference on a deep research or travel-planning task where agents can decide to spawn and delegate sub-problems to sub-agents. This produces a dynamically structured and recursive execution tree where each node corresponds to one agent instance attempting to solve an assigned task. RAO optimizes a single policy to act across all levels tree, teaching it to best take advantage of recursive inference.\", \"page\": 2, \"index\": 2, \"width\": 826, \"height\": 613}, {\"url\": \"assets/figures/arxiv/2605.06639v1/fig-003.webp\", \"caption\": \"Table 1: TEXTCRAFT-SYNTH results across evaluation difficulties. Success rate (SR) is computed over the evaluation set. Steps and wall-clock time are computed over the intersection of tasks successfully solved by both methods at each difficulty.\", \"page\": 8, \"index\": 3, \"width\": 836, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.06639v1/fig-004.webp\", \"caption\": \"Figure 4: TEXTCRAFT-SYNTH training curvess (moving average; window size 10).\", \"page\": 8, \"index\": 4, \"width\": 692, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2605.06639v1/fig-005.webp\", \"caption\": \"Figure 2: RAO Reward Design. Each node receives a local reward from its own success and a delegation bonus from the success rate of its children. The example uses λ = 0.4.\", \"page\": 4, \"index\": 5, \"width\": 323, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2605.06639v1/fig-006.webp\", \"caption\": \"Table 9: TEXTCRAFT-SYNTH token usage on the intersection of tasks solved by both single and recursive agents in the 40K/256K setting.\", \"page\": 17, \"index\": 6, \"width\": 506, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.06639v1/fig-007.webp\", \"caption\": \"Table 10: OOLONG-REAL token usage on the intersection of tasks with non-zero score for both single and recursive agents.\", \"page\": 17, \"index\": 7, \"width\": 498, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.06639v1/fig-008.webp\", \"caption\": \"Figure 5: OOLONG-REAL training curves (moving average; window size 10).\", \"page\": 9, \"index\": 8, \"width\": 651, \"height\": 232}]"
motivation: 旨在解决现有智能体在处理长上下文和复杂任务时难以有效扩展和泛化的局限性。
method: 提出一种强化学习框架，训练智能体学会递归地生成子智能体并进行任务委派与通信。
result: 实验表明递归智能体在训练效率、处理超长上下文能力以及对极难任务的泛化性上均优于单智能体系统。
conclusion: 递归智能体优化为实现推理时扩展提供了一种高效路径，通过分治策略显著提升了复杂任务的处理性能。
---

## 摘要
我们引入了递归智能体优化（Recursive Agent Optimization, RAO），这是一种用于训练递归智能体的强化学习方法。递归智能体可以递归地生成自身的新实例并向其委派子任务。递归智能体实现了一种推理时扩展算法，使其能够通过分治法自然地扩展到更长的上下文，并泛化到更困难的问题。RAO 提供了一种训练模型以充分利用此类递归推理的方法，教会智能体何时以及如何进行委派和通信。我们发现，以这种方式训练的递归智能体具有更高的训练效率，能够处理超出模型上下文窗口的任务，可以泛化到比训练任务困难得多的任务，并且与单智能体系统相比，可以缩短实际运行时间。

## Abstract
We introduce Recursive Agent Optimization (RAO), a reinforcement learning approach for training recursive agents: agents that can spawn and delegate sub-tasks to new instantiations of themselves recursively. Recursive agents implement an inference-time scaling algorithm that naturally allows agents to scale to longer contexts and generalize to more difficult problems via divide-and-conquer. RAO provides a method to train models to best take advantage of such recursive inference, teaching agents when and how to delegate and communicate. We find that recursive agents trained in this way enjoy better training efficiency, can scale to tasks that go beyond the model's context window, generalize to tasks much harder than the ones the agent was trained on, and can enjoy reduced wall-clock time compared to single-agent systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Recursive Agent Optimization》（递归智能体优化，简称 RAO）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型语言模型（LLM）智能体在处理长程（Long-horizon）、大内存需求和复杂探索任务时面临挑战。虽然“分治法”（Divide-and-Conquer）是解决此类问题的自然策略，但现有的递归或多智能体系统大多仅将递归作为推理时的“脚手架”（Scaffold），而模型本身并未经过专门训练来决定何时委派任务、如何制定子任务以及如何整合结果。
*   **研究动机**：旨在通过强化学习（RL）训练一种“递归智能体”，使其能够自主、动态地生成执行树，通过递归调用自身实例来扩展推理能力，从而突破上下文窗口限制并提升复杂任务的泛化性。

### 2. 方法论
*   **核心思想**：RAO 将递归执行建模为一个动态生成的树状轨迹，并对树中的所有节点（即智能体实例）进行联合策略优化。
*   **关键技术细节**：
    *   **动作空间扩展**：在 Python REPL 环境中引入异步函数 `launch_subagent`，允许智能体派生子智能体。
    *   **局部节点奖励（Local Node Reward）**：每个节点 $X$ 的奖励由两部分组成：
        $$R(X, \tau_X) = \tilde{s}(X, \tau_X) + \lambda \cdot \frac{1}{|C(X)|} \sum_{c \in C(X)} \tilde{s}(c, \tau_c)$$
        其中 $\tilde{s}$ 是任务成功信号，$\lambda$ 是委派奖励系数。这种设计提供了密集的信用分配（Credit Assignment）。
    *   **策略优化目标**：在递归生成的不同深度的任务分布上优化共享策略 $\pi_\theta$。
    *   **留一法基准（Leave-one-out Baseline）**：为了降低方差且保持无偏，使用同组内其他根任务奖励的平均值作为基准。
    *   **深度频率加权**：针对深层节点数量远多于根节点的问题，采用深度级别的逆频率加权，防止优化被深层轨迹主导。

### 3. 实验设计
*   **数据集/场景**：
    1.  **TextCraft-Synth**：受 Minecraft 启发的合成合成任务，可控制任务深度（简单、中等、困难）。
    2.  **Oolong-Real**：长文本推理基准，涉及超长（55K-220K token）的龙与地下城（D&D）游戏转录文本。
    3.  **DeepDive**：深度搜索任务，需要多步、顺序依赖的网络搜索和信息整合。
*   **对比方法**：主要对比了**单智能体（Single-agent）**基准（扁平化执行）与**递归智能体（Recursive-agent）**。
*   **实验设置**：包括受限上下文（如 8K/32K）和非受限上下文设置，测试模型在训练分布之外的泛化能力。

### 4. 资源与算力
*   **训练平台**：使用了 **Tinker** 训练服务（支持 LoRA 训练）和 **AReaL** 异步强化学习系统。
*   **硬件设备**：推理评估使用了 **8x H200 GPU** 节点。
*   **模型**：主要使用 Qwen-3-4B-Instruct 和 Qwen-3-VL-30B。
*   **算力细节**：文中提到 Oolong-Real 训练受限于 32K token 上下文窗口；TextCraft 训练约 250 步，DeepDive 约 75 步。具体总 GPU 小时数未明确标出，但强调了异步 RL 的使用。

### 5. 实验数量与充分性
*   **实验规模**：在三个截然不同的领域（合成合成、长文本聚合、深度搜索）进行了实验，涵盖了顺序委派和并行委派两种模式。
*   **消融实验**：针对“奖励设计（是否有委派奖金）”和“轨迹加权（是否使用深度加权）”进行了消融，证明了 RAO 各组件的必要性。
*   **客观性**：实验对比了相同模型在不同架构下的表现，并测试了在更难任务上的零样本泛化（Out-of-distribution generalization），评估维度包括成功率、步数、实际运行时间和 Token 消耗，较为全面。

### 6. 主要结论与发现
*   **突破上下文限制**：递归智能体能通过将长文本拆分给子智能体，在 8K/32K 的训练窗口下解决需要 175K+ token 的任务。
*   **更强的泛化性**：仅在中等难度任务上训练，递归智能体在困难任务上的表现远超单智能体（如 TextCraft 困难模式成功率 88% vs 20%）。
*   **训练效率高**：递归结构提供了天然的课程学习（Curriculum Learning），使模型学习速度更快。
*   **运行速度优势**：在可并行任务中，递归智能体通过并发执行显著降低了实际墙钟时间（最高提升 2.5 倍）。

### 7. 优点
*   **端到端训练**：不同于以往仅在推理时套用模板，RAO 真正教会了模型“如何委派”。
*   **动态性**：支持任意深度的动态执行树，而非固定的层级结构。
*   **推理时扩展**：展示了通过增加计算深度（Test-time compute）来解决更难问题的潜力。

### 8. 不足与局限
*   **领域局限性**：目前的实验是在特定领域内分别训练的，尚未实现跨领域的通用递归策略训练。
*   **推理成本**：在顺序依赖任务（如 DeepDive）中，递归会导致 Token 消耗和延迟大幅增加（比单智能体慢 18 倍）。
*   **对 Judge 的依赖**：在缺乏自动验证器的场景下，训练高度依赖于 LLM-Judge 的打分质量，可能引入偏差。
*   **采样开销**：完整的递归回滚（Rollout）成本极高，在大规模生产环境中可能面临训练效率瓶颈。

（完）
