---
title: "GEAR: Granularity-Adaptive Advantage Reweighting for LLM Agents via Self-Distillation"
title_zh: GEAR：通过自蒸馏实现的大语言模型智能体粒度自适应优势重加权
authors: "Sijia Li, Yuchen Huang, Zifan Liu, Yanping Li, Jingjing Fu, Li Zhao, Jiang Bian, Ling Zhang, Jun Zhang, Rui Wang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11853v1"
tags: ["query:ag-rl"]
score: 9.5
evidence: 提出了一种基于自适应信用分配的LLM智能体强化学习框架。
tldr: "针对LLM智能体在强化学习中面临的粗粒度奖励和信用分配难题，本文提出GEAR框架。该框架通过自蒸馏机制，利用学生模型与教师模型间的散度信号，动态识别轨迹中的关键偏差点，实现从Token到片段级的自适应优势重加权。实验证明，GEAR在数学推理和工具使用等长程任务中显著优于GRPO等基准，尤其在挑战性任务中提升高达20%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11853v1/fig-001.webp\", \"caption\": \"Figure 2: Frequency of top-20 tokens with normalized reverse-KL > 0.1 in 100 sampled math trajectories.\", \"page\": 15, \"index\": 1, \"width\": 664, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2605.11853v1/fig-002.webp\", \"caption\": \"Figure 3: Token-level visualization results of normalized KL divergence and normalized entropy.\", \"page\": 15, \"index\": 2, \"width\": 706, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2605.11853v1/fig-003.webp\", \"caption\": \"Table 1: Performance (%) on agentic tool-use and mathematical reasoning benchmarks. Agent: TS-M = ToolSandbox Multi-Tool, BFCL Avg. = average over BFCL Parallel-Multiple / Parallel / Multiple, ACE-E = ACEBench End-to-End accuracy, ACE-P = ACEBench Process accuracy. Math: Test(in domain), A24 = AIME24, A25 = AIME25, M500 = MATH500, GSM = GSM8K, MH = MATH. Best results are bolded.\", \"page\": 8, \"index\": 3, \"width\": 788, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2605.11853v1/fig-004.webp\", \"caption\": \"Table 2: Ablation study of GEAR. We evaluate the contribution of each component and the sensitivity to key hyperparameters. The default hyperparameter values are λH = 1.5, α = 0.2, and λKL = 0.1.\", \"page\": 9, \"index\": 4, \"width\": 398, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2605.11853v1/fig-005.webp\", \"caption\": \"Figure 1: Illustration of GEAR for fine-grained credit assignment in agent RL. (a) GRPO assigns the same trajectory-level advantage to all tokens. (b) GEAR preserves this trajectory-level advantage while redistributing credit at a finer granularity. It computes token-wise reverse KL divergence between the student and a groundtruth–conditioned teacher, then uses KL peaks to identify segment onsets and entropy to determine segment extent. Within each divergent segment, all tokens share the onset credit, propagating a localized discrepancy signal over coherent spans. For consistent regions with low KL, token-level reverse KL is used directly. The resulting signals are then modulated by a sign-aware weighting function to produce the final token-level credit.\", \"page\": 5, \"index\": 5, \"width\": 810, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2605.11853v1/fig-006.webp\", \"caption\": \"Figure 4: Training curves of GRPO, GEAR and its variants. The left panel shows the mean training reward, while the right panel illustrates the policy entropy. GEAR achieves the highest training reward while maintaining higher policy entropy compared to the GRPO baseline and other ablated versions.\", \"page\": 16, \"index\": 6, \"width\": 829, \"height\": 285}]"
motivation: 传统的强化学习主要依赖粗粒度的结果奖励，难以在长程轨迹中准确进行信用分配。
method: 通过对比学生模型与教师模型的输出散度，识别语义偏差锚点，并据此动态调整Token或片段级的优势权重。
result: 在八项数学推理和工具使用基准测试中，GEAR一致优于GRPO及其他信用分配方法，在低准确率任务上提升尤为显著。
conclusion: GEAR通过自适应粒度的信用分配，有效解决了LLM智能体在复杂任务中的训练效率和性能瓶颈。
---

## 摘要
强化学习已成为大语言模型（LLM）智能体广泛使用的后训练方法，其训练通常依赖于仅提供粗粒度监督的结果级奖励。虽然更细粒度的信用分配有望实现有效的策略更新，但获取可靠的局部信用并将其分配到长时程轨迹的正确部分仍然是一个开放性挑战。在本文中，我们提出了粒度自适应优势重加权（GEAR），这是一种自适应粒度的信用分配框架，它利用源自自蒸馏的 Token 级和片段级信号来重塑轨迹级的 GRPO 优势。GEAR 将同策略学生模型与以地面真值为条件的教师模型进行比较，以获取参考引导的散度信号，从而识别自适应片段边界并调节局部优势权重。这种散度通常在语义偏离开始时激增，而同一自回归延续中的后续 Token 可能会恢复到低散度。因此，GEAR 将此类激增视为自适应信用区域的锚点：在学生模型与教师模型保持一致的地方，保留 Token 级分辨率；在发生偏离的地方，GEAR 将相应的后续内容归组为一个自适应片段，并利用偏离点的散度来调节该片段的优势。在 Qwen3 4B 和 8B 模型上进行的八个数学推理和智能体工具使用基准测试表明，GEAR 一贯优于标准的 GRPO、仅自蒸馏的基准方法以及 Token 级或轮次级信用分配方法。在 GRPO 基准准确率较低的测试中，提升尤为显著，比 GRPO 高出约 20%，这表明所提出的自适应重加权方案在更具挑战性的长时程场景中特别有用。

## Abstract
Reinforcement learning has become a widely used post-training approach for LLM agents, where training commonly relies on outcome-level rewards that provide only coarse supervision. While finer-grained credit assignment is promising for effective policy updates, obtaining reliable local credit and assigning it to the right parts of the long-horizon trajectory remains an open challenge. In this paper, we propose Granularity-adaptivE Advantage Reweighting (GEAR), an adaptive-granularity credit assignment framework that reshapes the trajectory-level GRPO advantage using token- and segment-level signals derived from self-distillation. GEAR compares an on-policy student with a ground-truth-conditioned teacher to obtain a reference-guided divergence signal for identifying adaptive segment boundaries and modulating local advantage weights. This divergence often spikes at the onset of a semantic deviation, while later tokens in the same autoregressive continuation may return to low divergence. GEAR therefore treats such spikes as anchors for adaptive credit regions: where the student remains aligned with the teacher, token-level resolution is preserved; where it departs, GEAR groups the corresponding continuation into an adaptive segment and uses the divergence at the departure point to modulate the segment' s advantage. Experiments across eight mathematical reasoning and agentic tool-use benchmarks with Qwen3 4B and 8B models show that GEAR consistently outperforms standard GRPO, self-distillation-only baselines, and token- or turn-level credit-assignment methods. The gains are especially strong on benchmarks with lower GRPO baseline accuracy, reaching up to around 20\% over GRPO, suggesting that the proposed adaptive reweighting scheme is especially useful in more challenging long-horizon settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《GEAR: Granularity-Adaptive Advantage Reweighting for LLM Agents via Self-Distillation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）智能体在执行复杂、多步骤任务时，通常只能获得轨迹级的“结果奖励”（Outcome Reward）。这种粗粒度的反馈导致了**信用分配（Credit Assignment）难题**：难以确定长轨迹中哪些具体的 Token 或动作导致了最终的成功或失败。
*   **研究动机**：现有的细粒度信用分配方法要么依赖预定义的单位（如 Token 或轮次），要么依赖不稳定的内在信号（如熵或不确定性）。作者认为，有效的信用分配需要将信用信号与行为单元（语义片段）动态耦合，从而在不引入额外奖励模型的情况下，实现更精准的策略优化。

### 2. 方法论（GEAR 框架）
GEAR 的核心思想是利用**自蒸馏（Self-Distillation）**产生的信号来动态调整轨迹中不同部分的权重。
*   **自蒸馏信用信号**：计算同策略“学生模型”与以地面真值（Ground Truth）为条件的“教师模型”之间的**逆向 KL 散度（Reverse KL Divergence）**。散度越大，表示学生模型偏离了正确路径。
*   **自适应粒度分割**：
    *   **触发点**：当逆向 KL 散度出现峰值（通常在“所以”、“但是”等语义转折词处）时，标记为偏离的开始。
    *   **边界确定**：利用 **Token 熵（Entropy）**作为辅助信号。当熵值显著增加（代表不确定性或语义转换）时，确定为片段的结束。
    *   **片段传播**：在识别出的偏离片段内，将起始点的偏差信号传播至整个片段，避免更新过于破碎。
*   **优势重加权（Advantage Reweighting）**：
    *   根据轨迹优势函数 $A$ 的正负号进行非对称调节：
        *   若轨迹成功（$A > 0$），降低高偏差 Token 的权重，防止强化不稳定的偏离行为。
        *   若轨迹失败（$A < 0$），增加高偏差 Token 的权重，加大惩罚力度。
    *   该方法直接修改 GRPO 的目标函数，无需额外的过程奖励模型（PRM）。

### 3. 实验设计
*   **模型**：Qwen3-4B-Base 和 Qwen3-8B-Base。
*   **数据集/场景**：
    *   **数学推理**：GSM8K、MATH、MATH500、AIME 2024/2025（考察长程推理与工具调用）。
    *   **智能体工具使用**：ToolSandbox（状态化环境）、BFCL（函数调用）、ACEBench（多步执行）。
*   **对比基准（Baselines）**：
    *   **GRPO**：标准的轨迹级结果强化学习。
    *   **OPSD / OPSD+RL**：纯自蒸馏方法。
    *   **ARPO**：基于熵触发的分支探索方法。
    *   **MT-GRPO**：基于轮次级（Turn-level）信用分配的方法。

### 4. 资源与算力
*   **硬件**：使用了 **13 台 NVIDIA H100 GPU**。
*   **训练时长**：
    *   数学推理任务：4B 模型单 Epoch 约需 **26 小时**。
    *   工具使用任务：单 Epoch 约需 **10 小时**。
*   **效率**：作者指出 GEAR 的计算开销与标准 GRPO 相当，具有较好的训练效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个主流基准测试，跨越数学和工具调用两大领域。
*   **消融实验**：非常充分。作者对比了“无片段重加权”、“无熵终止条件”、“无 KL 触发”以及“固定工具调用边界分割”等多种变体，验证了自适应分割的必要性。
*   **客观性**：通过在不同规模（4B/8B）模型上验证，并进行了跨领域泛化测试（在混合数据上训练并在独立任务上评估），证明了方法的鲁棒性和公平性。

### 6. 主要结论与发现
*   **性能提升显著**：GEAR 在所有基准上均优于 GRPO。在任务难度越高（GRPO 表现越差）的场景下，GEAR 的提升越明显，最高可达 **20%**。
*   **泛化能力强**：在 AIME 等竞赛级数学题上，GEAR 表现出比 GRPO 更好的泛化性能，减少了对训练分布的过拟合。
*   **训练稳定性**：GEAR 在提升奖励的同时，保持了较高的策略熵，有效防止了策略过早坍缩到次优解。

### 7. 优点
*   **无需额外标注**：利用模型自身的预测动力学生成信号，不需要昂贵的步骤级人工标注或预训练的奖励模型。
*   **动态粒度**：突破了固定 Token 或轮次的限制，能够识别语义上的“关键决策点”。
*   **即插即用**：作为一种优势重加权方案，可以轻松集成到现有的 GRPO 或 PPO 框架中。

### 8. 不足与局限
*   **依赖地面真值**：GEAR 需要 Ground Truth 来构建教师模型。在完全开放、无标准答案的任务中（如创意写作或开放式对话），该方法难以直接应用。
*   **教师模型质量限制**：如果模型本身在某些领域极弱，即使以 GT 为条件，教师模型也可能无法提供高质量的引导信号。
*   **超参数敏感性**：虽然论文给出了推荐值，但 KL 触发阈值和熵终止阈值在不同任务间可能需要微调。

（完）
