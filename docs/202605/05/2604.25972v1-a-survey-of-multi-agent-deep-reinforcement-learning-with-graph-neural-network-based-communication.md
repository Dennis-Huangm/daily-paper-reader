---
title: A Survey of Multi-Agent Deep Reinforcement Learning with Graph Neural Network-Based Communication
title_zh: 基于图神经网络通信的多智能体深度强化学习综述
authors: "Valentin Cuzin-Rambaud, Laetitia Matignon, Maxime Morge"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25972v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 多智能体深度强化学习中基于GNN通信的综述
tldr: 本文针对多智能体强化学习（MARL）中基于图神经网络（GNN）的通信机制进行了全面综述。在MARL中，智能体通过共享信息来协调行动，而GNN能有效处理基于交互图的信息交换。针对该领域缺乏统一分类框架的问题，本文系统梳理了近期研究，并提出了一个通用的基于GNN的通信流程框架，旨在提升该领域概念的清晰度与可访问性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25972v1/fig-001.webp\", \"caption\": \"Figure 1: An example of MPNN: the green node, aggregates features from its 1-hop neighbors ∈ N (1)\", \"page\": 3, \"index\": 1, \"width\": 493, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.25972v1/fig-002.webp\", \"caption\": \"Table 1: Taxonomy of GNN-based communication methods, organized in two categories: proxy-based (upper part) and distributed-based (lower part) according to the generic Algorithm 2. E(·) encodes the local representation, N t r (·) defines reachable agents, Gbuild(·) builds a graph, GNN is the architecture used, and Inner integration shows how to leverage the final representation.\", \"page\": 7, \"index\": 2, \"width\": 1037, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.25972v1/fig-003.webp\", \"caption\": \"Table 2: Tested communication constraints during execution\", \"page\": 9, \"index\": 3, \"width\": 469, \"height\": 335}]"
motivation: 旨在解决多智能体强化学习领域中，基于图神经网络的通信方法缺乏明确分类结构和统一框架的问题。
method: 通过对近期相关文献进行系统性调研，提出了一个通用的基于GNN的通信处理流程。
result: 成功构建了一个能够区分和分类不同GNN通信MARL方法的结构化框架。
conclusion: 该综述为理解和开发基于图神经网络的多智能体协作通信机制提供了重要的理论参考和标准化视角。
---

## 摘要
在多智能体强化学习（MARL）中，通信机制的集成使智能体能够通过共享信息更好地学习协调动作并收敛于目标。基于交互图，一类子方法利用图神经网络（GNN）来学习通信，使智能体能够通过交换信息来丰富并改进其内部表示。随着研究的增长，我们注意到缺乏明确的结构和框架来区分和分类基于 GNN 通信的 MARL 方法。因此，本文综述了该领域的近期研究工作。我们提出了一个通用的基于 GNN 的通信过程，旨在使这些方法背后的底层概念更加清晰且易于理解。

## Abstract
In multi-agent reinforcement learning (MARL), the integration of a communication mechanism, allowing agents to better learn to coordinate their actions and converge on their objectives by sharing information. Based on an interaction graph, a subclass of methods employs graph neural networks (GNNs) to learn the communication, enabling agents to improve their internal representations by enriching them with information exchanged. With growing research, we note a lack of explicit structure and framework to distinguish and classify MARL approaches with communication based on GNNs. Thus, this paper surveys recent works in this field. We propose a generalized GNN-based communication process with the goal of making the underlying concepts behind the methods more obvious and accessible.

---

## 论文详细总结（自动生成）

这是一份关于论文《基于图神经网络通信的多智能体深度强化学习综述》（A Survey of Multi-Agent Deep Reinforcement Learning with Graph Neural Network-Based Communication）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在多智能体强化学习（MARL）中，智能体面临**部分可观测性**（无法看到全局状态）和**非平稳性**（其他智能体也在学习，导致环境动态变化）的挑战。
*   **核心问题**：虽然引入通信机制可以缓解上述问题，且图神经网络（GNN）因其处理图结构数据的天然优势而成为学习通信的热门工具，但该领域目前缺乏一个统一的框架来区分和分类各种基于 GNN 的通信方法。
*   **整体含义**：本文旨在通过系统性综述，提出一个通用的 GNN 通信流程模型，对现有主流算法进行分类对比，并探讨现实通信约束对算法的影响。

### 2. 论文提出的方法论
论文提出了一个**通用的基于 GNN 的通信处理流程**，将其抽象为四个关键阶段：
1.  **消息编码（Encoding）**：使用 MLP、RNN 或 CNN 将局部观测值 $o_i$ 映射为初始消息向量 $x_i$。
2.  **通信决策（Deciding whom to talk to）**：
    *   **中介模式（Proxy）**：所有智能体将消息发给一个中心节点（中介），由其构建全局图并处理。
    *   **分布式模式（Distributed）**：智能体仅与通信范围内的邻居交换信息。
3.  **消息组合（Combining messages via GNN）**：
    *   利用消息传递神经网络（MPNN）进行聚合。
    *   涉及多轮通信（Multi-round）以实现信息的远距离传播（L-hop）。
    *   支持动态图构建（如通过注意力机制学习边权重）。
4.  **内部集成（Inner integration）**：将 GNN 输出的通信感知表示集成到策略网络（Policy）或价值网络（Value/Critic）中。

### 3. 实验设计
作为一篇综述论文，本文并未开展新的实验，而是对 12 种代表性算法的实验设置进行了横向对比：
*   **常见场景**：捕食者-猎物（Predator-Prey）、星际争霸 II（SMAC）、多机器人导航、巡逻任务（Patrolling）。
*   **基准算法（Benchmarks）**：IDQN、IPPO、QMIX、MAPPO、MADDPG 等。
*   **对比维度**：论文通过 Table 1 和 Table 2，从编码器类型、邻居定义、图构建方式、GNN 架构、集成方式以及**通信约束**（范围、带宽、噪声、丢包）等方面进行了详细对比。

### 4. 资源与算力
*   **算力说明**：由于本文是综述性质，**文中未明确说明**具体的 GPU 型号、数量或训练时长。这些信息通常存在于被引用的原始论文中。

### 5. 实验数量与充分性
*   **覆盖范围**：论文深入分析了 12 种主要的基于 GNN 的 MARL 方法（如 DICG, DGN, MAGIC, HetNet 等）。
*   **充分性评价**：综述涵盖了从 2020 年到 2026 年（预测发布时间）的最新进展，分类逻辑清晰（中介 vs 分布式）。
*   **客观性**：论文不仅关注算法性能，还特别强调了现有研究在处理“现实通信约束”（如带宽限制、噪声）方面的不足，评价较为客观。

### 6. 论文的主要结论与发现
*   **GNN 的优势**：GNN 能有效建模智能体间的动态交互，通过多层堆叠实现超出通信范围的信息传播。
*   **模式对比**：中介模式有利于全局协调但依赖完美通信假设；分布式模式更具扩展性且符合物理现实，但学习难度更高。
*   **关键趋势**：引入时间信息（RNN）和结构信息（动态图）的结合是提升通信效率的关键。
*   **现实差距**：大多数现有方法在实验中忽略了带宽限制、消息噪声和通信丢失等现实约束。

### 7. 优点
*   **高度抽象**：提出的通用算法流程（Algorithm 2 & 3）非常简洁，能够涵盖绝大多数现有 GNN-MARL 变体，极具参考价值。
*   **分类明确**：将复杂的方法简化为“中介”和“分布式”两大类，降低了初学者的理解门槛。
*   **关注约束**：专门讨论了通信约束（Table 2），指出了该领域从理论走向实践的必要路径。

### 8. 不足与局限
*   **缺乏统一 Benchmark 建议**：虽然对比了各论文的场景，但未提出一个标准化的、包含各种通信约束的开源评测框架。
*   **深度有限**：对某些算法的内部数学推导简化较多，读者仍需查阅原著以了解细节。
*   **应用限制**：综述主要集中在协作式任务，对竞争或混合动机场景下的 GNN 通信讨论较少。

（完）
