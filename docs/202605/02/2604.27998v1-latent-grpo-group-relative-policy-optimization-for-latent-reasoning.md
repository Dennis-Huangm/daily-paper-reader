---
title: "Latent-GRPO: Group Relative Policy Optimization for Latent Reasoning"
title_zh: Latent-GRPO：面向隐式推理的群体相对策略优化
authors: "Jingcheng Deng, Zihao Wei, Liang Pang, Junhong Wu, Shicheng Xu, Zenghao Duan, Huawei Shen"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27998v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 使用组相对策略优化在潜空间进行强化学习
tldr: 本研究针对隐式推理在强化学习中不稳定的问题，提出了Latent-GRPO框架。隐式推理通过将中间过程压缩为连续表示来提高效率，但直接应用GRPO会遇到潜在流形缺失、探索与优化失配及潜在混合非闭合等瓶颈。Latent-GRPO通过引入无效样本优势掩码、单侧噪声采样和最优路径首词选择解决了这些问题。实验表明，该方法在多个基准测试上优于显式推理，且推理链缩短了3至4倍，实现了高效且稳定的隐式推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27998v1/fig-001.webp\", \"caption\": \"Figure 2: RL training dynamics on the low-difficulty setting for LLaMA-3.2-1B-Instruct. The figure shows validation-set performance together with the evolution of response length during training on GSM8K-Aug.\", \"page\": 9, \"index\": 1, \"width\": 818, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2604.27998v1/fig-002.webp\", \"caption\": \"Figure 3: RL training dynamics on the high-difficulty setting for Qwen2.5-Math-7B. The figure shows validation-set performance on Math500 together with the evolution of response length during training on DAPO-Math-En.\", \"page\": 9, \"index\": 2, \"width\": 818, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2604.27998v1/fig-003.webp\", \"caption\": \"Figure 1: Key discrepancies and bottlenecks in adapting GRPO to latent-token reasoning. (a) Explicit and latent reasoning differ in both probability density and sampling mechanism. (b) ExplorationOptimization Misalignment: The standard Gumbel density objective yields incorrect directional updates, inadvertently pushing down components from positive-advantage latent trajectories while pushing up components from negative-advantage ones. (c) Latent Mixture Non-Closure: jointly reinforcing several correct latent paths can produce an averaged continuous state that does not correspond to a valid reasoning path.\", \"page\": 2, \"index\": 3, \"width\": 824, \"height\": 229}]"
motivation: 隐式推理虽高效但强化学习过程极不稳定，且直接应用GRPO面临流形偏移、对齐失准和混合非闭合等三大挑战。
method: 提出Latent-GRPO，结合无效样本优势掩码、单侧噪声采样和最优正确路径首词选择来优化隐式空间的策略学习。
result: "在低难度和高难度基准测试中均显著提升性能，且推理链比显式推理缩短3-4倍，展现出更强的Pass@k表现。"
conclusion: Latent-GRPO证明了在隐式空间进行稳定强化学习的可行性，为实现高效且高性能的推理模型提供了新路径。
---

## 摘要
隐式推理（Latent reasoning）通过将中间推理过程压缩为连续表示并显著缩短推理链，为显式推理提供了一种更高效的替代方案。然而，现有的隐式推理方法主要集中在监督学习上，而隐式空间中的强化学习仍然极不稳定。我们从群体相对策略优化（GRPO）的角度研究了这一问题，并表明将 GRPO 直接应用于隐式推理并非易事：隐式推理改变了概率密度和采样机制，导致了三个耦合的瓶颈：内在隐式流形的缺失（无约束的探索会使展开过程偏离有效的隐式流形）；探索与优化的不匹配（轨迹级奖励可能导致错误的 token 级更新）；以及隐式混合的不封闭性（共同强化多条正确的隐式路径可能会产生无效的平均状态）。为了解决这些问题，我们提出了 Latent-GRPO，它结合了无效样本优势掩码、单侧噪声采样以及最优正确路径首个 token 选择。在四个低难度基准测试（如 GSM8K-Aug）和四个高难度基准测试（如 AIME）中，Latent-GRPO 在低难度任务上比其隐式初始化提高了 7.86 个 Pass@1 百分点，在高难度任务上比显式 GRPO 高出 4.27 个百分点，同时推理链缩短了 3 到 4 倍。它在 Gumbel 采样下也实现了更强的 pass@k 性能。这些结果证明了 Latent-GRPO 是实现稳定且高效隐式推理的一种有效方法。

## Abstract
Latent reasoning offers a more efficient alternative to explicit reasoning by compressing intermediate reasoning into continuous representations and substantially shortening reasoning chains. However, existing latent reasoning methods mainly focus on supervised learning, and reinforcement learning in latent space remains highly unstable. We study this problem through the lens of Group Relative Policy Optimization (GRPO), and show that directly adapting GRPO to latent reasoning is fundamentally non-trivial: latent reasoning changes both the probability density and the sampling mechanism, causing three coupled bottlenecks: absence of intrinsic latent manifolds, where unconstrained exploration pushes rollouts off the valid latent manifold; exploration-optimization misalignment, where trajectory-level rewards can induce incorrect token-level updates; and latent mixture non-closure, where jointly reinforcing multiple correct latent paths can produce an invalid averaged state. To address them, we propose \textbf{Latent-GRPO}, which combines invalid-sample advantage masking, one-sided noise sampling, and optimal correct-path first-token selection. Across four low-difficulty benchmarks (e.g., GSM8K-Aug) and four high-difficulty benchmarks (e.g., AIME), Latent-GRPO improves over its latent initialization by 7.86 Pass@1 points on low-difficulty tasks and surpasses explicit GRPO by 4.27 points on high-difficulty tasks while using 3--4$\times$ shorter reasoning chains. It also achieves stronger pass@$k$ performance under Gumbel sampling. These results establish Latent-GRPO as an effective approach for stable and efficient latent reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Latent-GRPO: Group Relative Policy Optimization for Latent Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：大语言模型（LLM）通过显式思维链（CoT）在复杂推理上表现优异，但这种离散生成方式带来了巨大的计算冗余和延迟。
*   **核心问题**：隐式推理（Latent Reasoning）将推理过程压缩在连续的向量空间中，能显著缩短推理链，但其强化学习（RL）过程极不稳定。
*   **瓶颈分析**：论文指出直接将 GRPO 应用于隐式推理存在三大挑战：
    1.  **内在流形缺失**：无约束探索会导致模型生成偏离有效推理空间的无效轨迹。
    2.  **探索-优化失配**：标准 Gumbel 噪声会导致梯度更新方向与轨迹奖励符号不一致（例如正奖励反而降低了采样组件的概率）。
    3.  **隐式混合非闭合**：在隐式空间中同时强化多条正确路径会导致模型学习到它们的“平均状态”，而这个平均状态往往是无效的。

### 2. 论文提出的方法论：Latent-GRPO
为了解决上述瓶颈，论文提出了 **Latent-GRPO** 框架，包含三个核心设计：
*   **无效样本优势掩码（Invalid Sample Advantage Masking）**：在计算组内优势（Advantage）前，识别并剔除那些未能在最大步数内终止的无效轨迹，防止离群样本污染梯度。
*   **单侧噪声采样（One-sided Noise Sampling）**：
    *   将 Gumbel 噪声约束为严格正向，确保采样目标的对数概率始终高于原始预测。
    *   引入**直通估计器（STE）**：当模型更新超过目标值时，通过翻转梯度方向来维持单侧约束，从而保证优化方向与奖励符号始终对齐。
*   **最优正确路径首词选择（Optimal Correct Path First Token Selection）**：
    *   针对“混合非闭合”问题，在首个推理步，仅选择组内平均对数概率最高的一条正确路径进行更新。
    *   这种策略避免了在共享前缀下对多个正确动作进行“模式平均”，同时保留了后续步骤的探索多样性。

### 3. 实验设计
*   **数据集与场景**：
    *   **低难度任务**：GSM8K-Aug, GSM-Hard, SVAMP, MultiArith。使用 LLaMA-3.2-1B-Instruct 作为基座。
    *   **高难度任务**：Math500, AIME24, AIME25, GPQA。使用 Qwen2.5-Math-7B 作为基座。
*   **对比方法（Baselines）**：
    *   **显式推理**：SFT（监督微调）和标准 GRPO。
    *   **隐式推理**：Latent-SFT（初始化模型）和 Soft-GRPO（现有的隐式 RL 方法）。
*   **评估指标**：Pass@1 准确率、推理链长度（#L）、Pass@k（采样性能）。

### 4. 资源与算力
*   **硬件设备**：所有实验均在 **8 台 NVIDIA A100 GPU** 上完成。
*   **训练细节**：
    *   低难度任务：训练步数达 10k 步，batch size 为 64。
    *   高难度任务：训练步数约 500 步，batch size 为 32。
    *   推理长度限制：低难度设为 128，高难度设为 4096（显式推理则需更长，如 8192）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小学数学到竞赛级数学（AIME）及研究生水平科学问题（GPQA）的 8 个主流基准测试。
*   **消融实验**：针对“单侧噪声采样”和“首词选择”做了详细的消融对比，证明了这两个组件对训练稳定性的关键作用。
*   **充分性评价**：实验设计较为充分，不仅对比了准确率，还深入分析了推理长度的压缩比以及在不同噪声水平下的采样能力（Pass@k），论证了方法的鲁棒性和效率。

### 6. 主要结论与发现
*   **性能超越**：在低难度任务上，Latent-GRPO 比隐式初始化平均提升 7.86 个百分点；在高难度任务上，它不仅远超隐式基线，还比显式 GRPO 高出 **4.27 个百分点**。
*   **极致效率**：在保持高性能的同时，Latent-GRPO 的推理链长度比显式 GRPO 缩短了 **3 到 4 倍**。
*   **采样潜力**：通过 Gumbel 采样，Latent-GRPO 在 AIME 竞赛题上展现了极强的 Pass@64 性能（超过 50%），证明隐式空间同样具备有效的随机探索能力。

### 7. 优点
*   **稳定性突破**：首次系统性地解决了隐式空间 RL 训练易崩溃的问题，使隐式推理从“仅能 SFT”跨越到“可有效 RL”。
*   **理论支撑**：对梯度失配问题进行了严谨的数学推导（见附录 A），为单侧噪声采样提供了理论依据。
*   **效率与性能兼得**：打破了“隐式推理必输准确率”的固有印象，证明了短推理链也能处理极高难度的逻辑任务。

### 8. 不足与局限
*   **初始化依赖**：Latent-GRPO 必须依赖高质量的 Latent-SFT 模型作为起点，无法直接从纯显式模型开始训练。
*   **Pass@1 与 Pass@k 的权衡**：在极难任务（如 AIME）中，开启采样会显著降低 Pass@1 表现，虽然 Pass@k 提升巨大，但这限制了其在单次推理场景下的应用。
*   **任务覆盖**：目前主要集中在数学和科学推理，对于需要极长上下文或非结构化文本生成的任务，其压缩效果和性能尚待验证。

（完）
