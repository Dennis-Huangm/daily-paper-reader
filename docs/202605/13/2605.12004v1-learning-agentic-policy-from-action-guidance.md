---
title: Learning Agentic Policy from Action Guidance
title_zh: 从动作引导中学习智能体策略
authors: "Yuxiang Ji, Zengbin Wang, Yong Wang, Shidong Yang, Ziyu Ma, Guanhua Chen, Zonghua Sun, Liaoni Wu, Xiangxiang Chu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12004v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 大语言模型的代理强化学习
tldr: 本研究针对大语言模型智能体在强化学习中因探索能力受限而难以获取奖励信号的问题，提出了ActGuide-RL框架。该方法利用日常交互中的动作数据作为计划式引导，帮助模型跨越探索障碍，并通过混合策略训练和自适应回退的最小干预原则，将引导带来的收益内化至基础策略。实验表明，该方法在GAIA和XBench等搜索智能体任务上显著优于纯强化学习，且在无需冷启动的情况下达到了SFT+RL的性能水平，为减少对昂贵SFT数据的依赖提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12004v1/fig-001.webp\", \"caption\": \"Table 1: Main results of ACTGUIDE-RL on search-agent benchmarks comparing foundation models, search-agent trained models, and RL baseline. The best results are indicated in bold.\", \"page\": 7, \"index\": 1, \"width\": 825, \"height\": 955}, {\"url\": \"assets/figures/arxiv/2605.12004v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of ACTGUIDE-RL framework. Conventional agentic RL can only obtain training signals within the base model in-capability region. ACTGUIDE-RL follows the principle of minimal intervention, dynamically introducing action data to guide the model toward out-region exploration. Such mixed rollouts are trained through mixed-policy optimization.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 341}]"
motivation: 解决大语言模型在强化学习中因基础策略探索能力不足而难以触达奖励状态的问题，并降低对昂贵SFT数据的依赖。
method: 提出ActGuide-RL框架，通过将动作数据作为计划式引导并结合混合策略训练与最小干预原则，实现探索能力的内化。
result: 在GAIA和XBench基准测试中，该方法显著提升了模型表现，且在无冷启动情况下达到了与SFT+RL相当的水平。
conclusion: 该研究证明了利用可扩展的动作引导替代重度SFT数据是实现高效智能体强化学习的一种有效新范式。
---

## 摘要
大语言模型（LLMs）的智能体强化学习（RL）关键取决于基础策略的探索能力，因为训练信号仅在其能力范围内产生。对于基础策略无法到达奖励状态的任务，需要额外的训练或外部引导来恢复有效的学习信号。我们没有依赖昂贵的迭代监督微调（SFT），而是利用了日常人类交互中产生的丰富动作数据。我们提出了 ActGuide-RL，它将动作数据作为计划式参考引导注入，使智能体策略能够克服通往奖励状态的可达性障碍。随后，通过混合策略训练对有引导和无引导的展开（rollouts）进行联合优化，将探索收益内化回无引导策略中。受收益-风险权衡的理论和实证分析启发，我们采用了最小干预原则，仅在作为自适应回退时调用引导，在匹配任务难度的同时最小化离策略（off-policy）风险。在搜索智能体基准测试中，ActGuide-RL 相比于零样本强化学习（zero RL）有显著提升（在 GAIA 上提升 10.7 个百分点，在 Qwen3-4B 的 XBench 上提升 19 个百分点），且在无需任何冷启动的情况下，表现与 SFT+RL 流水线相当。这为智能体强化学习提供了一种新范式，即通过使用可扩展的动作引导来减少对大量 SFT 数据的依赖。

## Abstract
Agentic reinforcement learning (RL) for Large Language Models (LLMs) critically depends on the exploration capability of the base policy, as training signals emerge only within its in-capability region. For tasks where the base policy cannot reach reward states, additional training or external guidance is needed to recover effective learning signals. Rather than relying on costly iterative supervised fine tuning (SFT), we exploit the abundant action data generated in everyday human interactions. We propose \textsc{ActGuide-RL}, which injects action data as plan-style reference guidance, enabling the agentic policy to overcome reachability barriers to reward states. Guided and unguided rollouts are then jointly optimized via mixed-policy training, internalizing the exploration gains back into the unguided policy. Motivated by a theoretical and empirical analysis of the benefit-risk trade-off, we adopt a minimal intervention principle that invokes guidance only as an adaptive fallback, matching task difficulty while minimizing off-policy risk. On search-agent benchmarks, \textsc{ActGuide-RL} substantially improves over zero RL (+10.7 pp on GAIA and +19 pp on XBench with Qwen3-4B), and performs on par with the SFT+RL pipeline without any cold start. This suggests a new paradigm for agentic RL that reduces the reliance on heavy SFT data by using scalable action guidance instead.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Agentic Policy from Action Guidance》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）智能体在进行强化学习（RL）时，极度依赖基础模型的探索能力。当任务难度超出模型当前能力范围（即处于“Out-region”）时，模型无法触达奖励状态，导致训练信号缺失（梯度坍缩为零），RL 过程停滞。
*   **研究动机**：传统的解决方法是先进行昂贵的监督微调（SFT）冷启动，但这增加了数据获取成本和流程复杂性。本文旨在利用互联网上大量现成的、不含推理过程的“动作数据”（如 GUI/CLI 交互记录、API 调用序列），将其作为引导信号，帮助模型跨越探索障碍，实现高效的智能体策略学习。

### 2. 方法论（ActGuide-RL 框架）
ActGuide-RL 的核心思想是：**将动作数据作为参考计划注入，通过最小干预原则引导探索，并利用混合策略优化实现能力内化。**

*   **可达性动力学分析**：论文定义了“有效状态访问质量”（Effective state-visiting mass $M^\pi_t$），指出当模型在关键步骤出现质量骤降时，即遇到了“可达性障碍”。
*   **计划式引导（Action Guidance）**：将动作序列 $g$ 以“参考计划”的形式附加在 Prompt 中，而非强制模型执行。这允许模型在参考动作的基础上进行自主推理和工具调用。
*   **最小干预原则（Minimal Intervention Principle）**：
    *   **自适应回退**：仅当无引导的 Rollout 全部失败时，才调用动作引导。
    *   **强度搜索**：通过二分查找确定最小的引导步数 $k^*$，既能保证触达奖励，又最大限度减少离策略（Off-policy）分布偏移风险。
*   **混合策略优化（Mixed-Policy Optimization）**：
    *   在训练中同时使用有引导和无引导的轨迹。
    *   使用重要性采样（Importance Sampling）调整权重，将有引导轨迹中获得的成功经验通过梯度更新“内化”到无引导的策略 $\pi_\theta$ 中，确保推理时无需引导也能成功。

### 3. 实验设计
*   **场景与任务**：主要聚焦于**搜索智能体（Search-agent）**，涉及多步推理、网页搜索和信息提取。
*   **数据集（Benchmark）**：
    *   **域内测试**：GAIA（通用 AI 助手）、WebWalkerQA（复杂网页遍历）、XBench（生产力缩放）、BrowseComp-ZH（中文网页浏览）。
    *   **域外泛化**：GPQA（研究生水平问答）、TruthfulQA（事实性）、IFEval（指令遵循）。
*   **对比方法**：
    *   基础模型（Qwen2.5-3B/7B, Qwen3-4B/8B, DeepSeek-V3 等）。
    *   标准强化学习（Vanilla RL/GRPO）。
    *   SFT + RL 流水线（传统的两阶段方法）。
    *   其他搜索智能体专用模型（WebSailor, ARPO 等）。

### 4. 资源与算力
*   **算力配置**：实验使用了配备 **8 张 NVIDIA H20 GPU** 的节点进行训练和 Rollout。
*   **评估资源**：另外使用了一个配备 **8 张 NVIDIA H20 GPU** 的独立节点运行 LLM-Judge，用于奖励分配和测试评估。
*   **训练细节**：训练数据包含 2000 个实例，总训练步数为 64 步，Batch Size 为 32，Group Size 为 8。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同规模的基础模型（3B 到 8B）上验证了 ActGuide-RL 的一致性提升。
*   **消融实验**：针对“最小干预原则”、“混合策略优化”、“引导强度”以及“动作噪声敏感性”等关键设计做了详尽的消融研究。
*   **客观性与公平性**：实验对比了 SFT+RL 这一强基准，并证明了在无冷启动的情况下，ActGuide-RL 能达到甚至超过 SFT+RL 的性能，且在域外泛化上表现更稳健（避免了 SFT 带来的过拟合风险）。整体实验设计严谨，覆盖了多语言和多维度的任务。

### 6. 主要结论与发现
*   **突破探索瓶颈**：动作引导能有效修复可达性障碍，使模型在原本无法成功的任务上获得学习信号。
*   **性能显著提升**：在 Qwen3-4B 上，ActGuide-RL 相比 Zero RL 在 GAIA 上提升了 10.7%，在 XBench 上提升了 19%。
*   **替代 SFT 冷启动**：ActGuide-RL 证明了“轻量级动作数据 + RL”可以替代“高质量 SFT 数据 + RL”，大幅降低了智能体开发的门槛。
*   **自适应性**：最小干预原则是成功的关键，过度引导会增加离策略风险，导致性能下降。

### 7. 优点与亮点
*   **数据效率高**：利用了易于获取的动作轨迹（Action-only），无需昂贵的思维链（CoT）标注。
*   **设计优雅**：通过二分查找实现自适应引导强度，平衡了“探索收益”与“分布偏移风险”。
*   **内化能力强**：通过混合策略训练，成功将外部引导转化为模型自身的推理能力。

### 8. 不足与局限
*   **场景局限性**：目前主要在搜索智能体（Search-agent）场景下验证，对于更复杂的 GUI 自动化或具身智能（Embodied AI）环境的适用性尚待进一步实验。
*   **数据处理**：论文未详细讨论如何从海量原始日志中系统化地清洗和筛选高质量动作数据。
*   **噪声容忍度**：虽然测试了 10%-20% 的噪声，但在极高噪声或错误引导下的鲁棒性仍有待加强。

（完）
