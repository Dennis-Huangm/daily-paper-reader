---
title: "HAGE: Harnessing Agentic Memory via RL-Driven Weighted Graph Evolution"
title_zh: HAGE：通过强化学习驱动的加权图演化利用智能体记忆
authors: "Dongming Jiang, Yi Li, Guanpeng Li, Qiannan Li, Bingzhe Li"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09942v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 大语言模型智能体的强化学习驱动记忆框架
tldr: 本研究针对大模型智能体内存检索中静态查找和固定图结构的局限性，提出了HAGE框架。该框架将内存构建为加权多关系图，通过LLM识别查询意图并利用路由网络动态调节边权重，实现查询驱动的序列化图遍历。此外，引入强化学习联合优化路由行为与边表示。HAGE在提升长程推理准确性的同时，优化了检索效率，为构建动态进化的智能体内存系统提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09942v1/fig-001.webp\", \"caption\": \"Table 1: LoCoMo comparison with LLM-as-a-judge score under different methods and backbone LLMs. Higher is better. Best results are shown in bold, and second-best results are underlined. HAGE is our proposed method.\", \"page\": 7, \"index\": 1, \"width\": 757, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2605.09942v1/fig-002.webp\", \"caption\": \"Figure 2: Architectural Overview of HAGE.\", \"page\": 4, \"index\": 2, \"width\": 752, \"height\": 231}]"
motivation: 传统的智能体内存检索依赖静态查找或固定图结构，无法捕捉事件间随查询变化的复杂关系强度。
method: 提出HAGE框架，将内存组织为加权多关系图，通过LLM意图识别和强化学习驱动的路由网络动态调整边权重并进行序列化遍历。
result: 实验表明，HAGE在长程推理任务中显著提高了准确性，并在准确率与效率之间取得了更好的平衡。
conclusion: HAGE证明了通过动态演化的加权图结构和强化学习优化，可以有效提升大模型智能体的内存检索与推理能力。
---

## 摘要
智能体大语言模型（LLM）系统中的记忆检索通常被视为静态查找问题，依赖于扁平向量搜索或固定的二元关系图。然而，固定的图结构无法捕捉事件之间关系的不同强度、置信度以及与查询相关的相关性。在本文中，我们提出了 HAGE，这是一个加权多关系记忆框架，它将检索重新概念化为在统一关系记忆图上进行的顺序、查询条件化的遍历。记忆被组织为共享记忆节点上的特定关系图视图，其中每条边都与一个可训练的关系特征向量相关联，该向量编码了多个关系信号。给定一个查询，基于 LLM 的分类器识别关系意图，路由网络动态调节边缘嵌入的相应维度。遍历分数通过语义相似度与这些查询条件化边缘表示的学习组合来计算。这使得记忆遍历能够优先考虑高实用性的关系路径，同时软性抑制噪声或弱相关的连接。除了自适应遍历，HAGE 还引入了一个基于强化学习的训练框架，利用下游任务联合优化路由行为和边缘表示。最后，实验结果表明，与最先进的智能体记忆系统相比，该方法提高了长程推理精度，并实现了良好的精度-效率权衡。我们的代码可在 https://github.com/FredJiang0324/HAGE_MVPReview 获取。

## Abstract
Memory retrieval in agentic large language model (LLM) systems is often treated as a static lookup problem, relying on flat vector search or fixed binary relational graphs. However, fixed graph structures cannot capture the varying strength, confidence, and query-dependent relevance of relationships between events. In this paper, we propose HAGE, a weighted multi-relational memory framework that reconceptualizes retrieval as sequential, query-conditioned traversal over a unified relational memory graph. Memory is organized as relation-specific graph views over shared memory nodes, where each edge is associated with a trainable relation feature vector encoding multiple relational signals. Given a query, an LLM-based classifier identifies the relational intent, and a routing network dynamically modulates the corresponding dimensions of the edge embedding. Traversal scores are computed via a learned combination of semantic similarity and these query-conditioned edge representations. This allows memory traversal to prioritize high-utility relational paths while softly suppressing noisy or weakly relevant connections. Beyond adaptive traversal, HAGE further introduces a reinforcement learning-based training framework that jointly optimizes routing behavior and edge representations using downstream tasks. Finally, empirical results demonstrate improved long-horizon reasoning accuracy and a favorable accuracy-efficiency trade-off compared to state-of-the-art agentic memory systems. Our code is available at https://github.com/FredJiang0324/HAGE_MVPReview.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **HAGE**（Harnessing Agentic Memory via RL-Driven Weighted Graph Evolution）的框架，旨在解决大语言模型（LLM）智能体在长程交互中记忆检索的动态性与准确性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的智能体记忆系统通常将检索视为“静态查找”过程（如向量搜索或固定拓扑的图检索），无法根据具体查询动态调整不同记忆关联（如时间、因果、语义、实体）的权重。
*   **研究动机**：在复杂的推理任务中，记忆节点之间的关系强度是随查询变化的。例如，回答“事件顺序”需要强调时间边，而“因果分析”则需要强调因果边。静态图结构容易引入噪声或忽略关键的“桥接”节点，导致长程推理失败。

### 2. 方法论
HAGE 将记忆检索重新定义为**在加权多关系图上的查询条件化序列遍历**。
*   **加权多关系记忆图**：记忆被组织为有向多重图，包含四种关系边：时间（Temporal）、语义（Semantic）、因果（Causal）和实体（Entity）。每条边关联一个可训练的特征向量 $e_{ij}$。
*   **查询意图识别**：利用 LLM 分类器识别查询的关系意图（如“寻找因果关系”），并将其转化为控制信号。
*   **查询驱动的路由网络（QueryRouter）**：一个轻量级 MLP，输入查询嵌入、边特征和节点相似度，动态计算边的结构权重 $w_{ij}(q)$。
*   **混合遍历评分**：最终跳转分数由语义相似度和学习到的结构权重加权求和得出：$S = \lambda \cdot \text{CosineSimilarity} + (1-\lambda) \cdot w_{ij}(q)$。
*   **强化学习联合优化**：
    *   将图遍历建模为马尔可夫决策过程（MDP）。
    *   使用 **REINFORCE** 算法联合优化路由网络参数和边特征。
    *   **奖励函数**：成功命中证据节点给予正奖励，同时对步数和超时进行惩罚。
    *   **锚点正则化（Anchor Regularization）**：防止边特征在训练中偏离初始语义含义太远，确保泛化性。

### 3. 实验设计
*   **数据集**：
    *   **LoCoMo**：超长对话数据集（平均 9k token），用于评估长程时间、因果检索。
    *   **HotpotQA**：多跳问答基准，测试跨文档证据的连接能力。
*   **对比方法（Baselines）**：
    *   Full Context（全上下文输入）。
    *   SOTA 记忆系统：A-MEM、Nemori、MemoryOS、MAGMA（静态图）、MemSkill（基于 RL 的技能演化）。
*   **评估指标**：LLM-as-a-Judge 评分（语义正确性）、Token 级别 F1 值、推理延迟和 Token 消耗。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 PyTorch 实现，并利用 GPU 加速图路由和边优化。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）及数量，也未给出总训练时长。但提到 Phase 2 的 RL 训练不需要调用 LLM（使用预计算的嵌入），因此计算开销相对较低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型基准上进行了全面测试。
    *   采用了 **5 折交叉验证（5-fold cross-validation）**，确保训练集和测试集在对话样本级别完全隔离，避免了数据泄露。
    *   进行了详细的**消融实验**，分别验证了静态边、LLM 评分边、可训练边和可训练路由器的贡献。
*   **充分性评价**：实验设计较为充分且客观。通过对比不同参数规模的基座模型（GPT-4o-mini 和 Qwen2.5-3B），证明了方法的普适性。

### 6. 主要结论与发现
*   **性能领先**：HAGE 在 LoCoMo 和 HotpotQA 上均取得了最优的 Judge 评分，显著优于 MAGMA 等静态图方法。
*   **协同效应**：实验证明“可训练边表示”与“可训练路由策略”具有互补性，两者结合效果最好。
*   **效率平衡**：虽然 HAGE 引入了多步遍历，但其 Token 消耗和延迟仍保持在与 SOTA 检索系统相当的水平，实现了良好的精度-效率权衡。

### 7. 优点
*   **动态性**：突破了传统 RAG 的静态限制，使记忆系统能根据查询意图“进化”关系权重。
*   **RL 驱动**：通过下游任务的反馈直接优化检索路径，而非依赖人工设计的启发式规则。
*   **结构化优势**：结合了图结构的显式关联和神经网络的隐式学习能力。

### 8. 不足与局限
*   **意图分类依赖**：系统性能高度依赖于第一阶段 LLM 对查询意图分类的准确性。
*   **任务覆盖面**：虽然在对话和多跳 QA 上表现良好，但在程序化推理或纯文档检索任务上的表现尚未验证。
*   **隐私风险**：持久化的、不断演化的记忆图可能比扁平存储更容易泄露用户的长期行为模式或敏感关联。

（完）
