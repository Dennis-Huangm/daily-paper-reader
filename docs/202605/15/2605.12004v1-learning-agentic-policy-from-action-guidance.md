---
title: Learning Agentic Policy from Action Guidance
title_zh: 从动作引导中学习智能体策略
authors: "Yuxiang Ji, Zengbin Wang, Yong Wang, Shidong Yang, Ziyu Ma, Guanhua Chen, Zonghua Sun, Liaoni Wu, Xiangxiang Chu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12004v1"
tags: ["query:ag-rl"]
score: 9.5
evidence: 利用动作引导克服探索障碍的LLM智能体强化学习
tldr: 针对大语言模型智能体强化学习中基础策略探索能力受限、难以触达奖励状态的问题，本文提出 ActGuide-RL。该方法利用日常交互中的动作数据作为计划式参考引导，通过混合策略训练将引导带来的探索增益内化到无引导策略中。实验表明，该方法在 GAIA 和 XBench 等基准测试上显著提升了性能，且无需昂贵的监督微调（SFT）冷启动，为智能体强化学习提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12004v1/fig-001.webp\", \"caption\": \"Table 1: Main results of ACTGUIDE-RL on search-agent benchmarks comparing foundation models, search-agent trained models, and RL baseline. The best results are indicated in bold.\", \"page\": 7, \"index\": 1, \"width\": 825, \"height\": 955}, {\"url\": \"assets/figures/arxiv/2605.12004v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of ACTGUIDE-RL framework. Conventional agentic RL can only obtain training signals within the base model in-capability region. ACTGUIDE-RL follows the principle of minimal intervention, dynamically introducing action data to guide the model toward out-region exploration. Such mixed rollouts are trained through mixed-policy optimization.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 341}]"
motivation: 基础策略在复杂任务中难以触达奖励状态，且传统的迭代监督微调（SFT）成本高昂。
method: 提出 ActGuide-RL，将动作数据作为参考引导，并采用最小干预原则进行混合策略训练。
result: "在 GAIA 和 XBench 任务上分别提升了 10.7% 和 19% 的成功率，性能可媲美传统的 SFT+RL 流程。"
conclusion: 该研究证明了利用可扩展的动作引导替代重度 SFT 数据是实现高效智能体强化学习的可行路径。
---

## 摘要
大语言模型（LLMs）的智能体强化学习（RL）关键取决于基础策略的探索能力，因为训练信号仅在其能力范围内产生。对于基础策略无法到达奖励状态的任务，需要额外的训练或外部引导来恢复有效的学习信号。我们没有依赖昂贵的迭代监督微调（SFT），而是利用了日常人类交互中产生的大量动作数据。我们提出了 ActGuide-RL，它将动作数据作为计划式参考引导注入，使智能体策略能够克服通往奖励状态的可达性障碍。随后，通过混合策略训练对有引导和无引导的展开（rollouts）进行联合优化，将探索收益内化回无引导策略中。受收益-风险权衡的理论和实证分析启发，我们采用了最小干预原则，仅在作为自适应回退方案时调用引导，在匹配任务难度的同时最小化离策（off-policy）风险。在搜索智能体基准测试中，ActGuide-RL 相比于零样本 RL 有显著提升（在 GAIA 上提升 10.7 个百分点，在 XBench 上使用 Qwen3-4B 提升 19 个百分点），且在无需任何冷启动的情况下，表现与 SFT+RL 流水线相当。这为智能体 RL 提供了一种新范式，即通过使用可扩展的动作引导来减少对大量 SFT 数据的依赖。

## Abstract
Agentic reinforcement learning (RL) for Large Language Models (LLMs) critically depends on the exploration capability of the base policy, as training signals emerge only within its in-capability region. For tasks where the base policy cannot reach reward states, additional training or external guidance is needed to recover effective learning signals. Rather than relying on costly iterative supervised fine tuning (SFT), we exploit the abundant action data generated in everyday human interactions. We propose \textsc{ActGuide-RL}, which injects action data as plan-style reference guidance, enabling the agentic policy to overcome reachability barriers to reward states. Guided and unguided rollouts are then jointly optimized via mixed-policy training, internalizing the exploration gains back into the unguided policy. Motivated by a theoretical and empirical analysis of the benefit-risk trade-off, we adopt a minimal intervention principle that invokes guidance only as an adaptive fallback, matching task difficulty while minimizing off-policy risk. On search-agent benchmarks, \textsc{ActGuide-RL} substantially improves over zero RL (+10.7 pp on GAIA and +19 pp on XBench with Qwen3-4B), and performs on par with the SFT+RL pipeline without any cold start. This suggests a new paradigm for agentic RL that reduces the reliance on heavy SFT data by using scalable action guidance instead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Agentic Policy from Action Guidance》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）智能体在强化学习（RL）中的“冷启动”与“探索瓶颈”问题**。
*   **研究动机**：智能体 RL 的成功极大地依赖于基础策略的探索能力。当任务难度超出模型当前能力范围（即处于“能力外区域”，Out-Region）时，模型无法触达奖励状态，导致训练信号消失（梯度塌陷）。
*   **现有痛点**：传统做法是先进行昂贵的监督微调（SFT）来提供“温启动”，但这需要高质量的思维链（CoT）数据，且难以扩展到新环境。
*   **核心思路**：利用互联网和日常交互中大量存在的、易于获取的**动作数据**（如 GUI/CLI 操作记录、API 调用序列），将其作为“计划式引导”注入 RL 过程，帮助模型跨越探索障碍。

### 2. 方法论：ActGuide-RL 框架
ActGuide-RL 的核心思想是“引导而不强制，内化而非模仿”。
*   **动作引导（Action Guidance）**：将动作序列（不含推理过程）作为参考计划（Reference Plan）放入 Prompt 中。它不是强制模型执行的 Prefix，而是作为一种提示，让模型在保持自身推理能力的同时参考正确路径。
*   **最小干预原则（Minimal Intervention Principle）**：
    *   **自适应回退**：仅当无引导的尝试全部失败时，才调用动作引导。
    *   **二分查找最优引导量**：通过二分查找确定能让模型成功完成任务的最短动作序列长度（$k^*$），以最小化“离策风险”（Off-policy Risk）和分布偏移。
*   **混合策略优化（Mixed-Policy Optimization）**：
    *   同时收集有引导和无引导的轨迹。
    *   使用重要性采样（Importance Sampling）调整权重，将有引导轨迹中获得的技能“内化”到原始的无引导策略中，确保推理时不再需要引导。

### 3. 实验设计
*   **实验场景**：主要针对**搜索智能体（Search-Agent）**，这类任务状态空间大、步骤长。
*   **基准测试（Benchmarks）**：
    *   **域内（In-domain）**：GAIA（通用助手）、WebWalkerQA（网页遍历）、XBench（深度搜索）、BrowseComp-ZH（中文网页浏览）。
    *   **域外（Out-of-domain）**：GPQA（研究生水平科学问答）、TruthfulQA（真实性）、IFEval（指令遵循）。
*   **对比方法**：基础模型（Qwen2.5/3 系列）、纯 RL（Zero RL）、SFT+RL 流水线、以及其他专门训练的搜索智能体模型（如 WebSailor, WebThinker）。

### 4. 资源与算力
*   **算力设备**：训练和 Rollout 实验在配备 **8 张 NVIDIA H20 GPU** 的节点上进行。
*   **评估资源**：用于奖励分配（LLM-Judge）和测试评估的推理过程额外使用了另一个配备 **8 张 NVIDIA H20 GPU** 的节点。
*   **训练细节**：训练步数为 64 步，Batch Size 为 32，最大交互轮数为 30 轮。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同规模的基础模型（Qwen2.5-3B/7B, Qwen3-4B/8B）上进行了验证，涵盖了从易到难的多个数据集。
*   **消融实验**：非常充分。作者对比了“无最小干预”、“无混合策略优化”、“不同引导强度”以及“对动作噪声的敏感度”等多个维度。
*   **客观性**：通过引入域外（OOD）测试，证明了该方法在提升智能体能力的同时，不会像 SFT 那样导致通用能力的退化（如在 GPQA 和 IFEval 上的表现更稳健）。

### 6. 主要结论与发现
*   **显著提升**：在 Qwen3-4B 模型上，ActGuide-RL 相比纯 RL 在 GAIA 上提升了 **10.7%**，在 XBench 上提升了 **19%**。
*   **媲美 SFT+RL**：在无需任何 SFT 冷启动的情况下，ActGuide-RL 的性能达到了与“SFT + 强化学习”流水线相当的水平。
*   **克服障碍**：实验证明动作引导确实修复了“可达性障碍”，使模型能够学习到原本无法触达的高难度任务。
*   **复杂交互能力演进**：随着训练进行，模型生成的回复长度和交互轮数稳步增加，表明其自主处理复杂任务的能力在增强。

### 7. 优点
*   **数据效率高**：动作数据（Action-only）比带 CoT 的 SFT 数据更容易大规模获取。
*   **自适应性强**：最小干预原则平衡了“探索收益”与“分布偏移风险”，避免了过度依赖引导导致的模型僵化。
*   **通用性好**：不改变模型架构，作为一种 Post-training 范式，可以适配各种基础 LLM。

### 8. 不足与局限
*   **场景局限**：目前主要在搜索智能体（Search-Agent）上验证，虽然理论上适用于 GUI/CLI，但尚未在这些更具动态性的环境中进行大规模测试。
*   **数据清洗依赖**：虽然对 10% 的动作噪声有鲁棒性，但如果原始动作数据质量极差（超过 20% 噪声），性能会明显下降。
*   **计算开销**：在训练阶段需要进行多次 Rollout（包括二分查找引导量的过程），相比纯 RL 增加了采样成本。

（完）
