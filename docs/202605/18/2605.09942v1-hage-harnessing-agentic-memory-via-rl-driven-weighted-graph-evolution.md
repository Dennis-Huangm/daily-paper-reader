---
title: "HAGE: Harnessing Agentic Memory via RL-Driven Weighted Graph Evolution"
title_zh: HAGE：通过强化学习驱动的有权图演化利用智能体记忆
authors: "Dongming Jiang, Yi Li, Guanpeng Li, Qiannan Li, Bingzhe Li"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09942v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于智能体LLM系统的强化学习驱动内存框架
tldr: 本研究针对大模型智能体内存检索中静态查找和固定图结构的局限性，提出了HAGE框架。该框架将内存构建为加权多关系图，通过LLM识别查询意图并利用路由网络动态调节边权重，实现查询驱动的序列化图遍历。此外，引入强化学习联合优化路由行为与边表示。HAGE在提升长程推理准确性的同时，优化了检索效率，为构建动态进化的智能体内存系统提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09942v1/fig-001.webp\", \"caption\": \"Table 1: LoCoMo comparison with LLM-as-a-judge score under different methods and backbone LLMs. Higher is better. Best results are shown in bold, and second-best results are underlined. HAGE is our proposed method.\", \"page\": 7, \"index\": 1, \"width\": 757, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2605.09942v1/fig-002.webp\", \"caption\": \"Figure 2: Architectural Overview of HAGE.\", \"page\": 4, \"index\": 2, \"width\": 752, \"height\": 231}]"
motivation: 传统的智能体内存检索依赖静态查找或固定图结构，无法捕捉事件间随查询变化的复杂关系强度和相关性。
method: 提出一种加权多关系内存框架，利用LLM识别意图并结合强化学习驱动的路由网络，动态调整图边权重以实现序列化遍历。
result: 实验结果显示，HAGE在长程推理任务的准确性上优于现有系统，并实现了更优的准确率与效率平衡。
conclusion: 通过动态演化的加权图结构和强化学习优化，HAGE显著提升了智能体在复杂任务中的内存检索与推理性能。
---

## 摘要
智能体大语言模型（LLM）系统中的记忆检索通常被视为静态查找问题，依赖于扁平向量搜索或固定的二元关系图。然而，固定的图结构无法捕捉事件之间关系的强度变化、置信度以及依赖于查询的相关性。在本文中，我们提出了 HAGE，这是一个有权多关系记忆框架，它将检索重新概念化为在统一关系记忆图上进行的顺序、查询条件化的遍历。记忆被组织为共享记忆节点上的特定关系图视图，其中每条边都与一个可训练的关系特征向量相关联，该向量编码了多种关系信号。给定一个查询，基于 LLM 的分类器识别关系意图，路由网络动态调节边嵌入的相应维度。遍历分数通过语义相似度与这些查询条件化边表示的习得组合来计算。这使得记忆遍历能够优先考虑高实用性的关系路径，同时软性抑制噪声或弱相关的连接。除了自适应遍历，HAGE 还引入了一个基于强化学习的训练框架，利用下游任务联合优化路由行为和边表示。最后，实证结果表明，与最先进的智能体记忆系统相比，该方法提高了长程推理准确度，并在准确度与效率之间取得了良好的平衡。我们的代码可在 https://github.com/FredJiang0324/HAGE_MVPReview 获取。

## Abstract
Memory retrieval in agentic large language model (LLM) systems is often treated as a static lookup problem, relying on flat vector search or fixed binary relational graphs. However, fixed graph structures cannot capture the varying strength, confidence, and query-dependent relevance of relationships between events. In this paper, we propose HAGE, a weighted multi-relational memory framework that reconceptualizes retrieval as sequential, query-conditioned traversal over a unified relational memory graph. Memory is organized as relation-specific graph views over shared memory nodes, where each edge is associated with a trainable relation feature vector encoding multiple relational signals. Given a query, an LLM-based classifier identifies the relational intent, and a routing network dynamically modulates the corresponding dimensions of the edge embedding. Traversal scores are computed via a learned combination of semantic similarity and these query-conditioned edge representations. This allows memory traversal to prioritize high-utility relational paths while softly suppressing noisy or weakly relevant connections. Beyond adaptive traversal, HAGE further introduces a reinforcement learning-based training framework that jointly optimizes routing behavior and edge representations using downstream tasks. Finally, empirical results demonstrate improved long-horizon reasoning accuracy and a favorable accuracy-efficiency trade-off compared to state-of-the-art agentic memory systems. Our code is available at https://github.com/FredJiang0324/HAGE_MVPReview.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **HAGE**（Harnessing Agentic Memory via RL-Driven Weighted Graph Evolution）的框架，旨在解决大语言模型（LLM）智能体在长程交互中记忆检索的动态性与准确性问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的智能体记忆系统通常将检索视为“静态查找”过程（如向量搜索或固定拓扑图），无法根据具体查询动态调整不同记忆关联（如时间、因果、语义）的权重。
*   **研究动机**：在复杂的长程推理中，记忆节点间的关系强度是随查询变化的。例如，回答“事件顺序”需要强调时间边，而“逻辑推导”则需强调因果边。静态图结构无法过滤噪声或优先处理高实用性的路径，导致检索精度随记忆规模增长而下降。

### 2. 方法论
HAGE 将记忆检索重新定义为**在加权多关系图上的查询条件化序列遍历**。
*   **加权多关系图构建**：记忆被组织为有向多重图 $G = (N, E)$，边集包含四种关系：时间（Temporal）、语义（Semantic）、因果（Causal）和实体（Entity）。每条边关联一个可训练的特征向量 $e_{ij}$。
*   **查询条件化路由（QueryRouter）**：
    1.  **意图识别**：利用 LLM 分类器识别查询的关系意图。
    2.  **动态权重计算**：路由网络（轻量级 MLP）结合查询嵌入、边特征和语义相似度，生成动态结构权重 $w_{ij}(q)$。
    3.  **遍历策略**：最终转移分数由语义相关性和习得的结构权重加权求和得出，允许智能体跨越语义距离远但结构关键的“桥接节点”。
*   **强化学习联合优化**：
    1.  **MDP 建模**：将图遍历建模为马尔可夫决策过程。
    2.  **奖励设计**：奖励函数包含“证据命中（Evidence Hit）”正向奖励，以及步数惩罚和超时惩罚，以鼓励高效路径。
    3.  **联合训练**：使用 REINFORCE 算法同时优化路由网络参数和边特征向量。引入“锚点正则化（Anchor Regularization）”防止边特征在训练中过度偏离初始语义。

### 3. 实验设计
*   **数据集/场景**：
    *   **LoCoMo**：超长对话基准（平均 9k token），评估长程时间、因果检索。
    *   **HotpotQA**：多跳问答基准，测试跨文档证据的识别与连接能力。
*   **对比方法（Baselines）**：
    *   全上下文（Full Context）输入。
    *   主流智能体记忆系统：A-MEM、Nemori、MemoryOS、MAGMA（静态图）、MemSkill（基于 RL 的技能演化）。
*   **评估指标**：LLM-as-a-Judge 评分（语义正确性）、Token 级别 F1 分数、推理延迟和 Token 消耗。

### 4. 资源与算力
*   **算力说明**：文中提到 HAGE 在 PyTorch 中实现，支持 GPU 加速的路由和边优化。
*   **具体细节**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）或具体的训练总时长。但提到 Phase 2 的 RL 训练不需要调用 LLM（仅在缓存的图结构和嵌入上操作），因此训练效率较高。

### 5. 实验数量与充分性
*   **实验规模**：进行了 5 折交叉验证（Sample-level cross-validation），确保无查询泄露。
*   **消融实验**：对比了静态边、LLM 评分边、仅训练边、仅训练路由以及全功能 HAGE 的表现。
*   **充分性评价**：实验覆盖了对话和非对话（QA）两种场景，涵盖了单跳、多跳、时间、对抗等多种查询类型。通过效率与准确性的权衡分析（Trade-off analysis），证明了该方法在实际部署中的可行性，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：HAGE 在 LoCoMo 和 HotpotQA 上均取得了最优的 Judge 评分。在 GPT-4o-mini 作为后端时，整体评分从最强基线的 0.700 提升至 0.739。
*   **推理优势**：在“对抗性查询”和“时间推理”类别中提升尤为显著，证明了动态路由在过滤干扰信息方面的有效性。
*   **效率平衡**：虽然 HAGE 增加了轻量级的路由计算，但其 Token 消耗和延迟仍保持在与主流 RAG 系统相同的数量级内，实现了良好的准确率-效率平衡。

### 7. 优点
*   **动态演化**：打破了固定图结构的限制，使记忆关系能够随任务反馈不断进化。
*   **联合优化**：通过 RL 同时优化“怎么走（路由）”和“路好不好走（边特征）”，比单一优化更具鲁棒性。
*   **结构与语义结合**：加法形式的分数计算确保了系统既尊重内容相关性，又能利用结构上的关键路径。

### 8. 不足与局限
*   **LLM 依赖性**：查询意图分类和最终评估仍高度依赖高性能 LLM，分类错误会直接影响检索质量。
*   **任务覆盖**：虽然在对话和 QA 上表现良好，但尚未在程序化任务（Procedural tasks）或大规模文档库检索中进行验证。
*   **隐私与偏差**：持久化记忆系统存在隐私泄露风险，且 RL 策略可能会放大训练数据中的检索偏差。

（完）
