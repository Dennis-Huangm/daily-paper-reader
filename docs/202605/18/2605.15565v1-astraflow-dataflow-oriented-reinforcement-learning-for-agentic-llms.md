---
title: "AstraFlow: Dataflow-Oriented Reinforcement Learning for Agentic LLMs"
title_zh: AstraFlow：面向数据流的智能体大语言模型强化学习
authors: "Haizhong Zheng, Yizhuo Di, Jiahui Wang, Shuowei Jin, Xueshen Liu, Yongji Wu, Z. Morley Mao, Ion Stoica, Jiawei Zhao, Beidi Chen"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15565v1"
tags: ["query:ag-rl"]
score: 9.5
evidence: 面向智能体大语言模型的数据流强化学习系统
tldr: AstraFlow是一个面向数据流的强化学习（RL）系统，旨在解决智能体大语言模型训练中成本高、系统扩展难的问题。它通过解耦生成、数据流管理和训练组件，取代了传统的以训练器为中心的控制架构。该系统原生支持多策略协作训练、弹性扩展及跨地域异构计算，在数学、代码等任务中实现了2.7倍的训练加速，且保持或提升了模型精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15565v1/fig-001.webp\", \"caption\": \"Figure 5 Multi-agent workflows evaluated: (a) Math solver + verifier, (b) Code solver + selector, and (c) Code solver + test-case generator with retry. Different policies serve different roles in a workflow.\", \"page\": 6, \"index\": 1, \"width\": 947, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2605.15565v1/fig-002.webp\", \"caption\": \"Table 1 System-level comparison of LLM RL frameworks. ✓ denotes full support, ✗ no support, and p partial support.\", \"page\": 2, \"index\": 2, \"width\": 1000, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2605.15565v1/fig-003.webp\", \"caption\": \"Figure 10 Per-iteration delta sparsity vs. training step, smoothed. (a) varies model scale on math (Qwen31.7B/8B/14B [41]); (b) varies task on Qwen2.5-7B-Instruct (AlfWorld, WebShop, Search at lr = 1e−6 and 5e−6). The Search lr=5e−6 run is a clear outlier in (b); rerunning it at lr=1e−6 aligns it with AlfWorld and WebShop, confirming that learning-rate magnitude—not task type—drives the gap.\", \"page\": 23, \"index\": 3, \"width\": 975, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2605.15565v1/fig-004.webp\", \"caption\": \"Figure 7 System-flexibility experiments. (a) Rollout GPU count and trainer waiting time under different rollout-pool settings. (b) Per-iteration rollout throughput from different RaaS pools. (c) Weight-transfer time across local and remote rollout pools. (d) Trainer and rollout downtime in the heterogeneous cross-region setting.\", \"page\": 8, \"index\": 4, \"width\": 985, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2605.15565v1/fig-005.webp\", \"caption\": \"Table 6 Per-run hyperparameters for the delta-sparsity measurement runs.\", \"page\": 17, \"index\": 5, \"width\": 804, \"height\": 186}]"
motivation: 现有的LLM强化学习系统采用以训练器为中心的架构，难以高效支持复杂的智能体多策略协作训练及异构计算资源调度。
method: 提出AstraFlow系统，通过数据流导向的抽象将生成服务、数据流管理和训练解耦为自治组件，实现灵活的任务编排。
result: 在数学、代码和AgentBench等负载下，AstraFlow在无需修改系统代码的情况下实现了2.7倍的训练加速，并保持了优异的准确率。
conclusion: AstraFlow通过原则性的组件抽象简化了复杂RL工作负载的实现，为大规模、高效的智能体模型训练提供了通用的系统框架。
---

## 摘要
强化学习 (RL) 正越来越多地用于提升大语言模型的推理、编程和工具使用能力，但智能体强化学习 (agentic RL) 的成本依然高得令人望而却步。将强化学习扩展到智能体大语言模型需要支持复杂的工作负载，包括多策略协同训练，同时高效利用弹性、异构和跨区域的计算资源。现有的 LLM RL 系统支持其中部分功能，但每次新增扩展通常都需要专门的系统工程工作。这种负担源于以训练器为中心的控制架构以及缺乏对 RL 系统组件的原则性抽象。为了解决这些局限性，我们提出了 AstraFlow，这是一个面向数据流的 RL 系统，它用原则性的组件抽象取代了传统的以训练器为中心的控制。在 AstraFlow 中，采样服务 (rollout services)、数据流管理和训练被解耦为自治组件，使系统能够原生支持复杂的多策略智能体 RL 工作负载，并高效利用多样化的计算资源。我们在数学、代码、搜索和 AgentBench 工作负载上对 AstraFlow 进行了评估，结果表明，同一系统在无需更改系统级代码的情况下，即可支持多策略训练、弹性扩展、异构跨区域执行和可组合的数据算法。在多策略协同训练中，AstraFlow 达到了与现有 RL 系统相当或更好的准确率，同时将训练时间缩短了 2.7 倍。

## Abstract
Reinforcement learning (RL) is increasingly used to improve the reasoning, coding, and tool-use capabilities of large language models, but agentic RL remains prohibitively expensive. Scaling RL to agentic LLMs requires supporting complex workloads, including multi-policy collaborative training, while efficiently using elastic, heterogeneous, and cross-region compute resources. Existing LLM RL systems support some of these capabilities, but each new extension often requires dedicated system engineering. This burden arises from trainer-centered control architectures and the lack of principled abstractions for RL system components. To address these limitations, we propose AstraFlow, a dataflow-oriented RL system that replaces conventional trainer-centered control with principled component abstractions. In AstraFlow, rollout services, dataflow management, and training are decoupled into autonomous components, enabling the system to natively support complex multi-policy agentic RL workloads and efficiently exploit diverse compute resources. We evaluate AstraFlow across math, code, search, and AgentBench workloads, showing that the same system supports multi-policy training, elastic scaling, heterogeneous cross-region execution, and composable data algorithms without system-level code changes. In multi-policy collaborative training, AstraFlow achieves comparable or better accuracy than existing RL systems while speeding up training time by 2.7x.

---

## 论文详细总结（自动生成）

这是一份关于论文《AstraFlow: Dataflow-Oriented Reinforcement Learning for Agentic LLMs》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）向智能体（Agent）演进，强化学习（RL）成为提升其推理和工具使用能力的关键。然而，现有的 LLM RL 系统（如 verl, AReaL 等）大多采用“以训练器为中心”（Trainer-centered）的架构，导致在处理多策略协作训练、弹性资源扩展以及异构/跨地域计算资源利用时，系统极其僵化，往往需要大量的定制化工程开发。
*   **核心目标**：提出一种通用的 RL 基础设施，通过解耦执行、训练和资源管理，支持复杂的智能体工作负载。

### 2. 方法论
*   **核心思想**：将传统的中心化控制转变为**面向数据流（Dataflow-oriented）的协调机制**。系统不再由训练器主导，而是由数据流驱动组件间的交互。
*   **关键技术组件**：
    *   **数据流层（Dataflow Layer）**：作为协调平面，管理 Prompt、轨迹和训练 Batch。它提供可编程接口，允许在不修改系统代码的情况下实现数据算法（如动态采样、过滤、重放）。
    *   **采样即服务（Rollout-as-a-Service, RaaS）**：将轨迹生成解耦为独立服务。RaaS 节点自主拉取任务并推送结果，支持异构和弹性扩展。
    *   **训练器抽象（Trainer Abstraction）**：训练器仅负责消耗数据流并发布权重，不直接控制采样调度。
    *   **异步稀疏权重传输**：针对跨地域低带宽环境，利用权重更新的极高稀疏性（>97%），采用基于请求的增量（Delta）拉取机制，大幅降低通信开销。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **数学推理**：使用 DAPO 和 DeepScaler 数据集，在 AIME24/25、MATH500 等 Benchmark 上评估。
    *   **代码生成**：在 LiveCodeBench v5/v6 和 Codeforces 上评估。
    *   **智能体任务**：包括 AlfWorld、WebShop 和基于搜索的问答（ASearcher）。
*   **对比方法**：
    *   **系统性能对比**：对比了 AReaL（代表性的异步解耦系统）和 verl（同步系统）。
    *   **多策略对比**：对比了 Dr.MAS（基于 verl 的多智能体 RL 实现）。
*   **灵活性验证**：设计了“智能体维护者”（使用 Claude Code）驱动的自动缩放实验，以及模拟跨地域异构 GPU 的训练实验。

### 4. 资源与算力
*   **硬件设备**：使用了多种 NVIDIA GPU，包括 **H100 (80GB)、H200 (141GB)、A100 和 L40S**。
*   **具体配置示例**：
    *   数学任务：单节点 8×H100。
    *   代码任务：2 节点 8×H200（共 16 张 GPU）。
    *   跨地域模拟：3 个节点，包含 4-GPU 训练器和 3 个 4-GPU 的 RaaS 池，通过限制功率和带宽模拟异构环境。
*   **训练时长**：实验涵盖了从 800 到 1200 个迭代步数，具体时长视模型规模（1.7B 到 14B）而定。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了数学、代码、搜索、智能体交互等多个领域，实验设计非常全面。
*   **充分性与客观性**：
    *   进行了多维度的消融实验，包括权重稀疏度分析、学习率对稀疏性的影响等。
    *   在相同硬件条件下与 SOTA 系统（AReaL）进行了端到端性能对比，确保了公平性。
    *   通过模拟真实世界的受限环境（如限电、限速），验证了系统的鲁棒性。

### 6. 主要结论与发现
*   **训练加速**：在多策略协作训练中，AstraFlow 相比现有系统实现了 **2.7 倍** 的训练加速。
*   **精度保持**：在数学和代码任务中，AstraFlow 达到了与现有专用系统相当或更高的准确率。
*   **资源效率**：通过自动缩放机制，在保持性能的同时减少了约 **13%** 的 GPU 消耗。
*   **通信优化**：权重更新表现出极高的稀疏性，使得跨地域训练在 4 Gbit/s 带宽下依然高效。

### 7. 优点
*   **架构优雅**：通过数据流抽象彻底解耦了控制逻辑，极大地提升了系统的可扩展性和模块化程度。
*   **零代码扩展**：展示了无需修改系统核心代码即可实现自动缩放和复杂数据算法的能力。
*   **实战导向**：针对智能体 RL 的长尾延迟、异构资源等痛点提供了切实可行的系统级解决方案。

### 8. 不足与局限
*   **算法依赖**：论文侧重于系统架构，其最终模型质量仍高度依赖于底层的 RL 算法、奖励函数设计和数据质量。
*   **覆盖范围**：虽然测试了多种任务，但尚未在超长时序（Long-horizon）的 Web 智能体或机器人控制等极端复杂环境下进行验证。
*   **应用门槛**：虽然系统灵活，但配置复杂的数据流策略可能仍需要用户具备较深的 RL 训练背景知识。

（完）
