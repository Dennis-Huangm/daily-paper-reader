---
title: "GEAR: Granularity-Adaptive Advantage Reweighting for LLM Agents via Self-Distillation"
title_zh: GEAR：基于自蒸馏的大语言模型智能体粒度自适应优势重加权
authors: "Sijia Li, Yuchen Huang, Zifan Liu, Yanping Li, Jingjing Fu, Li Zhao, Jiang Bian, Ling Zhang, Jun Zhang, Rui Wang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11853v2"
tags: ["query:ag-rl"]
score: 9.5
evidence: 使用优势重权的大语言模型智能体强化学习后训练方法
tldr: 针对大语言模型智能体在强化学习中面临的粗粒度奖励难题，本文提出GEAR框架。该方法利用自蒸馏技术，通过对比学生与教师模型的差异来识别语义偏离，并据此动态调整Token和片段级的信用分配权重。实验显示，GEAR在数学推理和工具调用任务中显著优于GRPO，尤其在复杂长程场景下表现突出。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11853v2/fig-001.webp\", \"caption\": \"Figure 2: Frequency of top-20 tokens with normalized reverse-KL > 0.1 in 100 sampled math trajectories.\", \"page\": 15, \"index\": 1, \"width\": 664, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2605.11853v2/fig-002.webp\", \"caption\": \"Figure 3: Token-level visualization results of normalized KL divergence and normalized entropy.\", \"page\": 15, \"index\": 2, \"width\": 706, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2605.11853v2/fig-003.webp\", \"caption\": \"Table 1: Performance (%) on agentic tool-use and mathematical reasoning benchmarks. Agent: TS-M = ToolSandbox Multi-Tool, BFCL Avg. = average over BFCL Parallel-Multiple / Parallel / Multiple, ACE-E = ACEBench End-to-End accuracy, ACE-P = ACEBench Process accuracy. Math: Test(in domain), A24 = AIME24, A25 = AIME25, M500 = MATH500, GSM = GSM8K, MH = MATH. Best results are bolded.\", \"page\": 8, \"index\": 3, \"width\": 788, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2605.11853v2/fig-004.webp\", \"caption\": \"Table 2: Ablation study of GEAR. We evaluate the contribution of each component and the sensitivity to key hyperparameters. The default hyperparameter values are λH = 1.5, α = 0.2, and λKL = 0.1.\", \"page\": 9, \"index\": 4, \"width\": 398, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2605.11853v2/fig-005.webp\", \"caption\": \"Figure 1: Illustration of GEAR for fine-grained credit assignment in agent RL. (a) GRPO assigns the same trajectory-level advantage to all tokens. (b) GEAR preserves this trajectory-level advantage while redistributing credit at a finer granularity. It computes token-wise reverse KL divergence between the student and a groundtruth–conditioned teacher, then uses KL peaks to identify segment onsets and entropy to determine segment extent. Within each divergent segment, all tokens share the onset credit, propagating a localized discrepancy signal over coherent spans. For consistent regions with low KL, token-level reverse KL is used directly. The resulting signals are then modulated by a sign-aware weighting function to produce the final token-level credit.\", \"page\": 5, \"index\": 5, \"width\": 810, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2605.11853v2/fig-006.webp\", \"caption\": \"Figure 4: Training curves of GRPO, GEAR and its variants. The left panel shows the mean training reward, while the right panel illustrates the policy entropy. GEAR achieves the highest training reward while maintaining higher policy entropy compared to the GRPO baseline and other ablated versions.\", \"page\": 16, \"index\": 6, \"width\": 829, \"height\": 285}]"
motivation: 现有的强化学习方法主要依赖粗粒度的结果奖励，难以在长序列任务中实现精确的信用分配。
method: 提出GEAR框架，通过自蒸馏对比学生模型与教师模型的差异，动态识别语义偏离点并调整Token及片段级的优势权重。
result: "在数学推理和工具使用等八项基准测试中，GEAR显著优于标准GRPO，在挑战性任务中性能提升最高达20%。"
conclusion: 这种自适应粒度的信用分配方案能有效解决长程任务中的监督不足问题，是提升大模型智能体性能的有效途径。
---

## 摘要
强化学习已成为大语言模型（LLM）智能体广泛使用的后训练方法，其训练通常依赖于仅提供粗粒度监督的结果级奖励。虽然更细粒度的信用分配（credit assignment）有望实现有效的策略更新，但获取可靠的局部信用并将其分配给长时程轨迹的正确部分仍然是一个开放性挑战。在本文中，我们提出了粒度自适应优势重加权（GEAR），这是一种自适应粒度的信用分配框架，它利用源自自蒸馏的 Token 级和片段级信号来重塑轨迹级的 GRPO 优势。GEAR 将同策略（on-policy）学生模型与以地面真值（ground-truth）为条件的教师模型进行比较，以获得参考引导的散度信号，从而识别自适应片段边界并调节局部优势权重。这种散度通常在语义偏离开始时出现峰值，而同一自回归延续中的后续 Token 可能会恢复到低散度。因此，GEAR 将此类峰值视为自适应信用区域的锚点：在学生模型与教师模型保持一致的地方，保留 Token 级分辨率；在发生偏离的地方，GEAR 将相应的后续内容归组为一个自适应片段，并利用偏离点的散度来调节该片段的优势。在 Qwen3 4B 和 8B 模型上进行的八个数学推理和智能体工具使用基准测试表明，GEAR 一贯优于标准的 GRPO、仅自蒸馏的基准方法以及 Token 级或轮次级信用分配方法。在 GRPO 基准准确率较低的测试中，增益尤为显著，比 GRPO 提升了高达约 20%，这表明所提出的自适应重加权方案在更具挑战性的长时程场景中特别有用。

## Abstract
Reinforcement learning has become a widely used post-training approach for LLM agents, where training commonly relies on outcome-level rewards that provide only coarse supervision. While finer-grained credit assignment is promising for effective policy updates, obtaining reliable local credit and assigning it to the right parts of the long-horizon trajectory remains an open challenge. In this paper, we propose Granularity-adaptivE Advantage Reweighting (GEAR), an adaptive-granularity credit assignment framework that reshapes the trajectory-level GRPO advantage using token- and segment-level signals derived from self-distillation. GEAR compares an on-policy student with a ground-truth-conditioned teacher to obtain a reference-guided divergence signal for identifying adaptive segment boundaries and modulating local advantage weights. This divergence often spikes at the onset of a semantic deviation, while later tokens in the same autoregressive continuation may return to low divergence. GEAR therefore treats such spikes as anchors for adaptive credit regions: where the student remains aligned with the teacher, token-level resolution is preserved; where it departs, GEAR groups the corresponding continuation into an adaptive segment and uses the divergence at the departure point to modulate the segment' s advantage. Experiments across eight mathematical reasoning and agentic tool-use benchmarks with Qwen3 4B and 8B models show that GEAR consistently outperforms standard GRPO, self-distillation-only baselines, and token- or turn-level credit-assignment methods. The gains are especially strong on benchmarks with lower GRPO baseline accuracy, reaching up to around 20\% over GRPO, suggesting that the proposed adaptive reweighting scheme is especially useful in more challenging long-horizon settings.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GEAR**（Granularity-Adaptive Advantage Reweighting）的框架，旨在解决大语言模型（LLM）智能体在强化学习（RL）后训练中面临的信用分配难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在智能体任务中，通常只能获得轨迹级的“结果奖励”（如任务是否成功），这导致了**信用分配（Credit Assignment）平坦化**问题。
*   **研究动机**：
    *   **监督信号粗糙**：单一的轨迹奖励无法区分长序列中哪些 Token 或动作是真正导致成功的关键，哪些是导致失败的诱因。
    *   **现有方法局限**：现有的细粒度方法要么依赖预定义的单位（如按轮次、按工具调用分割），要么依赖昂贵的中间过程奖励模型（PRM），难以兼顾灵活性与准确性。
    *   **长程任务挑战**：在复杂的数学推理或多步工具调用中，早期的微小偏差可能在很久之后才显现后果。

### 2. 方法论：GEAR 框架
GEAR 的核心思想是利用**自蒸馏（Self-Distillation）**产生的信号来动态调整轨迹中不同部分的权重。

*   **自蒸馏信用信号**：
    *   构建一个“教师模型”（将地面真值答案作为上下文输入模型）和一个“学生模型”（同策略生成的轨迹）。
    *   计算两者之间的**逆向 KL 散度（Reverse KL Divergence）**。散度峰值通常出现在语义偏离点（如“所以”、“但是”等转折词），预示着模型开始脱离正确路径。
*   **自适应粒度分割**：
    *   **触发机制**：当 KL 散度超过阈值 $\lambda_{KL}$ 时，识别为偏离的起点。
    *   **终止机制**：利用 **Token 熵（Entropy）** 来确定偏离的影响范围。当熵值显著升高（代表不确定性或语义转换完成）时，结束当前片段。
    *   这种设计使得信用分配不再局限于固定长度，而是根据行为动态形成“片段”。
*   **优势重加权（Advantage Reweighting）**：
    *   **符号感知调制**：
        *   若轨迹最终成功（优势为正），则**降低**高偏离片段的权重，以防强化不稳定的偶然行为。
        *   若轨迹最终失败（优势为负），则**增加**高偏离片段的权重，从而对错误路径施加更强的惩罚。
    *   最终将调制后的权重作用于 GRPO 的优势函数上。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：MATH, GSM8K, MATH500, AIME 2024, AIME 2025。
    *   **智能体工具使用**：ToolSandbox (多工具子集), BFCL (函数调用基准), ACEBench (端到端成功率)。
*   **模型规模**：基于 Qwen3-4B-Base 和 Qwen3-8B-Base。
*   **对比方法**：
    *   **GRPO**：标准的轨迹级结果奖励强化学习。
    *   **OPSD / OPSD+RL**：纯自蒸馏及其与 RL 的简单结合。
    *   **ARPO**：基于熵触发的分支探索方法。
    *   **MT-GRPO**：基于轮次级（Turn-level）信用分配的方法。

### 4. 资源与算力
*   **硬件环境**：使用了 **13 台 NVIDIA H100 GPU** 进行训练。
*   **训练时长**：
    *   数学推理任务（4B 模型）：约 **26 小时/epoch**（使用 8 张 H100）。
    *   工具调用任务：约 **10 小时/epoch**。
*   **框架**：基于 VolcEngine VeRL 框架实现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个主流基准测试，跨越数学和工具调用两个大类。
*   **消融实验**：非常详尽，验证了“动态分割”、“熵终止机制”、“KL 触发阈值”以及“调制强度 $\alpha$”等关键组件的有效性。
*   **客观性**：通过在不同规模（4B/8B）和跨领域（Cross-domain）数据上进行测试，证明了方法的稳健性和泛化能力。实验对比了多种最先进的细粒度 RL 变体，具有较高的参考价值。

### 6. 主要结论与发现
*   **性能显著提升**：GEAR 在所有基准测试中均优于 GRPO，尤其在 GRPO 表现较差的困难任务中，提升幅度高达 **20%**。
*   **泛化能力强**：在 AIME 等竞赛级数学题上表现优异，说明 GEAR 学习到了更本质的推理逻辑而非简单的模式记忆。
*   **训练稳定性**：相比于直接应用 Token 级 KL 信号，GEAR 的片段级重加权能保持更高的策略熵，有效防止模型过早陷入局部最优（策略崩溃）。

### 7. 优点与亮点
*   **无需额外标注**：不需要人工编写中间步骤的奖励（Process Rewards），完全依赖模型自身的预测动力学。
*   **动态粒度**：打破了“按 Token”或“按轮次”的固定思维，通过信号驱动自动寻找行为边界。
*   **即插即用**：可以轻松集成到现有的 GRPO 或 PPO 框架中，计算开销与标准 GRPO 相当。

### 8. 不足与局限
*   **依赖地面真值**：GEAR 需要地面真值（Ground-truth）来构建教师模型，这在开放式生成或缺乏标准答案的任务中难以应用。
*   **教师模型质量**：如果基础模型本身极弱，即使给定正确答案，其构建的教师分布也可能不够准确，从而影响 KL 信号的质量。
*   **超参数敏感性**：虽然论文给出了推荐值，但 $\lambda_{KL}$ 和 $\lambda_H$ 等阈值在不同任务间可能需要微调。

（完）
