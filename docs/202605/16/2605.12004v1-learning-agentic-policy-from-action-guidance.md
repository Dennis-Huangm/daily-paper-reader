---
title: Learning Agentic Policy from Action Guidance
title_zh: 从动作引导中学习智能体策略
authors: "Yuxiang Ji, Zengbin Wang, Yong Wang, Shidong Yang, Ziyu Ma, Guanhua Chen, Zonghua Sun, Liaoni Wu, Xiangxiang Chu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12004v1"
tags: ["query:ag-rl"]
score: 9.5
evidence: 利用动作引导的大语言模型智能体强化学习
tldr: 本研究针对大语言模型智能体在强化学习中因探索能力不足而难以触达奖励状态的问题，提出了ActGuide-RL框架。该方法利用丰富的动作数据作为计划式参考引导，通过混合策略训练将引导探索获得的收益内化至无引导策略中，并采用最小干预原则以平衡收益与风险。实验证明，该方法在GAIA和XBench等基准测试上显著提升了性能，无需冷启动即可达到与传统SFT+RL流程相当的效果，为智能体策略学习提供了更具扩展性的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12004v1/fig-001.webp\", \"caption\": \"Table 1: Main results of ACTGUIDE-RL on search-agent benchmarks comparing foundation models, search-agent trained models, and RL baseline. The best results are indicated in bold.\", \"page\": 7, \"index\": 1, \"width\": 825, \"height\": 955}, {\"url\": \"assets/figures/arxiv/2605.12004v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of ACTGUIDE-RL framework. Conventional agentic RL can only obtain training signals within the base model in-capability region. ACTGUIDE-RL follows the principle of minimal intervention, dynamically introducing action data to guide the model toward out-region exploration. Such mixed rollouts are trained through mixed-policy optimization.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 341}]"
motivation: 大语言模型在强化学习中常因基础策略无法触达奖励状态而导致学习失效，且传统的迭代监督微调成本极高。
method: 提出ActGuide-RL框架，将动作数据作为引导注入并结合混合策略训练，通过最小干预原则实现自适应的引导回退。
result: "在GAIA和XBench基准测试中，该方法相比零样本强化学习分别提升了10.7%和19%，性能与SFT+RL流水线持平。"
conclusion: 该研究表明利用可扩展的动作引导可以有效替代繁重的SFT数据，成为一种更高效的智能体强化学习新范式。
---

## 摘要
大语言模型（LLMs）的智能体强化学习（RL）关键取决于基础策略的探索能力，因为训练信号仅在其能力覆盖区域内产生。对于基础策略无法到达奖励状态的任务，需要额外的训练或外部引导来恢复有效的学习信号。我们没有依赖昂贵的迭代监督微调（SFT），而是利用了日常人类交互中产生的大量动作数据。我们提出了 ActGuide-RL，它将动作数据作为计划式参考引导注入，使智能体策略能够克服通往奖励状态的可达性障碍。随后，通过混合策略训练共同优化有引导和无引导的展开（rollouts），将探索收益内化回无引导策略中。基于对收益-风险权衡的理论和实证分析，我们采用了最小干预原则，仅在自适应回退时调用引导，以匹配任务难度并最小化离策略（off-policy）风险。在搜索智能体基准测试中，ActGuide-RL 相比于零强化学习（zero RL）有显著提升（在 GAIA 上提升 10.7 个百分点，在 XBench 上使用 Qwen3-4B 提升 19 个百分点），且在无需任何冷启动的情况下，其表现与 SFT+RL 流水线相当。这为智能体强化学习提供了一种新范式，即通过使用可扩展的动作引导来减少对大量 SFT 数据的依赖。

## Abstract
Agentic reinforcement learning (RL) for Large Language Models (LLMs) critically depends on the exploration capability of the base policy, as training signals emerge only within its in-capability region. For tasks where the base policy cannot reach reward states, additional training or external guidance is needed to recover effective learning signals. Rather than relying on costly iterative supervised fine tuning (SFT), we exploit the abundant action data generated in everyday human interactions. We propose \textsc{ActGuide-RL}, which injects action data as plan-style reference guidance, enabling the agentic policy to overcome reachability barriers to reward states. Guided and unguided rollouts are then jointly optimized via mixed-policy training, internalizing the exploration gains back into the unguided policy. Motivated by a theoretical and empirical analysis of the benefit-risk trade-off, we adopt a minimal intervention principle that invokes guidance only as an adaptive fallback, matching task difficulty while minimizing off-policy risk. On search-agent benchmarks, \textsc{ActGuide-RL} substantially improves over zero RL (+10.7 pp on GAIA and +19 pp on XBench with Qwen3-4B), and performs on par with the SFT+RL pipeline without any cold start. This suggests a new paradigm for agentic RL that reduces the reliance on heavy SFT data by using scalable action guidance instead.

---

## 论文详细总结（自动生成）

### 论文总结：Learning Agentic Policy from Action Guidance (ActGuide-RL)

#### 1. 核心问题与研究动机
论文关注的是**大语言模型（LLM）智能体在强化学习（RL）中的“探索困境”**。
*   **研究背景**：智能体 RL 的成功高度依赖于基础策略的探索能力。当任务难度超出模型当前能力范围（即处于“非覆盖区”/Out-Region）时，模型无法触达奖励状态，导致训练信号缺失（梯度塌陷），训练停滞。
*   **核心问题**：传统的解决方法是先进行昂贵的监督微调（SFT）进行“冷启动”，但这增加了数据获取成本和课程设计的复杂性。
*   **研究动机**：利用互联网和日常交互中大量存在的、易于获取的**动作数据**（如 GUI/CLI 操作记录、API 调用序列），将其作为引导信号，帮助模型跨越探索障碍，从而减少对高质量 SFT 数据的依赖。

#### 2. 方法论：ActGuide-RL 框架
核心思想是通过“最小干预”原则引入动作引导，并利用混合策略优化将引导产生的收益内化到模型中。
*   **可达性动力学分析**：论文定义了“有效状态访问质量”（Effective State-visiting Mass），指出当模型在关键步骤出现质量骤降时，即形成了“可达性障碍”。
*   **动作引导（Action Guidance）**：将动作序列作为“计划式参考”注入 Prompt。不同于强制模型模仿，这种方式允许模型在参考动作的基础上进行自主推理。
*   **最小干预原则（Minimal Intervention Principle）**：
    *   **自适应回退**：仅当无引导的采样全部失败时，才调用动作引导。
    *   **二分查找最优引导量**：通过二分查找确定能使任务成功的最小动作引导长度 $k^*$，以平衡“探索收益”与“离策略分布偏移风险”。
*   **离策略内化（Off-Policy Internalization）**：采用混合策略优化目标。在训练时，将有引导的轨迹视为离策略数据，通过重要性采样（Importance Sampling）调整梯度，使无引导的策略能够学习到有引导轨迹中的成功行为。

#### 3. 实验设计
*   **场景与数据集**：
    *   **领域内（In-domain）搜索智能体基准**：GAIA（通用助手）、WebWalkerQA（复杂网页遍历）、XBench（生产力工具）、BrowseComp-ZH（中文网页浏览）。
    *   **领域外（Out-of-domain）泛化测试**：GPQA（研究生水平科学问答）、TruthfulQA（真实性）、IFEval（指令遵循）。
*   **对比方法**：
    *   基础模型：Qwen2.5/3 系列、DeepSeek-V3、GPT-5 High 等。
    *   专门训练的智能体模型：WebSailor、ARPO、WebThinker。
    *   基准线：Vanilla RL（基于 GRPO 算法）、SFT+RL 流水线。

#### 4. 资源与算力
*   **算力配置**：论文明确提到使用了 **8 台配备 8 块 NVIDIA H20 GPU 的节点**进行训练和采样（Rollout）。
*   **评估资源**：另外使用了一个配备 8 块 H20 GPU 的节点作为 LLM-Judge，用于奖励分配和测试评估。
*   **训练细节**：学习率 1e-6，KL 系数 0.001，训练数据量为 2000 条，最大轮次 30 轮。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同难度的搜索智能体基准和 3 个通用能力基准，使用了 4 种不同规模的基础模型（Qwen2.5-3B/7B, Qwen3-4B/8B）。
*   **消融实验**：针对最小干预原则（自适应 vs 固定比例）、混合策略优化、动作噪声敏感性、引导注入方式（Prefix vs Plan）等进行了详尽的消融研究。
*   **充分性评价**：实验设计较为充分且客观。通过对比 SFT+RL 路径，证明了该方法在无冷启动情况下的竞争力；通过噪声实验验证了方法在现实不完美数据下的鲁棒性。

#### 6. 主要结论与发现
*   **性能提升**：ActGuide-RL 在所有测试模型上均显著优于纯 RL。在 Qwen3-4B 上，GAIA 提升 10.7%，WebWalkerQA 提升 27.8%，XBench 提升 19%。
*   **替代 SFT**：在无需 SFT 冷启动的情况下，ActGuide-RL 的表现能与传统的“SFT + RL”流程持平，甚至在保持模型通用能力（领域外测试）方面表现更好。
*   **复杂交互能力**：即使是小模型，通过该方法也能逐渐学会长程、复杂的工具调用（交互轮数和 Token 长度随训练稳步增长）。
*   **最小干预的必要性**：过度引导会增加离策略风险，导致训练不稳定；而自适应的最小引导能最有效地平衡探索与内化。

#### 7. 优点与亮点
*   **范式创新**：提出了一种比 SFT 更轻量、更易扩展的智能体训练新范式，利用“动作轨迹”而非“推理轨迹”进行引导。
*   **理论支撑**：通过可达性动力学和收益-风险权衡分析，为“为何需要引导”和“为何需要最小干预”提供了理论依据。
*   **实用性强**：对动作数据的噪声具有一定的容忍度（10% 噪声下性能稳定），适合大规模工业化应用。

#### 8. 不足与局限
*   **场景局限**：主要在搜索智能体（Search-Agent）这一无状态或弱状态场景下验证，在更复杂的 CLI、GUI 或具身智能（Embodied AI）环境中的表现尚待进一步探索。
*   **引导粒度**：目前采用的是计划式（Plan-style）引导，对于更细粒度的步骤级（Step-level）引导和反馈机制讨论较少。
*   **数据处理**：论文未详细讨论如何从海量原始日志中系统化地清洗和筛选高质量动作数据的工程化流程。

（完）
